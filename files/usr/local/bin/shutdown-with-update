#!/bin/bash
# Shutdown with Update - Updates firmware, dnf packages, Flatpaks, pipx tools and the Rust toolchain before shutdown
#
# Two names, one script. Installed as /usr/local/bin/shutdown-with-update and, by symlink,
# /usr/local/bin/reboot-with-update; the name decides what happens after the updates:
#   shutdown-with-update          shutdown -h now, with the inhibitor prompt below
#   reboot-with-update [--in N]   warn every running ccy/cc session through
#                                 `ccy-sessions notify`, count down N minutes (default 2),
#                                 warn again at one minute, then systemctl reboot. Sessions
#                                 that opted in to restore come back after the boot.
# Both run as root via sudo. The reboot mode needs SUDO_USER: the sessions belong to that
# user and are found through that user's tmux server.

# Trap Ctrl+C and exit cleanly
trap 'echo -e "\nShutdown cancelled by user."; exit 1' INT TERM

MODE="shutdown"
case "$(basename "$0")" in
shutdown-with-update) ;;
reboot-with-update) MODE="reboot" ;;
*)
    echo "Error: this script must be invoked as shutdown-with-update or reboot-with-update, not $(basename "$0")" >&2
    exit 64
    ;;
esac

REBOOT_MINUTES=2
while [ $# -gt 0 ]; do
    case "$1" in
    --in)
        if [ "$MODE" != "reboot" ]; then
            echo "Error: --in applies to reboot-with-update only" >&2
            exit 64
        fi
        if ! [[ "${2:-}" =~ ^[1-9][0-9]*$ ]]; then
            echo "Error: --in needs a whole number of minutes, at least 1; got '${2:-}'" >&2
            exit 64
        fi
        REBOOT_MINUTES="$2"
        shift 2
        ;;
    *)
        echo "Error: unknown option '$1'" >&2
        exit 64
        ;;
    esac
done

# The reboot mode's last step runs as the invoking user, whose ccy-sessions is the one that
# can see that user's tmux server. Checked here, and rehearsed with --dry-run, BEFORE the
# updates: a live session whose project has no daemon CLI is a refusal, and finding that out
# after twenty minutes of dnf would be the wrong order.
if [ "$MODE" = "reboot" ]; then
    if [ -z "${SUDO_USER:-}" ]; then
        echo "Error: reboot-with-update must be run through sudo from the user whose sessions are to be warned" >&2
        exit 1
    fi
    CCY_SESSIONS="$(getent passwd "$SUDO_USER" | cut -d: -f6)/.local/bin/ccy-sessions"
    if [ ! -x "$CCY_SESSIONS" ]; then
        echo "Error: $CCY_SESSIONS is missing; playbooks/imports/play-claude-yolo.yml deploys it" >&2
        exit 1
    fi
    echo "Checking that every running ccy/cc session can be warned..."
    if ! sudo -u "$SUDO_USER" -H "$CCY_SESSIONS" reboot --in "$REBOOT_MINUTES" --dry-run; then
        echo "Error: not every session can be warned (see above). Nothing was updated and nothing reboots." >&2
        exit 1
    fi
fi

echo "Starting firmware updates..."
if ! fwupdmgr refresh --force; then
    echo "Warning: Firmware refresh failed, continuing..."
fi

if ! fwupdmgr update -y; then
    echo "Warning: Firmware update failed or no updates available, continuing..."
fi

echo "Starting system package updates..."
if ! dnf -y upgrade; then
    echo "Error: Package updates failed"
    exit 1
fi

# Flatpak apps (Slack, videography tools) are installed system-wide by the
# playbooks (become: true) and are NOT covered by dnf, so update them explicitly.
# Update failures are non-fatal here: this runs right before poweroff, and a
# flaky mirror must never leave the machine unable to shut down.
echo "Starting Flatpak updates..."
if command -v flatpak >/dev/null; then
    if ! flatpak update --system -y; then
        echo "Warning: system Flatpak update failed, continuing..."
    fi
    # Prune runtimes no installed app still needs. Updating alone never removes
    # the version it superseded, so every driver bump leaves its old GL runtime
    # behind for ever: this machine had accumulated 14 nvidia GL runtimes at
    # ~900MB each, 13 of them dead, against one loaded driver.
    #
    # Ordered AFTER the update so a runtime orphaned by this very run is
    # collected now rather than surviving until the next shutdown.
    #
    # "Unused" means no installed app depends on it, so this only ever reclaims
    # what nothing can launch. The corollary worth knowing: uninstall the last
    # app using a runtime and the next run reclaims it too, so reinstalling
    # that app later re-downloads its runtime.
    if ! flatpak uninstall --system --unused -y; then
        echo "Warning: system Flatpak cleanup failed, continuing..."
    fi
    # Also update any per-user Flatpaks, as the invoking user rather than root.
    if [ -n "${SUDO_USER:-}" ]; then
        if ! sudo -u "$SUDO_USER" -H flatpak update --user -y; then
            echo "Warning: user Flatpak update failed, continuing..."
        fi
        if ! sudo -u "$SUDO_USER" -H flatpak uninstall --user --unused -y; then
            echo "Warning: user Flatpak cleanup failed, continuing..."
        fi
    fi
else
    echo "flatpak not installed, skipping."
fi

# pipx CLI tools (pdm, semgrep, huggingface CLI) are installed into the invoking
# user's ~/.local/pipx by play-python.yml — not root's — so upgrade them as that
# user. Non-fatal for the same reason as the Flatpak block above.
echo "Starting pipx tool updates..."
update_user="${SUDO_USER:-$USER}"
if sudo -u "$update_user" -H bash -lc 'command -v pipx >/dev/null'; then
    if ! sudo -u "$update_user" -H bash -lc 'pipx upgrade-all'; then
        echo "Warning: pipx upgrade-all failed, continuing..."
    fi
else
    echo "pipx not installed for $update_user, skipping."
fi

# Rust toolchain is installed into the invoking user's ~/.rustup / ~/.cargo by
# play-rust-dev.yml (optional), so update it as that user rather than root.
# Non-fatal for the same reason as the blocks above.
echo "Starting Rust toolchain updates..."
if sudo -u "$update_user" -H bash -lc 'command -v rustup >/dev/null'; then
    if ! sudo -u "$update_user" -H bash -lc 'rustup update'; then
        echo "Warning: rustup update failed, continuing..."
    fi
else
    echo "rustup not installed for $update_user, skipping."
fi

echo "Updates complete. Checking for kernel module builds..."
# Check if akmods is actually running (not just active/exited)
if pgrep -f "akmods" > /dev/null || systemctl status akmods 2>/dev/null | grep -q "Building"; then
    echo "Waiting for akmods to complete kernel module builds..."
    echo "(Press Ctrl+C to cancel shutdown)"
    
    count=0
    while (pgrep -f "akmods" > /dev/null || systemctl status akmods 2>/dev/null | grep -q "Building") && [ $count -lt 60 ]; do
        sleep 5
        ((count++))
        echo -n "."
    done
    echo ""
    
    if [ $count -ge 60 ]; then
        echo "Warning: akmods took longer than 5 minutes, proceeding anyway"
    else
        echo "Kernel modules built successfully."
    fi
fi

if [ "$MODE" = "reboot" ]; then
    # Warn, count down, warn at one minute — as the user, through ccy-sessions, so the
    # signals reach each project's own daemon. The reboot itself is root's: `systemctl
    # reboot` as the user is refused by polkit on a session that is not local and active,
    # which an SSH login is not, and this command is as likely to be run over SSH as not.
    echo "Warning running ccy/cc sessions: reboot in $REBOOT_MINUTES minute(s)..."
    sudo -u "$SUDO_USER" -H "$CCY_SESSIONS" notify reboot-warning --minutes "$REBOOT_MINUTES" || exit 1
    if [ "$REBOOT_MINUTES" -gt 1 ]; then
        sleep $(((REBOOT_MINUTES - 1) * 60))
        sudo -u "$SUDO_USER" -H "$CCY_SESSIONS" notify reboot-warning --minutes 1 || exit 1
        echo "Reboot in 1 minute..."
    fi
    sleep 60
    echo "Rebooting..."
    exec systemctl reboot
fi

echo "Attempting shutdown..."
if ! shutdown -h now 2>&1; then
    echo ""
    echo "Shutdown blocked by inhibitors or logged-in users."
    echo "Current inhibitors:"
    systemd-inhibit --list --no-pager | grep -v "^WHO"
    echo ""
    read -p "Force shutdown anyway? (y/N): " -n 1 -r
    echo ""
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        echo "Forcing shutdown..."
        systemctl poweroff -i
    else
        echo "Shutdown cancelled."
    fi
fi