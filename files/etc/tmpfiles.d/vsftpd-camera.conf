# Ensure vsftpd secure_chroot_dir exists in tmpfs /run after reboot
# Fedora's vsftpd package ships tmpfiles.d that only creates /var/ftp,
# but vsftpd.conf references /var/run/vsftpd/empty for privsep chroot.
d /run/vsftpd 0755 root root -
d /run/vsftpd/empty 0755 root root -
