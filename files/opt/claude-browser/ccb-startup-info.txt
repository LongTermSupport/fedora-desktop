You are running in Browser Automation Mode with Playwright MCP!

ENVIRONMENT:
• Container engine: __CONTAINER_ENGINE__
• Browser GUI support enabled (X11 or Wayland)
• Playwright MCP for browser control
• Chromium, Firefox, and WebKit browsers installed
• Working directory: /workspace (your project mounted from host)
• Isolated from host system (secure sandbox)

CAPABILITIES:
• Control real browsers through Playwright MCP
• Direct Chrome DevTools Protocol access via chrome-ws CLI
• Browsers open on your desktop for visual debugging
• Full development environment (git, gh, ripgrep, jq, Node.js, Python)
• GitHub CLI authenticated (can create PRs, issues, etc.)
• SSH keys mounted for git operations
• Running as root user - install packages freely (apt, npm, pip, etc.)
• Container is ephemeral - for permanent tools, extend the Dockerfile

BROWSER AUTOMATION:
• Use Playwright MCP tools to control browsers
• Use chrome-ws for lightweight Chrome DevTools Protocol operations
• Create acceptance tests interactively
• Debug and explore web applications visually
• Scrape and automate web workflows

IMPORTANT - ERROR HANDLING:
• If you get 'Browser is already in use' error: First use browser_close tool, then retry navigation
• Always close browser contexts when switching tasks or if navigation fails

Your project files are at /workspace. Browsers are ready!
