#!/usr/bin/env ansible-playbook
---
# Install lightweight IDEs as alternatives to heavyweight IDEs
# Provides Geany for users who prefer lighter editors

- hosts: desktop
  name: Install Lightweight IDEs
  become: true
  vars:
    root_dir: "{{ lookup('ansible.builtin.config', 'CONFIG_FILE') | dirname }}"
    scope: gnome   # general | gnome | server — see CLAUDE/AnsibleStyle.md
  tasks:
    - name: Scope guard — assert provisioning_profile is recognised
      ansible.builtin.assert:
        that:
          - provisioning_profile in ['desktop', 'server']
        fail_msg: |
          provisioning_profile={{ provisioning_profile }} is not recognised.
          Valid values: desktop, server.
          Auto-detected from `systemctl get-default`
          (see environment/localhost/group_vars/desktop.yml) or overridden
          via -e provisioning_profile=desktop|server.

    - name: Scope guard — end play if provisioning_profile does not match declared scope
      ansible.builtin.meta: end_play
      when: (scope == 'gnome' and provisioning_profile == 'server') or (scope == 'server' and provisioning_profile != 'server')

    # Geany - Lightweight IDE from standard repos
    - name: Install Geany IDE
      ansible.builtin.package:
        name: geany
        state: present
      tags: geany

    - name: Display Installation Complete Message
      ansible.builtin.debug:
        msg: |
          Lightweight IDE installed successfully:
          - Geany: General purpose lightweight IDE with syntax highlighting
            and basic IDE features for multiple languages

          Launch from applications menu or command line:
            geany

          Note: CodeLite removed - no Fedora package available yet.
          For C/C++ development, consider VS Code or CLion as alternatives.
