#!/usr/bin/env -S bash -c 'p=$(realpath "$0") && exec "${p%/playbooks/*}/run.bash" "$p" "$@"'
---
# Git is already installed as it was used to clone this repo,
# so we just need to configure it
- hosts: desktop
  name: Git Configure and Tools
  become: false
  vars:
    root_dir: "{{ lookup('ansible.builtin.config', 'CONFIG_FILE') | dirname }}"
    scope: general   # general | gnome | server — see CLAUDE/AnsibleStyle.md
  tasks:
    - name: Setup Git Config - user
      ansible.builtin.shell: |
        set -euo pipefail
        git config --global user.email "{{ user_email }}"
        git config --global user.name "{{ user_name }}"
        git config --global color.ui auto
        git config --global push.default simple;
        git config --global help.autocorrect 30;
        git config --global core.editor "vim"
        git config --global init.defaultBranch main
        git config --global fetch.prune true
        git config --global pull.rebase false
        git config --global branch.autoSetupMerge simple
        git config --global core.autocrlf input
      args:
        executable: /bin/bash

    - name: Ensure ~/.config/git directory exists
      ansible.builtin.file:
        path: "/home/{{ user_login }}/.config/git"
        state: directory
        owner: "{{ user_login }}"
        group: "{{ user_login }}"
        mode: "0755"

    # =========================================================================
    # Owner commit signing (Plan 00137 D3) — only where git_signing_key is set
    # =========================================================================
    # A server running the unattended self-update deploys only commits signed by the
    # owner's pinned key, and the signature vouches for everything below it. So signing
    # must be a deliberate human act, never a default an agent inherits:
    #   - commit.gpgsign is NOT set. ccy copies ~/.gitconfig into every container and cc
    #     agents run on the host, so a signing default would make every agent commit
    #     either fail or be signed on the owner's behalf.
    #   - The settings go in ~/.config/git/config, which git reads and ccy does not copy.
    #   - The key must be passphrase-protected (asserted below): signing then asks a person,
    #     so an agent without a terminal cannot sign. Keep it out of ssh-agent.
    # Sign with `git sign-deploy` (an empty signed commit vouching for HEAD), or
    # `git commit -S`. Generating the key is in docs/configuration.md.
    - name: Check the Signing Key Exists
      ansible.builtin.stat:
        path: "{{ git_signing_key }}"
      register: git_signing_key_stat
      when: git_signing_key is defined

    - name: Assert the Signing Key Exists
      ansible.builtin.assert:
        that:
          - git_signing_key_stat.stat.exists
          - git_signing_key_stat.stat.isreg
        fail_msg: >-
          git_signing_key is {{ git_signing_key }}, which is not a file. Generate the key
          first (docs/configuration.md "Commit Signing"), or remove git_signing_key from
          host_vars.
      when: git_signing_key is defined

    # Loading the key with an empty passphrase succeeds only for an unencrypted key.
    - name: Probe Whether the Signing Key Loads Without a Passphrase
      ansible.builtin.command:
        argv:
          - ssh-keygen
          - -y
          - -P
          - ""
          - -f
          - "{{ git_signing_key }}"
      register: git_signing_key_unlocked
      changed_when: false
      failed_when: false  # FAIL-FAST-OK: probe-then-fail — the verdict is the next task
      no_log: true
      when: git_signing_key is defined

    - name: Assert the Signing Key Is Passphrase-Protected
      ansible.builtin.assert:
        that:
          - git_signing_key_unlocked.rc != 0
        fail_msg: >-
          {{ git_signing_key }} has no passphrase, so any process running as you, a cc
          agent included, could sign a commit the self-updating server would then run as
          root. Add one: `ssh-keygen -p -f {{ git_signing_key }}`.
      when: git_signing_key is defined

    - name: Configure Owner Commit Signing in the XDG Git Config
      community.general.git_config:
        scope: file
        file: "/home/{{ user_login }}/.config/git/config"
        name: "{{ item.name }}"
        value: "{{ item.value }}"
      loop:
        - name: gpg.format
          value: ssh
        - name: user.signingkey
          value: "{{ git_signing_key }}"
        - name: alias.sign-deploy
          value: "commit --allow-empty --gpg-sign -m 'deploy: signed by the owner'"
      when: git_signing_key is defined

    - name: Set up Global Git Ignores
      ansible.builtin.blockinfile:
        create: true
        owner: "{{ user_login }}"
        group: "{{ user_login }}"
        mode: "0644"
        block: |
          /.idea
          *.secret
          /untracked
          /vendor
          .phpunit.result.cache
        path: "/home/{{ user_login }}/.config/git/ignore"

    - name: Enable Git Bash Prompt
      ansible.builtin.shell: "git clone https://github.com/magicmonty/bash-git-prompt.git ~/.bash-git-prompt --depth=1"
      args:
        creates: /home/{{ user_login }}/.bash-git-prompt/.git/config

    - name: Prompt to Start SSH Agent on SSH Sessions
      ansible.builtin.blockinfile:
        path: /home/{{ user_login }}/.bashrc
        marker: "# {mark} ANSIBLE MANAGED: SSH Agent Prompt"
        insertbefore: "# BEGIN ANSIBLE MANAGED: Git Bash Prompt"
        block: |
          # On SSH sessions without a working agent, offer to start one
          # (must run before bash-git-prompt to avoid passphrase prompt on stdin).
          # Only with a terminal on stdin: without one the read hits EOF, the
          # default answer is yes, and keys are loaded that nobody asked for.
          # Everything it prints goes to stderr, so a captured stdout stays clean.
          if [[ -n "${SSH_CONNECTION:-}" && $- == *i* && -t 0 ]]; then
              if ! ssh-add -l &>/dev/null; then
                  echo "SSH session detected without a working SSH agent." >&2
                  read -rp "Start ssh-agent and load keys? [Y/n] " _ssh_reply
                  if [[ "${_ssh_reply:-Y}" =~ ^[Yy]?$ ]]; then
                      eval "$(ssh-agent -s)" > /dev/null
                      if command -v ssh-agent-start &>/dev/null; then
                          ssh-agent-start --all >&2
                      else
                          ssh-add
                      fi
                  fi
                  unset _ssh_reply
              fi
          fi

    - name: Update ~/.bashrc File for the Bash Git Prompt
      ansible.builtin.blockinfile:
        path: /home/{{ user_login }}/.bashrc
        marker: "# {mark} ANSIBLE MANAGED: Git Bash Prompt"
        block: |
          GIT_PROMPT_ONLY_IN_REPO=1
          GIT_PROMPT_THEME=Solarized
          GIT_PROMPT_START=$PS1
          source ~/.bash-git-prompt/gitprompt.sh

    # https://github.com/cli/cli/blob/trunk/docs/install_linux.md#fedora-centos-red-hat-enterprise-linux-dnf
    # --overwrite re-fetches the repofile every run: upstream rotates the signing
    # key and edits the gpgkey= line, and a repofile written once keeps pointing at
    # the retired key, so `dnf update` fails signature verification on new gh RPMs.
    - name: Configure GitHub CLI Repository
      become: true
      ansible.builtin.shell: |
        set -euo pipefail
        dnf -y install 'dnf-command(config-manager)'
        dnf -y config-manager addrepo --overwrite --from-repofile=https://cli.github.com/packages/rpm/gh-cli.repo
      args:
        executable: /bin/bash
      changed_when: true

    - name: Install Github Client
      become: true
      ansible.builtin.dnf:
        name:
          - gh

    # https://github.com/newren/git-filter-repo
    - name: Install DNF Git Filter Repo
      become: true
      ansible.builtin.dnf:
        name:
          - git-filter-repo

    - name: Git Aliases
      ansible.builtin.blockinfile:
        path: /home/{{ user_login }}/.bashrc
        marker: "# {mark} ANSIBLE MANAGED: Git Aliases"
        block: |
          alias gu="git push"
          alias gd="git pull"
          alias gs="git status"
          alias c="git commit"
      tags: alias
