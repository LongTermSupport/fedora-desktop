# Plan 00040 Journal: 2026-09-02

## 22:10 · action · — — plan slimmed; old PLAN.md archived verbatim

PLAN.md had grown to 49 KB, past the 35 KB hard limit, because it carried the
full decision-gate questionnaire, nine technical decisions, a risk table and
seven dated "Notes & Updates" iteration blocks. The old file was moved with
`git mv` to [PLAN_archive.md](../PLAN_archive.md) and kept verbatim, with one
archive banner line at the top. A new lean PLAN.md replaces it with the same
status line, the task tree with every tick preserved as it was, the success
criteria and a Delivery & Milestones stub.

Durable material moved into [DECISIONS.md](../DECISIONS.md): the locked
defaults table, the decision-gate answers, Decisions 1 to 10, the `--json`
schema, the review-finding tags (H1 to L6) referenced by the task tree, and the
risk register.

Dated narrative that only existed in the old PLAN.md now lives in
PLAN_archive.md under "Notes & Updates". A resumer should read those sections
there rather than expect them here:

- 2026-05-20 first to third iteration: two-axis design, then the weighted-score refinement, then the subagent solidity review and how each finding was absorbed.
- 2026-05-20 fourth iteration: Phases 3 to 7 code landed in the CCY container; Phase 2 host probes and Phase 8 manual tests deferred to the host.
- 2026-05-20 fifth iteration: Lightroom catalog safety rail removed (no Lightroom on Linux).
- 2026-05-20 sixth iteration: silent-compute fix (`submit` + `as_completed`) and the black-cutoff default tightened from 1.05 to 1.01 after a 347-frame calibration run.
- 2026-05-20 seventh iteration: the real bug, roughly 10% of A7V pixels at literal value 0 gave every frame a 10% bclip baseline; fixed by excluding sub-`black_level` pixels from the ramp. Flagged frames dropped from 347/347 to 33/347. Test suite at 45 passing in a `/tmp` venv.

Known drift carried over unchanged: the task tree ticks only the Phase 3
test-file task, while the fourth-iteration notes say Phases 3 to 7 landed. That
inconsistency predates the slimming and was not resolved here, because the
instruction was to preserve tick state faithfully. Reconciling the ticks against
the code is the next plan-hygiene step.

## 22:45 · action · — — task tree reconciled against the landed code

Evidence checked: `files/home/.local/bin/clip-scan` (897 lines),
`tests/clip_scan/test_clip_scan.py` (45 tests across nine classes) with its
`conftest.py`, the `tags: clip-scan` block in `play-photography.yml`
(lines 228-265, summary lines 285-288 and 309), and the plan's four
implementation commits d7b0d9c0, c5b61b8c, 9e87fd0e, a09d7b3e.

Verdicts written into PLAN.md:

- Phase 1 both ticked: the `execute` directive accepted the defaults, recorded in DECISIONS.md.
- Phase 2 stays open (host probes). The rawpy install-path item is 🔄 because the playbook encodes the probe-then-branch at runtime; the host result is still unrecorded.
- Phase 3 fully ticked: every listed test case maps to a named test, and the implementation functions exist (the shipped names are `parse_clip_flags`, `score_white`, `score_black`, `extract_channel_pools`, not the planned spellings).
- Phases 4 to 6: every implementation item ticked with a `clip-scan` line reference. The nine "write failing test" items for the rawpy end-to-end path, corrupt-ARW fallback, JPG fallback, dry-run, sibling rename order, collision, `--json` schema, pool execution and `--jobs 1` stay ⬜: no test in the suite drives `main()`, `_analyse_group`, `analyse_jpg`, `execute_renames` or `plan_rename_order`.
- Phase 7: three install/deploy/summary items ticked; the idempotency verification stays ⬜ (host).
- Phase 8: `./scripts/qa-all.bash` ran clean in the container today (bash, python, ansible-syntax, docs, helper-tests). Manual tests, benchmark, calibration and live workflow stay ⬜.
- Phase 9: `--help` and the summary line ticked; the optional `docs/` paragraph is 🚫 because no photography workflow guide exists in `docs/`.
- Success criteria: dry-run listing, sibling-grouped apply, no-op re-run, threshold re-run, qa-all and decision-gate documentation ticked. Performance, Lightroom filter, playbook idempotency stay ⬜; coverage is 🔄 (never measured).

Test suite not re-run: the CCY container has no `pytest` and no `numpy`
(`python3 -m pytest tests/clip_scan` fails with "No module named pytest").
The last recorded run is the 45-pass result of 2026-05-20 in a `/tmp` venv
(PLAN_archive.md, seventh iteration). No playbook installs pytest or numpy for
the container, so running this suite in CCY is an IaC gap to raise separately.

Status header stays In Progress: the missing integration tests can be written
anywhere, so the remaining work is not host-only.
