# Plan 00042 — Journal 2026-09-06

> **Append-only activity log** for plan 00042. One file per day
> (`00042-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:20 · finding · — — darktable 5.6.1 carries the A7V natively

The `darktable-5.6.1.tar.xz` release tarball bundles a rawspeed
`cameras.xml` whose `SONY / ILCE-7M5` entry is byte-identical to the overlay
this plan pinned from rawspeed `aa9028a` (CFA, crop, sensor black/white,
colour matrix). Fedora 44 dist-git is at `5.6.1-2.fc44`, currently in
`updates-testing` (stable is still `5.4.1-1.fc44`). No patch or local rebuild
is needed once 5.6.x is installed.

## 10:25 · decision · — — plan cancelled; AI playbooks removed

The user asked to drop the pre-release AI path ("it never really worked").
Removed `play-darktable-ai-build.yml`, `play-darktable-ai-appimage.yml`,
`play-darktable-ai-gpu.yml` and `files/usr/local/bin/darktable-ai`.
`play-photography.yml` now sets `darktable_min_version: 5.6.0`, upgrades from
`updates-testing` while the installed version is below that floor, asserts
the floor after install, and removes the retired `/opt/darktable-ai` install,
launcher, desktop entry and user-local GPU ONNX Runtime. CUDA and cuDNN stay
in `play-nvidia.yml` as a general capability. The darktable row was dropped
from `scripts/check-pinned-versions.bash` since there is no pin any more.

## 10:30 · handoff · — — deploy on host

Host must run `playbooks/imports/optional/common/play-photography.yml`. The
locally built `darktable-5.4.1-100.ai` RPM is superseded by version ordering
(5.6.1 > 5.4.1) so dnf upgrades it away. Confirm an A7V `.ARW` opens.
