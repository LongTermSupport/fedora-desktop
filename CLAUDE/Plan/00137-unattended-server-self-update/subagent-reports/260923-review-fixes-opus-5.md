# Plan 00137 — the fixes for qa-reviewer round 1 (BLOCK)

The task: fix every finding of the full-diff qa-reviewer (round 1, BLOCK), test first. Task 5.2 is not ticked.

## By finding

| #   | Finding                                        | Fix                                                                                                                                                                                                                                                                              | Proof                                                                                                                                                                                                  |
| --- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | BLOCK: the first cycle trusted an unsigned tip | `update.py --anchor` (the play moves the clone to the newest pinned-signed commit, or fails); the sbin wrapper checks HEAD's signature and a clean tree before any import (else 20); `run_cycle` refuses an untrusted HEAD when nothing is deployed (20, phase `trust`, alerted) | `TestAnchor`, `TestVerifyHead`, the cycle trust tests; e2e: an unsigned tip with fresh state runs nothing; the real `--anchor` then moves it and a dry run passes; a tampered signed commit is refused |
| 2a  | The become password went to a mktemp file      | run.bash 1.25.0 pipes it to `sudo -S` and `ansible-playbook --become-password-file -`                                                                                                                                                                                            | `test-run-bash-single-play.bash`: the fake ansible sees a FIFO, and there are no temp copies                                                                                                           |
| 2b  | Plugin search paths under `~/.ansible`         | `play_environment` pins all 19 ansible-core 2.19 search-path env vars, sets `PYTHONNOUSERSITE=1`, and names the clone's `callback_plugins` and `roles/vendor` so they still match ansible.cfg                                                                                    | `test_no_code_is_searched_for_under_the_users_home` and its siblings; the e2e env log shows no path under `$HOME`                                                                                      |
| 2c  | Untrue claims                                  | PLAN D5, `docs/playbooks.md` and the `cycle.py` docstrings now state the residual: files a play writes as the user                                                                                                                                                               | —                                                                                                                                                                                                      |
| 3   | A stale index row, and the Overview order      | Both corrected                                                                                                                                                                                                                                                                   | —                                                                                                                                                                                                      |
| 4   | Arguments and sudoers                          | Four exact sudoers argument lists; acceptance [0] probes each one and a refused extra argument                                                                                                                                                                                   | e2e: five extra-argument cases, each 64 with nothing called                                                                                                                                            |
| 5   | Refusal cases through the real wrapper         | e2e: a wrong remote and a gate refusal, both 20 and recorded                                                                                                                                                                                                                     | `test-self-update-cycle.bash`                                                                                                                                                                          |
| nit | The "no paths" claim                           | DESIGN-cycle.md and the `cycle.py` docstring corrected                                                                                                                                                                                                                           | —                                                                                                                                                                                                      |
| nit | Readable secrets                               | `read_private(..., secret=True)` refuses group or other permission bits on the become and vault files                                                                                                                                                                            | `TestPrivateFile`; e2e: a 0640 vault gives 70 with nothing called                                                                                                                                      |

## Gaps

- `qa-all.bash` stops in this worktree: there is no `vault-pass.secret` and no `extensions/node_modules`.
  - Every other gate passed when run on its own, the syntax gate with a dummy vault password file.
  - `qa-js` was not run; this change touches no JS.
- `visudo` is not in the container. The sudoers template is checked by the play's `validate: visudo -cf %s` at deploy.
- The HOST proof is still Task 5.3.
- The search-path list is ansible-core 2.19's. A later release that adds a search path must be added to `cycle.py`; DESIGN-cycle.md says so.
