# Plan 00137 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00137. One file per day
> (`00137-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:45 · action · — — plan scaffolded

Plan 00137 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 15:34 · action · P1   — updater, trust gate and path-to-play map merged

T1.1 + T1.2 (b1407d48) and T1.3 (b8714c34) were built in parallel worktrees and merged.
Notes: `subagent-reports/260923-t11-t12-opus-5.md` and `260923-t13-opus-5.md`.

- `helpers/self_update/update.py` + `gate.py`: fetch, refuse unsafe checkout states with
  distinct exits (11–19), then fast-forward to the newest commit signed by the pinned
  principal. Unsigned commits and other keys are passed over; a tampered or revoked
  signature refuses the run. Every git call pins, on the command line, the allowed-signers
  file, the verifier programs, hooks off, fsmonitor off, and no `ext::`. So a hostile
  commit's repo config cannot change the gate.
- Two deviations from the dispatch, both accepted:
  - the Fedora pin is checked on the TARGET commit before the fast-forward, so a refusal
    never leaves the clone on an unrunnable commit;
  - untracked files count as a dirty tree.
- Left for Task 4.6: the remote URL and `core.sshCommand` still come from clone config.
  That is safe only once the deploy clone is root-owned and never mounted into a
  container.
- `helpers/self_update/affected_plays.py` + `unattended-plays.json` (seeded with
  `play-claude-yolo.yml`). Zero UNRESOLVED references across every play at HEAD. Known
  over-reach: a templated prefix in `play-basic-configs.yml` counts all of `files/`, so
  lib changes also print SKIP-NOT-ALLOWED lines for it. That is harmless while it is not
  allowlisted.

`qa-all.bash` is green on the merged tree in the main checkout. Next: Task 1.4 (the
shared lock) and Phase 2 (the headless single-play runner).

## 15:58 · action · P1-P3   — lock, headless single-play and restore-without-prompts merged

T1.4 + T2.1 (e060240c) and T3.2 (7afb4b3e) merged; qa-all green in the main checkout.
- T1.4: `helpers/play_lock`, flock on `$XDG_RUNTIME_DIR/fedora-desktop-plays.lock`; exit 75 names the holder; fd hand-off to children via FEDORA_DESKTOP_PLAY_LOCK_FD.
- T2.1: `run.bash --headless <play>.yml` (1.22.0) runs one play with only a sudo preflight; become password via /dev/fd/N; takes the lock.
- T3.2: CCY 3.61.0. A restore answers only Quick Launch (Y), the zombie menu (ignore) and the existing-container menu (carry on); every other prompt waits in the pane. New `ccy-sessions verify-restore [--wait]` reports OK / STARTING / WAITING-AT-PROMPT / DEAD, from prompt constants shared with the code that prints them.
HOST-only: runuser keeping the password fd open; a real password-sudo run; a real concurrent run.
Wrote DESIGN-cycle.md as the contract for the next two parallel pieces: the orchestrator (T3.3, T3.4, T4.1) and the IaC (T0.3, T2.2, T4.2, T4.6).

## 16:15 · decision · D5   — IaC merged; D5 hardened: system ansible + ptrace_scope=1

IaC (27f2cff8) merged: T0.3, T2.2, T4.2, T4.6. Opt-in play-self-update.yml (server scope, self_update_enabled). Commit signing lives in play-git-configure-and-tools.yml, written to the XDG git config ccy does not copy, with no commit.gpgsign default and a passphrase required, so agents can never sign.
The IaC review found that D5 does not hold against a same-uid process: the plays run as the user through the user's own Ansible, and the controller holds the password in memory (ptrace_scope is 0). The owner chose to trust the user account and add cheap hardening: a root-owned system ansible-core and collections path for the cycle, plus kernel.yama.ptrace_scope=1 on the server (new Task 4.7). Controller-as-root was rejected, because every play assumes it starts as the user.
The orchestrator fork was sent three contract additions from the IaC: the vault password on /dev/fd/N, the fact-cache override, and the system ansible and ANSIBLE_COLLECTIONS_DIR requirement.

## 16:40 · action · T3.3 T3.4 T4.1 T4.7 — orchestrator and D5 hardening merged

T4.7 (dcaf84a0) and the orchestrator (e1f9fe61, with run.bash 1.22.1 b1ccfbea) merged; qa-all green, including `test-self-update-cycle.bash` against a real signed repository.
- T4.7: dnf `ansible-core` plus a root-owned collections path, stamped by the requirements digest and read back as not group- or world-writable; `kernel.yama.ptrace_scope=1` in a sysctl drop-in, read back live. The config template gained `ANSIBLE_COLLECTIONS_DIR`.
- Orchestrator: `fedora-desktop-self-update run|run --dry-run|verify|status`, in `helpers/self_update/cycle.py`. Contract changes: the vault and become passwords travel as a pipe whose ownership is changed to the user, fresh per play, because Ansible reopens `/dev/fd/N` by path and a root-only file or pipe is refused; `ANSIBLE_CACHE_PLUGIN=memory`; run.bash 1.23.0 `RUN_BASH_ANSIBLE_PLAYBOOK` pins the ansible-playbook a headless run resolves. Added exit codes 24 (reboot refused), 130 (countdown cancelled) and 77 (not root). A `deployed` state file is the diff basis, and the first run executes every allowlisted play.
- HOST-only, still unproven: the lock handed from root to the user, `ccy-sessions` reaching the user's tmux server through `runuser`, a real play as the user from the root-owned clone, and a real reboot followed by `verify`.
Downstream review of the rollout (a private infrastructure repo) found that a guest provisioned through `RUN_BASH_*` variables has no route for `ccy_restore_sessions` or the `self_update_*` inputs. It also found that most of its guests have no GitHub identity, so an issue-filing sink cannot work there, and a status-file sink the host reads fits them. Both feed Tasks 0.4 and 4.5.

## 16:55 · action · rollout — RUN_BASH_CCY_RESTORE_SESSIONS (run.bash 1.24.0)

The self-update cycle needs `ccy_restore_sessions: true`, and a guest provisioned only through `RUN_BASH_*` had no way to declare it. `RUN_BASH_CCY_RESTORE_SESSIONS=0|1` now reconciles exactly one `ccy_restore_sessions:` line into `localhost.yml` on every headless run. It is written to the file, not passed as an extra-var, because a later single-play or self-update run of `play-claude-yolo.yml` decides the unit's state again without seeing the variable. Unset leaves the file alone. Ten new cases in `test-run-bash-headless-localhost-yml.bash`. The `self_update_*` inputs have the same gap; they wait on the Task 0.4 sink decision and the pin-vs-update decision downstream.

## 17:30 · action · T4.3   — the cycle's result reaches the host-health report

Every result the cycle records is also published to `/var/lib/fedora-desktop/self-update-status/result`, as the result keys plus `owed_boot` (`helpers/self_update/published.py`). The directory is `root:<user's group>` 2750, and the file is 0640, written atomically. The play creates the directory when self-update is enabled and removes it when not. `state/` stays 0700, because `deployed` and `owed-verify` decide root's next run.
The report's new `self-update` section (`helpers/host_health/self_update_check.py`) finds:
- a failed outcome;
- no result for more than 3 days, or no result at all 3 days after the directory was created;
- a post-boot check owed by an earlier boot once this boot is 35 minutes old.
An unreadable record, stamp or boot id is "not checked". No directory means nothing to say.
The panel hides the section while it is clean (`quietWhenOk`), because a desktop never self-updates. A missing section still renders as unavailable.
A test reads `cycle.py`'s source and fails on an outcome the reader has not classified. The entry point refuses (70) without the directory.
Tests: `test_published`, `test_self_update_check`, `test_cycle` (published copy), `test-self-update-cycle.bash` (87), and the panel sections tests (64). qa-all is green in the worktree, run with a temporary `extensions/node_modules` symlink and a dummy vault key file for the syntax gate.
HOST-only: the setgid group on a real `/var/lib`, and the user's report reading it.

## 17:40 · action · T4.3 T4.4 — host-health reads the cycle; deploy and acceptance scripts

T4.3 merged: the cycle publishes a user-readable copy of each result to `/var/lib/fedora-desktop/self-update-status/result` (dir root:<group> 2750, file 0640). The report's `self-update` section reports these cases:
- a failed outcome;
- no result within 3 days;
- a post-boot check still owed from an earlier boot;
- anything it cannot read, reported as not checked.

T4.4: `deploy.bash --role desktop|server` and `acceptance.bash`, which makes 10 read-only checks on the server:
- the sudo grant is exactly one command;
- the secrets are root-only;
- the toolchain is root-owned;
- ptrace_scope is 1;
- the units are enabled;
- the restore unit is pulled in;
- the clone is readable;
- the published directory has the right shape;
- a real `run --dry-run` and `status` both answer.

A real cycle, an unsigned commit being ignored, and a failed play stopping the reboot are left to T5.3 by hand, and listed as not establishable.

## 18:00 · action · T5.1   — operator docs

`docs/playbooks.md` already said what the cycle does. `docs/configuration.md` now adds the day-to-day operation:
- the trust model: the signed tip vouches for everything below it;
- pausing: stop signing; `self_update_enabled: false` turns it off;
- the schedule: 03:30 plus a random delay of up to 30 minutes, with `Persistent=true`;
- reading it: `status`, `run --dry-run`, the two units' journal, and `fedora-desktop-health`.
Next: T5.2, the full-diff qa-reviewer.

## 18:35 · action · T5.2   — round 1 fixes (the reviewer's verdict was BLOCK)

**The first cycle trusted an unsigned tip.** The gate judges only commits above HEAD, and a fresh clone's HEAD was whatever the branch tip was. The rule now is that HEAD is always a commit the pinned key signed, held at three layers:
- the play's new task runs `update.py --anchor`. It moves the clone back to the newest pinned-signed commit on the branch, or fails the play;
- the sbin wrapper checks `%G?:%GS` of HEAD and a clean `git status` before it imports anything from the clone, and exits 20 otherwise. The signing programs are pinned with `-c`;
- `run_cycle` refuses with no deployed record and an untrusted HEAD. The refusal is exit 20, recorded as phase `trust`, and alerted.

Import-before-verify is written up in DESIGN-cycle.md. In short, the wrapper uses only git and ssh-keygen before HEAD is judged, and a separate root-owned helper install was rejected. The e2e test starts from an unsigned tip with no state and shows that nothing runs. It also covers a tampered signed commit.

**D5 claims.**
- run.bash 1.25.0 passes the become password to sudo (`-S`) and to ansible (`--become-password-file -`) through pipes. A `/dev/fd` path does not work for ansible here: the CLI option applies realpath to it and gets a pipe name that does not exist.
- `play_environment` pins every ansible-core 2.19 plugin and role search path that defaults under `~/.ansible` to its `/usr/share/ansible` half. The list comes from `base.yml`, read from the installed ansible-core.
- The callback path leads with `<clone>/callback_plugins`, so the play ledger still loads. The roles path is `<clone>/roles/vendor`, matching ansible.cfg.
- `PYTHONNOUSERSITE=1` is set.
- PLAN.md D5, `docs/playbooks.md` and the `cycle.py` docstrings now say what stays open: files a play writes as the user, such as `~/.ansible/tmp`.

**The rest.**
- sudoers is four exact argument lists, and acceptance check [0] probes each one and a refused extra argument.
- The e2e test covers extra arguments (64, nothing called), a wrong remote and a gate refusal (both 20, recorded).
- `cycle.py` refuses a become or vault file that group or others can read.
- The plan index row and the Overview's step order were stale and are corrected.

QA:
- Every qa-all gate passed when run on its own: 43 gates, including `test-self-update-cycle.bash` (119) and `test-run-bash-single-play.bash` (51).
- `qa-js` was not run, because this worktree has no `extensions/node_modules`.
- The syntax gate used a dummy vault password file.
- `visudo` is not in the container, so the sudoers template is validated only by the play's `validate:` at deploy.

Next: a second qa-reviewer round. Task 5.2 stays open until it passes.

## 19:16 · action · T5.2   — round 2 fixes (the reviewer's verdict was BLOCK)

The condensed round-2 report is `subagent-reports/260923-qa-reviewer-round2-opus-5.md`, and the fix notes are `260923-review2-fixes-opus-5.md`.

**Ignored files in the clone reached root.** `git status` never lists ignored files, and Python imports a `.pyc` beside its source without checking it. The red e2e run showed that the wrapper itself was writing `__pycache__` into the clone. The fixes:
- The wrapper now runs `git ls-files --others` without `--exclude-standard`, which lists ignored files and a nested repository as well. It refuses (20) anything except the host_vars copy.
- `update.py --anchor` does the same (`--allow-untracked` names the exception), both before and after it moves HEAD.
- The play clones with `recursive: false` and runs the anchor with `-B`.
- Root runs `python3 -E -s -B -X pycache_prefix=<state>/pycache`.
- The child plays need nothing further, and DESIGN-cycle.md says why.

**Search paths are now judged by rule, not by list alone.** `check_toolchain` runs the system `ansible-config dump --format json` as the user, from the clone, with the plays' env. It refuses (70) if any `*PATH*` list element, or the inventory, is under the home. A real ansible-core 2.19 dump taught it two things: the dump ends with a `{"GALAXY_SERVERS": {}}` entry, and `INVENTORY_IGNORE_EXTS` holds `"~"`. Both are handled, with tests. Measured on 2.19: the unpinned dump has 18 findings and the pinned one has none. The e2e stub's dump adds a new search path under the home, and the cycle refuses it.

**Nits.**
- The wrapper refuses (70) an allowed-signers file that is a symlink, not a regular file, or writable by others, and does the same when its directory is writable by others.
- Acceptance check [0] passes a failed `sudo -n true` only when sudo says a password is required or the user may not run it. Any other failure is COULD NOT ESTABLISH.
- Check [2] also covers `ansible-config`.
- The password test searches TMPDIR, HOME and XDG_RUNTIME_DIR, and its claim now says so.

QA: `qa-all.bash` is fully green, 45 gates, including `self-update-cycle` (136) and helper-tests (2070). It ran with a temporary `extensions/node_modules` symlink, removed afterwards, and a dummy vault password file.

Next: a third qa-reviewer round. Task 5.2 stays open until it passes.

## 19:36 · action · T5.2   — round 3 fixes (the reviewer's verdict was FIX-BEFORE-MERGE)

The round-3 report is `subagent-reports/260923-qa-reviewer-round3-opus-5.md`, and the fix notes are `260923-review3-fixes-opus-5.md`. Round 3 found no route left for unsigned code to reach root.

**The two dirty-clone refusals named a remedy that cannot work.** The play clones with `update: false` and the anchor refuses a dirty clone (11), so "re-run the play" never clears them. Both now say to run the play once with `self_update_enabled: false`, which removes the clone, then once with it true. `docs/configuration.md` has the same note, and `docs/playbooks.md` points at it.

**The host_vars exception is an exact path.** The wrapper's `--exclude` pattern also excused a directory of that name and everything in it. It now reads `git ls-files --others -z` and compares exact paths, as the anchor does. The new e2e case, a directory at that path holding a file, failed against the old wrapper (exit 0) and passes now (20, naming the file).

**The search-path check trusts no setting name.** Every list-valued setting is judged. Dropping the name filter exposed a false finding the old filter had hidden: `INVENTORY_IGNORE_EXTS` and `MODULE_IGNORE_EXTS` hold a bare `~`, a backup suffix. ansible expands `~` in every path-typed value before dumping it, so a bare `~` is no longer read as the home; `~/…` still is. Against a real ansible-core dump, unpinned, the findings are exactly the 18 `~/.ansible` search paths, as before.

**Nits.**
- Acceptance check [0]'s extra-argument probe passes only when all four exact lists were allowed. Otherwise it is COULD NOT ESTABLISH.
- DESIGN-cycle.md says the pycache prefix does not stop a sourceless `.pyc`; only the stray-file check does.

Next: a confirming qa-reviewer round. Task 5.2 stays open until it passes.

## 19:56 · verdict · T5.2   — round 4 PASS WITH NITS; both nits fixed; Task 5.2 done

The round-4 report is `subagent-reports/260923-qa-reviewer-round4-opus-5.md`. It confirmed all three round-3 fixes against real ansible-core 2.19.13. `~`, `~user` and `~/../x` are all expanded before the dump in every path-typed setting, so the bare-`~` exemption cannot hide a home path. It found no route for unsigned or home-planted code to reach root.

**Nit 1, the string-setting wording.** `cycle.py` and DESIGN-cycle.md said a string-valued setting is never a place code is read from. `DEFAULT_LOCAL_TMP` holds the module cache, so that was wrong. Both now say it is not judged because ansible makes a fresh directory under it for each process, so nothing can be planted before a run. Tampering during a run is D5's caveat.

**Nit 2, the untested error branch.** A new e2e case puts a `git` stub on the wrapper's test PATH that fails on `ls-files`. With the wrapper's `wait "$!"` check disabled, two of its checks failed. With the check restored, it passes: exit 20, nothing called, and the message names the failure.

Task 5.2 is ticked. Next: Task 5.3, a real cycle on the server (host).
