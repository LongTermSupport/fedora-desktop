# Plan 00112 — Journal 26-09-14

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 08:55 · action · P1 — Phase 1 landed: the enabled list is declared state

The fix is two helpers and three task edits in `play-gnome-shell-extensions.yml`.

`helpers/gnome/enabled_extensions.py` is the pure half. It parses the GVariant `as`
literal `gsettings get` prints (`@as []`, `[]`, `['a', 'b']`, escapes and all), merges the
deployed UUIDs into it **additively** — nothing the user enabled is ever removed — formats
the result back, and discovers which UUIDs are deployed by reading each `metadata.json`
rather than trusting the directory name. A disagreement between the two is an error, not a
skip: GNOME Shell refuses to load such an extension, and skipping it would hand the play a
short list that then passes as "everything deployed is enabled". Same reasoning for a
missing `uuid` key, unparseable JSON, and a UUID containing a comma (the executor reports
the list comma-separated; one would silently become two).

`helpers/gnome/apply_enabled_extensions.py` is the side-effecting half, and the part that
had to be got right:

- **Session bus resolution.** `DBUS_SESSION_BUS_ADDRESS` if set, else the user's
  `$XDG_RUNTIME_DIR/bus` socket, else `dbus-run-session --`. The live socket means the
  running shell sees the change immediately; the fallback means a `run.bash` from a TTY
  still lands the value in the user's dconf database. That fallback is what lets the
  `failed_when: false` go — the no-session case is no longer a failure to tolerate.
- The socket is tested with `os.access`, not `os.path.exists`. `sudo -u` can leave a stale
  `XDG_RUNTIME_DIR` pointing at another user's `0700` runtime directory; the socket is
  visible there and unusable, and falling back succeeds where using it would fail.
- **It re-reads what it wrote.** dconf can accept a `set` and keep the old value (locked or
  read-only database). Without the read-back this task would report ok while the session
  came up with nothing enabled — Plan 00110's finding reintroduced one layer down, which is
  precisely the "check that cannot fail" class the 00110 review found three of.

In the play:

- The `Enable Workspace Names in Overview Extension` task is gone, and its
  `failed_when: false  # FAIL-FAST-OK: GNOME session may not be active` with it.
- `Declare Deployed Extensions Enabled` calls the applier once, after the schemas are
  compiled and the custom extension is copied. `--require workspace-names-overview@fedora-desktop`
  pins the copied extension, so a failed copy fails here instead of reading as "seven
  deployed, all enabled". `changed_when` keys on `GNOME-EXT-ENABLED-CHANGED`.
- `Verify Deployed Extensions Are Healthy` loops over **every** deployed UUID instead of
  the one custom extension it covered before. The list comes from the applier's
  `GNOME-EXT-DEPLOYED` marker, not from re-deriving the play's own literals, which would
  drift the moment an id is added to the installer loop. `extension_state.classify` is
  untouched, so the pending-Wayland-reload rule still holds and `INITIALIZED` still passes
  the play — demanding `ACTIVE` here would fail provisioning for a shell that has not
  rescanned yet. `ACTIVE` is what the desktop scenario demands, after the reboot, which is
  the honest place for it.

Task 1.4 needed no change: `run.bash`'s closing "System Reboot" step recommends a reboot on
every path — the interactive prompt, and both headless messages — so it already covers a
run where this play changed the list.

51 new unit tests, `qa-all.bash` green (793 files, 730 helper tests).

## 09:00 · thought · P2 — dash-to-dock is deliberately out of scope, and the run will say whether that is right

The play installs `gnome-shell-extension-dash-to-dock` from DNF, which lands in
`/usr/share/gnome-shell/extensions`, not the user directory — so the applier does not see
it and the desktop scenario's `deployed-extensions-active` check does not count it. Two
tasks later the play writes dash-to-dock's `intellihide-mode` dconf key, which does nothing
unless the extension is actually enabled.

That is plausibly the same defect in a second place, but it is not established: nobody has
read that session's `enabled_extensions_live`. Plan 00110's desktop scenario prints it as
evidence on every run, so the acceptance run below will answer it as data rather than
inference. Widening the applier to system extensions before that would be a guess.

## 09:05 · action · P2 — plan scripts, and the acceptance run requested through the bridge

`triage.bash` + `probe-gnome-extensions.bash` (read-only, host-only) and `deploy.bash`, both
on `_planlib.inc.bash`. The triage exists so Task 2.1's "nothing was removed from the host's
list" is a diff of two recorded reports rather than a claim: run it, deploy, deploy again for
idempotency, run it again. Its section 4 lists the system extensions and their live State,
which is where the dash-to-dock question gets answered on the host.

Phase 1 pushed as `cb88ec4e`. `vmtest run desktop-fresh-install` requested through the
Plan 00110 bridge as run `20260914T085408Z-desktop-fresh-install` — the scenario provisions
the branch tip **on the remote**, so the push had to come first. This is the first time a
plan's acceptance test has been driven from inside the container: no operator, no
credentials, one allowlisted verb.

## 09:10 · finding · P1 — the check-mode preview my own change would have crashed

`deploy.bash --check` advertises a preview that changes nothing. It would have died instead.
`ansible.builtin.command` has no check mode, so under `--check` the applier is skipped and
never prints its `GNOME-EXT-DEPLOYED` marker — and `Collect Deployed Extension UUIDs` then
dereferences `gse_enabled.stdout_lines` on a skipped task's result, which is an undefined
attribute, not an empty list. The play aborts before it previews anything.

Nothing in the repo would have caught this: `--syntax-check` passes, `qa-all.bash` passes,
and the desktop scenario never runs `--check`. It was found by reading the deploy script's
own promise back against the play.

The `set_fact` is now `when: not ansible_check_mode`, and the verify loop iterates
`gse_deployed_uuids | default([])` — under `--check` there is nothing this run deployed to
verify, and saying so is honest where inventing a list would not be. Both carry the reason
inline; the comment explains why the guard exists, not what it does.

## 09:24 · finding · T2.2 — the acceptance run PASSES, 16/16, and its evidence settles the dash-to-dock question

Run `20260914T085408Z-desktop-fresh-install`, requested through the bridge from inside the
container, provisioned from `cb88ec4e` and judged in the session after the reboot:

```
finished: pass
checks: planned=16 total=16 passed=16 failed=0 skipped=0
VMTEST-CHECK pass deployed-extensions-active COVERAGE: 8 of 8 declared ACTIVE (8 on disk)
```

`deployed-extensions-active` is green for the first time. The eight the repo installs are all
`ACTIVE` in the post-reboot session, and `enabled_extensions_settings` holds them all. The
defect Plan 00110's lab found on 2026-09-13 is fixed, and the same lab is what says so.

**And the same evidence closes the dash-to-dock question the 09:00 entry left open — against
that entry.** From this run:

```
system_extensions=...,dash-to-dock@micxgx.gmail.com,
enabled_extensions_live=appindicatorsupport@...,background-logo@...,blur-my-shell@...,
                        clipboard-indicator@...,just-perfection-desktop@...,space-bar@...,
                        tilingshell@...,Vitals@...,workspace-names-overview@fedora-desktop,
```

dash-to-dock is installed and **not** in the enabled list. So `Configure Dash to Dock - Hide
Behind Any Overlapping Window` has been writing `intellihide-mode` for an extension that is
not running: Plan 00102's fix, recorded as accepted, is a no-op on every fresh install. This
is the same defect in a second place, exactly as suspected — now established rather than
suspected.

## 09:26 · correction · P2 — the 09:00 entry was wrong on its facts, not just its conclusion

That entry deferred dash-to-dock on the grounds that "nobody has read that session's
`enabled_extensions_live`". Somebody had. Plan 00110's journal of 2026-09-13 records it twice
(`00110-Journal-26-09-13.md:1546` and `:1576`), read through both `gnome-extensions list
--enabled` and the settings key: the list held **only** Fedora's stock background-logo. System
extensions are enabled through that same key, so dash-to-dock was already known to be off.

The reasoning in that entry — do not widen on a guess, make the run answer it — was right. The
premise was not: the answer already existed in a plan I had read that morning. Wanting a fact
to be unestablished is not the same as it being unestablished, and the check was one grep away.

## 09:40 · action · P1 — the review's three blocking findings fixed; the deployed set is now DECLARED

`qa-reviewer` over `37818e65..e1591a08` returned FIX-BEFORE-MERGE with three blocking findings.
All three were real. The acceptance run above passed **with all three present**, because a
fresh VM has no user-installed extensions and `disable-user-extensions` is false there — so the
pass proved the mechanism, never the scoping. Worth stating plainly: a green acceptance run is
not a substitute for the review.

**1. The population was discovered, not declared.** `discover_deployed_uuids` enumerated
everything under `~/.local/share/gnome-shell/extensions` — which is also where the *user's* own
extensions live. Three consequences, all introduced by my change: the verify loop went from one
repo-owned UUID to every directory there, so one stale third-party extension in `ERROR` would
abort the whole provisioning run; an extension the user deliberately disabled got re-added on
every deploy, applying "never removes what the user added" in one direction only; and
`--require` pinned one of eight, so six-of-seven downloaded still read as complete success.
That last one is this repo's "guard the empty case, miss the partial" shape verbatim —
`nothing-deployed` guarded zero and nothing guarded partial.

Fixed by declaring the set. `vars/gnome-shell-extensions.yml` is now the single source, read by
the play, the guest acceptance check and the secret scanner. `resolve_declared` replaces
`discover_deployed_uuids`: disk **confirms** the declaration and a declared UUID that is not
there is `missing`, never silently dropped. An undeclared extension on disk is ignored.

**2. dash-to-dock.** Added to the declared set, with `/usr/share/gnome-shell/extensions` as a
second search path — it is enabled through the same key, so nothing else was needed. The guest
check now covers it too, which is what will prove it.

**3. `disable-user-extensions` was neither declared nor asserted.** That key true defeats every
user extension whatever the list holds, and `classify` returns OK for any state that is not
`ERROR` or `OUT OF DATE`. The whole path could have gone green over a session with nothing
enabled — the Plan 00110 outcome one key over. The applier now reads it before writing anything
and fails naming the remedy.

Also fixed: `| default([])` alone made the verify gate vacuous for any future reason the fact
goes unset, so an explicit assertion now holds the collected count against the declared one
(`loop` is templated before `when`, so the check-mode guard cannot live on the verify task);
`verify_extension` searches a list of directories and no longer tells you to run the "Deploy
Custom Extension" step for seven extensions that step never touches; the probe prints the error
text it captured instead of narrating a cause it did not confirm; the docs said the play writes
every extension it deploys, which was wrong in both directions.

**The secret scanner needed a change to allow this at all**, and the owner chose its shape: a
GNOME extension UUID is indistinguishable from an email address (`Vitals@CoreCoding.com`), so
five of the nine were rejected by the pre-commit hook. The allowlist is **derived** from
`vars/gnome-shell-extensions.yml` at scan time rather than hand-kept in the hook, so adding an
extension is one edit and not two. Only a value under a `uuid` key in that one tracked file is
ever exempt, each as an anchored whole-token literal. Proven both ways before committing: the
nine UUIDs pass, a real address is still flagged, and a line holding both still reports the
real one — per-token filtering, not per-line, which is the Plan 00066 leak this lib already
carries a warning about.

## 11:19 · finding · T2.2 — the acceptance run passed 16/16 and its extension check had judged almost nothing

`20260914T100220Z-desktop-fresh-install`, verdict pass, 16 of 16, against `d307ed28`. The line
that matters:

```
VMTEST-CHECK pass deployed-extensions-active COVERAGE: 8 of 1 declared ACTIVE (8 on disk)
```

Nine extensions are declared. The check expected **one**. Eight were active, one is not more
than eight, and it passed.

I nearly filed this run as Task 2.2's evidence on the strength of `pass 16/16`. The verdict line
is not the check; the coverage string is, and reading it is the only reason this was caught.

**What happened.** `vmtest` scp's the guest checker from the **host's deployed copy** at
`~/.local/share/vmtest/guest-acceptance-desktop.bash` (`files/home/.local/bin/vmtest:869`,
`:958`) — never from the guest's own checkout, though the guest is provisioned at the tested
commit and carries the file. The host's copy predates this plan. Its expected count came from
grepping the **play** for `id: <n>` lines, and Task 1.5 moved those declarations into
`vars/gnome-shell-extensions.yml`. The grep found nothing, `expected` became `0 + 1`, and the
check that Task 1.5 was written to strengthen was the check Task 1.5 disarmed.

Two details confirm which script ran rather than inferring it: the run emits
`VMTEST-EVIDENCE deployed_extensions=`, but `d307ed28` renamed that key to
`declared_extensions`; and the coverage string ends `(8 on disk)`, a suffix `d307ed28` removed.
It is `8e4968a1`'s checker or older.

**What the run does and does not prove.** The play, the applier and the verifier all came from
the guest's checkout at `d307ed28`, so the production code under test was correct. Fifteen
checks stand. `deployed-extensions-active` does not, and it is the one Task 2.2 names.

**Why nothing caught it.** `scripts/qa-deployed-drift.bash:204-209` compares exactly this glob
and would have — but it is host-only, skips in the CCY container, and nothing runs it before a
bridge request. `recipe_digest_now` (`vmtest:163`) hashes the cleanup script, kickstart and
session runner, correctly not the checker, since that digest is about how a base is built. And
the run records `repo_commit` as though the whole run were at that commit, so the divergence
does not appear in the evidence either.

Filed as [Plan 00117](../../00117-vmtest-acceptance-script-version-gate/PLAN.md): the checker's
version must be recorded in every run and a mismatch must fail as a **harness** failure. Not
this plan's to fix. It is this plan's blocker, so Task 2.2 is now 🚫 rather than ⬜, and Task 2.1
gains the lab redeploy the operator must do first.

The run in flight at `20260914T110446Z` against `be73d3b0` inherits the same stale checker, so
it cannot certify Task 2.2 either. Letting it finish anyway: its other fifteen checks exercise
`ade40aba`'s play and helper changes, which no run has covered yet.

## 12:04 · finding · T2.3 — the final review found the verdict split stopped at the helper, and a security gate whose key property was untested

`subagent-reports/260914-review-00112-final-opus-5.md`, verdict BLOCK, two findings. Both are the
same shape as everything else this plan has turned up, which is the point worth taking.

**The `PENDING_SCAN` fix was half-delivered and I did not notice I had stopped.** The verdict split
is real — the reviewer confirmed it against upstream `extensions-tool` source rather than against
my comment: `gnome-extensions list` exits 2 only when the shell is absent, `info` exits 2 for that
*and* for an unknown UUID, so `_session_available()` is a genuine independent probe. That half is
done. The half that was not: **making a pass say what it checked**. `PENDING_SCAN` and `OK` both
exit 0; the `Verify Deployed Extensions Are Healthy` loop registers `gse_verify` and nothing reads
it; Ansible does not print a command task's stdout without `-v`. So on the exact scenario this plan
exists for — a fresh install whose shell has not rescanned the extensions directory — all nine
iterations return `EXT-OK [pending_scan]` and the transcript is byte-identical to a genuine
all-clear. Task 1.9 was ticked asserting the conflation "let a nine-iteration gate report OK having
judged nothing", implying it no longer could. At play level it still could. `ade40aba`'s message
enumerates the five other round-2 items and is silent on this one; nothing recorded declining it.

Fixed by consuming the verdicts: `Assert Every Deployed Extension Produced A Readable Verdict`
reports `COVERAGE: n of m judged against a live session`, with the rescan and no-session counts
beside it. It deliberately does **not** fail on `pending_scan` — that is legitimate before the
reboot `run.bash` already requires, and failing would break every fresh install. It fails on a
verdict it cannot read, which is the case where the gate genuinely did not run. Verified against
Ansible's own templar on three shapes rather than trusted: fresh install 1-of-9 passes, all-healthy
9-of-9 passes, a missing marker fails. The templar also rejected two earlier drafts of the Jinja —
`regex_search` with a capture group, then bracket escaping that silently produced a nested-set
regex matching nothing and counting zero — neither of which `--syntax-check` would have caught.

**The secret scanner's anchors were load-bearing, documented as load-bearing, and untested.** The
emitter prints `"^" + re.escape(uuid) + "$"` under a comment saying that is precisely what stops a
real address riding on a UUID's coat-tails, and the reviewer measured that dropping both anchors
leaves all 24 cases green. The suite proved the exemption *worked*; it never proved it was *tight*,
which in this one file is the only property that matters — a silent regression here is a leak, not
a bug.

Three cases added, each derived from the declared UUID so the file grows no new address-shaped
literal: a longer address with the UUID as a strict prefix, one with it as a strict suffix, and a
deeper domain under the same left-hand side. Control experiment, because a gate change is worth
proving rather than asserting: a mirror of the tree with the anchors dropped fails exactly those
three and passes the other 24 — the reviewer's measurement reproduced, and evidence the new cases
are the ones doing the work.

A fourth case covers the review's nit, and it is a **different** property: `re.escape` is the
other load-bearing call, and the anchors do not cover it. A near-miss of the *same length* sits
inside the anchors, so only the escaping can reject it — substituting a letter for an interior
dot leaves a string the email pattern still matches and an unescaped `.` would match as a
wildcard. Its own control: a mirror with the anchors kept and `re.escape` dropped fails exactly
that one case and passes the other 27. So the two properties are now independently pinned rather
than jointly.

The control also caught my own bad fixture twice over. The first attempt used `.example` for the
deeper domain; that is RFC 2606 reserved and correctly whitelisted, so the case passed the filter
and looked like an anchoring hole in the scanner. And my first control harness copied the test to
`untracked/` — where `REPO_ROOT="${HERE}/.."` resolved to a directory with no vars file, so the
allowlist was empty, every UUID flagged, and the *first* case failed while the three near-misses
passed vacuously. A control that produces a surprising result is usually a broken control; both
were.

## 12:36 — the fix reproduced the defect it was written for, and a case that proved nothing

Round 2 of the review blocked on one clause, and it is the same shape as the finding it was
answering. `gse_marked | int == gse_total | int` compares two numbers both derived from
`gse_verify.results | default([])`. When that list is empty they agree at zero, the assert passes,
and it prints `COVERAGE: 0 of 0 deployed extensions judged against a live session` — which reads
like a completed gate. A `when:` added to the verify task is all it takes: the earlier
`Assert Deployed Extension UUIDs Were Collected` still passes 9 == 9, because the fact is still
set, and only the loop is skipped.

So the population is now pinned against `declared_extension_uuids` — what the repo *declares*,
which no `when:` on the loop can shrink — and the marker count against that. The second measurement
I built read the expressions out of the play with PyYAML rather than retyping them, because the
harness I wrote last round quoted the task by hand and a harness that can drift from its subject
vouches for nothing. Eight shapes through Ansible's own `Templar`: all-healthy, fresh-install
all-`pending_scan` and nothing-declared pass; an unreadable verdict, a short loop, `results: []`,
a register with no `results` key, and an undefined `gse_verify` all fail. Four of those five
failures used to pass.

The one remaining pass worth naming is `nothing declared` — `0 of 0` with an empty vars file is
genuinely nothing to verify, and pinning to a hard-coded nine would break the day a tenth
extension is added. The vars file is tracked, so an empty one is a repo-content state, not a
runtime skip.

The review's second finding is the better lesson. My `re.escape` case asserted a near-miss was
flagged — but an *undeclared* UUID's near-miss is flagged regardless, so if that extension ever
left the vars file the case would keep passing while proving nothing about escaping. The three
anchor cases do not have this problem: they derive from `DECLARED_UUID`, which the suite asserts
exempt, so removing it from the allowlist fails them loudly. The dotted one had no such anchor.
One assertion fixes it, and the control confirms it is the one doing the work: with that UUID
removed from the vars file mirror, exactly one of the 29 cases fails — the new one — while the
near-miss it guards still passes, vacuously, exactly as reported.

That is twice in two rounds that a case of mine proved the mechanism worked without proving it was
tight. The difference is not care, it is that neither suite had a control until someone ran one.

## 13:12 — a carried nit with no recorded decision is indistinguishable from a forgotten one

Round 3 passed both of round 2's findings and cleared Task 2.3, and its one substantive item was
not about the code: three nits from round 1 were still live and recorded nowhere. That is the right
complaint. This repo's practice is fix-or-record, and I had done neither for three rounds.

Two are fixed rather than recorded. The scanner's own comment cited two real declared UUIDs as
worked examples, which made `secret-scan.bash` committable only for as long as those extensions
stayed declared — the gate's source depending on the file the gate reads. It describes the shape
now. And the custom-extension deploy task spelled the UUID in its `src` and `dest`, so the vars
file header's "no consumer keeps a copy" was false about the play itself: a rename there would have
deployed one directory while the applier enabled another. It loops over
`gnome_shell_extensions.custom` now, which is what Task 1.5 already claimed.

The third is deferred and says so. `hook_extension_uuid_allowlist` does not call `validate_uuid`,
but the direction is already safe — a malformed `uuid:` hard-fails the hook — so what is missing is
an operator message, not a guard. It edits a live public-repo security gate and needs its own
control fixture, and bolting that onto a plan already blocked on a host redeploy is how a careful
change gets a careless one attached to it.

Two accuracy corrections to my own comments, both from measurement I had not done. "Each case below
fails if either anchor is removed" was too strong: dropping only `^` fails one case, dropping only
`$` fails two. The property that matters is that each anchor is held by at least one case, which is
what `PLAN.md` already said and what the comment now says. And "the three above have no interior
dot to substitute" is simply false — the declared UUID has one. The operative property is that
substituting it leaves a string the email pattern no longer matches, so the exemption is never
reached and the case would prove nothing.

Task 2.3 is not ticked yet, though round 3 cleared it. It cleared `d13c10cf`, and this commit
changes a play and a security gate's source that no reviewer has seen. Ticking a "qa-reviewer over
the diff" box against a diff that postdates the review would be the same species of claim as the
ones these three rounds have been about.

## 14:22 · correction · T1.5/T1.8 — I wrote "measured" twice for things I had not measured

Round 4 was narrow and caught two sentences that measurement contradicts. Both were mine, both
were confident, and neither had a measurement behind it.

The deferral note said a malformed `uuid:` "hard-fails the hook, confirmed". It does not. Driving
the real allowlist builder against the four shapes `validate_uuid` rejects: an empty value is
silently skipped, a value with a space emits a live allowlist entry, a value with a comma emits
one too, and only a newline-bearing value fails — at the *filter*, with `grep: Trailing
backslash`, not at the builder, which returns 0 in all four. The deferral's conclusion survives
(none of them widens the exemption to reach a real address, so the direction is safe), but
"confirmed" was doing work it had not earned, and it is exactly the sentence a future agent would
use to decide the nit is cosmetic. Round 3 asserted the same thing and had not measured it either;
I repeated it because it was already written down.

The second was the rename consequence I put into tracked code: "a rename there would then deploy
one directory while the applier enabled a different one." Measured, that is not what happens. The
applier hard-fails with `declared-extension-not-deployed`, because `_locate` needs a
`metadata.json` and a `missing` entry is rc 1; and if the repo directory is renamed too, the copy's
`src` stops resolving and Ansible fails the task. The divergence I described needs the new UUID
already on disk from somewhere else. The defect was real — the play *was* a second copy of the
declaration, and `docs/playbooks.md` was false about it — but the failure mode was **loud**. I
narrated a cause I had not confirmed, into a comment, in the same commit that fixed the thing.

Then the review's nit 2 sent me to measure the per-anchor split, and my first control was broken
in the now-familiar way. Substituting the emitter through two layers of quoting let `\$` reach
Python as a literal backslash-dollar, so the pattern matched nothing, nothing was exempt, and the
*exemption* cases failed while the near-misses passed. Surprising result, broken control — again.
The fix was a baseline: run the verbatim variant first, print the emitted pattern, and only trust
the mutations once the unmutated one passes 29. It then agreed with the review exactly — caret
dropped fails `UUID_WITH_HEAD` alone, dollar dropped fails `UUID_WITH_TAIL` and `EMBEDDED`. The
comment names the fixtures now, because naming them by operation is what made them read as
opposites in the first place.

Finding 3 was pre-existing but the commit had rewritten the task, which invalidated Plan 00049's
EXT-13 citation. Fixed here rather than routed onward: the files land `0644` with
`directory_mode: 0755` and owner/group, the `play-container-watch.yml` form for this exact
operation. `metadata.json` was being deployed executable. EXT-13 is marked closed in 00049's
research note, with its pre-fix citations kept.
