# Plan 00110 — Journal 26-09-13

> **Append-only activity log** for plan 00110. One file per day
> (`00110-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:31 · action · — — plan scaffolded

Plan 00110 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:52 · action · — — design authored into DESIGN.md

Wrote [DESIGN.md](../DESIGN.md): architecture, VM lifecycle state machine,
freshness policy, the desktop-profile problem, the container→host bridge,
performance plan, the 00063/00079/00092 discharge map, a 7-phase task
breakdown, and the risk table. `PLAN.md` deliberately untouched — the team
lead owns it and lifts Phase 0–7 from §9.

## 11:52 · finding · — — the installer-change signals, fetched live

All five upstream signals were fetched and parsed from this container today,
so §4 is evidence rather than recollection. The headline is an asymmetry:

```
releases/44/COMPOSE_ID                         -> Fedora-44-20260422.1        (frozen)
development/45/COMPOSE_ID                      -> Fedora-45-20260913.n.0      (nightly)
development/rawhide/COMPOSE_ID                 -> Fedora-Rawhide-20260913.n.0 (nightly)
releases/44/.../os/repodata/repomd.xml         -> revision 1776864872  = 2026-04-22T13:34:32Z
updates/44/.../repodata/repomd.xml             -> revision 1789172543  = 2026-09-12T00:22:23Z
bodhi /releases/                               -> F44 current, F45 pending, F46 pending
```

A Fedora GA tree is frozen at compose, so for `fedora_version: 44` the core
installer **cannot** change — the owner's question has a "no, and provably so"
answer for a released version, and a live daily signal only during a Branched
window. `.treeinfo` (1,427 B) carries sha256 for `boot.iso`, `install.img`,
`initrd.img` and `vmlinuz`, and `releases.json`'s hash for the netinst ISO is
byte-identical to `.treeinfo`'s `boot.iso` — the two sources cross-confirm.

Rejected, with reasons in §4.2: live respins (**no Workstation/GNOME respin
exists** for F44 — only Budgie, CINN, COSMIC, LXDE, LXQT, MATE, SOAS, XFCE,
i3), the `anaconda` package version (media is fixed at compose), mdapi
(answers from `updates-testing`, which no default install receives), and
ISO `Last-Modified`/`ETag` (three consecutive HEADs hit three different
mirrors).

## 11:52 · finding · — — a headless desktop run would make the repo's own extension gate vacuous

`helpers/gnome/extension_state.py:58-61` returns `Verdict.SKIP_NO_SESSION`
when no GNOME session is available, and `helpers/gnome/verify_extension.py:111`
then prints `EXT-OK` and exits 0. So a desktop-profile VM driven over plain
SSH with no logged-in session would report extension verification green while
asserting nothing — the `AgentNotes.md` "partial result read as a complete
one" class, row 13 shape. This is why §5.3 dispatches the provisioning run
*into* the autologin session with `systemd-run --user --wait` rather than
beside it.

Related: `releases/44/Workstation/x86_64/os/.treeinfo` is **404** — there is
no Workstation install tree, so the desktop base must come from the Live
ISO's squashfs via `liveimg` (the route `fedora-install/ks.cfg:429` already
uses) or from the Everything tree plus a package environment.

## 11:52 · decision · — — the verb set is the boundary, so the allowlist cannot live in the mount

The sandboxed agent can write anywhere in the bind-mounted checkout, so a
watcher that validated arguments against `vars/vm-test-scenarios.yml` would be
validating against a file the sandbox controls. Resolved in §2.3: the repo
manifest is the *source*, an Ansible-deployed
`~/.local/share/vmtest/scenarios.allowlist` is the *authority*, and drift
between them is reported with both digests and refused — never silently used.
Same shape as `scripts/qa-deployed-drift.bash` and as 00079's acceptance gate
refusing to vouch for a binary that differs from the repo.

Five verbs only: `list-scenarios`, `lab-status`, `run-scenario`,
`refresh-base`, `abort-run`. `run-playbook <name>` is deliberately absent —
it is `exec` wearing a domain name.

## 11:52 · handoff · — — next entry point

`DESIGN.md` is complete and self-contained. Six items are marked **UNVERIFIED**
(U1–U6, §0) because they cannot be answered from a container with no
`/dev/kvm`, no `virsh` and no systemd of its own; each is a probe in the
Phase-0 `triage.bash`, not an assumption. Nothing has been committed by this
session.

## 12:10 · decision · — — freshness policy inverted: revision primary, TTL demoted to backstop

Team-lead review caught a real defect in the first draft. §4.3 had `refresh`
requiring the updates revision to have advanced **and** TTL-U to have expired.
That is wrong in both directions: it leaves a stale base in service inside an
unexpired window after a large update lands, and it schedules pointless
`dnf upgrade` cycles when nothing moved. Now: **the revision is the trigger,
TTL-U applies only when the revision cannot be fetched.** Same inversion for
TTL-R against artefact identity.

Adopted with one addition of my own, because pure revision-primary would
otherwise mean a near-daily base-sized write (the updates repo regenerates
frequently). A refresh is now **two steps with a gate between them**: boot and
`dnf -y upgrade`, then re-flatten **only if the transaction actually changed
packages**. A revision that advanced for packages this base does not have costs
a boot and a transaction check, not a rebuild.

T1.2 gains three tests that must each be shown to go red when reverted —
notably "an advanced revision yields `refresh` even inside an unexpired TTL-U",
because a test exercising only the expired-TTL case would have passed against
the *wrong* policy. That is `AgentNotes.md` row 14 in advance rather than in
hindsight.

Also folded in: the answering **mirror is recorded** alongside the revision, and
a revision that goes **backwards** is mirror lag — retry against canonical
`dl.fedoraproject.org`, and a persistent regression resolves to `unknown` and
blocks, never to `current`.

## 12:10 · finding · — — the GA install tree has not moved in 142 days

Measured, not inferred: compose timestamp `1776865868` (2026-04-22) versus
updates revision `1789172543` (2026-09-12) = 12,306,675 s = **142 whole days**
with zero movement in the install tree.

Design consequence now stated explicitly in §4.1: a "reinstall when the
installer changes" trigger keyed to `.treeinfo`/`COMPOSE_ID` **will never fire
during a release's lifetime, and that is correct behaviour, not a broken
check**. On a GA release the real reinstall driver is `vars/fedora-version.yml`
changing, and TTL-R is the backstop that stops an inert signal being the only
thing between the lab and an indefinitely old base.

## 12:10 · decision · — — three bases, not two: the Cloud image is NOT an Anaconda install

The first draft said of the Cloud qcow2 "the official qcow2 *is* a fresh
install". **That was an overclaim and is corrected.** A Cloud Base image has a
different package set, different defaults (cloud-init, no cockpit, different
firewall/SSH defaults) and exercises **no partitioning or kickstart at all**.

New §3.5 splits the server profile into two bases that cannot substitute for
one another:

| base | proves | cadence |
| --- | --- | --- |
| `server-fast` (Cloud qcow2) | the repo's provisioning on a Fedora **Cloud** box | every run |
| `server-full` (Anaconda, Server tree) | the repo's provisioning on an Anaconda-installed **Server** | on `reinstall` |
| `desktop` (Anaconda + Workstation squashfs) | the repo's own installer shape + desktop provisioning | on `reinstall` |

00063's criterion row was split to match, so a fast-path pass can never be
cited for the Server half. Phase 3b added to §9.

One qualification worth recording against the team lead's framing: `server-full`
is **not** testing the repo's installer, because the repo ships no server
kickstart — `fedora-install/ks.cfg` installs Workstation via `liveimg`. The
`desktop` base is the one that tests the shipped installer. `server-full` tests
the product against a materially different starting OS, which is still worth
having and is what 00063's "Server **or** Cloud" wording actually asks for.

Also taken: the base economics are asymmetric and the layout now says so —
`server-fast` is nearly free and rebuilt casually; `desktop` is expensive and
reused aggressively, which is where the read-only base plus CoW overlay earns
its keep.

## 12:10 · finding · — — every variant's signed CHECKSUM filename carries the compose label

Confirmed present: `Fedora-Cloud-44-1.7-x86_64-CHECKSUM`,
`Fedora-Server-44-1.7-x86_64-CHECKSUM`,
`Fedora-Everything-44-1.7-x86_64-CHECKSUM`,
`Fedora-Workstation-44-1.7-x86_64-CHECKSUM`.

The build number `44-1.7` is **in the filename**, so a changed compose is
detectable from a directory listing before anything is downloaded. For the
Cloud base this is strictly better than `.treeinfo`, which does not describe
the Cloud image at all.

That produced the §4.3 authority rule, which answers the team lead's "recommend
which is authoritative for which base rather than using both indiscriminately":

> **Artefact-identity signals drive `reinstall`. The updates-repo revision
> drives `refresh`. Never the other way round.**

## 12:12 · handoff · — — next entry point (supersedes the 11:52 handoff)

`DESIGN.md` revised after team-lead review: freshness policy inverted
(revision-primary, TTL as backstop), the Cloud-image overclaim corrected, and
the server profile split into a fast and a full path as §3.5 with a new
Phase 3b in §9. Section numbering shifted — the old §4.3/§4.4 are now
§4.3 (authority) / §4.4 (policy) / §4.5 (total-outage fail-fast); all
cross-references were swept and resolve.

Still outstanding and unchanged: U1–U6 in §0 are UNVERIFIED and are Phase-0
`triage.bash` probes, not assumptions. Five open decisions for the owner in
§11. Nothing has been committed by this session — `PLAN.md` is untouched and
belongs to the team lead.

## 12:30 · finding · — — B1 confirmed verbatim: the bridge would have died silently, not throttled

Fetched `systemd.path(5)` upstream and the review is right word for word:

```
"Unlike other service failures, the error condition that the start rate limit is
 hit is propagated from the service unit to the path unit and causes the path
 unit to fail as well, thus ending the loop."

TriggerLimitIntervalSec= defaults to 2s, TriggerLimitBurst= to 200:
"If the limit is hit, the unit is placed into a failure mode, and will not watch
 the paths anymore until restarted."
```

So `StartLimitIntervalSec=60` / `StartLimitBurst=50` did not rate-limit a noisy
agent — it would have **killed the path unit**, leaving no oneshot to write the
"rejected, rate limited" response §10 promised, and no way for the sandbox to
distinguish that from "the bridge was never installed". The remedy would have
been a human typing `systemctl --user reset-failed`.

Worth naming the provenance: **this came straight from the published
host-action-bridge pattern and I copied it without checking what systemd does
with it.** A design that inherits a control from a proven article still has to
verify the control does what its name suggests.

Fixed in §6.4: the real limit is watcher-side (counted from the off-mount audit
log, writes a `rejected` response, exits 0); systemd limits are set high as a
last-resort loop breaker only. New §6.5 adds a **separate, non-path-triggered
timer** writing `diagnostics/bridge-heartbeat.json`, so a wedged path unit is
visible with its remedy rather than silent. T4.9 wedges the unit on purpose and
asserts the reader says "bridge wedged", not "timeout" and not "fail".

## 12:30 · finding · — — B2/B3: the spool is sandbox-controlled and the host wrote into it naively

The bridge assumed a mailbox. It is a directory tree the sandbox can restructure.
`rm -rf responses && ln -s ~/.ssh responses` and the host's `mv -f` lands the
file inside the link target **as the host user** — `mv -f` protects the final
component but nothing about a symlinked parent, and a transcript written with
`>` follows and truncates. Ownership checks cannot help: `claude-yolo:1946`
binds with a plain `-v "$PWD:/workspace"` under rootless podman, so container
root already maps to the host uid — the mapping that makes the bridge convenient
is the one that removes ownership as a control.

New §6.3 states the defence per directory: pinned spool-root dirfd,
`openat`/`renameat`, `O_NOFOLLOW|O_EXCL`, `realpath` containment, and a
**refusal** (not a quarantine) when a spool directory is a symlink — quarantine
implies recoverable malformed input; this is an attempt on the host account.

B3 folded in: the body is read **once** into memory, every check and the argv
build use that buffer, the file is never re-opened, and the filename verb must
equal the body verb. All of this becomes `helpers/vmtest/spool.py` (TDD'd,
stdlib-only) per `playbooks/CLAUDE.md` — which also removes the `jq` dependency
the bash watcher would have needed and that was missing from T2.1's package list.

## 12:30 · finding · — — B4: the plan's own discharge map contained the defect the plan exists to attack

Two overclaims, both mine, both verified against the source:

1. `server-optional-play-missing` exercises `run.bash:684-686` — **name
   resolution**, which `hl_abort`s before any playbook runs. The criterion is
   that a *failed* playbook propagates, which is `run.bash:700-702`. Proving
   argument validation and labelling it failure propagation is `AgentNotes.md`
   row 14, written into the plan whose purpose is to hunt that class.
   Replaced with two scenarios using only supported inputs: an unrecognised
   `RUN_BASH_PROVISIONING_PROFILE` (confirmed: `run.bash:2632-2634` forwards it
   verbatim with no validation, so `play-AA-preflight-sanity.yml:55-58` fails
   play 1), and a `hardware-specific/` play that runs and fails on a VM. The
   original is kept, correctly relabelled as an argument-validation test.
2. "No secret bytes enter the environment or user-data — dischargeable, and
   genuinely new" is **vacuous** on the default path: `GITHUB_ACCOUNTS=none`
   means there is no PAT and no SSH passphrase in the guest, so the grep
   searches for things that were never supplied. Downgraded to opt-in-only.

Arithmetic fixed too — "four of the eight" named three. The map now counts rows
instead of asserting a total, and picked up an item review found I had wrongly
marked unprovable: secret-file unlink and ssh-agent teardown (00063 Tasks
2.2/2.4) **are** VM-assertable.

## 12:30 · finding · — — B5: the desktop base had no way to boot an installer

`liveimg` is an Anaconda directive and the Workstation Live ISO boots a live
session, not an installer. §5.3 named only the Live ISO, and §7's fetch table
never mentioned `boot.iso` at all — so Phase 5 as written could not have
executed.

The prior art says so in its own header, and I had cited the wrong part of it
(`setup-netinstall-boot.bash:793-810`, the squashfs extractor, rather than
`:5-13`):

```
"extracts squashfs.img from the Live ISO ... extracts vmlinuz + initrd to /boot
 from the netinstall ISO ... Anaconda boots from the netinstall ISO
 (inst.stage2=...) and deploys the Workstation Live filesystem via liveimg"
```

§5.3 now requires **both** artefacts with their roles named, and T5.2 is a task
for the boot medium specifically. Also caught while there: `ks.cfg:359-364`
builds LUKS-encrypted btrfs, and an unattended VM has nowhere to answer a
passphrase prompt — the VM kickstart keeps btrfs, drops LUKS.

## 12:30 · decision · — — the DNF cache was the largest missed win; taken, with a check on the check

Review called a host-side package cache the single biggest omission and it is
right: DNF dominates both provisioning rows and the `server-full` base build,
and every run re-downloaded everything. Taken as a per-profile cache exported
over virtiofs (`virtiofsd` confirmed packaged for F44) with `keepcache=1`, plus
a pinned mirror `baseurl` replacing the metalink — which also removes mirror
flakiness from the *verdict*, not just from the clock.

Added something the finding did not ask for, because a cache is a divergence:
both go into `evidence.divergences`, and **T6.2b adds a periodic cache-cold
scenario** with the mount absent and the metalink restored. Otherwise a cached
run could quietly stop proving the repo works against a real mirror, which is
the same class this whole plan exists for.

Also taken: `cache=unsafe` over `cache=none` (the mode applies to the whole
chain, so `none` also blocks page-cache sharing of the read-only base across
parallel runs, and dnf/rpm are fsync-heavy; the overlay is disposable so there
is nothing to lose), `lazy_refcounts=on`, `cp --reflink=auto` making a refresh
O(1) on btrfs — the repo's own kickstart is btrfs — `--boot uefi` over the
low-level spelling, and `--cpu host-passthrough`. Dropped per-run hashing of the
multi-GB base to size+mtime+`qemu-img check`, with full sha256 on refresh and
`lab-status --deep`.

## 12:30 · decision · — — U3 and U6 settled from documentation; U7 added

Review was right that I deferred two settleable questions to probes, which
inflates the unverified list until it stops signalling. Both fetched and quoted
in §0: `systemd.unit(5)` defines `%f` as the unescaped instance name with `/`
prepended — the exact inverse of `systemd-escape --path` — and the virt-install
manual gives `--cloud-init`'s suboptions verbatim, including that it builds the
NoCloud ISO itself and attaches it for the first boot only (so the fast path
needs no separate seed-ISO step). Both probes stay as *confirmations*.

**U7 added** and it is load-bearing: §8's 00092 claim assumed CCY can start in a
guest with no credential and that `ccy --rebuild` works there. Neither was
checked. Claiming the biggest unlock on two unchecked assumptions is the bet
this plan exists to stop.

Numbering deliberately not compacted — U3/U6 are cited in earlier journal
entries, and reusing their numbers for different questions would make those
references wrong.

## 12:30 · decision · — — the all-skipped pass, the forgeable response, and the token transcript

Three smaller holes, all real:

- Rule 03 required `passed + skipped == total`, which `passed=0, skipped=total`
  satisfies — row 13 reappearing **inside the rule written to prevent it**. Now
  also requires `failed==0`, `total==planned`, `passed>=1`, and a per-scenario
  `max_skipped`.
- Responses live in a sandbox-writable directory and the reader trusted them.
  Now HMAC-signed with a host-only key; `forged` is a distinct non-zero outcome.
  Stated honestly: this does not make a dishonest agent honest, it makes a
  response the host did not write detectable, and the off-mount audit log stays
  the verdict of record.
- The token scenario's transcript would have landed in the shared spool — a
  PAT→sandbox channel through the component built to prevent one. Secret-bearing
  runs now write off-mount only; this repo has no log scrubber and
  `CLAUDE/Plan/.gitignore` says so.

## 12:30 · finding · — — the extension gate is worse than I reported, and it is not this plan's bug

Review confirmed §5.2 at line level and went further:
`verify_extension.py:69-78` treats **any** non-zero `gnome-extensions info` exit
as "no session", so a missing binary, a broken D-Bus, or **a UUID that does not
exist at all** also reaches `EXT-OK` with exit 0.

That is true on the owner's real desktop today, not only in a VM. Recorded as a
new §12 rather than quietly widening this plan's scope — it wants its own plan.

## 12:32 · handoff · — — next entry point (supersedes the 12:12 handoff)

Round-2 revision complete: all five blocking findings fixed, all twelve
should-fix items taken, all eight nits applied. Nothing was declined, so there
is no "rejected with reasons" list — where I extended a finding rather than just
applying it (the cache-cold scenario, the three-way negative-scenario split, the
liveness timer, T4.9) the design says so in place.

Structure changed: §3.5 (three bases), §6.3 (spool defences), §6.5 (liveness),
§12 (the repo defect) are new; §6 subsections renumbered and all cross-references
swept. §7 lost its duration column entirely, so `R-PLAN-TIME-ESTIMATE` has
nothing to catch at commit.

Unverified list is now U1, U2, U4, U5, U7 — all genuinely hardware- or
host-bound. Seven open decisions for the owner in §11. Still nothing committed;
`PLAN.md` untouched and the team lead's.
