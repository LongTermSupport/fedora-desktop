# Plan 00061 — Journal 26-07-20

> **Append-only activity log** for plan 00061. One file per day
> (`00061-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:08 · action · — — plan scaffolded

Plan 00061 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:12 · action · P1 — problem defined; brainstorms commissioned

Wrote `PLAN.md` problem definition (goals, non-goals, open design axes),
added README index row. Dispatched two independent brainstorm passes:
Fable → `brainstorm-fable.md`, Sonnet → `brainstorm-sonnet.md`. Both told
to ground in the real repo (playbook-main.yml, qa-ansible*.bash) and cover
selection mechanism, scope-declaration location, mixed-play handling, QA
gate design, server baseline classification, first slice, pros/cons.

Failsafe recovery cron: `884be96e` (hourly at :37, non-durable).

## 14:18 · decision · T1.3 — Sonnet wins the brainstorm-off (with Fable grafts)

Both `brainstorm-fable.md` and `brainstorm-sonnet.md` delivered; both
strong and repo-grounded. Chose **Sonnet's** design as primary:
native play-level `tags: [scope-*]` + `--skip-tags`, QA gate folded into
`qa-ansible.bash` (zero `qa-all.bash` changes).

Decisive points over Fable:
- Sonnet makes scope-declaration and selection the *same* native object;
  Fable carries an inert `# scope:` comment + a separate `when:` guard that
  must be kept in agreement (needs a drift cross-check Sonnet doesn't).
- Fable's headline reason to reject tags ("additive, not subtractive") is
  wrong for `--skip-tags`, which IS subtractive at task level — Sonnet
  proved it with `play-vpn.yml`. Also matches the user's own tag instinct.
- Sonnet's gate dodges the fragile positional `jq -s` merge in
  `qa-all.bash` that its own comments warn about; Fable's 7th-stage hits it.

Grafted from Fable: (A) split-into-file for *non-trivial* mixed plays
(Sonnet's task-tag override kept for trivial one-item cases); (B) both
canonical commands documented so a future `scope-server` play never
silently runs on desktop.

Synthesis flag: the two found DIFFERENT single mixed plays
(`play-basic-configs` USB-audio vs `play-vpn` NM-openvpn-gnome) → Phase 2
needs an exhaustive per-play task sweep, not a one-pass read.

## 14:18 · handoff · P2 — awaiting user go-ahead

Plan + problem defined, decision gate recorded in PLAN.md. Phases 2–3
tasks rewritten for the chosen design. Next action pending USER
confirmation of the verdict (and of the two flagged classification calls,
`play-rpm-fusion` + `play-terminal-emulators`) before implementing.
