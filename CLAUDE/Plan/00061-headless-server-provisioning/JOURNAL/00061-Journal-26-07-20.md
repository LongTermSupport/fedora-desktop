# Plan 00061 — Journal 26-07-20

> **Append-only activity log** for plan 00061. One file per day
> (`00061-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:08 · action · — — plan scaffolded

Plan 00061 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:12 · action · P1 — problem defined; brainstorms commissioned

Wrote `PLAN.md` problem definition (goals, non-goals, open design axes),
added README index row. Dispatched two independent brainstorm passes:
Fable → `brainstorm-fable.md`, Sonnet → `brainstorm-sonnet.md`. Both told
to ground in the real repo (playbook-main.yml, qa-ansible*.bash) and cover
selection mechanism, scope-declaration location, mixed-play handling, QA
gate design, server baseline classification, first slice, pros/cons.

Failsafe recovery cron: `884be96e` (hourly at :37, non-durable).

## 14:18 · decision · T1.3 — Sonnet wins the brainstorm-off (with Fable grafts)

Both `brainstorm-fable.md` and `brainstorm-sonnet.md` delivered; both
strong and repo-grounded. Chose **Sonnet's** design as primary:
native play-level `tags: [scope-*]` + `--skip-tags`, QA gate folded into
`qa-ansible.bash` (zero `qa-all.bash` changes).

Decisive points over Fable:
- Sonnet makes scope-declaration and selection the *same* native object;
  Fable carries an inert `# scope:` comment + a separate `when:` guard that
  must be kept in agreement (needs a drift cross-check Sonnet doesn't).
- Fable's headline reason to reject tags ("additive, not subtractive") is
  wrong for `--skip-tags`, which IS subtractive at task level — Sonnet
  proved it with `play-vpn.yml`. Also matches the user's own tag instinct.
- Sonnet's gate dodges the fragile positional `jq -s` merge in
  `qa-all.bash` that its own comments warn about; Fable's 7th-stage hits it.

Grafted from Fable: (A) split-into-file for *non-trivial* mixed plays
(Sonnet's task-tag override kept for trivial one-item cases); (B) both
canonical commands documented so a future `scope-server` play never
silently runs on desktop.

Synthesis flag: the two found DIFFERENT single mixed plays
(`play-basic-configs` USB-audio vs `play-vpn` NM-openvpn-gnome) → Phase 2
needs an exhaustive per-play task sweep, not a one-pass read.

## 14:18 · handoff · P2 — awaiting user go-ahead

Plan + problem defined, decision gate recorded in PLAN.md. Phases 2–3
tasks rewritten for the chosen design. Next action pending USER
confirmation of the verdict (and of the two flagged classification calls,
`play-rpm-fusion` + `play-terminal-emulators`) before implementing.

## 14:32 · decision · P2 — owner confirmed verdict + rpm-fusion=general; refinement loop started

Owner confirmed Sonnet's tags+`--skip-tags` design and noted Fable's
comment-based scoping was over-engineered — Fable rejected tags only
because it reasoned about additive `--tags` selection, missing that
`--skip-tags` is subtractive (the exact mechanism the owner proposed).
Owner ruling: `play-rpm-fusion` → `scope-general` (foundational repo
plumbing, not GNOME). Recorded as PLAN Decision 2.

New process (PLAN Decision 3): Sonnet authors full `PROPOSAL.md` →
Fable adversarially audits (`AUDIT-round-N.md`) → this agent judges each
round → loop until convergence, then Phase 3. Sonnet dispatched to write
the proposal (exhaustive per-play re-read this time — one-pass missed a
mixed play each last round).

## 14:40 · finding · P2 — Sonnet's PROPOSAL.md delivered; round-1 audit dispatched to Fable

`PROPOSAL.md` written. Exhaustive re-read found **three** mixed-concern
core tasks (vs one per model last round), vindicating the "one-pass is
unreliable" call:
- `play-basic-configs` USB-audio-fix task (Fable's r1 find)
- `play-vpn` `NetworkManager-openvpn-gnome` (Sonnet's r1 find)
- `play-prevent-ssh-suspend` `gsettings ... settings-daemon.power` task —
  NEITHER caught in r1, and it's a real latent bug: on a headless box
  `gsettings` hard-fails ("No such schema") and aborts the play.
Sonnet also self-corrected its own r1 error: `--check` is NOT
container-safe (gathers facts / hits the target); switched the
zero-regression proof to `--list-tasks` only (parse-only, like
`--syntax-check`). Also fast-pass-classified the 41 optional plays (the
gate scans all of `playbooks/`), flagging `play-container-watch`
(genuine file-split candidate) and `play-virtualbox-windows` (2nd
rpm-fusion-style owner call — VBox has a real headless mode).

Dispatched Fable as adversarial auditor → `AUDIT-round-1.md`. Told to
VERIFY claims against real files (qa-ansible.bash var names + insertion
point, the awk parser traced by hand, qa-all.bash zero-change claim, the
three mixed tasks actually exist, `--list-tasks` safety, and to hunt a
possible 4th mixed general play). Judge (this agent) triages next.

## 14:56 · decision · P2 — JUDGE ruling on Fable's round-1 audit; round 2 dispatched

Fable's `AUDIT-round-1.md` is high quality — it *executed* the code, not
just read it. Judge triage:
- **BLOCKER (confirmed):** Check 4's `valid_count=$(… grep -xcE …)` aborts
  `qa-ansible.bash` under `set -euo pipefail` (grep exits 1 on 0 matches) —
  on exactly the untagged-play case the check must catch. Cascade: empty
  `$TMP_ANSIBLE` → `jq -s` silently shrinks its positional array →
  `.checks.ansible`/`.checks.js` corrupted for unrelated checks. Reproduced
  empirically. One-line `|| true` × 2 fixes it. MUST fix.
- **SHOULD-FIX accepted:** (1.2) trailing `# why` comment defeats the `-x`
  match → strip in awk; (5.1) `play-virtualbox-windows.yml` has TWO
  `- hosts:` blocks (only such file in repo) → file-granular gate
  false-passes if only play 1 tagged → gate must not hide an untagged play;
  (6.1) `container-watch` interim edit has no exact diff for its 7 GNOME
  tasks → enumerate or pull out as named follow-up.
- **NITPICK folded in:** self-contradictory docs insertion line; stale
  per-play doc sections; TOTAL count symmetry.
- **Verified solid (no finding):** qa-all.bash zero-edit claim (conditional
  on blocker fix); the 3 mixed-task "Before" quotes + gsettings hard-fail
  (Fable found `ansible.cfg` sets `any_errors_fatal = true`, so it aborts
  the whole RUN — stronger than claimed); `--list-tasks` container-safety +
  zero-regression (Fable ran it for real: exit 0, empty diff on
  `--skip-tags scope-server`).

Agree with Fable: revision, not new round. Sonnet dispatched round 2 to
fix in place with a top-of-file revision log so Fable can re-audit deltas.

## 15:05 · finding · P2 — Sonnet round-2 revision applied; Fable round-2 re-audit dispatched

Sonnet fixed all round-1 findings in place (revision log at top of
PROPOSAL.md). Judge spot-verified the two highest-risk deltas directly:
- Blocker fix: `|| true` on both `valid_count`/`scope_like_count` count
  lines, commented to match the Check 1/3 idiom. Correct — empty
  `play_tags` now lands in "no scope tag" instead of aborting. The
  `bad=$(…)` line's branch guarantees ≥1 match, so it's genuinely safe
  unguarded (verified the branch condition).
- Multi-play guard: new `hosts_block_count=$(grep -cE '- hosts:' || true)`
  → hard ERROR + `continue` on >1 play/file; can't false-trigger (the
  outer `grep -qE hosts:` continue guarantees ≥1).
- awk now strips trailing `# comment` and `\r`.
Sonnet also caught an **8th** container-watch GNOME task Fable's r1 list
of 7 missed (`extension reload complete`, consumes a registered var →
undefined-var hard-fail on server if left untagged). Good adversarial
value flowing both directions.

Dispatched Fable → `AUDIT-round-2.md`, focused on the deltas only, told
to EXECUTE the revised Check 4 across untagged/tagged/multi-scope/invalid/
commented cases and confirm no new regressions. If clean → judge declares
convergence → Phase 3.

## 15:10 · decision · P2 — CONVERGED. Design gate passed; PROPOSAL.md is implementation-ready

`AUDIT-round-2.md` (Fable): **zero remaining findings**, every round-1 fix
verified by *executing* the revised Check 4 against six fixtures + two real
repo files (all branches correct, no `set -e` abort, exit 0); multi-play
guard fires on `play-virtualbox-windows.yml` and doesn't false-trigger;
comment/CRLF strip works without regressing the normal case; the
container-watch diff is exactly 8 tasks (traced the register/`when` chain —
the 8th-task hazard is real, `ansible.cfg` makes undefined vars fatal); no
regressions. Verdict: implementable as-is, no round 3.

Judge concurs → convergence declared. Phase 2 complete (2.1/2.2/2.3).
`PROPOSAL.md` §7 is the 12-step implementation checklist for Phase 3.

Loop scorecard (both models earned their place):
- Fable r1: found the real `set -e` BLOCKER by executing the code.
- Sonnet r2: fixed it + found an 8th container-watch GNOME task Fable's
  own list of 7 had missed.
- Fable r2: verified everything by execution, converged honestly.

## 15:10 · handoff · P3 — awaiting owner greenlight to implement

Phase 3 is a ~72-file change (tag all core + optional plays, 3 mixed-play
edits, container-watch override, split virtualbox-windows, add Check 4,
docs). CCY container = edit + commit only; the HOST headless-run
validation (Task 3.8) is owner-run. Two genuine owner-decision points
remain (virtualbox-windows scope values; Medium/Low optional rows).
Next agent: on greenlight, execute `PROPOSAL.md` §7 in order, QA green,
zero-regression `--list-tasks` diff, commit per Plan Commit Rule.

## 15:40 · decision · P2 — MECHANISM PIVOT to `when:` + auto-detect (Decision 4)

New owner requirement: auto-detect target, zero config/flags ("a hook up
front"). Key fact: `--skip-tags` is CLI-time → tags can't self-configure
(always need a flag/wrapper); `when:` is runtime → consumes an
auto-detected `provisioning_profile`. So the auto-detect goal flips the
mechanism choice. Owner also ruled server→desktop conversion out of scope,
killing my desired-vs-current-state caveat — `systemctl get-default`
reflects real intent.

**Prototyped it before committing** (`prototype-when-import.md`, 2.19.11):
- ✅ `when:` on `import_playbook` gated by `provisioning_profile` skips the
  gnome play at runtime when `=server` (`skipped=1`); runs it when
  `=desktop`. Mechanism holds under 2.19.
- ⚠️ `--list-tasks` does NOT honor `when:` (lists gnome task in all
  profiles) — so the cheap in-container skip-proof that worked for
  `--skip-tags` is gone; end-to-end skip verification is host-side only.

Recorded as PLAN Decision 4 (supersedes Decision 1's selection layer).
Everything mechanism-independent (classification, 3 mixed plays, the two
latent-bug finds) is REUSED verbatim — pivot is cheap, nothing built yet.
Next: re-run the Sonnet→Fable loop to revise PROPOSAL.md for `when:`
(reuse classification; QA gate now validates import-site profile-gates).

## 15:55 · finding · P2 — Sonnet round-3 (when: pivot) delivered; Fable round-3 audit dispatched

PROPOSAL.md rewritten for `when:` + auto-detect (revision log at top; §1
classification reused verbatim). Sonnet prototyped 3 behaviours in-container
before writing:
- Detection = `environment/localhost/group_vars/desktop.yml` with a
  `lookup('pipe','systemctl get-default')` ternary (graphical→desktop, else
  server; server-biased). Inert under `--syntax-check`/`--list-tasks`, runs
  on real run, `-e` override skips the lookup (extra-vars precedence).
- Added a fail-fast assert in play-AA for a typo'd override (`=srever`
  would be `!= 'server'`=true=desktop silently, so assert
  `provisioning_profile in [desktop,server]`).
- QA Check 4 now two sub-loops: 4a parses playbook-main.yml import+when
  lines (awk state machine, no `grep -c` → set-e-safe by construction), 4b
  parses optional plays' `vars.scope` + the multi-play-file guard. Confirmed
  a bare top-level `scope:` key is a hard Ansible parse error → marker must
  live in `vars:`.
- container-watch: 4 of its 8 gnome tasks already have register-dependent
  `when:`, so profile gate added as LIST-form `when:` (short-circuits), not
  scalar replacement.
- `--list-tasks` zero-regression proof dropped (can't see `when:`) →
  host-side `--check`/real-run procedure.

Judge flagged one hazard to Fable: 4a's awk flushes `pending` as UNGUARDED
on any non-import/non-when line, so a comment/blank BETWEEN an import and
its `when:` could silently misclassify a gnome play as general + drop the
orphan `when:` — and playbook-main.yml HAS comment blocks between imports.
Told Fable to run 4a against the REAL file and probe this. Also: verify the
group_vars path/group actually loads for localhost, the assert ordering,
the list-when short-circuit, and set-e safety.

## 16:35 · decision · P2 — JUDGE ruling on Fable round-3; round 4 dispatched (2 gate fixes)

`AUDIT-round-3.md` (Fable, all verified by execution against a real-shape
fixture):
- **BLOCKER (confirmed):** Check 4b's discovery excludes only playbook-main
  + archived, NOT the 31 core plays at `playbooks/imports/*.yml` — which by
  design carry no `vars.scope` → 4b flags all 31 "missing vars.scope" →
  `qa-all.bash` NEVER passes. Day-one, unconditional. Fix (Fable-verified):
  `[[ "$yml_file" != */optional/* ]] && continue` in 4b. Surfaced precisely
  because I told Fable to run against the real directory shape.
- **SHOULD-FIX (high, confirmed):** the orphaned-`when:` hazard I flagged is
  real (comment/blank between import and its when: → gnome silently read as
  general, when: dropped, no error). Doesn't hit the actual diff (real
  playbook-main.yml clean), but the gate must enforce its own grammar →
  folding in now. Fix: 4a `when:` rule fires unconditionally, emits
  `ORPHANED|` when pending empty → case errors.
- Verified CLEAN by execution: detection layer + the load-bearing inventory
  facts (localhost ∈ group desktop; ansible.cfg inventory path), the assert
  (fails loud when group_vars missing; catches typo), container-watch
  list-when short-circuit (both orders), reuse integrity.

Both fixes one-line-class and pre-verified by Fable. Round 4 = apply + a
focused re-confirm, not a redesign. Sonnet dispatched.
