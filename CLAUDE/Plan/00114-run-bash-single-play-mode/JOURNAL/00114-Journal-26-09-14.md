# Plan 00114 — Journal 26-09-14

> **Append-only activity log** for plan 00114. One file per day
> (`00114-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:14 · action · — — plan scaffolded

Plan 00114 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:40 · finding · — — why a bare play invocation fails on password sudo

A play run directly on a password-sudo box aborts at its first `become` task with
`Premature end of stream waiting for become success … sudo: a password is required`, and
`sudo ./play.yml` fails instead with `ansible-playbook: not found` (root has no pipx shim).
`run_playbook_with_issue_option` in run.bash already branches correctly on all three sudo
shapes; it is just unreachable outside the installer. Alternatives weighed and rejected:
`become_ask_pass = True` in ansible.cfg (verified in ansible-core's CLI that ask-pass is consumed
before `--become-password-file`, so headless would hang; also prompts on NOPASSWD), and a
`--ask-become-pass` shebang (hard CLI conflict with the headless password file).

The local plan counter read 108 while the remote already held 00113; set it to 113 before
scaffolding, so this is 00114.

## 12:05 · action · T1.1–T1.3 — `--play` built

Arg parsing is now a shift loop; `--play <path>` takes the path and everything after it as
ansible-playbook pass-through. Dispatch sits directly after `run_playbook_with_issue_option`
is defined and before the first install step; the runner gained a third parameter block
(`"${@:3}"`) forwarded to all three become branches. Repo root resolves from `BASH_SOURCE`,
so a streamed run.bash aborts. The headless exclusion is checked before `headless_preflight`
via `fatal` (which routes to `hl_abort` in headless mode); the `--optional-only` exclusion
lives in the dispatch block because interactive `fatal` calls the `error` helper, which is
defined after the preflight point — the first placement hit `error: command not found`.

## 12:10 · finding · T3.2 — every abort path exercised

Container is root and run.bash refuses root before dispatch, so the post-root-check paths ran
under `unshare -U` (whoami → nobody, file access retained). All exit 1 with their message:
no path; `--headless` + `--play` (interactive-only, via hl_abort); `--optional-only` + `--play`;
missing file; `README.md` (outside `playbooks/`); `playbooks/../ansible.cfg` (traversal,
caught by `realpath -m` prefix check); `PATH` without ansible. `--help` shows the mode.
`qa-bash` and `qa-patterns`: 232 files OK. `qa-python` cannot run here (ruff version
mismatch in this container, unrelated files); `qa-docs` reports 8 pre-existing failures, all
`.claude/rules/*` pointing at the daemon-installed `DirectoryRoles.md` absent from this
checkout. Live proof (T3.3) needs a password-sudo box: not runnable from the container.

## 12:30 · action · T2.1–T2.2 — one documented way

`rewrite-bare-play-invocations.py` (plan-local, explicit file list, exits 1 on a file it
did not change) rewrote 73 lines across 26 files: `ansible-playbook playbooks/…` and
`./playbooks/…` → `./run.bash --play playbooks/…`, trailing flags kept, the one
`--ask-become-pass` in `docs/post-upgrade.md` dropped since the runner supplies it. Left
alone on purpose: `playbooks/CLAUDE.md` lines 73 and 84, which describe the shebang mechanism
itself, and `.claude/plan/ccy-standalone-extraction.md`, a plan document. `docs/playbooks.md`
gained the paragraph explaining why a bare run fails on password sudo; `README.md` gained a
"Running One Play" section pointing at it.

Gates after the sweep: `qa-bash` and `qa-patterns` 232 files OK; `qa-docs` unchanged at the
8 pre-existing `.claude/rules` findings; ruff 0.16.0 clean on the plan script. `qa-all` as a
whole still needs the host (its ruff pin is not installed in this container).

## 12:32 · handoff · T3.1, T3.3 — owed to a host run

Both remaining tasks need a real box: `./scripts/qa-all.bash` under the pinned ruff, and
`./run.bash --play playbooks/imports/play-claude-yolo.yml` on a password-sudo guest, which
should prompt `BECOME password:` once and complete.

## 12:50 · finding · T3.3 — the prompt branch proven live on a password-sudo guest

From the estate controller, as the guest's desktop user in its own checkout (pulled to
`ba6baaf`), `./run.bash --play playbooks/imports/play-claude-yolo.yml --check` with stdin
closed: the runner printed `sudo needs a password — Ansible will prompt for it`, Ansible
printed `BECOME password:`, EOF answered it, and the first privileged task then failed with
`sudo: a password is required` — exactly the failure a bare run produces, now behind a prompt
that a terminal user answers. `--check` reached ansible-playbook, so pass-through works. The
completion half (a typed password, `changed=…`, exit 0) is interactive by construction and is
the operator's to run.

`qa-all.bash` on that guest aborts before any gate: `shellcheck not found`. The guest is
server-profile-provisioned by this repo, so that is a provisioning gap for a separate plan,
not a finding against `--play`.

## 13:05 · finding · T3.1 — qa-all green bar the pre-existing docs findings

Ran in the controller container with the repo's pinned ruff (`.ruff-version` 0.16.4, in a
throwaway venv with only the `ruff` binary on PATH — putting the venv's `python3` first broke
the manifest gate's yaml import) and the collections from `requirements.yml` installed. 16 of
17 gates pass, including ansible-syntax on all 79 playbooks and the 782 helper tests. The one
red gate is `docs`, and its 8 findings are all `.claude/rules/*` → `DirectoryRoles.md`, a
daemon-installed file this checkout does not carry. Nothing in that set is touched by this plan.

## 13:40 · decision · T1.1 — the flag is gone; the path is the argument

Owner ruling: run.bash takes no positional today and every option is `--x`, so a bare
argument ending `.yml` can only be a playbook. `--play` removed; `./run.bash <playbook>.yml
[args…]` is the form. The arg loop matches `*.yml`, everything after it is forwarded; an unknown
`--flag` is still rejected. All abort paths re-exercised (headless, `--optional-only`, missing
file, outside `playbooks/`, no ansible) and every doc line, the help text and the index row
re-swept to the new form. `--play` survives only in the append-only journal and in Plan 00077's
own acceptance script, which is unrelated.
