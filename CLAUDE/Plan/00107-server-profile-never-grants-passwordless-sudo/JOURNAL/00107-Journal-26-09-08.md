# Plan 00107 — Journal 26-09-08

> **Append-only activity log** for plan 00107. One file per day
> (`00107-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 22:01 · action · — — plan scaffolded

Plan 00107 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 22:04 · finding · T1.1 — where the grant is made

`playbooks/imports/play-basic-configs.yml` "Passwordless Sudo": an unconditional
`blockinfile` into `/etc/sudoers`, marker `## {mark} passwordless sudo for <user>`, block
`<user> ALL=(ALL) NOPASSWD: ALL`. Runs on every profile. `provisioning_profile` is already
resolved in this play (the USB-audio and fwupd tasks gate on it), so the profile is available
at this task with no plumbing.

Nothing in `run.bash` depends on the block: the headless preflight accepts an operator-owned
`NOPASSWD:ALL` or `RUN_BASH_SUDO_PASSWORD_FILE`, and the interactive path prompts. Removing
the grant on servers changes what the playbook leaves behind, not how it is driven.

## 22:06 · decision · T1.1 — `absent` on server, not merely "skip"

Skipping the task under `server` would leave a block written by an earlier desktop-profile
run in place, and the operator would have to know it was there. Declaring `state: absent`
makes a server converge to the state its profile says, which is the whole point: the
provisioner owns the posture, nobody downstream undoes it. Only the play's own marker is
removed. An operator-written drop-in (headless-server-install Step 0a) is a supported
credential and is not this play's to touch, so there is no "assert no NOPASSWD anywhere"
here.

One consequence documented rather than hidden: a server box that relied on the play-written
block as its only credential, with no password file, passes preflight and then loses the
block mid-play; later `become` tasks fail with `Missing sudo password`. That is the honest
outcome — the box has no credential the operator owns — and `docs/headless-provisioning.md`
now says so.

## 22:08 · action · T1.1 T1.2 — change made

- `play-basic-configs.yml`: task renamed to say what it does on each profile;
  `state: "{{ 'absent' if provisioning_profile == 'server' else 'present' }}"`; rationale
  comment.
- `docs/playbooks.md`, `docs/architecture.md`, `docs/headless-provisioning.md` updated.
- README Active row added.

Next: syntax check + qa-all (T2.1), then a live server-profile proof (T2.2).

## 22:14 · action · T2.1 — syntax, lint, qa-all

`ansible-playbook --syntax-check` rc=0; `ansible-lint` rc=0 (four pre-existing warnings, none
on the changed task). `./scripts/qa-all.bash` rc=1 on exactly the seven pre-existing dead
`DirectoryRoles.md` links under `.claude/rules/`, unrelated to this plan and present before it;
every other gate green (ansible-syntax 78 playbooks, helper-tests 255, patterns 200 files).
T2.1 done. T2.2 (live server-profile proof) follows on a real headless box.
