# Plan 00070 — Journal 26-08-12

> **Append-only activity log** for plan 00070. One file per day
> (`00070-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:10 · action · — — plan scaffolded

Plan 00070 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:14 · action · P1 — research workflow dispatched

Framing established from repo source before searching anything: CCY ships exactly one
browser (`agent-browser` = Playwright Chromium), installed in
`files/var/local/claude-yolo/Dockerfile` alongside ~17 Chromium apt runtime libs, and
configured headed against the host Wayland socket via ozone. The user's ask is a SECOND,
lightweight, JS-executing engine alongside it — not a replacement.

Dispatched a background Sonnet workflow (run `wf_46c3b7a3-091`, task `wrdzhahmz`):

- **Scan** (5 parallel): four independent search angles — agent-native headless browsers,
  alternative rendering engines, DOM+JS runtimes / slim Chromium builds, terminal browsers
  and scraping CLIs — plus one repo-reading pass producing the integration constraints.
- **DeepDive**: dedupe across angles in plain code, drop scan-stage disqualifications,
  rank by cross-scout agreement + JS completeness, cap at 7, one agent per survivor writing
  `research/candidate-<slug>.md` to a fixed template.
- **Critic**: completeness pass — unverified/vendor-only claims, contradictions between
  docs, whole option classes never searched.

Every agent was given the project's evidence discipline verbatim: cite every claim, tag it
`[MEASURED-INDEPENDENT]` / `[VENDOR CLAIM]` / `[UNKNOWN]`, and **render no verdict** — the
pick is a separate Opus triage step so that marketing copy cannot quietly become the
decision.

Cap and disqualification lists are `log()`ged by the script rather than silently applied,
so the triage can see what was left out.

## 10:14 · action · — — failsafe recovery cron

Created hourly non-durable recovery cron, id `dc0e0f9d` (fires at :23). Not a heartbeat —
work continues at full speed; it exists only to resume after an external stall.

## 10:30 · finding · P1 — the tool we already ship has a second engine built in

While the research workflow ran, measured the live baseline in this CCY container rather
than assuming it. Three findings, all reproduced from commands, not inferred.

**1. `agent-browser --engine lightpanda` already exists.** Installed version is 0.31.1 and
its `--help` carries:

```
--engine <name>   Browser engine: chrome (default), lightpanda (or AGENT_BROWSER_ENGINE)
```

Probed it in three steps, each failure naming the next obstacle:

```
$ agent-browser --engine lightpanda read https://example.com
✗ Headed mode is not supported with Lightpanda (headless only)

$ agent-browser --engine lightpanda --headed false read https://example.com
✗ Custom Chrome arguments (--args) are not supported with Lightpanda

$ agent-browser --config <clean.json> --engine lightpanda read https://example.com
✗ Lightpanda not found. Install it from https://lightpanda.io/docs/open-source/installation
  or use --executable-path.
```

So the engine is wired in and reaches the point of looking for the binary; the only thing
missing is the binary itself. `agent-browser install` does **not** fetch it (`browsers/`
holds only `chrome-150.0.7871.115`).

This changes the likely shape of the answer: the plan may not need a second *CLI* and a
second skill at all — it may need a binary in the image plus a config change. That is a
much smaller, much more maintainable integration than adding a parallel tool. Not treating
this as decided: the research + triage still has to establish that Lightpanda is the right
engine and that its JS fidelity is real. But "add a whole second stack" is no longer the
default framing.

**2. Our own config.json actively blocks the alternative engine.** Both settings the
Dockerfile writes to `/root/.agent-browser/config.json` — `"headed": true` and the Chromium
`"args"` string — are rejected outright by the Lightpanda path. Any integration must
restructure that config, not just add a binary. Neither obstacle was visible from reading
the Dockerfile; both came from running it.

**3. Our `browsing` skill documents a CLI that no longer exists.** `SKILL.md` teaches
`agent-browser run "navigate …; extract text"` throughout. Against 0.31.1:

```
$ agent-browser run "navigate https://example.com; extract text"
Unknown command: run
```

The real CLI is verb-per-invocation (`open`, `read`, `snapshot`, `click`, `get text`) with
`&&` chaining and a daemon holding the session. So every agent that follows our skill
issues a broken command first. Worse, agent-browser now **ships its own version-matched
skills** (`agent-browser skills get core --full`) and explicitly tells agents to prefer them
over guessing from flag docs — which makes our hand-maintained copy both wrong and
redundant. This is drift of exactly the kind Plan 00069 catalogued, in a file whose whole
job is to be correct. It is in scope here because this plan has to touch the browsing skill
anyway.

**Measured Chrome baseline** (for the triage to compare against), `example.com`, cold after
`close --all`:

| Metric                        | Value                                        |
| ----------------------------- | -------------------------------------------- |
| `open` + `read` wall time     | 1581 ms                                      |
| Processes spawned             | 14 (`chrome` + 2 `chrome_crashpad`)          |
| Summed RSS across them        | 1,307,748 kB ≈ 1.28 GB                       |
| Chrome binary on disk         | 281,693,432 B ≈ 269 MB                       |
| `browsers/` dir               | 381 MB                                       |
| `agent-browser` npm package   | 78 MB                                        |

Caveat recorded so it is not over-read later: summed RSS **double-counts shared pages**
across the 14 processes, so 1.28 GB is an upper bound on real memory, not a clean figure.
It is still the right order-of-magnitude anchor — this is a full Chrome 150, on the
simplest possible page.

## 10:45 · finding · T1.5 — triage.bash written and run; Lightpanda matches Chromium on JS

Wrote `triage.bash` (plan-local, read-only, re-runnable, report to `logs/`) rather than
pasting probes into chat, per `CLAUDE/PlanTriage.md`. It answers the only question that
decides this plan: **does the cheap engine actually run the JavaScript?** Footprint is
worthless if it does not.

Eight fixtures, each rendering a marker string only if one specific capability works,
served from a local `python3 -m http.server`, opened in both engines with the same 1s
grace period. **Lightpanda 0.3.6 matched Chromium 150 on all eight**, including the ones
that separate a real engine from an HTML parser:

| Fixture   | Capability                                     | Chromium | Lightpanda |
| --------- | ---------------------------------------------- | -------- | ---------- |
| static    | plain HTML (control)                           | YES      | YES        |
| dom       | synchronous inline DOM write                   | YES      | YES        |
| defer     | `DOMContentLoaded` listener                    | YES      | YES        |
| timeout   | `setTimeout` 300 ms                            | YES      | YES        |
| fetch     | `fetch()` + JSON + render                      | YES      | YES        |
| modern    | ES module, `#private` field, optional chaining | YES      | YES        |
| shadow    | custom element + shadow DOM                    | YES      | YES        |
| react     | React 18 UMD from CDN, client-side render      | YES      | YES        |

Cost of one cold `open` + rendered-text extraction of `example.com`:

| Metric        | Chromium (headless) | Lightpanda | Ratio          |
| ------------- | ------------------- | ---------- | -------------- |
| wall time     | 1177 ms             | 379 ms     | 3.1× faster    |
| processes     | 15                  | 1          | 15× fewer      |
| peak RSS      | ~1345 MB            | ~25 MB     | ~52× lighter   |
| binary/disk   | 381 MB browsers dir | 153 MB     | 2.5× smaller   |

Real pages (`news.ycombinator.com`, a Wikipedia article, `react.dev`) returned comparable
extractions from both; Lightpanda actually returned *more* text on the Wikipedia article
(8173 vs 6640 chars) and on `react.dev` (8313 vs 6655), so the light engine is not
silently truncating.

**Three harness bugs had to be fixed before any of this meant anything.** Recording them
because each produced a confident, wrong-looking table that could have been believed:

1. **`agent-browser read <url>` does not render.** Its own `--help` says it fetches a URL
   as agent-readable text — an HTTP GET plus HTML-to-text extraction, no browser. The first
   matrix had **Chromium failing every JS test**, which is obviously false, and that
   absurdity is the only reason the harness got audited instead of the engines. The render
   path is `open <url>` then `get text` (or a bare `read` with no URL, which does read the
   active tab's DOM). Byte-identical output between two different engines was the tell.
2. **Memory sampled after the command returned**, by which point the single Lightpanda
   process had exited — reporting `0 kB`, i.e. a misleading empty result dressed as a
   measurement. Now sampled continuously during the run and the peak reported.
3. **`measure_engine` was passed the display label `"LIGHTPANDA"` as its engine key**,
   while `engine_prefix` dispatches on a case-sensitive `"lightpanda"` — so the row
   labelled Lightpanda silently ran **Chromium**. Caught only because RSS came back 0 for a
   pattern no chrome process matches. Fixed by separating the engine key from the display
   label, and the script now **hard-fails** on a 0 kB measurement instead of printing it.

The fidelity matrix and real-site rows were unaffected by bug 3 (they always passed
lowercase keys); only section 3 was wrong, and it has been re-run.

Numbers are honest about their limits: summed RSS double-counts shared pages across
Chromium's 15 processes, so ~1345 MB is an upper bound, not a clean figure. The ratio is
directionally safe — one process versus fifteen is not a rounding artefact.

## 10:47 · thought · P2 — what the evidence is starting to say

Still holding the decision until the research + critique land, but naming the shift so it
is not smuggled in later: the question this plan was created to answer ("which second
engine should we add?") may have a smaller answer than "add an engine". The tool we
already ship has the engine wired in; what is missing is a 153 MB binary and a config that
does not forbid it. If the deep dives surface nothing better, Phase 3 is a Dockerfile layer
and a config fix, not a parallel browser stack.

## 11:20 · decision · P2 — Lightpanda, and why the research's own disqualification was overruled

Research workflow finished: 11 agents, 0 errors, ~1.74M subagent tokens, 5 deep dives plus
a completeness critique. All output in `research/`.

**The critique independently reached the same finding I had measured** — its §2a names
`agent-browser`'s existing `--engine lightpanda` flag as "the most consequential unresolved
contradiction in the folder", and its §4 proposes as the highest-leverage probe almost
exactly the commands I had already run. Two independent routes to the same place is about
as good as corroboration gets.

**Where I overruled the research.** A scan agent disqualified Lightpanda at the scan stage
— it never got a deep dive — with: *"Does not render the DOM at all… fails the 'still
rendering the DOM' requirement outright."* The underlying fact is correct: no CSS layout,
no paint. The inference is not. It builds the DOM and executes the JavaScript, which is
what reading, extraction and scraping need, and the fidelity matrix shows it doing so
identically to Chromium. The honest statement of the limitation is narrower: **it cannot
produce pixels or geometry.** Had I taken the scan's verdict at face value the plan would
have shipped one of the runners-up — all of which are heavier, deprecated, or the same
Blink engine in different packaging.

This is the reason the research agents were instructed to report and not to recommend. The
facts they gathered were good; this particular conclusion drawn from them was wrong for our
use, and only the in-container measurement could settle it.

**Runners-up and why they lost** — recorded in `PLAN.md` Decision 1. Worth noting that two
of the five deep-dive slots went to what turned out to be the *same* binary
(`chrome-headless-shell` via two packaging routes), which the critique caught (§2b). It is
genuinely interesting — same Blink engine at ~355 MB RSS against full Chrome's ~1204 MB —
but it is *tuning what we already ship*, not a second engine, so it is logged as a
follow-up rather than folded into this plan.

**Default stays Chromium** (Decision 2). Lightpanda's failure mode outside its competence
is silent, not loud: `screenshot` exits 0 while writing a placeholder PNG, `get box` exits
0 while returning `height: 100000000`. A cheap-by-default rule would hand agents
confidently wrong output. Opt-in via `agent-browser-lite` keeps every existing invocation
byte-for-byte unchanged.

## 11:25 · action · P3 — integration landed (container only; HOST rebuild pending)

- **Dockerfile**: pinned Lightpanda 0.3.6, verified against the sha256 GitHub publishes for
  that exact asset (`e438c0ad…`), which matched the artifact I downloaded independently.
  Installed to `/usr/local/bin/lightpanda`; the layer runs `lightpanda version` so a broken
  download fails the build rather than shipping quietly.
- **Its own config is mandatory, not cosmetic.** The shipped Chromium config
  (`headed: true` + ozone `args`) is rejected outright by the Lightpanda path, and
  `AGENT_BROWSER_HEADED=false` was measured *failing* to override it despite the documented
  env-over-config precedence — so a separate `--config` is the only mechanism that works.
  That is exactly the kind of thing that would have been guessed wrong from the docs.
- **Heredocs avoided.** I first wrote the config and wrapper as `RUN cat <<'EOF'` heredocs,
  then checked: the Dockerfile carries no `# syntax=` directive and CCY builds via buildah,
  where `RUN` heredocs are not safe. Rewrote both as `printf '%s\n'` (the style already
  proven in this file) and replayed the exact generated artifacts through `sh -n`,
  `shellcheck` and `jq` rather than trusting them to be right.
- **Version pins moved together**: Dockerfile label 2.22 → 2.23,
  `REQUIRED_CONTAINER_VERSION` 2.22 → 2.23 (mismatch causes infinite rebuild loops),
  `CCY_VERSION` 3.29.0 → 3.30.0, plus a changelog entry.
- **Skill rewritten and 730 lines deleted.** `COMMANDLINE-USAGE.md` and `EXAMPLES.md` are
  gone. Keeping them would have meant rewriting 97 wrong examples into a hand-maintained
  duplicate of documentation the CLI now ships version-matched — which is precisely how
  they came to be wrong. The play now also *removes* them from the build context, because
  the copy task only ever added files and the Dockerfile copies the whole directory, so a
  deletion in the repo would otherwise keep shipping stale docs forever.

QA green (425 files). **Not verified end-to-end**: the binary lands at
`/usr/local/bin/lightpanda` only after an image rebuild, which is a HOST action — this is a
CCY container, so edit-and-commit only. Everything measured so far used the binary at a
cache path via `--executable-path`. Task 3.6 re-runs `triage.bash` inside the rebuilt image
to confirm it works with no override.

## 14:20 · finding · T3.6 — first HOST run of triage.bash gave the wrong advice

The owner ran `triage.bash` from the host shell rather than inside a CCY session. The
dependency gate fired as designed, but its message was actively misleading:

```
ERROR: 'agent-browser' is not installed in this container.
  The CCY image is built by files/var/local/claude-yolo/Dockerfile.
  Add it there and rebuild — do NOT install it by hand.
  (note: close --all did not succeed: ... agent-browser: command not found)
```

Every line of that is wrong for the situation. It was not "this container" — it was the
host. `agent-browser` is not missing from the image, it has never existed on the host and
is not supposed to. And the remedy it prescribes — go and edit the Dockerfile — is the
wrong fix for a problem that does not exist. A gate that sends someone to modify the build
because they opened the wrong shell is worse than no gate.

The trailing `close --all` note came from the EXIT trap calling `close_sessions` after the
early exit, stacking a second confusing error under the first.

Two fixes, both in the script rather than in advice:

1. **Location check runs BEFORE the dependency gate.** `/run/.containerenv` (podman) or
   `/.dockerenv` present ⇒ in a container; absent ⇒ host, and the script now says so and
   prints the two commands that actually help (start `ccy`; deploy first if the image
   predates 2.23). Verified both branches: this container has `/run/.containerenv`, and
   the host branch was exercised directly.
2. **`close_sessions` returns early when `agent-browser` is not on PATH** — nothing was
   opened, so there is nothing to close, and the note only ever added noise under a real
   error.

**Also added section 7 — and it closes a real gap I had left.** Sections 1-6 all drive
Lightpanda from a probe cache via `--executable-path`, which proves the *engine* works but
says nothing about whether the *Dockerfile* installed it. Task 3.6 is exactly that
question, and the script could not answer it. Section 7 now checks
`/usr/local/bin/lightpanda`, `/root/.agent-browser/lightpanda.json` and
`/usr/local/bin/agent-browser-lite`, then drives `agent-browser-lite` with **no overrides
at all** — what an agent would actually type.

Run just now in this (pre-rebuild) container it reports all three ABSENT and names the
deploy command, which is the correct answer for an image at 2.22. After the host rebuild
it should report PRESENT and return `MARKER-DOM`. That is the acceptance condition for
Task 3.6, and it is now machine-checked rather than a claim.
