# Plan 00039 — Journal 2026-09-02

## 22:10 · action · — plan slimmed; old PLAN.md archived verbatim

PLAN.md had grown to 33,507 bytes, past the 25 KB warning tier. It was
renamed to [PLAN_archive.md](../PLAN_archive.md) unchanged apart from an
archive banner, and a lean PLAN.md was written in its place with the same
header, status and task tree (every tick preserved as it was).

Content relocated rather than deleted:

- Research summary, the resolved decision-gate list, Technical Decisions 1-6
  and the Risks table now live in [DECISIONS.md](../DECISIONS.md).
- The dated narrative from the old "Notes & Updates" section is summarised
  here; the full prose is in PLAN_archive.md. In order: plan created with
  geeqie `--remote` as the assumed primitive; host probe showed geeqie 2.6.1
  has no `--remote`, `imv` briefly promoted; manual test proved geeqie 2.x
  is implicitly single-instance, geeqie reinstated; user redesigned `--view`
  as an orthogonal modifier with strict class filter, conditional pre-warm,
  two-step TUI and a startup banner; a live 3-frame burst exposed the
  window-not-refreshing bug, root-caused to D-Bus session inheritance and
  fixed with `geeqie --file=` under `setsid -f`.

## 22:15 · finding · — plan state lags the code

Commits e6048c51 and 669fe51f already landed `--view` / `--view-jpg`, the
`gum choose` launcher and the refresh fix in `files/home/.local/bin/ftp-camera`,
yet the plan still reads "Not Started" with every task ⬜. The slimming
pass deliberately did not change any tick; reconciling status against the
shipped script is a separate piece of work for the plan owner.
