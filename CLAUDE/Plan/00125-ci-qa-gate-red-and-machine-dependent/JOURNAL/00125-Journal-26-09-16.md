# Plan 00125 — Journal 26-09-16

> **Append-only activity log** for plan 00125. One file per day
> (`00125-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 01:03 · finding · T3.4 — the skip count read the whole capture, and both decoys beat it

The timestamp problem the previous day-file's closing entry admitted resolves itself here:
this is a new day-file, so the first honest stamp has nothing to overshoot. `date` reads
01:03 and that is what this says. Yesterday's file ends with entries stamped 01:02–01:04
that were invented; they stay as they are, because the journal is append-only.

The round-3 prompt I wrote asked one question about my own round-2 fix: the skip count came
from `[[ "$helper_out" =~ skipped=([0-9]+) ]]` against the **entire** captured output, and
`=~` takes the first match anywhere. A line earlier in the capture containing the text would
win the match ahead of `unittest`'s result line. I wrote in the prompt that I had not tested
it. Measured both ways before changing anything:

```
OLD code, decoy warning line : 99 (should be 2)
OLD code, decoy skip reason  : 42 (should be 1)
```

Reachable, then — and by the most ordinary route imaginable. A skipped test in this suite
prints its own reason, and this plan's Task 3.1 *wrote three of those reasons*. One of them
mentioning a count would have silently replaced the number on the stage line. The failure
mode is the one this whole plan is about: a line that looks like a measurement and is not,
with nothing to distinguish it from the real thing.

The count now comes from `unittest`'s own `^(OK|FAILED)` result line — the only place it is
authoritative — and is only then matched:

```
OK                                                         -> 0
OK (skipped=1)                                             -> 1
OK (skipped=1, expected failures=1)                        -> 1
FAILED (failures=1, skipped=3)                             -> 3
decoy: warning line says skipped=99, result says 2         -> 2
decoy: a skip REASON contains skipped=42                   -> 1
```

Six shapes, four real and two adversarial, all correct. A real local run still reports
`helper-tests: Ran 1456 tests, 1 skipped`, so the hardening is not paid for with a
regression.

**`TOOL_ABORT` re-verified against the source rather than against my memory of it.** Round 2
found the `no-run-summary` guard refusing `qa-all.bash`'s `exit 2` paths. The fix added a
pattern, and a pattern is worth only what it matches, so it was checked against every
`ERROR:` string the script can actually print — grepped out of `qa-all.bash`, not recalled:

```
True  ERROR: Missing required tools (ansible-playbook). Install them and re-run.
True  ERROR: Missing required tools (node / extensions node_modules). Install them and re-run.
True  ERROR: Missing required tools (semgrep). Install with: pipx install semgrep
True  ERROR: Missing required tools for ansible check. Install them and re-run.
True  ERROR: Missing required tools. Install them and re-run.
True  ERROR: docs gate could not produce a result (zero-file scan or checker crash).

overlaps a stage line? False False
unrelated ERROR ignored? True
```

All six terminal states recognised, no overlap with a stage verdict in either direction, and
an unrelated `ERROR:` line still leaves the capture judged truncated — which it would be.

## 01:04 · finding · T5.2 — CI at HEAD is down to the one open decision, and the divergence is now on the page

Run `35041998528` (`29ceee97`) — the newest, at HEAD:

```
✗ docs: 8 finding(s) across 71 files
✗ QA FAILED: 8 errors in 914 files
```

That is the whole failure. All 35 other stages pass, and `914` matches a local run exactly.
Every gate the three unmaskings revealed — `panel-sections`, `freezelib`, `js` — is green on
both machines. What remains red is Cause A alone: Task 2.1, the owner's decision.

The line worth reading twice:

```
✓ helper-tests: Ran 1456 tests, 2 skipped        (runner)
✓ helper-tests: Ran 1456 tests, 1 skipped        (here)
```

Before this plan those two lines were byte-identical, because `unittest` counts a skipped
test inside `testsRun`: two machines running different subsets of the same suite reported
the same sentence. They no longer do. `PLAN.md`'s Task 5.1/5.2 citations move from
`35038606426`/911 files to this run, so the page cites the run that is current.

`./scripts/qa-all.bash` locally: exit 0, `✓ QA passed: 914 files checked`.

## 01:09 · finding · T2.1 — the decision gate had three options and only one of them was real

Task 2.1 has sat as "the owner's call between (a), (b) and (c)" since Phase 2 was written.
Before leaving it there any longer I checked the arguments I had recorded *for* the owner,
because a decision gate presenting options that are not available is not a decision gate —
it is a delay with a table attached.

The docs gate **passes here** (`✓ docs: 71 files OK`) and reports 8 findings in CI. That
alone is worth stating plainly: the gate this plan has been calling "Cause A" is itself
machine-dependent, which is the plan's own subject rather than an exception to it.

All 8 CI findings are one line repeated — `.claude/rules/*.md:15` →
`../hooks-daemon/CLAUDE/DirectoryRoles.md` → `target does not exist`. Then the fact that
narrows everything:

```
8   .claude/rules/*.md files carrying <!-- hooks-daemon-rule-version -->
8   findings reported by CI
```

The same eight. Of fifteen tracked rule files, the seven this repository hand-wrote
contribute nothing; every finding is content the daemon's installer generates
(`install/directory_role_rules.py`, via `directory_roles_link()` and re-deployed by
`sync_directory_role_rules()`).

That kills **(c)** more firmly than "it would regress at the next upgrade" — the installer
re-renders the link on every sync, and the daemon's own `docs_qa` `rules-file-shape` check
enforces the pointer-only contract the edit would break, so it would be fighting a check
for as long as it survived. And the daemon's module docstring records the reasoning
deliberately: the target exists "at a fixed, predictable path the moment the daemon itself
is installed", because a client install clones the whole daemon repo. **The link is not
broken. Its premise is false in CI** — `.claude/hooks-daemon/` is gitignored
(`.gitignore:53`) and no file in `.github/workflows/` mentions the daemon at all.

**(b)** was already on thin ice and the repo's own rule names the shape: "Missing
Dependencies — Fail Fast, Fix in IaC" prohibits "make the script tolerate the missing tool
(skip-if-absent, `|| true`, advisory-only mode)". A link check that stops checking when the
tree is absent is that, one indirection away, and it would pass a genuinely broken link in
the only environment that cannot tell.

So **(a)** — install the daemon in CI before QA — is what is left, and it is what the
Missing-Dependencies rule prescribes: the daemon is a real dependency of the docs graph and
CI does not have it.

**Not implemented, deliberately.** What the rules settle is which options are admissible;
what they do not settle is whether every QA run should depend on an external repository's
installer being reachable. That is a real cost and it is the owner's to weigh, so the gate
stays open — but it is now a one-option confirmation rather than a three-way study, and the
work is ready to land the moment it is confirmed. `FINDINGS.md` carries the citations.

`qa-all.bash`: exit 0, 914 files, `✓ docs: 71 files OK`. `PLAN.md` 17,382 bytes, back under
the 18,000 advisory — the Task 2.1 block now points at `FINDINGS.md` instead of restating it.

## 01:17 · finding · T4.1 — the suite already contains both designs, and my own headline claim was too broad

Reading the CI log for the Task 2.1 work, something did not fit. `✗ docs: 8 finding(s)` at
line 746 is followed by **25 passing gates** and only then `✗ QA FAILED`. If this plan's
headline is "the abort at the first failing gate hid everything", that log contradicts it.

It does not, and the reason is worth having written down. `qa-all.bash` runs its stages two
ways, counted mechanically rather than by eye:

```
accumulating stages (|| rc=$? then FAILED++): 7
total 'exit 1' lines:                         29
=> hard gates that abort immediately:         28
'exit 2' missing-tool aborts:                  7
```

`docs` is one of the seven accumulating stages — `|| rc=$?`, `FAILED++`, run continues, all
reported together at `:595-602`. So **Cause A never masked anything.** It went red on
2026-08-31 and every gate behind it kept running for eleven days. Cause B landed in hard
gates, and that is what masked three.

So `PLAN.md`'s "the mechanism that hid it all" was overstated, by me, in the plan's own
summary. It hid Cause B. Corrected on the page rather than left as a flourish that reads
well and is not true.

**And the 25 is now enumerated, not arithmetic.** This plan already got this number wrong
once (26 → 25), so `grep -c` was not trusted twice. Listing every `exit 1` after line 152
gives **27**; subtract the final run summary (`:602`, not a gate) and `helper-tests` itself
(`:153`, the abort point rather than something after it) → **25**. The figure on the page
was right and is now checked by enumeration.

**This narrows Task 4.3 as well.** Its option (1) — run every gate, report all verdicts,
exit non-zero at the end — is not a design to invent. It is the design already in force for
seven stages of the same script, and the final summary at `:595-602` was written for it. The
open question is whether to extend it to the other 28. The repo's recurring lesson turned
out to apply to the plan documenting it: the right answer already existed one directory
over — here, sixty lines up in the same file.

Also corrected in passing: Task 1.1 claimed `verdicts.py` had **32** tests; it has **35**,
confirmed twice (`grep -c` on `def test_` and `Ran 35 tests ... OK`). And Task 1.1's
paragraph restating `triage.bash`'s R2 argument is now a pointer to `triage.bash:18-23` —
the script header owns that fact, and `PLAN.md` duplicating it is the exact shape R1
prohibits. That reclaimed the headroom the additions had spent: 17,717 bytes.

`qa-all.bash`: exit 0, 914 files checked.

## 01:39 · action · T4.4 — round 3, and the line that had been wrong twice now has a test

Round 3 came back FIX-BEFORE-MERGE with 2 blocking, 3 should-fix and 3 nits. Its Blocking 1
was the uncommitted `qa-all.bash` — already landed as `698f7548` before the report arrived.
The rest, each verified here before acting rather than taken on the reviewer's word.

**Blocking 2 — my `podfreeze` comment stated a mechanism that does not hold there.** It
claimed `assert_on_host "$@"` would feed the tool's argv into the container-marker slots.
Reproduced both shapes:

```
podfreeze shape  (top-level shift loop, then the call): 0 arg(s): []
lxcfreeze shape  (main "$@", parse_args shifts a copy): 2 arg(s): [freeze somename]
```

The reviewer is right. `podfreeze`'s arg loop shifts every branch, so `$#` is 0 by the call
— the remedy there is *meaningless*, not dangerous. `lxcfreeze`'s call is inside `main()`,
whose positionals survive `parse_args "$@"`, so its comment was accurate and mine was a copy
of it that did not apply. The conclusion (stay bare) holds in both; the reason differs, and
now each says its own. Also dropped *"no caller supplies them"* — `test-freezelib.bash`
supplies both at four call sites, which is the sentence immediately before it. A comment a
reader can falsify in thirty seconds discredits the ones around it.

**Should fix 2 — the host instruction would have sent the owner to one play when two are
needed.** `tasks/deploy-freeze-lib.yml` deploys only `freeze-common.bash`;
`play-podfreeze.yml:75` and `play-lxcfreeze.yml:91` each deploy their own binary and merely
*include* the shared task. All three files changed, so "either freeze play" leaves the other
binary drifted and the host still red — 28 hard gates short, since the drift gate aborts
before `helper-tests`. Corrected to name both.

**Should fix 1 — the coverage denominator traded a false 100% for a false alarm.** Round 2's
fix counted symbols with an unanchored `search` on the raw line, which counts a symbol
*mentioned* rather than a line that *is* a verdict. Measured on the real shape
`qa-patterns.bash:362` emits:

```
✗ patterns: 2/260 files failed
  ✗ scripts/a.bash          <- counted
  ✗ scripts/b.bash          <- counted
=> 1 of 4 ... 2 unrecognised
```

`unrecognised` is supposed to mean *the parser lost a stage line*, so a failing gate would
have manufactured evidence of this parser being broken. Now anchored with a permissive
prefix — BOM, harness tab-fields, an optional token-and-space for the timestamp — so a
malformed timestamp still lands in the denominator while indentation and prose do not. ANSI
is stripped *before* the count rather than tolerated inside it, which also fixes the
measured `-1 unrecognised` when colour sits between the symbol and the name. **Delta 0 on
all four real captures** (38 either way, `unrecognised=0`), which is exactly when to move it.

**Should fix 3 — the line revised three times with no test. This was the right finding.**
Both readers now live in `scripts/lib/qa-helper-summary.bash`, sourced by `qa-all.bash` and
driven by `scripts/test-qa-helper-summary.bash` (16 cases), wired in as its own gate. The
test drives the *shipped functions*; a copy of the expression could not catch a change to
the thing it copied. Falsified against all three historical defects:

```
mutant: the whole-capture match (round 3)   -> 6 failures
mutant: requires the closing paren (round 2) -> 2 failures
mutant: answer 0 rather than refusing        -> 3 failures
restored                                     -> passed: 16 failed: 0
```

The third mutant is the reviewer's nit 3 made real: an unreadable capture used to report
`0 skipped`, indistinguishable from a clean run — the same defect a fourth time, in the one
line whose entire job is to tell those apart. It now fails the run.

**A gate's inventory check called the sourced library a gate.** `helpers/docs/link_check.py`
derives the gate list from `qa-all.bash` and matched my `source` line, demanding a row in
`CLAUDE/QA.md`. Adding one would have made the table claim a library checks something — the
antipattern of editing reality to satisfy a checker. The discovery now skips `source`/`.`
lines, with three tests; the genuinely-new gate got its row.

**Nits 1 and 2 done too**: a tool abort now prints `QA-VERDICTS-ABORT <side> <message>` so
the reader learns *which* tool was missing rather than that one was, and the marker
`no-run-summary` became `no-terminal-line`, which is what it has meant since round 2.

**Every line citation in `PLAN.md` and `FINDINGS.md` re-derived, because my own changes
moved them.** Adding two aborts shifted the counts: 28 hard gates → 30, 35 stages → 37,
`:595-602` → `:613-620`, drift abort `:130` → `:137`. Enumerated, not arithmetic — my first
pass computed 28 gates behind `helper-tests` and the list showed 27, because `NR > 159`
swept in the abort's own `exit 1` at `:160`.

The distinction that matters more: **25 was a historical number and 27 is a current one.**
25 gates stood behind the abort *at the masked commit*; saying 27 there would rewrite what
CI actually skipped. Both figures are now labelled with which commit they describe, and
`FINDINGS.md` says so once at the top of the table rather than per line.

`qa-all.bash`: exit 0, **918 files**, 1464 helper tests, `✓ helper-summary-readers: passed:
16`. `PLAN.md` 17,974 bytes — Task 4.1's three-causes prose moved to `FINDINGS.md`, which is
where depth belongs anyway.

## 01:46 · finding · T2.1 — I recommended an option without checking it works or what it costs

Handing Task 2.1 back, I told the owner option (a) was the only admissible choice, that I
could land it immediately, and that its cost was "a dependency on an external repository's
installer". Two of those three I had not verified. Recommending a fix on the strength of
having ruled out the alternatives is not the same as knowing the fix works — and this plan
exists because a check that was never really run still printed something confident.

**Does it actually fix the gate?** Yes, and now checked rather than assumed:
`.claude/rules/../hooks-daemon/CLAUDE/DirectoryRoles.md` normalises to a file that exists on
an installed machine. Nothing else about the eight findings has to change.

**What does it cost?** The first measurement was alarming and wrong to quote as-is:

```
452M  .claude/hooks-daemon          <- the installed tree here
336M  .claude/hooks-daemon/untracked  <- the daemon's OWN runtime state
 49M  .claude/hooks-daemon/.git
 16M  .claude/hooks-daemon/.venv      <- built locally, not cloned
```

Three quarters of it is runtime state a fresh clone never carries, and the venv is built
rather than fetched. So the honest figure is a shallow clone of one public repository, not
half a gigabyte — and if I had passed on the 452M without breaking it down, the owner would
have been weighing a number that is not the cost of anything.

The point that survives the correction, and is the actually useful one: **CI needs a single
12KB file**, and option (a) as I phrased it fetches an entire repository to obtain it.
`--depth 1` is the sane form. What the owner is really weighing is not bytes but whether
every QA run should depend on another repository being reachable — which is a genuine
trade-off, and still theirs.

## 02:14 · finding · T2.1/T4.4 — round 4: I foreclosed a live option, and left the same bug one function away

Round 4 confirmed all six round-3 fixes real, and CI had already run `ced20d08` (run
`35045164017`): sole failure `✗ docs: 8 findings`, all 37 stage names present,
`helper-summary-readers` green on a runner first try, and `2 skipped` against `1` here. The
signal works. Four findings against it, all verified here before acting.

**The one that matters most: Task 2.1 was never single-option, and I told the owner it was.**
There is a **(d)** — exclude daemon-GENERATED files from the link check, keyed on the
`hooks-daemon-rule-version` marker. My instinct was that this is (b) wearing a hat. It is
not, and the distinction is what `CLAUDE.md`'s rule actually turns on:

| | (b) conditional on the tree | (d) exclude daemon-generated |
| --- | --- | --- |
| Depends on the environment | **yes** | no |
| Can pass a broken link | yes, where it cannot tell | these were never ours to audit |
| Needs a network | no | no |

And it is not even a new kind of judgement here — `link_check.py:214-223` **already**
excludes `.claude/hooks-daemon/`, `ccy/`, `skills/` and `agents/` on exactly that reasoning.
The 8 rule files are daemon-owned content deployed outside the daemon's directory; the
exclusion follows ownership rather than path.

I had argued myself from "three options" to "one" and reported that with confidence. Ruling
out two is not the same as proving no third exists, and the evidence for (d) was in a file I
had read and quoted in the same session. Task 2.1 is reopened with two live options.

**The same bug, one function away, in the file I wrote about it.** `helper_test_summary`
still used an unscoped `grep -oE`, and `-o` prints EVERY match:

```
✓ helper-tests: Ran 3 tests
Ran 1464 tests, 1 skipped
```

The stage line becomes **two lines** — worse than a wrong number, because the verdict parser
reads the first as the stage and loses the second. I fixed `helper_skip_count`'s scoping,
wrote three paragraphs about why scoping mattered, and left its sibling in the same file
with the identical cause. Now scoped to a line unittest wrote, taking the LAST match (a
decoy at column 0 would otherwise win). Four tests added; the gate is 20 cases.

**The counts were wrong a third time, and the method was the problem.** I had been counting
`exit 1` occurrences as a proxy for gates. That proxy died the moment Task 4.4 gave
`helper-tests` a second abort — so I counted sites, not gates. Correct figures, derived from
the stage names a real run prints rather than from the source:

```
hard gates                      29   (not 30)
behind the drift abort          27   (not 28)
behind helper-tests             26   (not 27)
at 29ceee97, behind helper-tests 25  (unchanged — and re-derived the same way)
```

37 stage names was right, but `7 + 30` was never its decomposition: the seven accumulating
gates emit **eight** named lines, because `qa-bash.bash` prints `bash` and `shellcheck`. So
36 gates, 37 names. `CLAUDE/QA.md:19`/`:41` had said thirty-five/twenty-eight — drift my own
commit introduced, and `link_check` compares gate NAMES, not counts, so nothing caught it.

The lesson is the method, not the arithmetic: **count the output, not the source.** The run
is ground truth and this plan already ships a parser for it. Written into `FINDINGS.md` with
the snippet, so the next recount does not repeat mine.

Also fixed: `:602` → `:597` (the historical citation was pinned to `29ceee97` but read off a
later commit), `qa-deployed-drift.bash:194` → `:192`, `PLAN.md` "914 files" → 918, the
`SYMBOL_BEARING` docstring's "whatever shape it has taken" (a two-token timestamp would drop
out of *both* sides — stated rather than left to be found), the `podfreeze` comment's "shifts
every branch" (two branches exit instead), and a comment in my own test claiming a diagnostic
"is captured rather than discarded" while `2>/dev/null` discarded it — the channel is now
asserted in its own case rather than described.

`qa-all.bash`: exit 0, 918 files, 1464 helper tests, `helper-summary-readers: passed: 20`.
`PLAN.md` 17,571 bytes — the Delivery & Milestones commit list became a pointer to
`git log --grep`, since a second copy of the log is a second thing to keep in step.

## 02:42 · finding · T4.4/T4.1 — my fixtures tested an ordering a pipe cannot produce, and the headline 25 was measured against the wrong commit

Round 5, no blocking findings, eight should-fixes. Two are substantial and both are mine.

**The decoy fixtures test the impossible direction.** I fixed `helper_test_summary` with a
"last match wins" rule and three fixtures that put the decoy BEFORE unittest's summary.
Round 5 pointed out that a pipe cannot produce that ordering. Measured with a real test
process rather than argued:

```
1: .
3: Ran 1 test in 0.000s
5: OK
6: Ran 3 tests in a scenario     <- the decoy, AFTER everything
=> helper_test_summary said: Ran 3 tests   (truth: Ran 1 test)
```

unittest writes its summary to **stderr**; a test's `print` goes to **stdout**, which Python
**block-buffers** to a pipe and flushes at process exit. `qa-all.bash` captures through a
pipe. So the decoy always lands last, and "last wins" picks it every time. My three fixtures
described a world where a stdout decoy arrives early, which cannot happen — the exact defect
this plan is named after, committed by me inside the test written to prevent it.

Neither first nor last is right: a *stderr* decoy arrives before, a *stdout* decoy after.
What holds is unittest's own structure — the `Ran` line most recently seen **when the result
line arrives**, which anchors on the same `^(OK|FAILED)` that `helper_skip_count` already
trusts. Both readers now depend on one fact about the format instead of two. Three fixtures
replaced with the real orderings, plus the mirror case for `helper_skip_count` (a
result-shaped decoy flushed after the real one). Gate: 23 cases.

Three comments claiming *"unittest does not buffer stdout"* were the same error stated as
fact, and are corrected rather than deleted — the true version is more useful, because it is
what makes first-match correct for one reader and last-match wrong for the other.

**The 25 was a real measurement of the wrong commit.** I attributed *"25 gates never ran in
CI"* to `29ceee97`. That is **this plan's own round-2 commit** — the test fixes had landed,
`helper-tests` passed there (`✓ Ran 1456 tests, 2 skipped`), and the run reached its final
summary. **Nothing was masked at that commit.** I measured a real property of the script and
pinned it to a commit that had stopped exhibiting it. At the commits that were actually red:

```
9a79dd77  2026-09-11   5 hard gates masked
b3f6e909  2026-09-14  11 hard gates masked
```

And the corrected figure is a *worse* finding than the wrong one. Not a fixed 25, but a set
that **grew as gates were added behind a gate that could not pass**: every gate added after a
standing abort is born unexecuted. That is the argument for Task 4.3, sharpened.

Worth naming the pattern, since this is the same number a fourth time: the first three
errors were counting *sites* instead of gates, and this one is counting the *right thing
against the wrong commit*. A number is not verified until both halves are — what it counts,
and what it counts it in.

**Option (d) checked for over-correction, and one part of my argument was unsound.** Round 5
agrees (d) is admissible and that I did not accept it too readily. But my claim that
`_EXCLUDE_PREFIX` makes "the same judgement" is wrong in mechanism: that excludes by PATH, so
the covered set is visible by reading the tuple and a file cannot drift into it without
moving. A marker-based exclusion is decided by file CONTENT — invisible until counted, and it
would widen silently the day another file grew the marker. So (d) is admissible **with a
coverage line**, the way `version-pins` reports `COVERAGE: 9 of 9`. Without one it is a skip
nobody can see, which is the family (b) belongs to even though (d) does not. Recorded in both
documents, and a table cell that contradicted its own prose ((d) "cannot pass a broken link"
— it can, just not in files we own) is fixed.

Smaller: `FINDINGS.md` had 28-vs-27 and 28-vs-29 where the recount had not reached, and an
unattributed "25 passing gates" that is 28 for the run cited; `PLAN.md` said 16 cases (23)
and 35 verdicts tests (40).

`qa-all.bash`: exit 0, 918 files, 1464 helper tests, `helper-summary-readers: passed: 23`.

## 02:48 · finding · T4.1 — 25 was the right number after all, on a commit neither of us had looked at

Dispatching round 6, I asked it to check one of my own new claims: that `9a79dd77` →
`b3f6e909` brackets the masking. Rather than wait for the answer I checked it, and it does
not.

`b3f6e909` is not the last masked commit — it is merely the last commit that *added a
defective test*. Masking continued until the first **fix** (`cedc9426`), and 21 commits
touched `qa-all.bash` in between. Measured at the real endpoint:

```
9a79dd77  2026-09-11   masked =  5    helper-tests first goes red
b3f6e909  2026-09-14   masked = 11
497370ba  2026-09-15   masked = 25    last red commit before the first fix
```

So **25 was the right number all along, attached to the wrong commit** — twice, now, and in
opposite directions. I first pinned it to `29ceee97`, where nothing was masked. Round 5
correctly rejected that and offered 5 → 11, which I adopted; but those were the two commits
*I* had named in the plan as introducing defects, so the range inherited my framing rather
than being derived. The endpoint nobody had looked at is `497370ba`, and it reads 25.

The lesson is narrower than "check your numbers", which I have now written three times. It
is: **a range is two claims, and accepting a correction to one end does not verify the
other.** Round 5 corrected the attribution and I treated the replacement figures as verified
because they arrived with the correction.

The corrected arc is also the strongest form of the finding. The masked set grew **fivefold
in four days**, and it did so because every gate added in that window landed behind an abort
already out of reach. Round 6 was asked whether *"every gate added after a standing abort is
born unexecuted"* outruns its evidence. It does not: **20 of the 25 had never run in CI even
once** by the time the first fix landed. That is the argument for Task 4.3, and it is now a
count rather than a phrase.

`qa-all.bash`: exit 0, 918 files checked.

## 03:10 · decision · T4.4 — three revisions chasing a match rule, when the fix was to stop merging the streams

Round 6: no blocking findings, and it broke my ordering fix twice. Both reproduced here
before acting.

**"A stdout decoy can never precede the result line" is false, and I should have seen it.**
Block buffering flushes when the buffer is FULL, not only at exit. So:

```python
print("Ran 3 tests in a scenario"); print("OK (skipped=99)"); print("X" * 9000)
```

pushes the decoy past the 8KB boundary and it lands *ahead* of unittest's summary. Measured:
`helper_skip_count` answered **99** for a run whose real answer was **0**. The fixture I
added last round passed only because the real result line happened to carry a count; against
a bare `OK` the decoy wins.

And the two readers had drifted apart — `helper_test_summary` effectively used the *last*
result line, `helper_skip_count` the *first*. On the capture above they disagreed with each
other, which is a worse state than either being wrong.

**The fix is not a cleverer rule. There isn't one.** A small decoy flushes at exit and lands
after; a padded decoy flushes early and lands before. Both are reachable, so no first-or-last
rule over a merged stream can be right. I spent three revisions looking for one.

`qa-all.bash` captured with `2>&1` and created the ambiguity itself. unittest writes its
summary to **stderr**; `qa-helper-tests.bash` writes its progress line to **stdout**; a
test's `print` goes to stdout. **Parsing stderr alone deletes the entire class.** On stderr
unittest's summary is always last — a test writes during the run, the summary after every
test finishes — so "last match wins" becomes sound, and both readers can use it, symmetrically.

The same attack against the new code:

```
stderr-only capture:  Ran 1 test in 0.000s / OK
helper_test_summary: Ran 1 test   (truth: Ran 1 test)
helper_skip_count  : 0            (truth: 0)
```

The decoy is not defeated; it never enters the parse stream.

**And I introduced a leak while fixing it.** My first version added
`trap 'rm -f "$helper_err_file"' EXIT` — a second `trap ... EXIT` **replaces** the first in
bash rather than adding to it, so the seven jq temp files would have leaked on every run.
Caught by reading the script rather than by any gate, which is worth noting: nothing in QA
checks for a clobbered trap. Folded into the single existing trap with a comment saying why
there is only one.

Round 6 also confirmed, independently, that the masking figures at `4ada2342` are right — it
computed **all 21 intermediate values** rather than the two named commits, and found the
series monotonic 5→25 with maximum 25. So the arc stands. `"born unexecuted"` is supported by
11 CI runs across the window, all aborting at the same gate; it is evidence, not a flourish.
Option (d)'s coverage line is confirmed necessary — the bidirectional inventory is
structurally blind to it.

Smaller: `FINDINGS.md` said "three gates never ran" beside a measured "20 of the 25" — the
first is now the second, with the two that actually needed fixing named; and `497370ba` is
labelled `cedc9426~1`, the high-water mark, rather than by a phrase that implied something
about that commit's own content.

`qa-all.bash`: exit 0, 918 files, 1464 helper tests, `helper-summary-readers: passed: 23`.

## 03:58 · decision · T4.4 — the fifth reader is not a reader: the counts were never in the text

I dispatched round 7 and then, while it ran, went to check its first question myself —
whether "unittest's summary is always last on stderr" is actually true. It took one attempt
to break. A test that does `atexit.register` on a function printing to `sys.stderr` puts its
text *after* unittest's summary, because atexit handlers run at interpreter shutdown:

```
Ran 1 test in 0.000s
OK
Ran 3 tests in 9.999s
OK (skipped=99)
```

Both readers then answered `Ran 3 tests` / `99` where the truth was `Ran 1 test` / `0`. The
fourth revision fails in exactly the way the first three did.

What I got wrong was not the match rule, four times over. It was the belief that there was a
match rule to find. A test can write anything to either stream in any order — that is the
whole population, and no rule over it can be sound. Last round I thought separating the
streams was structural rather than another guess; it was narrower than a guess about
ordering, but it still rested on an ordering claim, and the claim was false.

So the scrape is gone. `helpers/qa_environment/unittest_counts.py` runs the named modules
itself and takes `result.testsRun` and `len(result.skipped)` off the `TestResult` object —
where both numbers exist exactly, and where nothing a test prints can reach them — then
writes `tests=N` / `skipped=N` to the path `qa-helper-tests.bash --counts-file` is given.
`qa-all.bash` reads that file. No stream is parsed anywhere in the path now.

Verified the result object first rather than assuming it: across a module holding a plain
pass, a decorated skip, a runtime skip, an expected failure, an unexpected success and a
subTest loop, the object's numbers match unittest's own printed summary exactly —
`testsRun=6 skipped=2 expectedFailures=1 unexpectedSuccesses=1`, against
`Ran 6 tests … FAILED (skipped=2, expected failures=1, unexpected successes=1)`. Including
that `testsRun` counts the two skips, which is the original blindness, confirmed at source.

Two bugs died with the design rather than being fixed. The two readers had drifted into
opposite match rules and disagreed with each other about the same capture (round 6's
finding) — there is one reader now, so they cannot. And the decoy class is not defended
against, it is unreachable: the regression test fires a padded stdout decoy, a stderr decoy
and an atexit decoy at a real subprocess, and asserts both that the counts are right *and*
that the forged text really did reach the streams, so the case cannot pass by the attack
failing to run.

Mutation-tested both halves before believing either: 6 mutations of the runner (skip count
hardcoded to zero, derived from testsRun, each key dropped, failure reported as success,
counts file never written) and 8 of the reader (missing key answers zero, missing file reads
as a clean run, unknown and duplicate keys ignored, digit check dropped, skip count dropped
from the payload, pluralisation frozen, last line dropped). All 14 caught.

One real bug found by the reader's own tests rather than by me: `while read` drops a final
line with no trailing newline. The runner always writes one, so it was unreachable — but a
reader that silently ignores part of its input is precisely the shape this library exists to
avoid, so it reads all of it now.

## 04:02 · finding · T4.5 — the same defect, 21 more times, and not fixed here

While rewriting the gate block I checked whether the unscoped `grep -oE 'passed: [0-9]+'`
that round 4 caught in `helper_test_summary` existed anywhere else. It is in **21** of the
hard gates. `-o` prints every match, so a second occurrence makes the stage line two lines,
and `verdicts.py` reads the first as the stage and loses the rest.

I started to fix all 21 and stopped, because they do not agree on a format: `passed: N`
alone, one, two and three spaces before `failed:`, and two prefixed with the gate's own name
(`ccy selinux-verdict: passed: …`). A shared reader has to be designed for that spread, not
extracted from one call site, and doing it inside this commit would put every hard gate into
a diff whose subject is one of them. Filed as Task 4.5 with the format survey, so it is
scheduled rather than quietly dropped.

Also renamed the gate's stage line `helper-summary-readers` → `helper-counts-reader`, since
"readers" plural is now wrong and this plan is about lines that tell the truth.

`qa-all.bash`: exit 0, 920 files, `helper-tests: Ran 1479 tests, 1 skipped`,
`helper-counts-reader: passed: 24`. 37 distinct stage names over 38 emitted lines
(`patterns` prints a ⚠ and a ✓), unchanged — this commit adds no gate and removes none, so
`CLAUDE/QA.md`'s thirty-six/twenty-nine still hold.

## 04:31 · review · round 7 reviewed both designs, and found four holes in the new one

Round 7 was dispatched against `27d7b342` and I messaged it mid-flight to say I had already
broken its first question, so it reviewed the **replacement** as a design as well. That was
the right call and it paid: it confirmed the core claim and then found four holes.

It independently verified the premise rather than taking my word: one run with every
category present, `TextTestRunner`'s text against the object, agreeing exactly — including
that a `subTest` skip is counted by both, and that `expectedFailures` and
`unexpectedSuccesses` are separate lists contaminating neither figure. It also flagged, in
advance of my writing it, that the exit code must come from `wasSuccessful()` and not from
`failures or errors`, because an unexpected success leaves both lists empty while unittest
reports FAILED. My implementation already used `wasSuccessful()`; the comment now says why,
because the wrong version turns a red suite green and looks identical.

**Hole A — `testsRun` and `skipped` are not commensurable.** `testsRun` counts test methods;
`skipped` counts skip events, and one method with three `subTest` skips gives
`Ran 1 test … 3 skipped`. Faithful — unittest prints the same — but `CLAUDE/QA.md`'s
argument that the four machine shapes give four distinct counts quietly assumed the skip
count was bounded by the test count. Documentation corrected, and the reader now has a case
asserting it must NOT clamp a file that is faithful to its run.

**Hole B — an unimportable module is reported as a test that ran.** unittest substitutes a
`_FailedTest`, so testsRun inflates by one. Worth recording that this cannot produce a
silently-wrong green line: the placeholder is an error, `wasSuccessful()` is False, the
runner exits 1 and `qa-all.bash` aborts before the stage line is printed. The review's
concrete instance (1464 → 1465) was my own new test module arriving, not an import failure.
The **module count** it proposed is still worth having, for a different and better reason:
a machine that COLLECTED a different set of modules is this plan's own subject one level up,
and the test count alone cannot show it. The line now reads `Ran 1482 tests in 65 modules`.

**Hole C — the counts file is not unreachable, and I said it was.** Its path travels in
`argv`, so a test can read `sys.argv` and overwrite it — and this module's own tests are
collected by the runner they test. "No stream a test can write to is parsed" was true and
insufficient; the claim I actually made was wider. `qa-all.bash` now passes a
`--counts-token` that only that run knows and the reader requires it back, so a clobbered
file FAILS the gate instead of reporting a number nobody questioned. Stated honestly in the
code: that is a detector for an accident, not a lock against a test that is trying. The
streams, by contrast, could be hit with an ordinary `print`.

**Hole D — a hard exit leaves an empty file and exit code 0.** Reproduced: a test calling
`os._exit(0)` skips atexit, the runner never returns, nothing is written, and the
interpreter exits 0, leaving the zero-byte file `mktemp` created. The reader already refuses
it (`tests= is missing or not a number`, exit 1) — the one property round 7 said must
survive the rewrite did survive. Added as a named case rather than left as a coincidence.

## 04:36 · decision · T4.5 was the wrong filing — round 7 found the real one, and its fix already existed twice

I filed Task 4.5 for 21 gates that scrape `passed: N` unscoped. Round 7 found something
sharper in the script I was editing, and it is the better finding.

`mapfile -t test_files < <(find tests/helpers ...)` reports **mapfile's** exit status, not
find's, and `pipefail` does not reach inside a process substitution. A partly-failed walk
yields a shorter module list, unittest runs it, the script exits 0, and a smaller
`Ran N tests` reads exactly like the whole population. The `-eq 0` guard catches only the
empty case — an under-match cannot announce itself, which is the same property this entire
plan is about, sitting in the gate the plan has spent seven rounds on.

And the fix already existed twice, in the same directory: `qa-bash.bash` (Plan 00076) and
`qa-python.bash` (Plan 00081) each cross-check discovery against `git ls-files` and refuse a
partial result. `scripts/qa-discovery.bash` already exported the primitives. This was the
third discovery site and the one that never got the generalisation — `AgentNotes.md`'s "a
lesson written down beside the thing it fixed, never generalised", verbatim.

`qa_tracked_helper_tests` now sits beside its two siblings and `qa-helper-tests.bash` uses
it. Proved with a control that could have failed: narrowing the walk to one subtree makes it
report `discovery missed 43 tracked helper test file(s)`, exit 1, and write no counts file.
My first attempt at that control ran the modified copy from `untracked/scratch/`, where
`ROOT_DIR` resolves elsewhere — it died before reaching the guard and I nearly recorded a
pass. Re-run from `scripts/`, which is the only place the path resolves.

Round 7 also caught line-number drift I had re-introduced: the drift gate's abort moved to
`qa-all.bash:138`, and `FINDINGS.md`'s `qa-all.bash:613-620` for the jq merge was wrong
before this commit and after — it is `610-627`. Both corrected, along with `PLAN.md`'s
"three" unmaskings where `js` is an accumulating stage that ran on every CI run, so the real
figure is two.

`qa-all.bash`: exit 0, 920 files, `helper-tests: Ran 1482 tests in 65 modules, 1 skipped`,
`helper-counts-reader: passed: 29`. Mutation-tested again after the token and module
additions: 5 further mutations of the reader, all caught.

**Still owed to the user, unchanged by any of this**: Task 2.1's decision, and the two
freeze plays on the host. Also now four rounds running: the reviewer sessions have no
`Write` tool and have to author their reports through a heredoc.

## 04:52 · decision · T4.4 — round 8 found a regression I introduced and a claim I over-stated

Round 8 reviewed the replacement against a tree I kept frozen for it, and came back with
three should-fix items. All three were real and all three are now closed.

**The regression is mine and it is the worst of the three.** Removing the scrape also
removed the capture around `qa-helper-tests.bash`, so its stdout was inherited and flowed
into `qa-all.bash`'s own stdout — the stream `verdicts.py` parses for stage lines. Of the 29
hard-gate invocations, 28 capture their child's output; mine was the one that did not, which
is the evidence it was an oversight rather than a decision. Round 8 measured a test printing

    ✓ helper-tests: Ran 1 test in 1 module, 0 skipped

straight into the machine-read verdict stream. Zero bytes come off that stream today, so it
was latent — but I had replaced a guard with an unenforced precondition over 1,482 tests,
which is this plan's own thesis about how things fail. It is captured now AND required to be
empty, so the precondition is a gate. Proved by control: with a printing test present,
`qa-all.bash` exits 1 and shows the forged line it would otherwise have swallowed.

**The token claim was the fifth measurement-written-down-as-a-law.** I wrote "a value only
this run knows" in four places. The token is in the same `argv` as the path, so anything
that can find the file already has it. Worse, the accident I cited as the motivation — this
module's own tests being collected by the runner they test — is not what the token stops.
Round 8 measured the actual behaviour: a forgery written mid-run is **overwritten**, because
the runner writes after the suite finishes; a forgery written from `atexit` survives with a
valid token and a wrong number. So the real scope is "a file written by something that never
read this run's argv" — a stale file, a concurrent run — which is worth having and is not
what I claimed. All four copies rewritten, and the undetected case written down as
undetected.

Also inverted the lesson while writing it: I said the previous revisions "failed by stating a
narrower guarantee than they had". They failed by stating a **broader** one. A narrower claim
than the truth costs nothing, which is the entire point of the sentence.

**Round 7's finding 3 was half-closed.** The cross-check catches tracked-but-not-discovered
and nothing catches discovered-but-not-tracked. An untracked test runs, is counted, and no
tracked file is missing — so the gate passes and the number moves. That is not hypothetical:
it is exactly why round 7 watched the count go 1464 → 1465 → 1479 with no change to its
review target, and the number it moves is the one two machines are compared on. There is now
a `COVERAGE: 65 of 65 tracked helper test modules` line, and untracked files are named.
Proved by control: a stray file reports `plus 1 UNTRACKED file(s)` and the counts move to
66 modules / 1483 tests, visibly.

Ten nits, all actioned. Two were real holes rather than wording: duplicate-key detection
tested `[[ -n "$var" ]]` — *has a value* — not *have I seen this key*, so `tests=` followed
by `tests=5` was accepted, and the suite could not see it because both its fixtures were
non-empty; and the empty-file case (the one the header calls reachable) was being reported
as a token mismatch, sending the reader to hunt a culprit that does not exist. Both now have
their own cases. The rest: `in 1 modules` singularised, the git prelude extracted to
`qa_require_git_checkout` after my function made it a **fourth** verbatim copy rather than
the third round 8 counted, the coverage failure exiting 2 like its two siblings rather than
1, a `wasSuccessful()` comment moved above the line it describes, the `subTest` caveat
reworded so the next person to grep does not conclude it is stale, and `PLAN.md`'s mutation
figure corrected from 8 to 13 — it was written before the token and module additions added
five more.

Round 8 confirmed my Hole B reasoning holds and is stronger than I stated, verified the gate
arithmetic (36/37/38) and both line citations from a real run, and recorded that the
deliverable works in CI: `35053479172` fails on `docs` alone and produces
`Ran 1482 tests in 65 modules, 2 skipped` against `1` locally.

`qa-all.bash`: exit 0, 920 files, `helper-counts-reader: passed: 32`.

Task 4.5 is open and — round 8 is right to point this out — **not blocked on the user**. It
is deferred work, and the plan now says so rather than leaving it in a list that reads as
though everything remaining needs a human.

## 05:24 · decision · T4.4 — the retraction of the fifth over-claim contained a sixth

The targeted re-check of `0b1623fa` closed findings 1 and 2 and refused finding 3, and the
most interesting thing it found was inside my correction rather than in the code.

**The sixth over-claim.** Rewriting the token paragraph, I wrote "anything that can find the
file already has the token". Two lines above, the same paragraph lists *a hardcoded path* as
a case the token detects — and something writing to a hardcoded path finds the file and does
not have the token. So read literally the sentence says the token buys nothing, while the
row beneath it says the opposite. The missing words are **by reading argv**. One copy,
`CLAUDE/QA.md`, was already right because it named the mechanism ("rides in the same argv as
the path") instead of quantifying over a population; that is the difference, and it is worth
keeping as the pattern. Fixed at the other three sites.

Five over-claims, then a sixth while retracting the fifth. The common factor is not the
measurement — every one was measured correctly — it is the sentence written immediately
after a measurement, generalising from the case measured to the population. Naming the
mechanism instead of the population is what stops it, and that is now written down beside
the one copy that did it right.

**Finding 3 was refused, and correctly.** I added `COVERAGE: 65 of 65` to stderr;
`qa-all.bash` captures the child's stderr and `cat`s it only in the failure branch, so on a
green run the line is written and thrown away. `CLAUDE/QA.md` names `qa-all.bash` as the
only sanctioned invocation, so in practice nobody ever saw it. The re-check put it exactly
right: round 8 asked for coverage to be reported rather than implied, and it now is —
**into a stream the operator's only sanctioned command discards on success**. Produced but
not delivered, one step short of the class this plan exists to remove, inside the fix for
that class.

The obvious repair — print it on stdout like `version-pins` does — is now forbidden by the
gate I added an hour earlier for finding 1, which requires that stdout to be empty. So the
count travels in the counts file instead: `--tracked-modules` is passed by
`qa-helper-tests.bash`, recorded as `tracked=`, and rendered in the stage line, which is the
one place two machines are actually diffed. `Ran 1482 tests in 65 modules (65 tracked)`.

I did not take the re-check's suggested wording ("65 of 65 tracked modules") because with an
untracked file present it reads "66 of 65", which is arresting but not informative. The
parenthetical says the same thing and stays grammatical in both states. Control: with a
stray untracked test, `qa-all.bash` now prints
`Ran 1484 tests in 66 modules (65 tracked), 1 skipped` — visible in the stage line, through
the sanctioned command, which is the whole point. The file NAMES stay on stderr, since a
stage line cannot carry them.

Deliberately NOT made fatal. `qa-discovery.bash` keeps `find` as the discovery source on
purpose so a brand-new, not-yet-`git add`ed test is still gated; failing on untracked files
would break ordinary TDD and contradict that decision.

**Nit 7 residue, and it is the same shape as the over-claims.** I corrected round 8's count
from three copies to four in the commit message, and left the code comment saying "extracted
when the THIRD caller arrived: three verbatim copies". The comment contradicted its own
commit message. Also, inserting `qa_require_git_checkout` above `qa_tracked_shell_scripts`
orphaned that function's doc comment — it ended up introducing the new function instead, and
`qa_tracked_shell_scripts` was left with none. Both fixed; the function order now puts each
comment above what it describes.

Smaller: the stdout gate's remediation text said "Send it to stderr", which is wrong advice
for the other leak route the re-check named — a subprocess a test spawned, which has to be
captured rather than redirected. Both routes are named now. `CLAUDE/QA.md` also documented
only the tracked-but-not-discovered half of the coverage story; it now has both directions
and says why the second number has to be in the stage line.

The re-check confirmed all three of my controls reproduced exactly as described, verified
the trap by set difference rather than by eye (ten mktemp assignments, ten names, empty
difference both ways), re-derived the `subTest` claim independently (69 occurrences, all
three `skipTest` sites at method top level), and found a real precedent for the stdout gate:
Plan 00104 round 5 recorded this exact leak — `cli.main()` printing, four stray `COVERAGE:`
lines after `OK` — and the repo fixed it with `redirect_stdout`. So the gate would have
fired on a real historical case and the verdict then was "defect", which is better evidence
than my reasoning about whether it might be too strict.

## 05:51 · decision · T4.5 — the same defect in 21 untested copies, now shared

Task 4.5 was the last unblocked work on this plan and I had deferred it twice, both times
for a real reason: the 21 gates disagree on a format, so a shared reader needed designing
rather than extracting. Deferring twice is where "scheduled" turns into "dropped", so it is
done.

The defect is round 4's, verbatim, in 21 more places. Each gate ran
`grep -oE 'passed: [0-9]+'` over its child's whole capture; `-o` prints EVERY match, so an
earlier `passed: <digits>` was emitted alongside the real one and the stage line became two
lines — which `verdicts.py` half-drops, reading the first as the stage. Round 4 found it in
the helper-tests reader, I fixed it there, and none of the 21 copies was looked at or
tested.

The design question was what to scope to. The formats, measured from the scripts rather than
assumed: one, two and three spaces before `failed:`; `passed: N` with no `failed:` at all;
and two lines prefixed with the gate's own name (`ccy selinux-verdict: passed: 14`). No
anchor fits all five. So the rule is positional — the LAST line carrying a count — which is
sound here for a reason it was not sound for unittest: these are repo-authored bash scripts
writing in real order to one stream, with no Python buffering and no test-controlled content,
and a summary is last because it is a summary. A sixth shape has no count at all
(`planlib-tests` prints `PASSED (library version 1.2.0)`), so it degrades to the word
`passed` rather than to a number, matching the other reader — a wrong count reads as a
measurement and a word cannot.

Unlike `helper_counts_summary` this does NOT hard-fail on an unreadable capture, and the
difference is worth stating because the two sit in the same file. The helper-tests counts
distinguish two machines, so a blind read there is the defect this plan exists to remove.
This is a case count whose gate has already reported pass or fail through its exit status.

The control is the one that matters for a refactor: I captured every stage line from a full
run before and after, and diffed them. Identical, except `helper-counts-reader: passed: 34`
→ `43`, which is this suite's own count going up by the nine cases I added. 21 call sites
changed, no output changed.

`qa-all.bash`: exit 0, 920 files, 43 reader cases.

## 05:52 · finding · T4.5/T5.3 — the final review found two more copies, and the sentence that hid them

A note on the clock first, because it affects every timestamp above: the times in this file
have been running ahead of the container's clock — the 05:51 heading was written while
`date` said roughly 05:02, and the drift has been growing all night. They are monotonic, so
the ordering is honest, but they are not wall-clock accurate and should be read as sequence
rather than as measurement. I am keeping this entry monotonic with the rest.

The round filed four findings against the 05:51 entry's work, and two of them are
retractions of claims I made in that entry.

**"A summary is last because it is a summary" was circular, and false.** I measured it: 8
of the 21 gates print something after their summary — `OK`, `VERDICT: PASS`, an explanatory
echo. The justification I wrote was not a measurement at all, it was a restatement of the
rule I had just chosen. What actually holds, and what the comment says now, is narrower and
checkable: no gate emits a second `passed: <digits>` line, and every count-bearing line comes
from the single parent bash process on stdout, so a `2>&1` merge cannot reorder them. That is
the seventh over-claim on this plan and it has the same shape as the other six — a property
measured once, correctly, then written down as a law over a population nobody enumerated.

**The `planlib-tests` row was worse**, because it was offered as evidence that the
word-fallback path is live. It is not a caller. It has its own reader and never reaches this
function, so the path is DEFENSIVE — only this library's own tests exercise it. I had used a
real-looking example to argue a path was exercised, which is exactly the move that makes a
blind check look sighted. Eighth over-claim, and the comment now says defensive.

**Two more copies of round 4's defect, and one had never worked in its life.**
`nokill-containerwatch` read `[0-9]+ call site[s]? checked` from a gate whose only commit has
ever printed `N container-watch file(s) clean`. Zero matches, ever — and the `||` fallback
printed the sentence `no forbidden kill call sites` on every run, so the stage line asserted
a fact that nothing had verified, in the place a measurement belongs. Six lines above it sits
a comment complaining that a rule had been "written down beside the drift gate and never
applied to this one". Both stragglers now use `qa_gate_detail`, whose fallback is the
deliberately useless `summary unreadable`: a fallback that asserts something is worse than no
fallback, because it cannot be told apart from an answer. `✓ nokill-containerwatch: 3
container-watch file(s) clean` has now printed for the first time.

**The sentence that let them through** was mine, in `FINDINGS.md`: "Every other hard gate in
`qa-all.bash` read its case count with an unscoped `grep -oE 'passed: [0-9]+'`". It was 21 of
the 29 non-merged gates, and 2 of the remaining 8 had the same defect with a different regex.
I swept for the pattern text instead of for the defect, then wrote the sweep up as total. The
finding is corrected in place with the real numbers.

Also done: `--tracked-modules` is `required=True` with the default deleted — deleting the
state beats pinning it, and `counts_text` ignoring the flag is a separate defect, now covered
by a case that passes `tracked=7` over one module and was mutation-verified to fail without
the fix. The library header said "defines one function" when it defines three. The runner's
docstring usage example had silently become an invalid invocation the moment the flag went
required. And the three `qa-all.bash` line-number citations are now unique anchor text: `jq -s`
has sat at five different line numbers across this plan's revisions, one of them moving while
a reviewer was citing it, and a stale line number still resolves — a wrong citation reads
exactly like a verified one, where a failed `grep` for an anchor is a failure you can see.

I also gave a wrong reason for declining the `tracked > modules` guard. "Untestable through
the production path" does not distinguish it: the reader already refuses duplicate keys,
unknown keys and a foreign token, none of which the runner can produce either. The reason that
does distinguish it is placement — `tracked ≤ modules` is enforced upstream by a guard that
exits 2 and names the missing files, so a reader-side copy would fire later on less
information. The reader now says so where it relies on it.

`qa-all.bash`: exit 0, 920 files. `helper-tests: Ran 1485 tests in 65 modules (65 tracked),
1 skipped`. `helper-counts-reader: passed: 51`. Runner suite: 21 tests.

The tree moved under the reviewer for the fourth consecutive round — HEAD stayed at
`ac706483` but five files went modified while it was writing, and its own line citations were
stale eleven minutes after filing. The verification round after this commit goes in a git
worktree.

## 06:15 · finding · T5.2 — the two machines disagree on the page now, on the same commit

Run `35060364019`, commit `8a5354a7`. One failure, `✗ docs: 8 finding(s) across 71 files`,
which is Task 2.1 and nothing else — the fourth consecutive run whose only red stage is the
one open decision.

The part worth writing down is the stage that PASSED on both sides:

```
CI:    ✓ helper-tests: Ran 1485 tests in 65 modules (65 tracked), 2 skipped
local: ✓ helper-tests: Ran 1485 tests in 65 modules (65 tracked), 1 skipped
```

Same commit, same 1485 tests, same 65 modules, same 65 tracked — and the two machines skip a
different number of them. Before this plan both sides printed `Ran 1485 tests` and the lines
were byte-identical, because `unittest` counts a skipped test inside `testsRun`. Two machines
running different subsets of the same suite produced the same sentence for weeks, and that is
precisely how the six host-dependent tests stayed invisible.

So the plan's deliverable is not an argument any more, it is a diff. The divergence is on the
page, in a passing stage, which is the only place it could have been caught: a pass/fail
comparison cannot show it, because both sides pass.

The `65 tracked` agreeing on both sides is the other half and matters just as much. It rules
out the explanation that would otherwise be reached for first — that the two machines
collected different files — and leaves exactly one reading: the same 65 modules were run, and
one test skipped itself on one machine and not the other. That is a fact about the test, not
about discovery, and it is now a fact a reader can act on without a second run.

`nokill-containerwatch: 3 container-watch file(s) clean` and `helper-counts-reader: passed: 51`
both appear in the CI log too, so neither of this commit's new readers is container-only.

## 06:45 · decision · T2.1/T2.2/T4.5 — the owner drew the boundary, and it is a better one than I had recorded

**Task 2.1 is decided: not (a).** The daemon is not installed in CI. The owner's rule is
broader than either option I had written down — *we do not QA another repo's files* — and
covers the daemon, the vendored Ansible roles, and whatever gets vendored next.

It also beat option (d), which I had spent two rounds refining. (d) excluded whole FILES by
a content marker, which is why it needed a coverage line to compensate: the excluded set was
invisible and could widen silently the day any other file grew that marker. The boundary
excludes by resolved TARGET, so a daemon-generated file's own other links stay checked and
nothing can drift into the exemption by having something written into it. The cost I had
documented as (d)'s one real weakness simply does not exist in this shape.

**And my first implementation of it was wrong in this plan's own way.** I built "ignored by
git, so skip it" and had it passing before the owner pointed out the hole: a link into
`untracked/` is ignored too, and that one is a genuine defect — docs pointing at something
no clean checkout has and no other repository owns. Collapsing the two would have traded a
false failure for a silent skip. That is the defect class this entire plan exists to remove,
and I had just re-created it inside the fix for an instance of it. There are three outcomes,
not two, and the third is what makes the second safe.

The mechanism is `git check-ignore`, which answers for paths that DO NOT EXIST. That is the
whole design: `.gitignore` is tracked, so CI asks the same question about the same string
and gets the same answer with no tree to probe. Anything that consulted the filesystem would
answer one way here and another there — reproducing the divergence instead of removing it.

**The dynamic version I was asked for, tried and thrown away.** I wrote the tree walk that
looks for undeclared `.git` directories, so the declared list could not go stale. It
reported eleven, and every one was a gitignored acceptance-run fixture under a completed
plan. Every vendored repo is gitignored, so a sweep either skips all of them or drags in
every stray clone: the population it can see is not the population that matters. What
replaced it is a hint computed only for a target that is ALREADY a finding — walk up, and if
a repository is nested there, say so. It costs nothing on a clean run, and the instruction
lands on the machine that can see the repo, which is the machine the fix gets made on.

Proved against a tree with no daemon — the review worktree, which is exactly the CI
condition: 0 findings, 8 vendored links. Remove the declaration in memory and the same 8
come back, each naming `.claude/hooks-daemon`; restore it and they go away; a typo in our
own tree is still `target does not exist`. Three controls, because "0 findings" and "blind"
look identical otherwise.

**Round 9 found Task 4.5's sweep short for the third time**, and the classification is the
point. Sweep one swept for the pattern text and missed 2 with a different regex. Sweep two
swept for the `||` fallback and missed 2 more that had no fallback because they interpolated
the whole capture. Sweep three enumerated all 29 hard gates and classified how each derives
its stage line. Each earlier sweep generalised exactly as far as the text I had been reading
rather than to the defect — *a stage line derived from an unscoped read of the capture*.

`vmtest-manifest` was still emitting it: three unconditional echoes, a three-line stage
line, and `verdicts.py` keeping the first and dropping two coverage measurements on every
run since it landed. It is three scoped reads joined into one line now, rather than one
chosen line, because each is a different measurement and choosing would discard two on
purpose where the old code discarded them by accident.

**"Pure refactor: every stage line byte-identical" is retracted.** 21 of 21 case-count lines
are byte-identical; three others changed, all three because they were broken — `nokill`,
which I had described as a repair three clauses earlier in the same bullet, `vmtest-manifest`,
and `extension-compat` losing a stray full stop. Ninth over-claim, same shape as the other
eight: measured correctly, then carried forward across a population that had moved.

**The fix that stops this recurring** is the one round 9 filed as a should-fix, and it was
the root cause all along: nothing in the tree compared a pattern against the gate that
produces it. `test-qa-helper-summary.bash` now extracts every `qa_gate_detail` pattern out of
the real `qa-all.bash` and runs it against the real gate — neither side a fixture. Two
mutations, both caught: re-introducing the dead `[0-9]+ call site[s]? checked` fails it, so
the suite would have caught `nokill` the day it landed; and a call site whose capture
variable is not registered fails too, so the next pattern cannot be added uncoupled.

Also closed from round 9: `qa-all.bash` and `CLAUDE/QA.md` both still called the library one
reader when it is three, and neither shared reader was documented anywhere outside this plan
— `CLAUDE/QA.md` now has the table and the rule for adding a gate. `qa-js.bash` is in the
machine-dependence table: it exits 2 on an absent `extensions/node_modules`, so in a linked
worktree it stops every hard gate behind it, which is Task 4.1's mechanism live in the
environment round 9 was asked to use. The test count that had rotted in two files and
disagreed with itself is deleted rather than corrected — a number that must be re-measured
to stay true does not belong in prose, and the stage line prints the live one every run.

**A note on the checkout.** Four files from Plan 00122's lxcfreeze work appeared in
`git status` mid-session, from another session sharing this working tree. They are not mine
and are not in my commit. Worth recording because the tree moving under a reader is what
five review rounds were spoiled by, and it can evidently happen to a writer too.

## 07:20 · decision · T2.2 — "out of scope" was throwing away the one case worth reporting

The owner refined the vendored branch again, and the refinement is the difference between
an exemption and a check: **a vendored target is still looked at, in the one case where
looking means something.**

    repo present, target there    -> verified      (silent)
    repo absent                   -> unverifiable  (soft; nothing here could say)
    repo present, target MISSING  -> broken        (its own ⚠ line, listing the links)

I had built the flat version — "inside a vendored repo, do not follow" — and it discarded
the third case entirely. That case is the useful one: the repo is right there, so "cannot
say" is false. The link is demonstrably broken, and the usual reason is that the vendored
repo moved the file and our generated pointers went stale. That is exactly the event this
gate should catch, and my version would have printed `not followed` and moved on.

It warns rather than fails, because it is still not ours to fix — and because of an
invariant I had been stating too loosely. I kept writing "the verdict must not depend on
what is installed". The precise version is: **the exit code must not; what the gate SAYS
may, and should.** A machine that can see the vendored repo can say more about it, and
saying more never flips a verdict. Taken literally, my looser phrasing argued for throwing
the `broken` case away, which is how I came to build it that way. The distinction is now
written into `CLAUDE/QA.md` and the function's docstring rather than left implicit.

Anchors into a vendored repo are still not followed even when it is present. Their headings
are theirs to rename, and going red on another repo's churn would be a dependency on it for
a defect we could not fix. Existence is the line; the anchor is not.

Three controls, one of them partial and worth naming as such:

- daemon present, links good -> `8 verified, 0 unverifiable, 0 broken`, exit 0
- daemon absent (the review worktree, which is the CI condition) -> `0 verified,
  8 unverifiable, 0 broken`, exit 0, **0 findings** — the three-week-old failure, gone
- repo present, file missing -> the `⚠` line lists the links, then the `✓` line, exit 0.
  This one ran the real `jq` and the real echo over a crafted payload rather than a real
  broken tree, because producing one means mutating the daemon's own checkout. The
  classification behind it IS covered end to end by `test_a_present_vendored_repo_with_a_
  broken_link_warns_seriously`; what the crafted payload proves is only the rendering.

A `⚠` plus a `✓` for one stage is not new — `qa-patterns.bash` already emits that pair and
`verdicts.py` treats `⚠` as a stage symbol — so the shape was checked before being copied
rather than assumed to work.

One more thing caught by an existing test rather than by me: the reader table I added to
`CLAUDE/QA.md` listed the three functions as `` `helper_counts_summary` `` in a table's
first cell, and `check_qa_gate_inventory` reads any such row as **a gate this document
claims**. Three gates that do not exist, asserted by a documentation table, caught by that
check's own control test. They carry `()` now, and the reason is written beside them so the
next person does not quietly remove it.

## 07:55 · action · T5.2 — green, and the docs line says what it could not check

Run `35076071578`, commit `7e85ff49`. `✓ QA passed: 920 files checked`, no failing stage.
The first successful `QA` run on `F44` in three weeks.

The two lines worth keeping:

```
✓ docs: 71 files OK (...) — VENDORED: 0 verified, 8 unverifiable (repo absent), 0 broken
✓ helper-tests: Ran 1504 tests in 65 modules (65 tracked), 2 skipped
```

The docs line is the design working rather than the gate merely passing. CI does not claim
those 8 links are fine; it says it could not check them and why. Locally the same line reads
`8 verified, 0 unverifiable` because the daemon is installed here. Two machines, two honest
answers, one exit code — which is the distinction I had to be corrected into stating
precisely: the EXIT CODE must not depend on what is installed, the DETAIL may and should.

The `helper-tests` line still reports `2 skipped` in CI against `1` here, with `65 tracked`
agreeing on both sides. That is the divergence this plan was opened to expose, sitting in a
stage that PASSES on both machines — the only place it could ever have been caught, and the
reason weeks of green-vs-red comparison found nothing. It is not residue to be cleaned up
before closing; it is the deliverable.

A note on getting here: `72d21ffc`'s own run was **cancelled**, not failed — another session
sharing this checkout pushed on top and the workflow's concurrency group killed it. The same
sharing moved my `HEAD` under me twice. My commits kept their files out, but that is
discipline rather than a mechanism: one `.git/index`, and a bare `git commit` from either
side can absorb the other's staged work silently. Recording it because five review rounds
were already spoiled by a tree moving under a reader, and this is the same hazard reaching a
writer.

Task 5.2 and the first success criterion are closed. 5.3 is the only thing left, and round 10
is reviewing the vendored boundary in the pinned worktree as this is written.

## 08:40 · decision · T2.2/T4.5 — round 10 found Cause A's own shape inside the fix for Cause A

Round 10 reviewed the vendored boundary in the pinned worktree. The pin held at both checks
for the second round running, and for the first time nothing it found was about the design:
it confirmed the invariant by enumerating all **326** file-links across the 71 in-scope
documents rather than sampling, tried four ways to smuggle a target into a vendored root, and
traced the exit code through every branch including the crash path. What it found instead was
four more instances of this plan's own defect classes, in the artefacts that close it.

**The one that matters most: the question I asked was narrower than the finding I raised.**
`git check-ignore` answers "does `.gitignore` name this path". The finding said "target is
not tracked by this repository". Those come apart for a file sitting on one disk that was
never `git add`ed and matches no ignore rule — not ignored, so green here; absent in CI, so
red there. **Cause A's exact shape, inside the classification built to remove Cause A.**
Round 10 reproduced it in a throwaway repo. Zero such links exist today, which is how it
survived a round and how I came to write three documents asserting the divergence was gone.

The fix made the code simpler, which is usually the sign the question was wrong rather than
the answer: ask `git ls-files` and the directories it implies. The index is in every clean
checkout, so the property `check-ignore` was chosen for is kept, and the finding's wording is
now what the code actually tests. Existence is checked FIRST and trackedness second, so a
typo still reads `target does not exist` — and a present-but-untracked target fails here and
fails in CI for the other reason. Same exit code, different sentence: the distinction this
gate turns on, finally applied to itself.

**A citation to nothing.** `_VENDORED_ROOTS` carried a comment saying the declaration was
kept honest by `check_vendored_declaration` — the tree walk I wrote, measured (eleven
findings, every one an acceptance-run fixture) and deleted earlier today. The sentence stayed
behind. It was the answer to the obvious objection to a declared exemption list, and it
pointed at a function that exists in no file in the repository. A claim printed where a
measurement belongs, which is the thing I have now written down three times as the class to
watch for. The comment says plainly what is true instead: nothing checks that a declared root
is really a vendored repo, adding one exempts every link under it, and only review stops it.

**"Parents, not leaves" did not cover the parent.** The roots carry a trailing slash and the
test was `startswith`, so a link to `../hooks-daemon` matched no root — and missed the ignore
branch too, because a directory-only rule cannot classify a path git has never seen. It came
out as a hard CI failure that passes wherever the tree is installed. The slash stays, because
dropping it would exempt `roles/vendor-extra/`; the test is equal-or-under now.

**The guard against a silent zero had a silent zero in it.** `V_OK` and `V_UNVERIFIABLE` both
carried `// "?"` under a comment explaining why a missing key must not read as a clean zero.
`V_BROKEN` — the only one whose zero can make an entire `⚠` stage line and its list of links
disappear — had no guard at all, and `jq` answers 0 for `.absent | length`. The two counters
that cannot silence anything were guarded; the one that can was not. All three keys are now a
hard precondition.

**The enumeration table did not enumerate.** Offered as proof that the population had finally
been counted, it said `qa_gate_detail` covered 4 gates where the code says 6 in two other
places, summed to 27 under a sentence claiming 29, and its last row was not a row — an
unescaped `|` inside `` `|| summary="OK"` `` broke the cell split. The code's numbers were
right every time; my summary of them was wrong every time. That is the argument for deriving
a count rather than writing one down, made against me by my own table.

**And the retraction over-claimed again.** "Three stage lines changed, and all three changed
because they were broken" — two were. `extension-compat` lost a stray full stop. Round 9's
finding 2 was that a retraction contained a fresh over-claim; so did this one. The fix is to
state what each case IS rather than what the set has in common, which is the same lesson as
the sweeps.

**The extraction I built to stop patterns drifting guarded only zero.** It keyed on one
spelling — `"$lower_snake"` with a single-quoted pattern — and four realistic spellings went
unmatched: `${braces}`, uppercase, digits, a double-quoted pattern. The suite refused an
empty extraction and said nothing about a partial one, which is verbatim the class
`AgentNotes.md` names. Worse, the correct lesson was applied 250 lines away in the same
commit: `link_check.py`'s `_QA_SCRIPT_GATE` had been deliberately widened to three spellings
with a comment saying keying on one would exempt the others without saying so. I read that
file, fixed a defect in it, and did not carry the lesson across. The regex is widened and
there is now a `COVERAGE: n of m` against an independently counted denominator — mutation-
tested with a line-continuation call site, which reports `extracted 7 … but it has 8`.

Round 10's own judgement on the control I had flagged as partial: it improved on it, built a
real broken tree, ran the real classifier, and fed genuine output through the gate's `jq` and
`verdicts.parse`. Both stage lines survive, nothing is dropped. The remaining gap is that
`qa-docs.bash` has no root seam, so no invocation of the script itself has ever taken the
`V_BROKEN -gt 0` branch. Worth saying rather than calling it end-to-end.

## 09:05 · finding · T4.5 — I attacked my own denominator and it had the defect it was added to catch

The `COVERAGE: n of m` guard added an hour ago compares a strict extraction against an
independently counted denominator, so that an under-matching regex cannot read as complete.
Before dispatching round 11 I tried to defeat the denominator myself, which is the question I
had put to the reviewer, and it fell over immediately.

`grep -cE '[^_]qa_gate_detail '` requires a character BEFORE the name. A call site at column 1
has none, so the denominator missed it — **and so did the extraction**. The two agreed, and
agreement was the whole signal. Measured, on a real call site added at column 1 with a line
continuation:

```
OLD: denominator=8  extraction=8  -> agree? YES — SILENT PASS
NEW: denominator=9  extraction=8  -> FAIL  extracted 8 ... but it has 9
```

A guard whose clean result is indistinguishable from a blind one, in the guard written to
stop exactly that, one commit after writing it. Seventh instance, and the second inside the
fix for an earlier one. The pattern is now `(^|[^_[:alnum:]])qa_gate_detail[[:space:]]`, and
comment lines are excluded so a prose mention cannot over-count and fail the suite for the
wrong reason — a false failure is the safe direction but it still erodes the signal.

Worth being precise about why the first version looked right: I checked it against the call
sites that exist, all eight of which sit inside `x=$(...)` and therefore have a preceding
character. Measuring against the current population and generalising to the possible one is
this plan's meta-defect, and it is what a denominator is FOR — so the denominator has to be
the one thing not derived from what is there today.

`|| true` was blocked while writing this, correctly: `grep -c` exits 1 on zero matches, which
is a result the next branch reports rather than an error to swallow. The status is consumed
by an explicit `if`.

CI on `e6ec507a` is green, so the `git ls-files` swap holds on a clean checkout as well as
here — the second green run in a row, after three weeks of red.

## 09:35 · finding · T2.2 — a link to the repository itself reads as "not tracked by this repository"

Kept attacking my own code rather than waiting for round 11 to do it, and the trackedness
branch has a hole at its own root. `repo_relative()` answers `"."` for a link that resolves
to the repository root — `[the repo](..)` from a file one directory down. `git ls-files`
never lists an entry for the root, because the root is not a file, so `"."` was in neither
the tracked-files set nor the derived-directories set and the target came back:

```
target is not tracked by this repository
```

A hard finding, and a false one: the thing it says is untracked is the repository doing the
checking. Written as a RED test first (`test_a_link_to_the_repository_root_is_not_a_finding`),
then fixed where the sets are built rather than at the comparison — `tracked_paths()` now
seeds `directories = {"."}`, with a comment saying why `ls-files` cannot supply it.

Same shape as the last three: the derived set was built from what `git` reports and then
treated as the complete truth, when one member of the domain is true by construction and
`git` has no reason to mention it. The tracked-files half of the pair is sound; it is the
directory half, which I derived, that had the gap.

Zero such links exist in the repo today — I checked before deciding it was worth fixing. So
this is latent, not live, and it would have surfaced the first time someone wrote a
relative link up to the repository root. Two neighbouring cases are correct and now
confirmed by hand: `../../etc/hostname` (escapes the repo) reports `target does not exist`,
and `../.git/config` reports `target is not tracked by this repository` — which is right,
`.git` is not the repository's tracked content.

Full QA green at 920 files, `docs: 71 files OK … VENDORED: 8 verified, 0 unverifiable, 0
broken`, 80 tests in the link-check suite.

## 09:47 · decision · T2.2 — the warning branch had never run, so I deleted the branch

Round 10 raised this and judged the state adequate; I do not, because it is this plan's
defect class sitting inside this plan's own deliverable. `qa-docs.bash` had:

```bash
if [[ "$V_BROKEN" -gt 0 ]]; then
    echo "⚠ docs: $V_BROKEN link(s) into a PRESENT vendored repo are broken — …"
    jq -r '.vendored.broken[] | "    \(.file):\(.line)  \(.target)"' "$TMP_RAW"
fi
```

That branch needs a vendored repo PRESENT and a stale pointer into it. No invocation of the
script has ever been in that state, so the jq had never interpolated `.file`, `.line` or
`.target` from a real entry — three field names nothing had checked, in a path that would
first execute on the day it was most needed, watched by nobody.

Round 10's suggested fix was to move the content into the Python. The better version is to
move it AND drop the condition: `vendored_warning_lines()` returns a list, empty when nothing
is broken, and the script prints it with `jq -r '.vendored_warning[]'` unconditionally. Empty
list, empty output, and the formatting code runs on every single QA run. A rare branch became
a common one — which is the same move as giving a failure-only gate a pass line, and for the
same reason.

Six tests, RED first. The one worth naming asserts the detail lines are indented and carry no
stage symbol, because `verdicts.STAGE` anchors at column 0 and would otherwise count each
broken link as a gate.

Then I stopped trusting the tests and drove the real thing: a throwaway `docs/` file linking
at `.claude/hooks-daemon/CLAUDE/THIS-FILE-DOES-NOT-EXIST.md`, which is a genuinely present
vendored repo here.

```
⚠ docs: 1 link(s) into a PRESENT vendored repo are broken — it has probably moved the file:
    docs/zz-vendored-probe.md:3  ../.claude/hooks-daemon/CLAUDE/THIS-FILE-DOES-NOT-EXIST.md
✓ docs: 72 files OK (…) — VENDORED: 8 verified, 0 unverifiable (repo absent), 1 broken
exit=0
```

Warns, does not fail — the invariant holds. And `verdicts.parse()` over that exact text
returns ONE stage, `docs`, with two lines; the indented line is not read as a stage. The
comment in `qa-docs.bash` claimed that already, but it had been measured against a string I
typed rather than against output the script produced. Now it is the latter.

Full QA green, 920 files, 1518 helper tests (1512 before these six).

## 09:51 · finding · T4.3 — a failing gate erases itself, which kills one of the two options

Went looking for a cheap way to close Task 4.3 without making the structural call, and found
instead that one of its two options does not work.

The premise I had been carrying — the plan's, and the reviewers' — is that the abort hides
the gates BEHIND it. It also hides the gate that failed. Every hard gate aborts with

```bash
echo "✗ QA FAILED: helper unit tests" >&2
```

and `verdicts.py` matches `^[✓✗⚠] QA (?:passed|FAILED):` as a RUN SUMMARY before `STAGE` gets
a look. So the failing gate contributes no stage line at all. Measured, not read off the
regex:

```
stages recorded: ['bash', 'nokill-containerwatch']
is a stage recorded for the FAILING gate?  False
```

All 31 gate aborts have that shape, and the prose is not the gate's name in any of them —
`helper unit tests` against the stage `helper-tests`, `secret scanner unit tests` against
`secret-scan-tests`, `the panel and the status document producer disagree` against
`panel-contract`. Nothing could map one to the other.

That kills option (2). "Compare executed against declared and fail on a shrink" assumes a
failed gate looks different from an absent one, and it does not — the detector would call the
gate that failed a gate that never ran, on every red run, which is the only kind of run it
exists for. Making the failure lines stage-shaped first is the prerequisite, and that edit is
most of option (1).

So the recommendation is (1), and it is now a costed one rather than a preference: the 29
hard gates share a single shape, the edit at each is `✗ gate-name: <reason>` + `FAILED++` +
the `✓` line moved into an `else`, and the seven `exit 2` missing-tool aborts stay aborts.
Still the owner's call — I have not made it — but the choice is now between doing the work
and not doing it, rather than between two designs.

Worth naming what went wrong in the earlier framing: I had read the abort's behaviour off the
source and never run the parser over what it actually prints. The same shape as everything
else this plan has found, in the plan's own decision gate.
