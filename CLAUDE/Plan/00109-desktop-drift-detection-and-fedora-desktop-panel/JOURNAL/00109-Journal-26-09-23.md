# Plan 00109 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00109. One file per day
> (`00109-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:45 · action · — — plan scaffolded

Plan 00109 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 08:45 · action · T4.3   — play runner built and merged; HOST checks remain

T4.3 built in a worktree and merged (05f7eb32). Notes: `subagent-reports/260923-task43-opus-5.md`.

Decisions settled by the coordinator and recorded in DESIGN-panel.md §9:

- The runner lists the plays the ledger has seen here (§9's evidence answer), sorted by
  path, each with its freshness state. GONE plays are omitted, because the health section
  already reports them.
- The per-play list travels in `host-status.json` under `plays`. `check_freshness.run`
  hands over its verdicts only on a complete judgement, so the panel never shows a
  partial list. Schema stays 1, because the key is additive.
- A click launches `fedora-desktop-health --run-play <play> --hold` through the existing
  `xdg-terminal-exec` argv route. The command re-validates the play from the ledger and
  refuses anything else, with exit 64. The panel never claims the play ran (§6).

`qa-all.bash` is green on the merged tree in the main checkout, including ansible-syntax
and ESLint. The worktree could run neither. The T5.4a quote of the extension header is
corrected in PLAN.md. HOST checks remain, as listed under the task.

## 17:25 · finding · T3.2   — first VM run of the server route: fixture failed at prepare

The bridge is live, so `./scripts/vmtest-request.bash run-scenario server-host-health-kernel-change` ran from the container (run `20260923T164540Z-server-host-health-kernel-change`). Verdict `error` at `assert`: "the fixture exited 1 (stage: prepare)". The fixture's own `ERROR: prepare: …` line is only in the host-side transcript (`~/.local/share/vmtest/runs/<run-id>/`), so the sandbox cannot see which step failed. Nothing about the route is established.

Harness fix: `vmtest` gains `prepare_failure_reason`, which copies the fixture's last `ERROR: prepare:` line into the verdict reason. Only fixture-authored text is copied, never arbitrary guest output. Two cases are in `test-vmtest-prepare-record.bash`. It reaches the host with `play-vm-test-lab.yml`; after that, a re-run names the failing step.
