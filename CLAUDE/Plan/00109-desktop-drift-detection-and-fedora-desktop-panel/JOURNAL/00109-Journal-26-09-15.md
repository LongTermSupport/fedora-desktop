# Plan 00109 — Journal 26-09-15

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 10:01 · delivery · T3.2 — the server route, and the one line that could have broken scp

Picked up as the only Task 3.2 sub-item that needed neither the host nor a decision: the
delivery half of the server route. `status_document.py` (producer) and `login_message.py`
(renderer) were already written and tested; nothing ran either of them on a server.

**Three artefacts.** `host-health-collect.service.j2` runs
`login_report --no-notify` — the same collection the desktop unit does, minus the
`notify-send` there is no session bus for. `host-health-collect.timer` triggers it.
`host-health-report.bash.j2` goes into `~/.bashrc-includes/` and prints what the timer
left. `play-host-health-server-report.yml`, `scope: server`, deploys all three.

### The cadence is derived, not chosen

The task said "cadence to be decided against `STALE_AFTER_DAYS`", and that bound
(`login_message.py:47`) is 14 days: past it, the consumer reports the document's own age
as a finding, because a clean document nobody has refreshed for a fortnight describes the
host as it was a fortnight ago.

Daily, therefore. It absorbs fourteen consecutive misses, so one failed run, one reboot or
a day powered off is not a false alarm, while a collector that has genuinely stopped is
reported well inside the bound. Weekly gives two attempts inside the same window — the
margin would be a single missed run, and the report would then be crying stale on a
healthy host or silent on a broken one depending on which way the miss fell. The
reasoning is written into the timer beside the value, because a number in a unit file
with no derivation is a number the next person will change without knowing what it was
balanced against.

`OnStartupSec=5min` as well. This plan exists because a reboot into a new kernel left a
DKMS module unbuilt and nothing said so; a purely calendar-driven collector would take up
to a day to notice that, which reproduces the incident with a delay.

### The interactive guard is the load-bearing line

bash reads `~/.bashrc` for a **non-interactive** shell too when sshd is what started it.
That is why a `.bashrc` that prints anything breaks `scp`, `sftp` and rsync-over-ssh with
a protocol error: the transfer parses stdout. This snippet is the first thing this repo
puts in `~/.bashrc-includes` that prints at all — the existing four only define aliases —
so the guard is new territory here, and a health report that breaks file copies to the
host it reports on is worse than no report.

`scripts/test-host-health-login-snippet.bash`, 12 assertions, renders the template and
sources it in both a real `bash -i` and a plain `bash -c`. Six mutants, all killed:

1. the interactive guard deleted → the non-interactive case speaks
2. `cd` into the checkout instead of `PYTHONPATH=` → the caller's `$PWD` moves
3. `PYTHONPATH` exported → it survives into the caller's environment
4. the missing-checkout guard deleted → an ImportError traceback at every login
5. that guard returning non-zero → a login shell under `set -e` would die
6. a bare `python3` instead of `/usr/bin/python3` → the pyenv shim, not the interpreter
   the play installs `python3-pyyaml` into

**The sixth survived the first run**, and the reason is worth keeping. The assertion was
`grep -q '/usr/bin/python3' "$RENDERED"` — and the snippet carries a comment explaining
*why* the interpreter is named by path, which contains the string. So the check was
satisfied by its own documentation while the invocation it vouched for had been mutated
to a bare `python3`. It now selects the non-comment line that names the module and tests
that, and fails if it does not find exactly one such line.

The guard against a findings document rather than a clean one matters for the same
family of reason: with a clean document the snippet is silent for the wrong reason, and
mutant 1 would have passed.

### Two smaller decisions

**The snippet asserts nothing about its own plumbing; the play does.** `~/.bashrc-includes`
and the loop that sources it belong to `play-basic-configs.yml`, which is in
playbook-main's core path. This play slurps `~/.bashrc` and asserts the marker is there,
failing with an instruction to run playbook-main first. Deploying a snippet into a
directory nothing sources would leave a host reporting as healthy while nothing read the
report — this plan's own defect, one layer down.

**The timer is started as well as enabled.** `enabled: true` alone writes the
`timers.target.wants` symlink and leaves the timer unarmed until the next boot. Starting
a *timer* arms the schedule; it does not run the collection now.

### Not done

The HOST run. The timer arming, a document appearing, an interactive login showing
findings, and an `scp` to the host still completing are four claims this container cannot
make. Recorded as the remaining ⬜ under Task 3.2.

`./scripts/qa-all.bash`: 878 files green, `host-health-login-snippet: passed: 12`.

PLAN.md is now 348 bytes over the 18,000-byte advisory threshold. The three task lines
added here are already the terse form; the bulk is elsewhere in the document and trimming
it is a separate job from this one.

## 10:30 · finding · T3.2 — a fresh document can be entirely about the previous boot

Found while checking whether `RandomizedDelaySec=30min` weakens the `OnStartupSec=5min`
trigger. It does, by up to half an hour — but chasing that turned up something worse
sitting underneath it.

**The desktop route cannot hit this and the server route can.** `host-health.service`
re-runs the producer at every graphical login, so the document always describes the boot
the reader is in. The timer route does not: a host reboots, the document from before the
reboot is minutes old, well inside `STALE_AFTER_DAYS`, and says `ok`. Every DKMS module
on the box can be unbuilt for the kernel that actually booted and the login report stays
silent.

That is this plan's founding incident with the delivery mechanism I had just built in
front of it. Staleness does not cover it — age and boot are independent, and the document
can be one minute old and still be about a different kernel.

The document already carried `kernel`; nothing compared it to anything.
`login_message.render` now takes `running_kernel` and reports a mismatch in its own
right, in the not-checked group — nothing is known to be *broken*, what is known is that
these results do not describe the running kernel.

**Required keyword, not a defaulted one.** The precedent is `ledger_present` on
`collect_sections` (26-09-14): a default makes nineteen existing call sites pass
untouched and lets any future caller silently omit the check. Here it cost five call
sites in the test file and one in `main`.

Four mutants, all killed: the comparison forced false, the `and running_kernel` guard
dropped (so "could not tell" would render as "they differ"), the mismatch reported as a
fault rather than as not-checked, and the message naming only the running kernel instead
of both.

### The bash suite caught its own fixture

`test-host-health-login-snippet.bash` went 9/12 the moment this landed, and it was right
to. Its documents were stamped `kernel: "0.0.0-test"`, so with the new rule in place
every case spoke — including the three written to prove the snippet stays *silent*. The
fixture now stamps `uname -r`. Worth recording because the failure was the gate working:
an invented value in a fixture stopped being inert the moment something started reading
it, and the three silent-case assertions would otherwise have quietly become assertions
about nothing.

`./scripts/qa-all.bash`: 878 files green, 1,260 helper tests, snippet suite back to 12/12
with all six of its own mutants still dying.

### Left alone

`RandomizedDelaySec=30min` stays. The post-boot window it opens is now covered from the
other end: whatever the document says during it, the reader is told it predates this
boot.

## 11:20 · review · T3.2 — the qa-reviewer pass, and the finding that mattered

Report: `subagent-reports/260915-qa-reviewer-00109-t32-opus-5.md`. Verdict
FIX-BEFORE-MERGE, no blockers, six things. All six are now fixed; the reasoning that
outlives this entry moved to [DESIGN-server-route.md](../DESIGN-server-route.md).

### The one that decided whether the surface was usable

**A healthy server would never have been silent** — and the reviewer measured it rather
than inferring it. `dkms` is installed by exactly two plays, both optional and both
desktop hardware, so a stock server has neither the command nor a module tree, and both
the health probe and the pin check emitted a permanent unchecked finding because of it.
Every interactive SSH login would have printed "fedora-desktop: this machine needs
attention" plus two lines that could never become actionable. That is the exact failure
`login_message.py`'s own docstring says the surface exists to avoid, shipped inside the
thing built to avoid it.

Two different answers, because the two checks ask different questions:

- The probe now treats *no dkms command **and** no registered module tree* as an answer.
  A host with no DKMS subsystem cannot have a module missing a build. Command gone with
  trees still registered is reported, and is worse than either half. An unreadable state
  directory answers `None` and still reports — the tri-state matters, and folding it into
  `[]` would let a permissions problem buy permanent silence on the axis the incident
  happened on.
- The pin check now takes applicability from the **play ledger**. A pin belongs to a
  play; a play this host has never run installs nothing here for the pin to be about.
  That is not a new policy — it is Task 1.3's rule ("a play with no record has never been
  run here, and silence is correct for it") applied to the axis that had missed it. The
  zero-coverage guard counts applicable pins, or it would simply replace the noise the
  filter removed.

Verified end to end in the container, which has no dkms and no ledger: both sections now
return nothing.

### The one I disagreed with, and did not silence

The timer's `git fetch` has no credentials, so an SSH-remote checkout reports "never
reached the remote" on every login, permanently. The reviewer called it the same class as
the first finding. It is not: unlike a DisplayLink pin on a headless box, this names a
real gap in *this host's* setup with a remedy the operator can apply. Silencing it would
be the mistake. It is documented in the unit, in `docs/playbooks.md` and as a HOST item
instead — and deliberately without an `Environment=SSH_AUTH_SOCK` pointing at a socket
that may not exist, which would trade a clear finding for a confusing one.

### The play was a copy, and merging it dissolved a second finding

Four byte-identical tasks and five comment blocks differing only by line-wrapping.
`play-host-health-login-report.yml` is now `scope: general` with two profile-gated
delivery blocks, and `play-host-health-server-report.yml` is gone. That also made
`CLAUDE/AnsibleStyle.md:240` true again — it says no play is `scope: server`, which the
deleted file had falsified.

`qa-ansible.bash` then caught something worth keeping: a task named "Scope guard —" on a
general-scope play. It was right. The assert stays because both branches key on
`provisioning_profile` and an unrecognised value would quietly take the desktop branch on
a server — but it is a recognition assert, not a scope guard, and is now named for what
it is.

### The QA inventory is derived now, not re-enumerated

`CLAUDE/QA.md` claimed seventeen gates and ten non-merged ones; the real numbers are
twenty-eight and twenty-one, and ten rows were missing. It had been repaired before by
swapping one hand-written list for a fresher hand-written list, which is why it went
stale again. `helpers/docs/link_check.py` now parses the gate invocations out of
`qa-all.bash` and fails the docs gate for any without a row. Controlled two ways: a
discovery finding nothing is itself a finding, and a scratch copy with one row deleted is
reported by name.

### Three assertions were comparing the wrong thing

`sourcing leaves PYTHONPATH unset` and the two exit-status checks captured the snippet's
*entire* stdout and compared it to `[unset]` and `0`. They only held while the snippet
happened to be silent — which is why the kernel-mismatch change broke them earlier today
with failure output pointing at PYTHONPATH instead of at the cause. The inner shell now
marks its answer and the probe extracts that line; a missing marker returns
`NO-PROBE-LINE` rather than an empty string, because empty is a legitimate answer for
some of these. The fixture also builds through `status_document.build`/`write_atomic`
now, so it tracks the schema instead of restating it.

All six snippet mutants still die. `./scripts/qa-all.bash`: 876 files green, 1,282 helper
tests.

## 11:55 · finding · T3.2 — the kernel-mismatch rule contradicted itself

Round 2 of the same review. Verdict still FIX-BEFORE-MERGE, and the finding is a good
one: the rule closes a real gap and then makes the report say something false in the
exact scenario it was written for.

Reproduced before touching anything:

```
  - evdi: no DKMS module installed for the running kernel 7.1.9-200.fc44.x86_64
  Not checked — these are NOT clean results, nothing is known about them:
  - these results were collected under kernel 7.1.9-… and this host is now running 7.2.4-…
```

Two consecutive lines, two different values for "the running kernel", and the first
presented as a known fault about now. `dkms_findings` bakes the collecting kernel into
its text, which is written at collection time and read later — so before this rule the
line was simply never shown after a reboot, and making the report speak surfaced a
latent contradiction rather than creating one.

**And a test of mine asserted the pairing and recorded it as correct**, which is the part
worth remembering: the assertion said "the mismatch is reported *alongside* real
findings", which is true and was the wrong property to pin. Nothing would have found it
later.

**Fix: demote, do not repeat.** On a mismatch the boot-scoped section's findings move
into the not-checked group, under the explanation that causes them. Which section that is
is now `status_document.BOOT_SCOPED_SECTION`, declared beside `SELF_SECTION` rather than
in the producer — a *consumer* is what needs to recognise it, and two spellings would let
the two disagree silently. `login_report.HEALTH` reads it from there.

Scoped deliberately. Only `post-boot-health` is boot-scoped; the ledger, play freshness
and installed-vs-pinned survive a reboot unchanged, so demoting them would be the mirror
image of the same overclaim. The wording was narrowed for the same reason — "nothing here
describes the running kernel" was false about three sections out of four.

Four mutants, all killed: the demotion disabled, *every* section demoted, the explanation
appended after what it explains, and the wording reverted to the overclaiming form.

### The two nits were already fixed, and I checked rather than assumed

The reviewer was reading an intermediate tree — before `7fa9edef`. `uname -r` is gone
from the fixture (it builds through `probe.running_kernel()` now), the three assertions
already extract a marked line rather than comparing whole stdout, and the scope-guard
error it flagged in `qa-all.bash` was the same intermediate state. Verified all three
against the current files.

### The recognition assert stays, and now says why

The reviewer read `AnsibleStyle.md`'s "a general play carries **no** guard" as requiring
the assert deleted. That rule is about the two-task `meta: end_play` block, which would
be wrong here — this play has work for both profiles and `qa-ansible.bash` rejects that
block on a general scope. What the assert validates is an input a **human types**:
auto-detection can only yield desktop or server, but `-e provisioning_profile=…` is the
documented override and takes highest precedence, so a typo would leave `is_server` false
and quietly deploy the notify-send unit to a box with no session bus. The play now
records that distinction instead of leaving it to be re-litigated.

`./scripts/qa-all.bash`: 876 files green, 1,287 helper tests.

## 12:20 · finding · T3.2 — I transplanted a rule instead of deriving one, and it silenced the incident

Round 3. The reviewer drove `check_pins.check` across five ledger states and found that
my own fix had turned off the drift this plan exists to catch. Reproduced before
changing anything:

```
  ledger unreadable (None)      -> ['evdi_version (behind): pinned 1.15.0, installed 1.14.16']
  ledger empty                  -> []
  ledger rows, no displaylink   -> []
  ledger has the displaylink row-> ['evdi_version (behind): …']
```

The middle two are every host in existence. **Task 1.3 chose no backfill**, so nothing
has a `play-displaylink.yml` record until that play next runs — and `installed 1.14.16`
against `pinned 1.15.0` is the 2026-09-11 state exactly. I had switched off the report
for the incident that started the plan.

Worse, a ledger with rows and no pinned play left the population empty, which skipped the
`applicable and tracked == 0` guard entirely: no output at all, indistinguishable from
every pin matching. A check that cannot fail, produced by a fix for noise.

**The error was reasoning by transplant.** Task 1.3's rule — "a play with no record has
never been run here, and silence is correct for it" — was derived for the *freshness*
axis, where "has this play run here" IS the question. On the install-state axis it is
not: an installed version that disagrees with the pin is drift whatever the ledger has
seen. I quoted the precedent as if it settled a question it had never been asked.

Re-derived, and the two host facts now act on what each can actually answer:

- `dkms_registered == []` makes a **DKMS-resolved pin** unanswerable here. That is the
  whole reason a server spoke: `dkms()` raises "command not found" and every DKMS pin
  became "could not be checked". Same evidence as the probe fix, so the two cannot
  disagree about whether this host has DKMS.
- The ledger disambiguates **one verdict**, `ABSENT`. "Pinned 1.15.0, nothing installed"
  is a fault on a host that ran the play and expected on one that never did. `BEHIND`,
  `AHEAD` and `UNDETERMINED` mean the software is here and was compared, so no ledger
  answer can make them uninteresting.

The zero-coverage guard counts the whole manifest again, which is what it was always
about. Verified: the incident reports on all four ledger states, the server is silent.

### My verification could not have failed, and the reviewer said so

"No dkms, no ledger → nothing" passes whether or not `pin.playbook` and the ledger key
share a spelling. There is now a test that the tracked pins' `playbook` values are
repo-relative and resolve from the repo root — the same form `collector._resolve`
produces — plus the positive case driven through a ledger that contains the real path.

### Two smaller ones

**The tri-state had a hole.** `os.path.isdir()` answers False for anything it cannot
stat, so an unstattable entry dropped out of a list whose *emptiness* is what licenses
silence — a partial read rendering as `[]`. `os.scandir` with a per-entry `except
OSError: return None`, and a dangling symlink now answers `None` too: `DirEntry.is_dir`
swallows FileNotFoundError, so a link that does not resolve had read as "not a module".

**`qa_gates` derived by one spelling.** `${SCRIPT_DIR}/x.bash` and
`$REPO_ROOT/scripts/x.bash` both parsed to nothing, so a future gate in either form would
have been silently exempt — and the zero-discovery guard only fires when *every* form
fails. It was also one-directional. Both fixed: the pattern matches a filename after any
variable- or path-shaped prefix, and a documented-but-not-run gate is now reported, which
is the exact failure QA.md narrates below its own table. Module gates are keyed by their
full dotted path, which is what the table's first cell already writes — the last-component
form matched by substring one way and disagreed the other, so the reverse check could
never have been clean. The forward direction now compares against ROWS, not the whole
document, so prose naming a gate no longer counts as documenting it.

### Withdrawn

The reviewer withdrew the fetch-credentials finding and agreed with the distinction, with
one correction I have taken: `offline_finding(last=None)` fires on the first login, not
after seven days, so the HOST item confirms the remote rather than assuming it.

### Closed at the merge

Neither branch removed the other's artefacts, so correcting a mis-set
`-e provisioning_profile` left both deliveries installed. Not a regression — the two
separate plays had it too — but the merge is where it closes. Each branch now removes the
other's unit files and their `.wants/` symlinks, since enabling writes a symlink that
outlives the unit file.

`./scripts/qa-all.bash`: 876 files green, 1,296 helper tests.

## 12:45 · finding · T3.2 — the boot predicate lived in one of the document's two readers

Round 4 verified the demotion and `BOOT_SCOPED_SECTION`'s move, and answered the
producer question I had asked: leave `dkms_findings`' wording alone, my reasoning holds —
but the root cause is not the wording. **"Is this document about the boot I am in?" is a
property of the document, implemented in one of its two declared consumers.**

Verified rather than accepted: `statusDocument.js:64` carries `kernel: ''` and compares
it to nothing. The panel renders `post-boot-health` findings as current faults whichever
boot produced them. And the reason a desktop looked immune — the producer runs at every
graphical login — holds only while `host-health.service` works, which is one of the
things this plan exists to detect. The reviewer's argument, and it is right.

`status_document` now owns `is_boot_stale(document, *, running_kernel)` and
`collected_kernel(document)`; `login_message` calls them instead of computing the
predicate inline. Six assertions, including that neither raises on a document it cannot
read. All four demotion mutants still die after the refactor.

The panel side is Phase 4 and recorded as a Task 4.2 item: it needs the running kernel in
GJS, a rendering decision for a stale section, and a Wayland log-out-and-in to verify.
Doing it here would be inventing a UX answer in a container that cannot see the result.

### Not chased

The reviewer's "still open" list — H1, H3, the convergence gap, and fix 2's `last=None`
qualification — was written against `2790e163` and is covered by `665e64de`, which it had
not read; it noticed two of those in the working tree while they were uncommitted. It also
corrected my record: the `uname -r` and `PROBE:` points were round-2 findings its round 3
had already recorded as fixed, so there was never a stale finding to chase.

`./scripts/qa-all.bash`: 876 files green, 1,302 helper tests.

## 13:10 · finding · T3.2 — "no DKMS modules" is not "no DKMS", and Task 0.2 would have proved it

Round 5. H1 closed and the re-derivation verified across all four ledger states plus a
stock server. Three new findings, all real, all mine.

**H4 — the empty registry.** My skip condition was "the module list came back empty",
and the reviewer measured why that is wrong. The `dkms` rpm **owns `/var/lib/dkms`**, and
`play-displaylink.yml` installs `dkms`, so every host that ran that play has the
directory — and an empty registry there means the module was *removed*. That is exactly
what this axis exists to report, and **Task 0.2 of this plan is about to create that
state** by deleting orphaned DKMS trees. Its counterfactual is the part worth keeping: a
single unrelated module in the registry restored the correct finding, so the bug was
invisible on any host that happened to have one.

The deeper problem it named: the two consumers were reading the same value but needed
different facts. `build_report` required `dkms.missing AND registry == []`; `check_pins`
required `registry == []` alone. `probe_results.DkmsRegistry` now carries them apart —
`present` is a tri-state (no directory / present / could not tell), `modules` is what it
holds — so one read gives one coherent value and the two cannot hold inconsistent halves.
Verified: only "no state directory at all" is silent now.

**H5 — the BROKEN sentinel.** The ABSENT scoping leans on "an empty or unreadable ledger
is `ledger_presence`'s finding", and that holds everywhere except the one state where
`ledger_presence` returns `[]` *deliberately* — while the sentinel exists, because
`check_freshness` already prints the reason. So in the single state where this repo has
declared the ledger incomplete, `plays_run_here` read it anyway and would have suppressed
every ABSENT whose row was in the hole, with nothing saying so. It now returns `None`
there. The sentinel IS the declaration that the question is open.

**H6 — the resolver with no ABSENT branch.** `_rpm_version` catches "is not installed"
and returns `None`; `_command_version` was a bare `_run`, so a missing command raised,
the broad `except` turned it into an *unchecked* finding, and the original permanent
noise came back through the one resolver the ABSENT scoping cannot reach — it never gets
as far as `classify`. Not live today: the manifest has nine pins and the one tracked pin
is DKMS-kind. Invisible when that stops being true, which is the whole reason to fix it
now.

**The accepted consequence is now a decision (§5.3).** A host that ran a play and then
deliberately removed what it installed reports ABSENT for ever. That is correct rather
than tolerated — the repo declares the pin, so software the repo installs and the host no
longer has is drift — and the manifest already carries the remedy: declare the pin
`untracked` with a `why`. Suppressing it silently would make "I removed this on purpose"
and "this vanished" the same answer.

### Pattern worth naming

Three rounds, three variants of one mistake: a predicate that conflates two facts because
one value happened to represent both. `[]` for "no directory" and "empty directory"; the
population filter for "not installed here" and "never checked here"; `missing` present in
one consumer's condition and absent from the other's. Each time the fix was to stop
collapsing and start carrying the distinction in the data.

`./scripts/qa-all.bash`: 876 files green, 1,310 helper tests.

## 13:35 · finding · the panel contract gate is a vocabulary check, and reads like a behaviour one

Round 6 arrived stale — both it and round 5 were written against `b0679155`, and the
"34 helper-test errors from a concurrent half-applied refactor" it saw was my H4 work
mid-flight. H4, H5 and H6 are fixed in `8392c406`; HEAD is green, 876 files, 1,310 helper
tests. Its record correction is accepted: `665e64de` **was** reviewed, in round 5, and I
was wrong to call it unseen.

The one thing in round 6 that was new is worth more than the correction.

**`check_panel_contract` demands the panel mention `kernel`, and it passes.** The panel's
only uses of that key are a prose comment at `statusDocument.js:20` and `kernel: ''` at
`:64`, compared to nothing. So the gate is satisfied while the panel asks no question at
all — which is how the boot-staleness gap sat behind a green gate that lists `kernel`
among the seven document keys it verifies.

It is a **vocabulary** check. It proves both sides use the same words; it cannot prove
either side does anything with them, and a cross-language literal matcher never could.
The reviewer's framing is the useful part: extending it to cover the behaviour would be a
category error, and would make its silence more convincing rather than less.

Recorded where it will be read: the gate's own docstring now says what it proves and what
it cannot, naming `kernel` as the live example; the Task 4.2 item says the proof is a test
rendering a boot-stale document through the panel's section code, **not** another required
mention.

That is the fourth instance of this session's recurring shape — a check whose green
depends on a distinction it does not actually make.

## 14:00 · finding · the fifth instance was inside the fix for the fourth

Round 7 verified H4, H5 and H6 with measurements — the decisive row now reports where it
was silent in rounds 5 and 6 — and then answered the question I had asked: yes, there is
another instance of the shape, and it is in the code I wrote to close H6.

`_command_version`'s new ABSENT branch matched `"command not found"` **in the exception
message**. `_run` raises that phrase for the OS refusing to exec a binary, and separately
builds its other error out of the tool's own output. So a wrapper script that exists,
exits non-zero and reports that phrase about something inside itself resolved to `None`,
became `ABSENT`, and rendered as *"pinned X, nothing installed"* — a confident claim about
the host from a probe that broke. `_rpm_version` had carried the identical shape from the
start, matching a phrase anywhere in a merged stdout+stderr blob, so H6 propagated it
rather than inventing it.

The reviewer's best line: **the fix already existed eight files away.** `probe.run_probe`
answers exactly this question structurally with `ProbeOutcome.missing` — the same
distinction I had reached for twice in this diff — and `_run` had the same two branches in
front of it and flattened them into one string.

`NotInstalled(ResolutionError)` is raised only from the `FileNotFoundError` branch, where
the OS is the one saying the binary is absent, and caught by type. `ResolutionError` now
carries `returncode`, `stdout` and `stderr`, so `_rpm_version` matches rpm's phrase on the
stream rpm uses and against the package it asked about. Five mutants, all killed —
including "goes back to matching the message" and "ignores which package was asked about".

Also fixed: two stale references it caught. `DESIGN-server-route.md:126` still named
`dkms_registered_modules()` and still asserted "the two cannot disagree about whether this
host has DKMS" — a function that no longer exists under that name, stating as current the
exact claim H4 disproved. And `check_pins.check`'s docstring still named the old
`dkms_registered` parameter.

### The shape, five times over

- `[]` for "no state directory" and "directory present, empty" (H4)
- the population filter for "not installed here" and "never checked here" (H1)
- `dkms.missing` in one consumer's condition and absent from the other's (H4)
- a contract gate proving vocabulary and read as proving behaviour (Q3)
- a string in an error message for "the OS could not exec it" and "the tool said so"

Every one is the same defect: a predicate whose answer depends on a distinction it does
not make, and every fix was to stop collapsing and carry the distinction in the data or
the type.

`./scripts/qa-all.bash`: 876 files green, 1,316 helper tests.

## 14:30 · finding · a malformed status document read as a healthy host

Round 8. It verified `f48a5bed` as accurate and correctly placed, and then found the
sixth instance — the one it said it would not close over, and it was right not to.

Reproduced before touching anything. Five shapes, each carrying a current timestamp, the
running kernel and `schema: 1`, so nothing else flagged them:

```
sections is a string      -> SILENT (reads as healthy)
sections is a list        -> SILENT
a section is a string     -> SILENT
findings is a string      -> SILENT   <- the document's own state field says "findings"
findings holds dicts      -> SILENT
(control) a real finding  -> "- evdi: no DKMS module"
```

The fourth row is the one to remember: **the document says `state: "findings"` and the
reader prints nothing**, because `render` never reads `state`. A document contradicting
itself, and the consumer agreeing with the wrong half.

`_texts` answers "not a dict", "key missing", "not a list" and "genuinely empty"
identically, and on this surface empty means healthy. The asymmetry sat inside one
module: `status_document.read` turns absent, unparseable and unknown-schema into
`unavailable` on the stated rule that ignorance is not health — and then a document that
parses with a known schema and unreadable sections becomes silence. `SCHEMA_VERSION`
guards only the top-level integer.

**It hid because the trade was made for a good reason.** A login shell must not lose its
prompt to a traceback. But *never raise* and *never go silent* are not the same claim,
and `collect` already had the third answer: a producer that raises becomes an `unchecked`
finding naming its section. The covering test pinned "does not raise" and said nothing
about "is not silent", which is exactly how it stayed invisible.

`status_document.unreadable_reasons` is that answer for a reader, and it lives with the
document rather than in `login_message` for the reason round 4 established — the panel is
the other consumer, and `health.js` branches on `section.state` while the login route
reads the lists, so on that fourth shape the two do not agree about what the document
says. Folded into the Task 4.2 rendering test rather than guessed at from here.

Six mutants, all killed, including "the consumer stops asking at all" and "the reasons
are reported as faults rather than as not-checked". The control that a genuinely clean
document stays silent is in the same class, because without it the whole class would pass
with the report shouting at every login.

### Sixth instance, same shape

A predicate whose answer depends on a distinction it does not make — here, `[]` meaning
both "unreadable" and "nothing to say". Every one of the six has been fixed by carrying
the distinction in the data, the type, or a named reason, rather than by adding another
special case.

`./scripts/qa-all.bash`: 876 files green, 1,333 helper tests.

## 14:55 · finding · the type I introduced claims more than the OS told it

Round 9 verified `5f14b20e` and found the next one, inside the type I had just added to
close the last one.

`NotInstalled` comes from `FileNotFoundError`, and its docstring said "the command itself
is absent, established by the OS". What `exec` established is narrower: **the name did
not resolve on this process's PATH.** Measured:

```
/usr/bin/env exists on disk: True
with PATH=/nonexistent, _command_version('env') -> None   <- ABSENT: "nothing installed"
```

An installed binary reaching ABSENT — the same harm the round-7 fix removed for the
wrapper case, surviving for the PATH case. It is not academic: the consumer is a systemd
`--user` unit, whose PATH is narrower than the login shell an operator would test in.

**And `shutil.which` does not close it.** The reviewer offered it as one of two options;
I measured it before choosing, and it consults the same PATH and returns `None` for the
same input. It relocates the gap rather than closing it. Nothing cheap separates
"absent from the machine" from "absent from this PATH", so the honest move is the other
option: claim only what was established, in every place the claim is made —
`NotInstalled`, `ProbeOutcome.missing`, and `build_report`'s user-facing line, which now
says "dkms could not be run here" rather than "dkms is not installed". A test pins the
PATH case so the wording cannot quietly widen again.

### The ledger, without ordinals

The reviewer is right that `5f14b20e` fixes a different member of the set than my commit
title claimed, and right that it matters only because the ledger is the durable output.
Our counts diverged because we were enumerating different things — so the set is now
recorded in `DESIGN-server-route.md` §6a as a table with **no numbers**. Membership is
the point; the ordinals were worth nothing and were the only thing we disagreed about.

Seven members now, one shape: a predicate whose answer depends on a distinction it does
not make. Not one of them was fixed by adding a special case.

`./scripts/qa-all.bash`: 876 files green, 1,334 helper tests.

## 15:20 · closing · both open questions answered, and the last instance was a test's name

Round 10 answered the two I had put back rather than decided.

**Q1 — yes, `sections: {}` was the same bug wearing "deliberate", and I verified the
argument rather than taking it.** `collect_sections` returns **four keys even when every
producer raises**, because `collect` guarantees a key per producer; `_cannot_read` emits
one. So a zero-section document has no legitimate origin — version skew, truncation or a
hand-edit, exactly the population `unreadable_reasons` was written for. It is now
reported.

**And the test was itself an instance of the shape.**
`test_an_empty_document_is_still_silent_if_fresh` pinned *a document with no sections is
silent* while its name claimed *a clean fresh document says nothing* — a property already
pinned twice over by the two cases beside it. Nothing was lost by repointing it, and that
gap is how the malformed-document silence survived a suite that looked like it covered
this. Added to the §6a table as its own row, because a test named for one property while
guarding another is the version of this defect that hides all the others.

**Q2 — my call was right, for a firmer reason than the one I gave.** I said "cannot
currently occur". The real reason is that `state` is not independent data: `section()`
derives it from the lists, so reading it in `render` would be a consistency check between
a value and its own derivation — and a second mechanism for one fact, living in one of
two consumers, is precisely how the boot predicate went wrong. The residual is on the
panel side, where `health.js` branches on `state` while `render` reads the lists, and
`unreadable_reasons` cannot see it because such a document is structurally well formed.
One more row in the Task 4.2 rendering-test population; no code in `render`. Recorded as
§4.3.

**The reviewer corrected itself on `shutil.which`** — it consults the same PATH, so it
answers the identical question and cannot separate an installed-but-unreachable binary
from an absent one. Wording the verdict for the evidence was the whole available fix.
Worth noting that I measured that before choosing rather than taking the recommendation.

### Container-side work on Task 3.2 is done

Eight members of one shape, all fixed, none by adding a special case. The remaining gate
is the HOST list, and the reboot-into-a-different-kernel item is the one to run first: it
is the only claim that exercises the route end to end on the scenario the plan was opened
for.

`./scripts/qa-all.bash`: 876 files green, 1,335 helper tests.

## 15:45 · decision · the reboot claim belongs in the VM lab, not on a host

The owner asked the obvious question I had not: *"reboot into a different kernel — can you
test this with the vm? isn't that the point of the vms?"*

Yes, and it is a better test than the host one I had listed. A HOST run cannot make this
claim on demand — it has to wait for Fedora to ship a kernel update, reboot into it, and
log in before the timer next fires. A guest can simply be **given** a second kernel. I had
left the item as `**HOST**` without checking whether the lab this repo already owns could
make it, which is the same reflex as writing a check for a population that turns out to be
empty: the answer looked settled because nobody asked.

### What the lab could already do, and the one thing it could not

The extension points were there. `vmtest` resolves `guest-acceptance-<scenario-id>.bash`
before falling back to the profile's, `run_env` already allows
`RUN_BASH_OPTIONAL_PLAYBOOKS`, and the bridge is live, so a scenario can be requested from
the container. The gap was that the mid-run reboot was gated on `BASE_PROFILE == desktop`:
a server scenario could not ask for one at all.

**That gate was reading a scenario's decision off a profile.** A desktop reboots because
Wayland cannot reload the shell and GNOME loads extensions at session start — a fact about
what its checks look at, not a law of the profile. So `reboot_before_checks` is now the
scenario's answer and the profile supplies only the mechanics of getting a guest back.
Three refusals fall out, each closing a path that would otherwise have no symptom:

- a **desktop** scenario must declare it either way — defaulting would let a new one be
  judged in the session that installed its extensions, a pass for the wrong reason that
  looks exactly like a pass for the right one;
- a profile with **no mechanics** dies rather than skipping the reboot, because not
  rebooting is invisible downstream: the checker runs, the transcript is green, and it
  judged the boot that provisioned the guest;
- a **fixture with no reboot** is refused by `qa-vmtest-manifest.bash`, since the harness
  runs a fixture inside the reboot branch and nothing else would notice — the manifest is
  valid, the checker's count is right, and the file is there.

### The fixture had to break something on purpose

Two of the claims contradict each other on one host: a clean server login must be silent,
and a boot-scoped fault must be demoted after a reboot. The second needs a fault and the
first needs none. So the fixture collects on a clean guest and captures the login, then
makes a unit fail, collects again, and *that* document is the one that survives the boot.

Without the deliberate failure **the demotion check would have passed because the
population it judges is empty** — no dkms on a server, no failed units on a healthy guest,
so the boot-scoped section has nothing in it and a reboot has nothing to demote. §6a's
shape again, this time in the verification rather than the code. That is the second time
today the defect showed up in something written to catch it.

### Verified the matchers against the renderer rather than by eye

Before writing the scenario I ran the checker's string matching against
`login_message.render` itself with a boot-stale document: the fault classifies as
`findings` at the collecting kernel and `unchecked` at a different one, and both kernel
names appear in the mismatch line. Copying those strings from the source by eye would have
produced a checker that agrees with a vocabulary neither side got from the code under
test — §4.2's distinction, applied to my own test.

### Falsified, not assumed

Six mutants against a green baseline, all killed: the desktop-declaration rule removed,
the bool check removed, the marker field dropped, `reboot_guest`'s unknown-profile refusal
turned into a no-op, `guest_prepare`'s failure abort removed, and the tracked manifest
flipped to `reboot_before_checks: false` with its fixture still in place. The QA gate
inventory caught the missing `CLAUDE/QA.md` row on its own, which is what it was made
derived for.

### One thing the lab still cannot settle

The `scp` claim runs through the guest's own `sshd` with a key the fixture generates —
the real `SSH_SOURCE_BASHRC` path, not a shell sourcing the snippet. But §6 asks about
**this** checkout's `origin`, and a guest cloned over https says nothing about how a
particular machine's remote is configured. That one stays a host fact, and the plan now
says so instead of implying the VM covers it.

`./scripts/qa-all.bash`: 882 files green, 1,341 helper tests.

**Next**: `play-vm-test-lab.yml` on the HOST once, so the scenario reaches the deployed
allowlist and the two guest scripts reach `~/.local/share/vmtest` — the bridge refuses an
id that is only in the manifest, deliberately. Then the run can be requested from here.

## 16:30 · finding · "needs GJS" was true of rendering and false of everything else

Task 4.2 carried a tooling blocker I had written down and then inherited without
checking: the panel could not be tested here because testing it needs GJS. The owner had
just caught me doing the same thing with the kernel-reboot item, so I checked instead of
assuming.

`statusDocument.js` imports `Gio` and `GLib`; `sections/health.js` adds `PopupMenu`.
Every decision they make is a plain function over a plain object. A Node ESM loader
resolving `gi://` and `resource:///org/gnome/shell/` to stubs runs the **shipped** files
— `tests/extensions/gjs-loader.mjs`, and a fifteen-line spike proved it before anything
was built on it.

**Three gates already touched this extension and not one of them could fail on this.**
`qa-js.bash` parses it, ESLint lints it, and `check_panel_contract.py` proves it uses the
same words as the producer. None can tell a demoted finding from a current one, and
neither can a screenshot — which is what made a Wayland-only answer so expensive.

### A proper RED, and what it caught

Fifteen tests, four passing. The four that passed are the controls, so every later
assertion is a change from a known-good baseline rather than a claim about a renderer
that might be producing nothing at all.

- **`state` was a second mechanism for a fact the lists already carried.** `sectionOf`
  passed `section.state` through and both the menu and the icon branched on it, while
  `status_document.section()` DERIVES that field from the lists. A section saying
  `state: "ok"` over a populated `findings` list rendered "nothing to report" on the
  panel while the login report showed the fault. §4.3 predicted this and left it here;
  it was real.
- **A malformed document read as a clean host** — the `_texts` defect one language over.
- **The demotion had to live in one place**, or the icon would report a fault the menu
  had already explained away. `resolvedSection` is that place and `overallState` reads it.

### The tenth instance was in the helper I wrote to catch the ninth

`groupIn` started as a flat scan for the not-checked heading across the whole menu. The
menu renders a heading **per check**, so one demoted section made every later section's
findings read as demoted — and several of my assertions had passed only because the
ordering happened to suit them. A predicate whose answer depends on a distinction it does
not make, in the test helper written to catch exactly that shape. Scoped per check now,
and the one test it was failing was right to fail.

### Decisions worth keeping

The running kernel is read **once, in `enable()`** — it cannot change without a reboot and
a reboot ends the shell, so re-reading per render would be repeated synchronous I/O in the
compositor for an answer that cannot have moved. An unreadable `/proc` gives `''`, which
suppresses the claim rather than inventing one.

`BOOT_SCOPED_SECTION` went into the contract gate as well. The gate is still a vocabulary
check and still cannot prove the panel asks anything — but both readers must demote the
SAME section, and that much is a vocabulary question.

`log()` rather than `console.debug`, to match the four other extensions in this repo. The
test harness stubs `globalThis.log` rather than stripping the diagnostic, because a
diagnostic that only exists when nobody is testing is one nobody has ever seen work.

Six mutants, all killed: the demotion removed, the demotion applied to every section, the
both-sides-known guard removed, `state` read off the document again, a malformed group
reading as nothing, and a zero-section document saying nothing.

`./scripts/qa-all.bash`: 884 files green, thirty gates.

**Still HOST-only, and the gate says so rather than implying otherwise**: whether St
renders the demoted lines legibly, whether the caveat heading reads as a caveat, and
whether the icon colour is the right thing to look at.

## 17:15 · finding · the scenario I wrote to catch empty-population passes could not run at all

A second reviewer took `216dd9c5..a32739ed` and returned **BLOCK**, reproduced rather than
inferred. It was right on every blocking point, and I verified each before touching
anything.

### The record was unparseable bash

`record()` wrote `KEY=value` unquoted. One recorded value is the exact text
`probe_results.failed_unit_findings` produces for a failed unit, and it ends
`(system scope)`. `(` is a shell metacharacter: `source` exits 2 and **every key after
that line is unset**. Measured here — `PREPARED_TIMER_ENABLED` survived, everything past
it did not.

The checker then tested the record for EXISTENCE and sourced it without judging the
result, so a half-loaded record read as a loaded one, and `set -u` killed the run
part-way through the checks. The reviewer ran it: seven checks emitted, no
`VMTEST-CHECKS-DONE`, verdict `error` — which points a reader at the lab rather than at
the fixture.

Fixed in `record()` with `%q`, so no future value can opt out of quoting, and in the
checker with a judged `source` plus a `REQUIRED_KEYS` completeness assertion.

### And the silence check was fail-open, in the scenario written against fail-open checks

`clean-login-is-silent` read `${CLEAN_LOGIN_B64:-}`. So when the record failed to source,
the check decoded an empty string and **passed** — reporting silence it had never
observed. Every sibling defaults to a failing sentinel; that one defaulted to the answer
it was supposed to prove.

It was also wrong in the other direction. Both sides captured `bash -lic true 2>&1`, and
an interactive shell with no controlling terminal always emits `bash: cannot set terminal
process group / no job control in this shell` — 116 bytes, measured, and `ssh` without
`-t` guarantees it. So on a real guest the check would have failed for a reason with
nothing to do with this plan. `scripts/test-host-health-login-snippet.bash` had already
settled which stream matters; I had not carried that across. The two streams are separate
now and the check judges stdout, with stderr recorded as evidence so the noise is visible
rather than mysterious.

### What no gate could see

The reviewer's sharpest structural point: **six mutants, every one aimed at the
dispatcher, and nothing ever ran the fixture→checker contract** — which is precisely
where both blocking defects lived. A seam with no test is not protected by testing the
things either side of it.

`scripts/test-vmtest-prepare-record.bash` drives the real `record()` into the real
checker: a complete record must let the accounting finish, a parenthesised value must
round-trip, an unparseable record must stop the run before a single check, an incomplete
one must be refused by name, and an absent one too.

**Its third mutant survived.** Discarding the `source` status again still passed all six
cases, because the completeness assertion happened to catch every record I had thought to
write. So the guard I had just added was untested — a check passing because its
population was empty, one layer further out. A seventh case fixes it: a record that is
complete AND unparseable, where only judging `source` can catch the dropped tail.

### The generalisation, handed over by the other reviewer

`max_skipped: 0` guards a check that SKIPS. It says nothing about one that runs, judges an
empty population and passes. All fifteen were walked: fourteen fail on an empty or absent
value because each compares against an asserted key; check 12 iterates a list and now
asserts that list is non-empty; check 4 passes on nothing by definition and is therefore
paired with check 5, which requires a live fault through the same login. The audit is
written into the checker's header, not here, so the next person adding a check meets the
question rather than the conclusion.

### The overclaim

I had ticked *"the VM lab runs this route end to end"* directly above the unticked run of
it. That is a claim about a scenario which had never executed and, per the above, could
not have. Gone — the task now says the scenario exists, has never run, and that nothing
under it is established until it does.

Also fixed: `with_fileglob` skips silently on an empty match (now `loop` + `lookup` with
an explicit assert, matching the sibling six lines up), and my comment claiming
`qa-deployed-drift.bash` made the sets agree "by construction" was simply wrong — that
gate globs `*` against the play's `*.bash` and passes over never-deployed files.
`server_reboot` now requires the boot id to change, because waiting for SSH proves only
that SSH answers.

`./scripts/qa-all.bash`: 886 files green, thirty-one gates.
