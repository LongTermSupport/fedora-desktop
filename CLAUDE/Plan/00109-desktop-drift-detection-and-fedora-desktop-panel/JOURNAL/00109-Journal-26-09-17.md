# Plan 00109 — Journal 26-09-17

> Continues `00109-Journal-26-09-16.md`. Append-only; corrections are new entries.
>
> Times here are **UTC**, the CCY container's clock (`/etc/localtime` is `Etc/UTC`).
> Entries written on the host are BST, an hour ahead. Both are "local 24h" per
> `PlanJournalling.md` and neither says so, which is worth knowing before comparing a
> container-written entry against a host-written one.

## 08:35 · finding · T3.1 — the enable task's `daemon_reload` can never see the symlink it exists for

The batch deploy PASSED and `acceptance.bash` then REJECTED with 9 of 21 assertions
failing. Diagnosed in full at
`untracked/agent-reports/260917-00109-acceptance-diagnosis.md`. Five of the nine are a
cascade, three are explained, and **one is a real code defect**.

`play-host-health-login-report.yml` enabled the unit like this:

```yaml
    - name: Enable The Login-Time Health Report
      ansible.builtin.systemd:
        name: "{{ unit_name }}"
        enabled: true
        daemon_reload: "{{ hh_unit.changed or hh_server_removed.changed }}"
```

The `ansible.builtin.systemd` module runs the reload **first** — it is the opening
statement of the module's main path, well before it touches the enabled state. So the
reload re-read a unit directory that did not yet contain the wants-symlink the same task
was about to create. The running user manager never learned about the unit.

Observed on the host, three minutes after a `changed` deploy:

```
systemctl --user is-enabled host-health.service            -> enabled
systemctl --user list-dependencies graphical-session.target -> does not name it
```

Those two disagree because they read different things: `is-enabled` reads the filesystem,
`list-dependencies` reads the live manager's in-memory graph. Both answers are correct
about their own source, and the pair is the signature of this defect.

**Lingering makes it worse, not better.** `play-systemd-user-tweaks.yml` enables linger, so
`user@UID.service` survives a logout — the stale graph does not clear by logging out and
back in.

Fixed: the reload is now its own task **after** the enable on the desktop branch. The
server branch gets two — one before, because that task also `state: started` and a start
must act on a manager that has already read the new files, and one after, for the
`timers.target.wants` symlink that the enable itself creates. Both orderings are deliberate
and commented; neither is expressible through the module's `daemon_reload:` parameter,
which always reloads first and so can only ever serve the second case.

Five `register:` declarations (`hh_unit`, `hh_server_removed`, `hh_collect_service`,
`hh_collect_timer`, `hh_desktop_removed`) existed solely to feed those `daemon_reload`
expressions. Grepped across every `*.yml` and `*.j2` in the repo: one occurrence each, the
declaration itself. Removed.

## 08:35 · finding · T3.1 — the other eight, and only one of them is ours

- **[1]–[5] status document missing** — pure cascade from [12]. `login_report.py` is the
  only writer and it runs from `host-health.service`, which has never started. Check [9]
  does invoke `login_report`, but with `--no-handoff`, which by its own documented contract
  writes neither the handoff file nor the status document — so re-running acceptance
  without a login reproduces these five identically. They clear when [11] and [12] do.
- **[6] the ledger BROKEN sentinel** — **independent, and already fixed in code.** The
  sentinel is dated 2026-09-15 and names the ansible-core 2.19 `ansible_pos` removal
  (issue #46); `plugin_support.py` reads the new `Origin` shape now. The proof it is stale:
  check [7] PASSED and the ledger holds 15 rows with `MALFORMED -`, one for every play this
  deploy ran. By design the sentinel never clears itself. Needs one human command in the
  checkout — `python3 -m helpers.play_ledger.check_freshness --clear-broken` — and no code
  change. It is also the sole cause of check [9]'s play-freshness finding.
- **[15] the panel extension is not loaded** — expected, and the play says so itself: on
  Wayland a logout is the only way to load new extension code. Checks [13] and [14] passed,
  so everything that can be true before a shell restart is true. Plan 00112 reported its 9
  extensions ACTIVE in the same batch because those were on disk before the session started;
  this one was written three minutes before the gate ran.

**A gate-design note worth carrying.** Check [15] as written is *unsatisfiable in the same
session as a first deploy*. A clean deploy+acceptance sequence can therefore never be green
in one pass, which is a property of the check rather than of the host. Not changed here —
the assertion is the right one and the plan's footer does say to log out and re-run — but a
gate that cannot pass on a correct first run should say that in its own output rather than
leaving the operator to infer it.

## 08:35 · handoff · T5 — one command, then a logout, then re-run

Nothing further is container-doable on this plan. In the checkout, on the host:

```bash
python3 -m helpers.play_ledger.check_freshness --clear-broken
./playbooks/imports/optional/common/play-host-health-login-report.yml
```

The play re-run is what applies the reload fix. Then check it took:

```bash
systemctl --user list-dependencies graphical-session.target | grep host-health
```

If that names the unit, log out and back in and re-run `./acceptance.bash` — [1]–[5], [11],
[12] and [15] should all clear in one pass. If it still does not name the unit after the
play, the reload ordering was not the whole story and the `WantedBy=graphical-session.target`
choice itself needs re-examining; upstream guidance prefers `PartOf=` plus a DE-specific
target, but that wording could **not** be verified from this container and is not asserted.

## 08:53 · action · T5 — the handoff above was prose where it should have been a script

The operator's response to the entry above, near enough verbatim: *you mean in a
deploy.bash script?* Correct, and the entry was wrong to hand over a command list.

Half of it was already scripted and I did not check before writing the handoff:
`play-host-health-login-report.yml` is the first play leg of this plan's `deploy.bash`, so
"re-run the play" was only ever "run `./deploy.bash` again".

The genuinely unscripted half was the ledger sentinel clear. It is a leg now, and placed
**before** the plays rather than after. That ordering is the whole point: the ledger
records each play as it runs and records nothing while a BROKEN sentinel is live, so
clearing afterwards would leave this very deploy's four plays unrecorded — the hole closed
and immediately re-opened by the run that closed it.

Written as `env --chdir="${PLAN_REPO_ROOT}" python3 -m …`, because `python3 -m helpers…`
resolves the package from the cwd and `plan_deploy_leg` refuses to be called inside a
subshell — so the directory change has to belong to the command, not to the script around
it. `shellcheck -x` clean.

What remains genuinely human: the logout. On Wayland nothing else loads the panel's new
code, and the login is what fires the health report unit. `deploy.bash` already prints
that as its closing instruction.

## Check [11]: the probe and the gate disagreed, and the gate was wrong

The bracketed batch run answered check [11] without anybody pasting anything, which is
what the probes were for.

`triage.bash` ran after the deploy and its live-graph probe shows the unit plainly:
`graphical-session.target` lists `host-health.service` among its dependencies. The play's
own read-back assertion agreed — the deploy passed. Minutes later `acceptance.bash` check
[11] read the same host and reported that the target *does not* name the unit.

Two readings of one machine, taken minutes apart, contradicting each other. The earlier
worry was that the reload had silently failed; the evidence says the opposite — the live
manager is correct and the gate is misreading it.

The one difference between the two commands was `--no-pager`, which the triage probe
passed and the gate did not. That is now the same on both sides: two checks that are meant
to establish the same fact must not be two different commands.

Not asserting that was the bug, because the disagreement is not yet *explained* — parity
removes the only visible difference, it does not prove it was the cause. So check [11]
now also keeps what it actually read: on failure it writes the captured
`list-dependencies` output to the run directory under `untracked/` and reports its byte
count in the failure message. If the next run disagrees again, the evidence survives the
run instead of requiring a third round of speculation.

The remaining seven failures in that acceptance run are one root, not seven: the unit has
never started, so no status document exists, so every check that reads the document fails
too. The logout is still owed.

## 13:55 · correction · T3.1 — check [11] was not a `--no-pager` difference, it was SIGPIPE

The entry above narrowed check [11]'s contradiction to the one visible difference between
the two commands and made them match. That was the wrong cause. Parity was worth having,
but it did not fix anything, and the entry was right to say the disagreement was *not yet
explained* — it then stopped one step short of explaining it.

The gate read:

```bash
printf '%s' "$deps" | grep -q -F -e "$unit"
```

Under `set -o pipefail` that pipeline returns **141 when the string IS found**. `grep -q`
exits the instant it matches, `printf` is then killed by SIGPIPE writing into a closed
pipe, and `pipefail` reports the pipeline by its worst member. A match becomes a miss.

It only bites once the left-hand side exceeds the 64 KiB pipe buffer — below that `printf`
finishes writing before `grep` can close, so nothing is signalled. This check's
`list-dependencies` output is 330 KiB, so it inverted on **every** run; the other checks in
the same file are small and never did. That is the whole of why it read as intermittent and
machine-specific, and why the triage probe — which does not pipe — disagreed with it.

Three sites converted to a bash pattern match, which spawns no process and so cannot be
signalled:

```bash
elif [[ "${deps}" == *"${HEALTH_UNIT}"* ]]; then
```

The others were `[[ "${declared}" == *"${EXT_UUID}"* ]]` and `[[ "${verify_out}" == *"[ok]"* ]]`.

**The blocker this dissolves.** "Check [15] is unsatisfiable in the same session as a first
deploy — on Wayland only a logout loads new extension code" was reasoned from a failing
[15]. [15] was failing because it was one of the three inverted sites, not because the shell
had not loaded the extension. The Wayland fact is true in general; it was not what that
check was reporting, and the plan should not carry it as a standing gate-design defect.

The same pattern exists at roughly 56 other sites across the repo. All are latent — small
outputs — and none are touched here.

## 13:55 · evidence · T3.1/T4.5 — ACCEPTED, 19 of 19

`acceptance.bash` at `20260917-143702`:

```
coverage: 19 of 19 checks executed, 21 passed, 0 failed
ACCEPTED — every declared check ran and every assertion passed.
```

[11] enabled and named among `graphical-session.target`'s dependencies; [12] the unit ran at
a login and succeeded; [15] `State ACTIVE`. No failure-capture file was written, which is
its own evidence — the capture added above fires only on a failing read.

So the logout **is** discharged, not still owed: [12] records a real login and [15] a shell
that has loaded the panel. Nine findings are reported rather than rejected (two play-freshness,
the `CLEARED` marker, five failed units the report is correctly surfacing) — that is the
gate working, not a defect in this plan.

What remains genuinely human is now a short list, and the report prints it itself: the
rpm-ownership answer for T0.2, seeing the panel icon and the demoted lines, a notification
appearing on screen, the VM scenario run, the vault file's permissions, and T4.3's unwritten
runner.

## 14:20 · correction · the "roughly 56 other sites" figure above is wrong

It is **94**, across 38 files. I carried "roughly 56" forward from an earlier estimate and
repeated it as a fact without counting — which is the same defect this plan has now spent
two days on, in the sentence describing that defect.

Counted with the pattern below, excluding `untracked/`, the vendored daemon tree, and lines
that are comments (the first pass counted this file's own explanatory comment as a site,
and counted 95):

```bash
grep -rnE '(printf|echo)[^|]*\|[[:space:]]*grep[[:space:]]+(-[a-zA-Z]+[[:space:]]+)*-[a-zA-Z]*q' \
    --include='*.bash' --include='*.sh' .
```

`00109/acceptance.bash` is clean — its only match is the comment explaining the fix.

**Eight of the 94 are in ACTIVE plan scripts**, which matters because two of them are
scripts Plan 00130 still owes a host run of:

| script                                 | sites | left-hand side       |
| -------------------------------------- | ----- | -------------------- |
| `00055/acceptance.bash`                | 3     | PID list             |
| `00079/acceptance.bash`                | 2     | container-name list  |
| `00080/triage.bash`                    | 2     | container-name list  |
| `032/acceptance.bash`                  | 1     | directory listing    |

All four scripts do set `pipefail`, so the hazard is real rather than theoretical. All eight
left-hand sides are small — PID and container-name lists — so none can reach the 64 KiB pipe
buffer, and 00130's owed runs of `00079/acceptance.bash` and `00080/triage.bash` are not at
risk of the inverted verdict. Latent, not active. Still not touched here.
