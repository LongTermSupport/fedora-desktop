# Plan 00109 — Journal 26-09-14

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 09:30 · finding · T5.4 — three sub-items were shipped and still showed as open

Read while working out what of this plan is reachable from the container. All three verified in
the code before ticking anything — the plan said open, the tree said done:

- **"Decide the home"** was decided, and the decision is visible in the shape of the code: the
  background refresh extends `helpers/displaylink_recovery/` rather than becoming a sibling, so
  it reuses the existing udev rule and suspend service instead of inventing a trigger path.
  `Action.REFRESH_BACKGROUND` at `recovery.py:64`, dispatched at `run_recovery.py:411`, shipped
  in `9a79dd77`.
- **"Idempotence and loop-safety: the toggle writes the key it watches"** is handled at
  `recovery.py:114`: `needs_background_refresh` returns False once
  `attempted_background_refresh` is set, so the write cannot re-trigger the watcher, and False
  while `session_locked`, which is what keeps it off the `gnome-shell#9188` leak path the task
  above it warns about.
- **"Tests first, stdlib-only, mirroring the helper path"** — `tests/helpers/displaylink_recovery/test_recovery.py`
  carries three assertions on this action.

The fourth sub-item on that line, "deploy on HOST, verify", was bundled into the tests item and
is now its own unticked line. It is a different claim from the one two lines above it: *"the
shipped code runs on the host"* is not *"the toggle cures the fault"*, and the second still needs
the fault to actually appear. Splitting them stops a host deploy being read as confirmation of
the mutter#4767 conclusion Phase 5 rests on.

Ticking a box for work someone else did is only safe when the tick cites where the work is, so
each one names its file and line.

## 09:35 · decision · T1.1 — the ledger designed, and two things in the task turned out to be wrong

[DESIGN-play-ledger.md](../DESIGN-play-ledger.md), summarised as Decision 2 in PLAN.md.

Two corrections to the task's own framing, both found by checking rather than accepting:

**The hash's justification was wrong.** Task 1.1 says `play_sha256` matters "because commit
alone cannot tell you whether *this play* changed". Given two commits, `git log -- <play>`
answers that precisely. The hash's real job is the case where **the commit is a lie**: a run
from a dirty tree, where a hand-edited play is otherwise indistinguishable from the committed
one and would be reported fresh for ever. That is a narrower claim and a better one, and it
changes what Phase 2 is allowed to conclude — freshness is driven by git history over the play
path, with the hash as the dirty-tree guard, not by hash comparison.

**Fail-fast cannot be done the obvious way.** Task 1.2 requires "a ledger write failure must not
silently produce a blank ledger". Ansible **catches exceptions raised inside a callback and
carries on**, so raising in the plugin gives a green run with nothing written — the precise
failure the requirement forbids, arriving through the mechanism meant to prevent it. The design
records the failure instead of raising it: a `BROKEN` sentinel, and Phase 2 refuses to answer
freshness questions while it exists. An unfailable hook becomes a failable check.

Confirmed at source: `ansible.cfg` declares **no** `callback_plugins` path and enables only
`ansible.builtin.default`, so this is a new mechanism here rather than an extension of one. The
plugin is close to unbypassable because every playbook's shebang `cd`s to the repo root before
exec, so a direct `ansible-playbook <path>` picks it up too — but `ANSIBLE_CONFIG` defeats it,
and §3 says so rather than claiming completeness the design does not have.

Task 1.3 is answered as a reporting rule, not a backfill: a play with no record has never been
run here and silence is correct for it, so nothing is invented about the past. The only thing
written at creation is a `genesis` record, so the ledger's own age is knowable and its silences
are unambiguous.

§5 is a table of what Phase 2 may and may not conclude. It exists because the tempting sentence
— "this play is up to date" — is one the ledger cannot support: `play_sha256` covers the play
file, and a play that imports `tasks/` or reads `vars/` can change materially with its own bytes
untouched.

## 10:08 · action · T1.2 — the ledger's pure half, test-first

`helpers/play_ledger/ledger.py`, 24 tests. Paths, record construction, validation, one-line
serialisation, the genesis record, and the fold to latest-per-play. No I/O; the append, the
sentinel and the callback plugin that drive them are the rest of Task 1.2.

Three decisions in it are worth naming, because each is somewhere a reader would expect
leniency and the design cannot afford it:

- **`fold_latest` decides by `finished`, not by position in the file.** The file is appended to
  by concurrent runs, so its order is not run order. Reading the last matching line would give
  the wrong answer exactly when two plays ran at once.
- **A corrupt line raises; it is not skipped.** A half-written record means the ledger has a
  hole, and a fold that skips it answers from a history it knows is incomplete — the
  silently-wrong-check failure this whole plan exists to catch on the host.
- **An unknown schema raises rather than being read anyway.** `schema` earns its place only if a
  reader actually refuses a shape it does not understand.

`dirty` is validated as a real `bool`, not merely truthy. The string `"false"` is truthy, so a
stringly-typed caller would record every run as dirty and make `play_sha256`'s entire reason for
existing unreadable — and nothing downstream would look wrong.

Next in Task 1.2: `store.py` (append, `BROKEN` sentinel) and the callback plugin, plus the
`ansible.cfg` `callback_plugins` path and `callbacks_enabled` entry — neither exists today.

## 10:12 · action · T1.2 — the ledger's filesystem half

`helpers/play_ledger/store.py`, 17 tests. Creation, appends, and the `BROKEN` sentinel.

Everything here raises on failure. The one caller that cannot afford to raise is the callback
plugin, and its job is to catch and call `mark_broken` — that asymmetry is the whole shape of
the fail-fast answer, so it is stated in the module docstring rather than left to be inferred.

Four details that are load-bearing rather than incidental:

- **`ensure_ledger` uses `O_EXCL`**, so two concurrent first runs cannot both write a genesis
  record. An existing runs file is left alone: a second genesis would reset the ledger's
  recorded age and make every silence ambiguous again, which is the one thing genesis exists
  to prevent.
- **`append_record` serialises BEFORE opening the file.** A record that cannot be serialised
  leaves the file untouched instead of half-written — and a half-written line is exactly what
  `fold_latest` refuses to read past.
- **`append_record` raises if the ledger is absent** rather than creating it. Creating it there
  would silently lose the genesis record.
- **`mark_broken` does not require the ledger to exist**, because "could not create the ledger
  at all" is precisely a failure that has to be recordable. It keeps the **first** reason: the
  first failure explains the hole, later ones are usually consequences of it, and overwriting
  would lose the cause.

`clear_broken` exists and is never called automatically. Clearing a recorded hole is a
deliberate act, and a sentinel something else can clear on its own is not a gate.

Remaining for Task 1.2: the callback plugin itself, plus `ansible.cfg` gaining a
`callback_plugins` path and a `callbacks_enabled` entry — it has neither today, so this is a
new mechanism in this repo rather than an extension of one.

## 12:58 — the hole was in the code whose subject is holes

Task 3.1's executor. Writing it surfaced a defect in the classifier I committed an hour earlier,
and it is this plan's own cardinal defect, one source down.

`build_report` took a `ProbeOutcome` for `dkms` — `ok`, text, error — so a missing `dkms` became a
finding. The two `systemctl` scopes were passed as plain **text**. A `systemctl` that cannot run
therefore contributed an empty failed-unit list, which is byte-identical to a host with nothing
failing. The design document I wrote alongside it says, in §3, "`dkms` absent, `systemctl`
unavailable, output in a shape the parser cannot read — each becomes a finding rather than a skip".
It covered one of the three. I wrote the rule and then implemented a third of it, in the plan whose
entire subject is checks that cannot fail.

What caught it was building the executor and asking what it would pass in. Not review, not a test —
the tests were green and stayed green, because they only ever passed strings for those two
arguments. A test suite that only exercises the shapes the author had in mind confirms the author's
mind.

The container turned out to be the right proving ground rather than a limitation. It has no `dkms`
and no systemd bus, so a real `python3 -m helpers.host_health.probe` run here exercises all three
probe-failure paths at once. Before the fix that run would have reported one finding; it reports
three, and the two extra are exactly what the hole swallowed. It also showed up a second thing the
unit tests could not: `systemctl` answers an unreachable bus in two lines, so the finding arrived as
two lines and would have reached the notification as a finding nobody wrote. Collapsed now, with its
own test.

I also cut a seam I had added out of habit. `main` took a `stderr` parameter nothing wrote to, and a
test asserted it stayed empty — which it always would. That is a check that cannot fail, sitting in
a file arguing against them. The parameter is gone and the test now redirects the *real* stderr,
which a stray `print` would actually reach.

Left undone deliberately: the login unit. It is a `--user` service `After=graphical-session.target`
and the pattern is already in `play-container-watch.yml`, so it is not hard — but a unit whose
output nothing surfaces is not a deliverable, and Task 3.2 owns the surfacing. There is a real
decision in the way, recorded in the design doc rather than guessed at: the freshness check answers
UNTRUSTWORTHY when its `git fetch` fails, and offline-at-login is ordinary. "I could not look must
never read as nothing is wrong" and "a check that speaks every login gets muted" point in opposite
directions there, and neither wins on its own terms.

## 13:58 — the control experiment found the bug in the gate that was checking for bugs

Task 2.2's manifest extraction. The pin manifest was a heredoc inside
`scripts/check-pinned-versions.bash`, so this plan's installed-vs-pinned check could only have
copied it — and two checks asking different questions of populations that can differ is how you
get one of them silently narrower than the other.

It is `vars/version-pins.yml` now, and both read it. I did not trust myself to transcribe nine
rows: the extraction is verified by taking the heredoc straight out of `git show HEAD:` and
diffing it against the rows the real validator emits from the YAML. Identical. That is a cheap
check and it is the difference between "I moved it carefully" and "it is the same manifest".

The gate needed to exist at all for a reason worth writing down. Neither consumer runs in
`qa-all` — the review tool needs an authenticated `gh`, the installed-vs-pinned check needs a
real host — so a manifest row that had drifted away from the playbooks would surface only when
somebody happened to run a review tool. And the specific rot is quiet: a row naming a var that
was renamed reports the old pinned value for ever, which reads as "up to date".

Eight mutations against a tree mirror, each caught: a renamed var, a missing playbook, a typo'd
key that would silently demote an automated pin to a manual one, a manual pin with no note for a
human to act on, a GitHub URL where `owner/repo` belongs, a duplicated row, a `|` inside a field
that would shift every later field by one, and the whole thing emptied.

Two of those eight caught something I had not mutated. They exited non-zero and printed
**nothing at all**. `$?` inside `if ! cmd; then` is the status of the negation — always 0 — so
every arm of the `case` that was supposed to say *which* row was wrong missed, and the gate
failed mutely. I had written the two distinct messages and then made both unreachable. A gate
that fails without saying why is barely a gate, and I would not have found it by reading the
code, because reading it is what produced it.

Then one more control, through `qa-all` itself rather than the gate standalone: break the real
manifest, run the suite, confirm it fails with the right message, restore, confirm the file is
byte-identical again. That one is not paranoia — a gate that fails alone and passes inside the
suite is a thing that has actually happened in this repo, and `CLAUDE/QA.md` still carries the
note about two gates that were documented and unrun for months.

## 14:40 — the pin I would have guessed wrong, and why the manifest now refuses to let me

Task 2.2's remaining half: resolve what is actually installed, and prove the check fails against
the incident it was built for.

I nearly wrote a generic rpm resolver. The evidence stopped me. The journal's own incident entry
records `Installed displaylink-1.14.16-2` against a `displaylink_version` pin of `v6.3.0-1` —
those are not the same versioning scheme at all. The pin is the displaylink-rpm *release tag*; the
installed rpm's version tracks *evdi*. A plausible-looking `rpm -q displaylink` resolver would
have reported that pin as drifted on every login for ever. A check that cries wolf gets muted
exactly as thoroughly as one that never speaks, and I would have shipped it believing it was the
careful option.

So resolution is declared per pin, and a pin with **no** `installed:` block is rejected outright.
Not defaulted to untracked — defaulting would turn a typo into a decision nobody made, and
silently stop comparing something somebody meant to watch. `untracked` is available, but it needs
a `why:`, because "untracked" with no reason is an omission wearing a decision's clothes.

That leaves 1 of 9 pins actually tracked, which is an uncomfortable number and is the point: the
gate prints `1 with install state tracked, 8 declared untracked` on every qa-all run. Eight rows
say "not yet established — needs a host". The gap is a number that stares back rather than an
absence that reads like a clean result.

The gate itself is asserted in both directions against the strings this plan's journal recorded,
not against invented ones. `evdi/1.14.16` versus a `1.15.0` pin is a finding. And
`evdi/1.15.0-1.github_evdi, 7.2.4-200.fc44.x86_64, x86_64: installed` — the real post-fix line,
release suffix and all — is **clean**. That second half took as much care as the first: a
comparison that could not see past `-1.github_evdi` would have reported drift on a healthy host
in perpetuity, and I would only have found out on a host.

`installed_from_dkms` reuses `probe_results.parse_dkms` rather than re-parsing the same format,
so the two halves of this plan cannot come to disagree about what a `dkms status` line means.
Where several builds of a module are listed it takes the highest **numerically** — as strings
`1.14.16` sorts before `1.14.9`, which is the trap `compare.py` already exists to avoid.

Smoke-run here, where `dkms` does not exist: the one tracked pin reports "could not be checked —
dkms: command not found", the eight untracked ones say nothing at all, exit 1. Both halves of the
intended behaviour, from a container, without pretending the container is a host.

## 15:06 — the question was posed wrongly, which is why neither answer worked

The open decision from this morning's design note: what should an offline login say when the
freshness check's `git fetch` fails?

Two rules this plan keeps invoking point straight at each other. *"I could not look" must never
render as "nothing is wrong"* says report it. *A check that speaks on every login gets muted* says
do not — offline at login is ordinary, a train, a hotel, a laptop that woke before the wifi did.
I spent a while trying to decide which rule outranks the other, which was the wrong exercise.

Neither wins because the question was wrong. **"Can I reach the remote right now"** is a fact
about the network, and this surface does not report on networks. **"How long is it since I last
could"** is a fact about this host, and that is the one worth having: a freshness verdict computed
from refs fetched an hour ago is worth reading, and the same verdict from refs three weeks old is
not. Once posed that way there is nothing to trade off — the check stamps each successful fetch,
stays silent while the stamp is recent, and reports the gap once it is not.

Three states, not two, and the third is deliberate: never-fetched gets its own message. A host
that has never successfully reached the remote has a different problem from one that last reached
it a month ago, and a single message would describe neither.

Two traps I only saw by writing the cases. A corrupt stamp has to degrade to "never fetched" — a
finding — and not to "fetched just now", which would be a silent pass by way of a parse error. And
a stamp in the *future* has to be a finding too, or a clock that ran backwards buys unlimited
silence.

This **reversed a ticked behaviour.** `check_freshness` already returned UNTRUSTWORTHY on a failed
fetch, with a test asserting exactly that and a docstring arguing for it. I rewrote the test to
pin the new rule rather than deleting it, and its docstring now says it previously asserted the
opposite and why. A reversal that leaves no trace is how a plan comes to contradict its own
history — and this plan has spent all day finding that shape in other files.

`EXIT_UNTRUSTWORTHY` stays, because it is still right for a `BROKEN` ledger and for a ledgered
commit that cannot be resolved — states where no verdict is available at all. What changed is
that a failed fetch alone stopped being one of them.

## 15:48 — the surface exists now, and two of its rules are about not trusting itself

Tasks 3.1 and 3.2's code. `login_report` runs the three checks this plan built, merges their
findings into one report, and notifies only if there is something to say.

The merge is the load-bearing part and it is deliberately not a chain. Each check is called
independently behind its own guard, so a raising one becomes a finding that **names itself** — "the
play-freshness check could not run: …" — and cannot suppress the other two. Chained, a broken
ledger would have hidden a dead DKMS module, which is this plan's own subject one level up.
Host-health findings sort first: something broken on this machine now outranks something that has
merely drifted.

The second self-distrusting rule: the notification is not the only channel. A failing notifier —
no session bus, `notify-send` missing — still leaves every finding on stdout, keeps the exit
status, and reports its own failure as a line. Otherwise a broken notifier silently converts a
broken host into a healthy-looking one, which is the failure mode wearing a different hat.

Two things I caught while writing the unit rather than after deploying it. `WorkingDirectory` was
a hardcoded `%h/Projects/fedora-desktop` in the first draft — a guessed path, in a public repo,
for a checkout location I have no way to know. It is a `.j2` templated from `root_dir` now. And
the sibling `play-container-watch.yml` writes `/run/user/{{ uid | default(1000) }}`; copying that
would have been the easy thing, but a wrong default there does not fail — it points the probe at
another user's runtime directory and the enable step then reports "no user manager" on a host that
has one. This play resolves the uid with `getent` and asserts it.

`SuccessExitStatus=0 1` because exit 1 means "there are findings", which is a *successful* run of
the check. Without it a drifted host would also show up as a failed service, and two alarms for
one fact is how both come to be ignored.

What is left here is a host: the play has to run, the unit has to fire at the end of a login, a
real notification has to arrive, and a clean login has to be genuinely silent. None of that is
provable from a container, and the unit tests deliberately do not pretend otherwise — they pin the
decisions, not the delivery.

## 16:02 — "nothing installed" about a host nobody asked

Found this while writing the review dispatch, by listing what I was least sure of. The wiring in
`login_report` passed `probe.run_probe(["dkms", "status"]).text` straight into the pin check.
`ProbeOutcome.text` is `""` when the probe **failed** — and empty `dkms status` output is a
legitimate healthy state, a host with no DKMS modules. So the two collapse.

Measured before fixing it: on this container, where `dkms` does not exist, the pin check reported
`evdi_version (absent): pinned 1.15.0, nothing installed`. That is a confident claim about the
host, in the report a user is meant to act on, derived from a probe that never ran. It is not a
silent pass — it is a finding either way — but it is the same family, an unmeasured fact wearing
a measured one's clothes, and arguably worse: a silent pass is at least not a fabrication.

`dkms_text` is the seam now, and it raises when the probe failed, which `check_pins` turns into
"could not be checked". Empty output from a *successful* probe still passes through, because that
distinction is the whole point. The real end-to-end run here now says
`evdi_version: could not be checked — dkms: command not found`.

Worth noting what this cost to find: nothing but writing down the thing I was least sure about.
The unit tests were green, the end-to-end smoke run had already happened and looked plausible —
four findings, exit 1, no crash — and I had read the plausible one as correct because the shape
was right. The defect was in the one line that joined two tested components, which is where the
tests by construction were not looking.

## 16:18 — the handoff file, and the section this plan could not do without

Task 3.3's file half. The session that diagnosed the 2026-09-11 failure was manual archaeology —
reading journal logs, comparing `dkms status` against a pin set months earlier, working out which
kernel had actually booted. This writes that starting point out so it is not repeated from scratch.

The part worth arguing for is the second section. The prompt file splits *"this is wrong"* from
*"this was not looked at"*, and says of the second group, in terms, that these are **not** clean
results. A single flat list mixing a dead DKMS module with a probe that never ran reads like a
complete picture of a machine — and an agent handed that list would reason about the four things
in it and say nothing about the things nobody checked. That is the incident's shape, in a new
place, so the file names it.

The real run here proves the split works in the direction that matters: every one of the four
findings in this container lands under *could not be checked*, and the "what is wrong" section is
absent entirely. On this machine there is nothing known to be wrong — there is nothing known.

Two constraints held deliberately. The prompt asks for a diagnosis and a discussion and says not
to apply a fix or run a playbook, because strict IaC and this plan's Non-Goals both put that
decision with the operator. And `offer()` returns a **string** — it does not launch anything.
There is a test whose whole job is to make a future subprocess call there visible, because
"offered, never automatic" is a property that erodes by convenience.

`claude`, not `ccy`: diagnosing a broken host from inside a container cannot see the host. Mode
0600, because the file is a description of what is wrong with this machine.

What is not done is the *one-click* offer. A printed command is an offer, and a real one, but the
clickable version needs a surface that can receive a click — Phase 4's panel. Marked as such
rather than ticked.

## 16:29 — the health report was not deployed on exactly the hosts that needed it

Two defects, same shape, found in the two files written most recently. Both substitute a
reassuring value for a failure, which is this plan's subject wearing a different hat.

**The play.** `play-host-health-login-report.yml` guarded its enable step on
`systemctl --user is-system-running`, and enabled the unit only on `rc == 0`. systemd's own
source is `return streq(state, "running") ? EXIT_SUCCESS : EXIT_FAILURE` — so **`degraded`
exits 1 as well**, and a user manager that is running perfectly well but has one failed unit
was read as *no user manager at all*. The enable was then skipped. A host with a failed user
unit is precisely the host this plan exists to detect, and it was the one host where the
report did not get deployed. Measured from the source, not recalled: the container's own
system-scope probe prints `offline` at rc 1, which confirms non-zero for absence but says
nothing about `degraded` — the C is what settles it.

The fallback was worse than the skip. Its `success_msg` said the unit "is WantedBy
graphical-session.target and the next login picks it up". It does not. `WantedBy=` in
`[Install]` is inert until `enable` writes the `graphical-session.target.wants/` symlink, so
that sentence described a report which would never fire, in a play whose entire job is to
make it fire. And the assert carrying the sentence was `hh_user_systemd.rc is defined` —
which, behind `failed_when: false`, is guaranteed true. A check that cannot fail, printing a
claim that was false, about the mechanism it was reassuring the operator about.

The fix is subtraction: no probe, no `when:`, no reporting-assert, no `failed_when: false`.
The enable is its own probe — an unreachable user bus fails it, loudly, which is the answer
the probe was reaching for. That removes both bugs and the annotation. The play now states in
its header that it wants to be run from inside a graphical session, because the precondition
is real and was previously hidden behind a tolerance that did not work.

Worth naming why the probe existed: to tolerate the CCY container, where there is no user
systemd. Running a play there is prohibited anyway, so the tolerance was engineering for a
case that must never happen — and it bought a false negative on the case that does.

**The triage script.** `qa-all` rejected `triage.bash` for `|| echo` in two probes
(`ls … || echo "(none present)"`). The rule is right, and the shape was redundant on top of
being wrong: the `probe` function in that same file already prints every command's own rc,
which exists so nothing reads as absent when it was merely unasked. The `||` was fighting the
function written for the job. Both now go straight through `probe`, and the listing became
`ls -ld` — a tree's date says which kernel era left it behind, which is the actual triage
question. QA green after: 847 files, 237 pattern files.

One more thing measured rather than assumed. `shellcheck triage.bash` exits 1 on an SC1091
info about the sourced `_planlib.inc.bash`; `shellcheck -x` — the form `qa-all` uses, and the
form the file's `# shellcheck source=` directives are written for — exits 0 silently. Checked
by reading the exit codes, after a `cmd && echo ok` of mine swallowed the non-zero: in bash a
failing non-final command in an `&&` list is exempt from `errexit`, so the list fails, nothing
exits, and the absent `ok` is the only evidence. The same class as the two defects above.

## 17:10 — the review found eleven more, and the worst was a finding count of five for one problem

The `qa-reviewer` round on today's 00109 work. Sixteen findings plus nits, every one with
executed evidence rather than an opinion. Findings 1 and 2 were the play defects already fixed
at 16:29 — it reached them independently and its option 2 is what had been done. The rest were
new, and three of them were producing wrong statements to the user on every login.

**The freshness seam had no tests, and three defects.** `_freshness_findings` handed **one**
sink to both of `check_freshness`'s channels, then returned every captured line as a finding.
So: a `git fetch failed` diagnostic became a user-facing finding — reported as a fault in the
host, which §8 had explicitly decided against — and in the long-gap case appeared twice, once
as the raw exception and once as `offline_finding`'s sentence. Worse, the indented commit lines
under a stale play became peer findings, so one stale play with three commits counted as
**four problems** and the notification listed commits as though each were something to fix.
And its `UNTRUSTWORTHY` answer returned a hardcoded string pointing at "its stderr output
above" — there is no above, the only channel out of this module is stdout, so the BROKEN
sentinel's reason, whose entire job is to say *why*, was dropped and the reader sent looking
for something they were never shown.

Two sinks now. Diagnostics go to stderr, where diagnostics go; a finding's detail lines fold
into the finding; the untrustworthy answer carries the reason. The runner is injected, like
`dkms_text`'s, because this seam is only testable if it can be driven.

The reviewer's sharpest observation was not the defects but where they were: `dkms_text` — the
same shape, one file over — got its own test class this morning **because that lesson had
already been paid for**. This seam had not, and that is precisely where the defects were. The
tests were written first here, confirmed RED, then the fix; then four mutation variants with a
baseline, and each mutation fails only the test that pins it. A passing test proves nothing
until it has been shown to fail.

**A successful fetch reported as a failed one, and then as never having happened.**
`record_success` sat inside the fetch's own `try`, so a stamp that could not be written — full
disk, read-only state directory — was blamed on the fetch, and `offline_finding` then read the
absent stamp and reported that the remote had never been reached. Two confident statements
about a fetch that had just succeeded, both false, one of them on the user-facing channel. Its
own `try` now. The test injects the failure by putting a **directory** where the stamp file
belongs: a real `IsADirectoryError` through the real code path, because permissions prove
nothing when the tests run as root.

**The only network call on the login path had no timeout.** Every sibling had one. Unbounded,
the backstop was systemd's 90s `TimeoutStartSec`, which *kills* the unit — so a slow network
would have registered as a failed unit, the exact outcome `SuccessExitStatus=0 1` exists to
prevent, in the same file that explains why. Twenty seconds, plus `GIT_TERMINAL_PROMPT=0`: a
login path has no terminal for a credential prompt to appear on, so git would have waited for
an answer nobody could give and the timeout would have been the only thing ending the login.

**`_rpm_version`'s ABSENT branch could not be reached.** It matches "is not installed" in the
error text, and `_run` built that text from stderr alone — but a failing `rpm -q` reports on
**stdout**. So a missing package reported "could not be checked" rather than ABSENT, which
`installed_from_dkms`'s own docstring calls the incident's worst case. Neither resolver had a
single test. `_run` now includes stdout in the detail, and sets `LC_ALL=C` — the check matches
English text, so a translated message would have made it miss: working on a developer's machine
and failing on a user's. Six tests, and the mutation control confirms the ABSENT one fails the
moment the detail goes back to stderr-only.

**The `extra` argument, described in five places and used in none.** `probe.collect` and
`build_report` both carried it, two design documents and a ticked plan line asserted Phase 2's
findings merged through it, and the only production caller never passed it. Deleted rather than
wired up, because the merge belongs where it already happens: `login_report.collect` is the only
layer that guards each check separately, and findings passed into `build_report` would have to
be computed first, so a raising check would take the host report down instead of becoming a
finding that names itself. Merging where the guards are is what makes "merged, not chained" true
rather than aspirational. The two tests that pinned the dead parameter went with it — the
property they were protecting is pinned where it actually lives.

**The gate written to catch partial results had the partial-result hole.** `qa-version-pins.bash`
guarded `rows -eq 0` and nothing else, so a marker declaring nine pins with one row checked
exited 0 — with `9` and `1` printed on the same line, disagreeing, and nothing comparing them.
That is this repo's highest-recurrence defect, in the gate built to prevent it. The declared
count is now held against the rows that arrived, and the line says `COVERAGE: 9 of 9`.

Two more in the same file. Control 1 asserted only that the INVALID marker appeared, so a
breakage *upstream* of the validator — a YAML converter failing and handing it empty stdin —
made it "pass" having never tested emptiness; it now asserts the rejection mentions emptiness,
and a control proves it: with the converter broken, the gate says the control never tested
emptiness instead of quietly approving. And the comment crediting the in-file controls with
finding the `$?` bug was wrong — those controls call `pin_is_live` directly and never touch the
`case` dispatch below, so with the bug present they both passed and the gate exited mutely. The
tree-mirror fixture that did find it had been discarded, which `CLAUDE/QA.md` warns against, so
the dispatch is now a function with a control that drives it and asserts the **message**, not
just the status. Restoring the `$?` bug in a mirror: control 4 catches it. That fixture is in
the gate now rather than in a scratch directory.

The rest were accuracy: a design section still headed "Open decision" after §8 closed it, two
test counts wrong again, a play count this range had itself changed, and a module pointing at
the wrong design document. The test counts are simply gone — they had been wrong twice in one
day, which makes them a recurring drift source rather than information, and `qa-helper-tests.bash`
reports the real total on every run. Same reasoning as the launcher line numbers in Plan 00068:
a number that decays faster than anyone re-reads it is not documentation.

1131 helper tests, 847 files, QA green. A smoke run confirms the split holds end to end: four
findings on stdout, one per line, stderr empty.

## 17:35 — a second review, and the unit was calling a dead health surface a success

A second `qa-reviewer` ran against the working tree and reviewed the round-one fixes as well
(all three it checked, correct). It found three more blocking items, and two are the same
defect class one layer out from where it was just fixed.

**The unit declared a crash a success.** `SuccessExitStatus=0 1` existed so that a drifted
host would not also register as a broken service — two alarms for one fact is how both get
ignored. But **Python exits 1 for any uncaught exception**, and so does a missing module: a
wrong `WorkingDirectory`, a renamed helper, a typo in an import. So a permanently dead login
surface reported `Result=success`, delivered no findings, and left `is-failed` silent. The
unit's own comment and `docs/playbooks.md` both asserted the opposite — that only a crash was
a unit failure. The findings status is now **3**, and the unit lists only 3, leaving 1 and 2 as
real failures. 2 is excluded deliberately as well: `argparse` exits 2 on a usage error, so a
unit started with a bad flag must not read as findings either. There is a test whose whole job
is that `EXIT_FINDINGS` is not in `(1, 2)`, because the value is the property.

This is the plan's own incident rebuilt inside the code written to prevent it, and it is worth
being precise about why it slipped: the reasoning in the comment was *correct* — a drifted host
should not look broken — and the implementation of that correct reasoning created a second
state collapse. Getting the intent right is not the same as getting the encoding right.

**A fact alias the QA gate cannot see.** Three uses of bare `getent_passwd` instead of
`ansible_facts['getent_passwd']`, which `CLAUDE/AnsibleStyle.md` bans because the top-level
alias is removed in ansible-core 2.24 — at which point it becomes an undefined variable
mid-play, on the machine, after earlier tasks have changed state. The interesting half is why
no gate caught it: `qa-ansible.bash` matches `\bansible_(<FACT_NAMES>)\b`, and `getent`'s
injected fact has **no** `ansible_` prefix, so no extension of that list could ever match it.
Nine other sites in the repo use the correct form; this was the only one that did not. Fixed;
the gate's blind spot is recorded as a follow-up rather than claimed as fixed.

**And the one I had flagged as my own top suspicion was right.** `handoff._split` classifies
findings by substring-matching prose — `"could not run"` plus a literal `"could not be
checked"`. Executed over every finding string the three producers can emit: it covers seven
wordings and **misses six**, and all six land under *"What is wrong"*. In the file whose entire
thesis is that "this was not looked at" must not read as "this is broken", and under a heading
that tells the reader they are known-wrong. The sharpest instance is that the untrustworthy
message rewritten at 17:10 to carry its reason is one of the six — a fix generalised past its
own file, exactly as AgentNotes warns.

That one is **not fixed yet**, and deliberately so. The correct fix is that producers say
whether they managed to look, rather than the consumer guessing from wording, and that crosses
`probe_results`, `check_pins`, `check_freshness`, `fetch_clock`, `login_report` and `handoff`
plus their tests. Rushing a classification refactor into the module whose subject is
misclassification is the worst available outcome, so it goes in as its own change with its own
controls. `DESIGN-panel.md` is about to consume this same split for its `unavailable` state,
which is the argument for doing it properly and the reason it cannot be left.

1132 helper tests, 847 files, QA green.

## 18:20 — the classification moved into the data, and the guessing stopped

B2 from the second review, fixed properly rather than patched. The handoff file decided whether
a finding meant *"this is broken"* or *"this was not looked at"* by matching two substrings in
the prose. Measured before: seven of the messages the three checks emit matched, **six did
not**, and all six landed under "What is wrong" — a heading that tells the reader those things
are established faults, in the file whose entire purpose is keeping the two apart.

Every finding now carries `probe_results.Finding.checked`: the producing check's own answer to
"did I manage to look". The consumer reads the field. A new message cannot be misfiled by being
phrased differently, which is the property that matters, because the messages get reworded and
the classifier does not get revisited — the untrustworthy message rewritten an hour earlier was
itself one of the six.

`broken()` and `unchecked()` rather than the bare constructor, so the choice is a word at each
call site instead of a boolean nobody reads. Every construction site now states which it is:
a probe that could not run, output nothing could parse, a pin that could not be resolved, every
guard in `collect` (reaching one means the check did not finish, so `unchecked` by
construction), and the freshness check's untrustworthy answer.

The one that needed thought was the refs-are-old finding. It is a genuine finding — §8 settled
that — but it means "nothing was compared against upstream", not "this host has a fault", and
it arrived on stdout mixed in with the stale-play findings, where nothing could tell them apart.
`check_freshness.run` now takes an `unchecked` sink that defaults to `stdout`, so run by hand it
prints exactly as before, while the login surface passes a separate sink and gets the
classification from the check rather than from the wording. The alternative — the caller
inferring it from position or phrasing — is the defect being fixed, one layer along.

Two measurements. The reviewer's own: every finding the three producers can emit, rendered
through the real `render()`, counting how many unchecked ones land in the wrong section. It was
6 of 13; it is now **0 of 13**. And the control: restoring the substring split in a tree mirror
fails exactly the two new tests that pin the behaviour, while the third — the one that guards
the fixture's own wording, so the case cannot go vacuous if a message is reworded to contain
"could not run" — correctly still passes.

Also from that review: the unit had `PartOf=graphical-session.target` on a `Type=oneshot` that
has already exited, propagating nothing. Removed, with the reason written down, because it is
the idiom for a long-running session service and carrying it suggested this was one.

The end-to-end run in this container is the clearest statement of what the change buys: four
findings, every one of them under *"What could not be checked"*, and **no "What is wrong"
section at all**. On this machine nothing is known to be wrong, because nothing was checked —
and now the file says exactly that instead of listing four faults.

1141 helper tests, 847 files, QA green.

## 18:55 — the should-fix half, and a comment that was confidently wrong about systemd

The rest of review two. Seven items; one had already been fixed, and one of the remaining six
turned out to be a correction to something written earlier today.

**The timeout comment was wrong in the direction that mattered.** At 17:10 the fetch timeout
landed with the rationale that the backstop underneath it was systemd's 90s
`TimeoutStartSec`, which kills the unit. The reviewer flagged that as PLAUSIBLE-not-confirmed
because `Type=oneshot` may disable the start timeout entirely, and there are no systemd man
pages in this container. Settled from the source: `src/core/service.c` carries the comment
*"Oneshot services have disabled start timeout by default"* and sets
`timeout_start_usec = USEC_INFINITY` unless the unit names one. So there was **no** backstop at
all — a hung fetch would have held `graphical-session.target` in `activating` with nothing ever
ending it. The timeout is more necessary than the comment claimed, not less, and the unit now
sets `TimeoutStartSec=120` explicitly so nothing rests on either reading.

Worth recording as a pattern, not just a fact: the comment was not a guess about something
unknowable, it was a plausible recollection of a default, written into a file as though
measured. The reviewer could not settle it either — the difference is that it said so.

**A second copy of a subprocess, with the diagnostics removed.** `_declared_pins` ran the same
YAML conversion as `check_pins.main` but through a bare `subprocess.run`, so a failure reported
`returned non-zero exit status 1` and threw away the `ModuleNotFoundError` naming the missing
module. `probe.run_probe` carries the comment that says exactly why that is unacceptable —
*"'could not run: ' with nothing after it tells the user precisely nothing"* — one directory
away. Both callers now go through `check_pins.declared_pins`, and the failure names the module.

**PyYAML was an assumption about the host, on the pin axis.** The unit runs
`/usr/bin/python3`, and the pin check converts the manifest through it, while Ansible's own
PyYAML may live in a pipx venv that the system interpreter cannot import. Nothing in the repo
declared it; two comments asserted *"the PyYAML that Ansible itself depends on"*. Now
`python3-pyyaml` is declared in the play, per the missing-dependency rule. Combined with the
previous item the failure mode was a permanently dead pin check reporting an unreadable
message.

**`libnotify` was in the wrong place in the IaC graph.** Declared by this new opt-in play, while
three things send desktop notifications and two of them — the DisplayLink recovery and the
kernel-version manager — are reached from core paths and had been relying on the package being
present by luck. It belongs to `play-gnome-shell.yml`, which owns the desktop.

**The triage script said READ-ONLY and was not.** Running the login report does a `git fetch` in
the operator's checkout, stamps the fetch clock, and wrote `host-health-findings.md` — the one
artefact an operator might have been about to read. A `--no-handoff` flag now suppresses that
write, triage passes it, and the header states what it actually does instead of claiming
something tidier. Three tests drive the flag through `main` rather than asserting it exists.

**And the pin axis had no floor.** Every pin can be declared `untracked`, one at a time, each
with a good reason — and at the end of that road the login check compares nothing, finds
nothing, and looks exactly like a host whose every version matches. The gate would have printed
`0 with install state tracked` and exited 0: two sides of one shrinkable population again. Now
the gate fails at zero and `check` reports `compared 0 of 9 declared pins` as an `unchecked`
finding. Partial coverage stays silent, because that is a decision somebody wrote down; zero
coverage is a check that cannot fail.

Two of the existing untracked-pin tests had to change, and the reason is instructive: they
asserted `check([one untracked pin]) == []`, using a single-pin fixture that was *also* the
zero-coverage case. They meant to test per-pin silence. Each now keeps a tracked pin alongside,
so they test what they claim rather than that an empty comparison comes back empty — which is
a better test than the one the change displaced.

1148 helper tests, 847 files, QA green.

## 19:20 — two of my own claims corrected, and the sibling defect that was worse than reported

The nits from review two, which included two corrections to entries above. Both are recorded
here rather than edited in place: this journal is append-only, and a corrected claim is more
useful than a tidy one.

**"Measured from the source, not recalled" (16:29) overstated its provenance.** The systemd
`is-system-running` finding is correct — `streq(state, "running") ? EXIT_SUCCESS :
EXIT_FAILURE`, so `degraded` exits non-zero — but I obtained it by fetching
`src/systemctl/systemctl-is-system-running.c` from systemd's repository during the session, and
left no artefact behind. Nothing in this repo lets a reader check it. "Measured" was true of
what I did and misleading about what anyone could verify afterwards, which is the distinction
the phrase exists to protect. The same applies to today's `Type=oneshot` timeout finding, also
read from `src/core/service.c` over the network. Where a claim rests on upstream source,
either vendor the excerpt or name the file and let the reader fetch it — the second is what
both now do.

**"The real run here proves the split works" (16:18) was a narrower sweep than its wording.**
The container's four findings happen to be four of the seven phrasings the old substring rule
*did* match, so that run demonstrated the split for 4 of 13 reachable cases and was silent
about the six it got wrong. The full sweep at 18:20 — every message the three producers can
emit, 6 of 13 misfiled before and 0 of 13 after — is what actually establishes it. A run that
exercises the cases that already worked is not evidence about the ones that did not.

**A test in this plan's own suite could not fail.** `test_nothing_here_launches_anything`
asserted `isinstance(handoff.offer("/x"), str)`, and its docstring claimed it would catch a
subprocess call appearing there. It would not: adding one above the return keeps it green. It
now checks the module's own source for launcher call syntax, with a second case proving the
pattern list matches what it looks for, and a mirror control confirms that adding a real
`subprocess.run` to `offer()` fails it. The patterns are assembled from fragments because the
repo's security guard blocks the literals on sight — correctly, since it cannot tell a pattern
list from a call site.

**The `is defined` half of the uid assert was dead.** `getent`'s `fail_key` defaults to true
(checked with `ansible-doc`), so a missing key fails the task outright and the assert is never
reached. `fail_key: false` now hands the verdict to the assert, which makes its `fail_msg` the
report the operator actually gets instead of a generic module error.

**And the sibling instance was worse than the review reported.**
`play-container-watch.yml` still carried `XDG_RUNTIME_DIR: /run/user/{{ user_login_uid |
default(1000) }}` — the exact shape the new play's comment describes. Checking rather than
assuming: **`user_login_uid` is assigned nowhere in this repo.** So that was not a fallback for
a rare case, it was a hardcoded 1000 on every single run, wearing a variable's clothes. Nine
other sites use the `getent` form.

The same file also had both defects fixed in the new play this morning, and one more the new
play never had: an `is-system-running` guard skipping the enable, a `failed_when: false`
swallowing the enable's own failure, and a `debug` task announcing *"will be enabled on next
user session start"* — a **prohibited skip-and-warn** (CLAUDE.md, Fail Fast) carrying the same
false claim about `WantedBy` that was disproved eight hours earlier. All four removed; the play
resolves the uid, asserts it, and enables unconditionally so an unreachable user bus fails
loudly.

That is a scope step beyond Plan 00109 and worth being explicit about: it is another plan's
play. It was taken because the finding is a hard-rule violation plus a claim this session had
already proved false, and leaving a known-inert activation path in place because of plan
ownership would be the wrong call. `CLAUDE/Plan/00062-disk-reclaim-tui/podman-store-repair.yml`
carries the same `default(1000)` shape and is left alone — it is a plan-local artefact, not a
deployed play, and is recorded here rather than changed silently.

1149 helper tests, 847 files, QA green. `PLAN.md` back to 24,114 bytes, with Phase 5's
reasoning extracted into the research document and its task state left where it belongs.

## 19:45 — closing the gate that could not have caught it

The last item from review two, and the most useful one: `qa-ansible.bash` was structurally
blind to the defect it had just let through, so fixing the two instances without fixing the gate
would have guaranteed a third.

**Why no extension of the existing list could have worked.** Check 5 matches
`\bansible_(<FACT_NAMES>)\b`. `getent`'s facts are injected with **no** `ansible_` prefix —
ansible-core's `config/base.yml` says these "will have the exact names that are returned by the
module" — so the anchor itself was the blind spot, not the list behind it. Adding
`getent_passwd` to `FACT_NAMES` would have produced `ansible_getent_passwd`, which nothing
writes. That is a different and more interesting kind of gap than a missing entry: the rule was
looking in a place the defect could never appear.

Two rules added. The first matches the un-prefixed injected names, with three lookbehinds that
exempt the **correct** form — `ansible_facts['getent_passwd']` necessarily contains the same
string, so without them the rule would flag every one of the nine sites that already got it
right.

The second is the one that would actually have caught the instance found today:
`/run/user/{{ … }}` without a resolved uid in it. That is the shape of the defect rather than
one spelling of it, and it catches `user_login_uid | default(1000)` — which no name-based list
ever could, because the variable was invented locally and assigned nowhere.

Controls, because a gate is the highest-value place to be wrong: the gate passes on the clean
tree, then each defect was reintroduced separately into a real play and each fired with its own
distinct message, and the tree was restored byte-identically afterwards and the gate confirmed
green again. Two rules, two messages, two controls — a single shared message would not have
told the two apart, which matters here because the remedies differ.

Left deliberately: the `check_freshness` stamp-failure message stays a stderr diagnostic rather
than a finding. The review called that defensible and it is — the next run's gap finding is the
real surfacing — but it is recorded rather than forgotten. Likewise
`login_report.main` mixing findings and the handoff offer on one stdout stream, which stays
harmless while `DESIGN-panel.md` has the panel calling `collect()` directly rather than parsing
text. And `CLAUDE/Plan/00062`'s `default(1000)`, a plan-local artefact rather than a deployed
play — the new gate does not scan plan folders, which is correct for its search scope and worth
knowing.

1149 helper tests, 847 files, QA green. Both reviews are now fully worked through.

## 20:05 — "are you testing server as well as desktop?" — no, and it found two things

A user question, and the honest answer is no: nothing in today's work was exercised against a
`provisioning_profile=server` host, and nothing can be from this container. What follows is what
the question turned up, which is more useful than the answer.

**A precondition I had written down was wrong.** The login-report play's header said to run it
"from inside a graphical session", on the reasoning that enabling a `--user` unit needs the
user's manager. Checking instead of reasoning: `play-systemd-user-tweaks.yml` is import 8 of
`playbook-main.yml`, `scope: general` — so it runs on **server and desktop alike** — and it
enables linger and then explicitly starts `user@UID.service` through the `systemd` module, which
blocks until the unit signals READY=1. Its own comment says this exists precisely to close the
`enable-linger` async race. So the user manager is a deterministic guarantee on any host
provisioned by main, with no session involved. My stated precondition was both stricter and
less accurate than the truth.

That matters beyond the comment: `play-container-watch.yml` is **`scope: general`**, so the
unconditional enable I gave it this evening runs during server provisioning too. It is correct
there for the same reason — linger, not a session — and the honest caveat is the ordering: run
container-watch on a fresh host *before* main and the enable now fails loudly instead of
skipping silently. That is the intended fail-fast behaviour, but the operator sees Ansible's raw
error rather than "run playbook-main first", so the diagnosis is on them.

**And the real gap: a server gets no drift detection at all.** The login-report play is
`scope: gnome`, which ends the play on a server. That is right for the *delivery* — `notify-send`
needs a session bus and `graphical-session.target` never activates on a server — but it is not
right for the *checks*. The ledger, play-freshness and installed-vs-pinned axes have nothing
desktop about them, and a server is where drift goes unnoticed longest, because nobody logs into
it to see a notification in the first place. Recorded as an open Task 3.2 item needing a second
delivery route (a timer plus the journal, or a login-shell message) rather than a scope change,
which would deploy a unit that can never fire.

Worth naming the shape: `scope: gnome` was the correct answer to "where can this be delivered"
and was silently taken as the answer to "where is this needed". One question from outside the
work found it; nothing in the gates would have, because a play that correctly ends early on a
profile is indistinguishable from a play that has no business there.

What IS profile-independent and covered: the Python helpers branch on nothing to do with
profile and are unit-tested; `qa-ansible.bash` checks scope and guard declarations repo-wide for
both; `--syntax-check` passes on all three changed plays. What is not covered is any actual run,
on either profile — that is the standing HOST list.

## The status document, and a test of mine that could not fail

The server gap changed the build order rather than adding a task. The right shape for a server is
not a check at SSH login — a `git fetch` on every login adds latency to every login and can hang.
It is: check on a schedule, write down the verdict, have the login shell print what was written.
Which is exactly the aggregate document `DESIGN-panel.md` had already specified for the GNOME
panel. One producer, two deliveries, and it has to exist before either of them. So
`helpers/host_health/status_document.py` came first, ahead of the Phase 4 scaffold.

Three states in the data — `ok`, `findings`, `unavailable` — because a neutral icon over an empty
menu is what a healthy host looks like *and* what a missing file, an unparseable one and a crashed
producer look like. Collapsing those is this plan's own incident rebuilt one layer up, in the UI.
`unavailable` is read from `Finding.checked`, never from the wording. A section with both kinds
reports `findings` but keeps the unchecked list, or the panel shows a partial picture as a
complete one. An absent document reads as ignorance, not health: `container-watch` falls back to
an empty findings list and is right to, because its subject is live processes, but a dead DKMS
module for the running kernel stays true, so absence here means "not checked".

Then the controls, which is where this got interesting. Six mutants against the unmutated
baseline. Five died:

| Mutation                                          | Killed by                                   |
| ------------------------------------------------- | ------------------------------------------- |
| `unchecked` reads as `ok`                         | 5 tests                                     |
| a missing file returns an empty document          | `test_a_missing_file_is_unavailable_not_clean` |
| a raising producer's section becomes `[]`          | 3 tests                                     |
| the schema check dropped                          | the future-schema test                      |
| **`write_atomic` writes straight to the destination** | **nothing. It passed.**                 |

All four write tests passed with the atomicity removed — including the one I had named
`test_a_reader_never_sees_a_half_written_file`, which wrote a document and read it back. Contents
match, directory created, second write wins, no stray temp file: every one of those is equally
true of `open(path, "w")`. The suite written to pin *"a check that cannot fail is worse than no
check"* contained one, and the mechanical gates were green over it the whole time — as they were
over the six misfiled findings yesterday, and over the incident itself.

The two implementations only part company when the write **does not finish**. `json.dump` with an
indent serialises incrementally, so a value it cannot serialise emits the opening lines and then
raises: straight-to-destination leaves half a document where a whole one was, and the guard's own
`unlink` then deletes the destination outright. The test now writes a good document, forces a
failing write over it, and asserts the good one is still readable. It fails against the mutant and
passes against the real thing, which is the only evidence that it tests anything.

The honest correction is the smaller half: the old test is renamed to `test_what_is_written_reads_back`,
which is what it always did. A test named for a property it does not check is worse than no test,
because the name is what gets read during review.

1171 helper tests green, `qa-all.bash` green, and the atomicity claim now has something behind it.

## Wiring the producer, and a green tick I could not justify

`status_document.py` had no caller, so it was a designed module and nothing else. Wiring
it turned out to be the interesting half.

The document needs **sections**; `login_report.collect` flattened three checks into one
list. The panel's registry matches a section by id (`DESIGN-panel.md` §5) and renders
`unavailable` for an id it cannot find, so flattening would have made every section
permanently unavailable. `collect_sections` now returns the section-keyed dict and
`collect` is the flatten *of that* — derived, not collected again, so the notification and
the document cannot disagree about which checks ran.

That also removed a duplicate. `login_report.collect` and `status_document.collect` were
two implementations of merged-not-chained, the rule this plan exists to enforce; two copies
is two chances to fix only one of them. There is now one, and the section ids are the thing
its failure message quotes back to the user, which makes them interface rather than labels.

The write goes in `publish()` — a named function, because `main` has **no tests at all**
and inline there it would have inherited that. Unconditional, unlike the handoff file: a
document that only appears when something is wrong makes a healthy host look exactly like
a host nothing has ever checked. The handoff is correctly conditional, because a healthy
host has nothing to hand to Claude Code.

Five mutants, five dead: publishing only when dirty, `collect` reordering, empty sections
dropped, `state_dir` forgetting the app directory, the guard dropping the check's name.

**Then the smoke run, which is where `main` finally got exercised.** Against a temporary
`XDG_STATE_HOME`: exit 3, the document at `<state>/fedora-desktop/host-status.json`, the
handoff beside the ledger, four findings — and this:

```
post-boot-health:    unavailable  (3 unchecked: no dkms, no systemd, no user bus)
play-freshness:      ok
installed-vs-pinned: unavailable  (1 unchecked: evdi_version needs dkms)
```

`play-freshness: ok` on a host whose state directory did not exist until that command ran.
A green tick, from a check that had nothing to look at.

I nearly changed `check_freshness` for it, and was wrong to want to. That check answers
*"has any ledgered play drifted since it ran?"*, and with no ledgered plays the answer
really is "none has". `EXIT_OK` there is tested twice with its reasoning written down, an
empty ledger has no holes in it — which is what `EXIT_UNTRUSTWORTHY` is actually for — and
the check has other callers whose contract that exit code is. Reversing a documented
decision because it looked wrong through a new consumer would have been the wrong repair
at the wrong layer.

The gap is a **check that does not exist**: nothing asserts the ledger has any content.
And the login case is narrower than it looks, which is what makes it worth having — the
unit is deployed by a play, and `run.bash` ledgers every play, so an empty ledger *at
login* cannot mean "new host". It means the ledger was lost. Recorded under Task 4.2 as
its own section rather than as a reinterpretation of this one.

Worth naming the pattern, because it is the third time today: the defect was not in the
new code or the old code. It was in a value that had one meaning until a second consumer
gave it two. `EXIT_OK` meant "say nothing" and now also means "show green".

## The server route, and a test that was wrong about its own bound

`login_message.py` — the second consumer, the one a server gets. The producer writes the
document on a schedule; this prints what it left. Nothing runs a `git fetch` at login.

Two rules that pull against each other, and both have to hold. *Silent when clean*,
because a message on every login gets ignored and then it is not a report. And *silence
is a claim*: a clean document nobody has updated for a month describes the host as it was
a month ago. So the document's own age is a finding past `STALE_AFTER_DAYS` — the shape
§8 already settled for the fetch clock, arrived at again from the other end.

`main` always exits 0, which is a contract rather than sloppiness. A non-zero status from
a snippet sourced by a profile can trip `set -e` in the surrounding shell; a health
reporter that costs the user the login to the host it reports on is worse than none.

Six mutants, six dead: stale-is-silent, unknown-age-reads-as-fresh, unchecked-dropped,
the two groups run together, the type guards removed, and raising on a malformed
document.

One test failed on the first run and **the test was wrong, not the code**: I asserted
`13 days` against a 14-day bound, so silence was the correct answer. Worth writing down
because the instinct on a red test is to look at the code, and a test that encodes a
duration inside the very bound it means to exceed is a test that never tested the branch
it named. Fixed by choosing 30 days; the mutant confirms the branch is now reachable.

Rendered against the real document from the earlier smoke run, which is the only evidence
that the two halves fit:

```
fedora-desktop: this machine needs attention
  Not checked — these are NOT clean results, nothing is known about them:
  - the dkms probe could not run: dkms: command not found (dkms status)
  ...
```

and with no document at all, `no host status has been recorded yet, so nothing is known
about this host` — not silence, which is the entire point.

Still owed for the route: a `--user` timer (nothing triggers the producer on a server
without `graphical-session.target`), a profile snippet, and a play to deploy both.

## PLAN.md, third trim, and the actual fix

The size gate fired on three separate edits today. Trimming the edit in front of me each
time was treating the symptom. The real cause is that Phases 0–2 are **complete** and were
still carrying their full reasoning in the live task tree, all of it already linked to the
design doc that owns it.

Compressed to state plus link: 25,940 → 23,131 bytes. A completed phase needs its task
state and its open HOST items; the argument for why it was built that way belongs where
the argument lives. Also dropped three `file.py:NN` references while passing — line
numbers in a plan go stale within a day, and this repo's own note says remove rather
than reset.
