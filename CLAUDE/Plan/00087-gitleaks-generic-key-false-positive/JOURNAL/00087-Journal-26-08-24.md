# Plan 00087 — Journal 26-08-24

> **Append-only activity log** for plan 00087. One file per day
> (`00087-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 16:18 · action · — — plan scaffolded

Plan 00087 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:19 · finding · T1.1 — reproduced with the exact CI gitleaks version, not a guess

Noticed this failure repeatedly while merging PR #35 and opening PR #36 this session —
`mergeStateStatus: UNSTABLE` on both, each time traced back to the same unrelated
`gitleaks` finding. Rather than keep noting it as "pre-existing, not my problem" a third
time, downloaded the OSS gitleaks binary (no system install — extracted a tarball to
scratch, ran it directly) at 8.21.2 first (what happened to be the latest release I
reached for) — scanning the flagged file found NOTHING, which would have been a wrong
conclusion if trusted. Checked `.github/workflows/qa.yml` and found CI pins `VER=8.30.1`
specifically; downloaded that exact version and re-scanned — reproduced immediately:
`Medium/byteiota` in `CLAUDE/Plan/Completed/00070-.../research/scan-alt-engines.md:222`,
`generic-api-key`, entropy 3.506891. The gitleaks default ruleset changed between 8.21.2
and 8.30.1 to catch this shape; nothing in the repo regressed.

## 16:22 · decision · T1.2 — rephrase, don't allowlist

`.gitleaks.toml` already has one scoped allowlist entry (containerwatch test fixtures).
Could have added a second, matching this line, but chose to rephrase the prose instead
(`Medium/byteiota` → `Medium (Byteiota)`) — this repo's own anti-pattern catalogue names
"an allowlist that grows" as a smell, and a citation to two blog names has no reason to
be a single slash-joined token in the first place. Re-ran gitleaks 8.30.1 against just
that file: clean. Then git-archived HEAD-with-this-edit and scanned the exported tree
(matching a real `actions/checkout` clean state, not my working tree, which has
gitignored `.claude/hooks-daemon` and `untracked/` content that CI never sees and that
produced 20 misleading local-only findings on the first unscoped attempt): 0 leaks.
