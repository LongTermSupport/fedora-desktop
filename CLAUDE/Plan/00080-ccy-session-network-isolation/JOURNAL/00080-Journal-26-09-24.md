# Plan 00080 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00080. One file per day
> (`00080-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.
>
> This day-file's preamble was written without the template's "plan scaffolded" seed
> entry: the plan was created on 26-08-20, and the seed would have said otherwise.

## 07:26 · finding · T1.3   — run 4 (--reachability): F29-F38; P7 and P11 defective; 2.1 done, 2.2 awaits owner

The host batch `_meta-deploy/20260924-080001` ran `triage.bash --reachability`. This is run 4, and the first run of the active probes (P6 to P12). It reached `END OF REPORT`, and its cleanup asserted that no probe container or network remained. Everything here is anonymised. The raw log stays under `untracked/plan-runs/`. Per-probe evidence, with the limits of each finding, is in `subagent-reports/260924-triage-findings-opus-5.5.md`.

**Findings (F29 to F38):**

- **Confirmed.** H1 is confirmed on the host: P6 `REACHED` (F32). Everything on `podman` is a CCY session: 5 sessions, 5 projects and 2 GitHub identities (F29). For the third snapshot running, nothing a neighbour can reach is listening. The one listener is a database on loopback (F30). `network connect` works from a user-created bridge (F34). A fresh network has egress and the host alias (F35), and it is DNS-enabled with no options (F36). The 3 networks with no members hold no bridge interface (F38).
- **Code reading (F31).** `save_network_preference` is called only from the `--connect` path, and the preference is applied as a launch-time `--network`. So P13's count of 4 projects measures projects that have used `--connect` at least once. What the bridge buys is the first, mid-session attach. F28's "Option 4 would break real use" is narrowed accordingly.

**Probe defects** (recorded as defects, not read as findings):

- **P7 (F33)** is confounded. The P6 listener is a one-shot BusyBox `nc -l`, and P6 used up its one connection. So the cross-network silence cannot be read as isolation. It is also against expectation for netavark 1.17 (F24).
- **P11 (F37)** measured nothing. The kill container was started `-d` without `--rm`. `-d` means the client had already exited, so `pkill` matched nothing (rc=1), and "running" is just `sleep 300`. The only valid fact is that `network rm` refuses (rc=2) while a container is attached.
- **P12**'s config paths were wrong (rc=2 is not absence).
- **P10** dropped the `servers=` field.

These are not fixed in `triage.bash`. They matter only if Task 2.2 creates networks, so they became a precondition of Task 3.1.

**Tasks.** Task 1.3 is ✅. Task 2.1 is ✅: `THREAT-MODEL.md` concludes "little in practice today, and one bind flag away from a cross-project, cross-identity exposure". Task 2.2 is prepared in `DECISIONS.md` and **awaits the owner**. The options are 1 (keep and document), 4 or 4b (pasta; 4b fails `--connect` fast and saves the preference so a relaunch joins), 2 (per session) and 5 (per project), each with its evidence and reversal conditions. Success criterion 1 is not met, because H4 is open and H5 is partial.

## 07:26 · handoff · T2.2   — owner decision next

Entry point for the next session: **Task 2.2 is the owner's decision.** Read `DECISIONS.md`. The deciding question is how often a *running* session is attached to a project network, as opposed to launching into it. Once decided, record the decision as D3 in `PLAN.md`. If the choice is Option 2 or 5, fix P7, P10, P11 and P12 first (the "Probe defects" list in the findings report) and re-run `--reachability` before Task 3.1. If the choice is Option 1, the untested assumption is a P4 run while a dev server is up.
