# Plan 00080 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00080. One file per day
> (`00080-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.
>
> This day-file's preamble was written without the template's "plan scaffolded" seed
> entry: the plan was created on 26-08-20, and the seed would have said otherwise.

## 19:12 · finding · T1.3   — run 3 (passive): F24-F28; P13 answers Task 2.2 input (b)

The host batch `_meta-deploy/20260923-194037` ran `triage.bash` in passive mode, P1 to P5 and P13. This is run 3. It reached `END OF PASSIVE REPORT`. The active probes (`--reachability`, P6 to P12) were not run. Sessions, projects and networks are anonymised; the raw report stays under `untracked/plan-runs/`.

- **F24 (P1): below the isolation change.** Podman is 5.8.7, netavark 1.17.2 and aardvark-dns 1.17.1, and the rootless network command is `pasta`. That is below the netavark 2.0 / Podman 6.0 line. On this host, a per-session bridge network is **not** strictly isolated by default, so Option 2 would need an explicit `--opt isolate=`. This answers the "third hypothesis" the research raised.
- **F25 (P2): the default network as configured.** `podman` is one `bridge` on `podman0`, a single `/16` subnet, with `dns_enabled: false`, `internal: false` and no isolate option. This confirms H2 on the host: any cross-session reach is by IP only. Its six members are exactly the six CCY sessions.
- **F26 (P3/P4): the whole fleet is labelled, and nothing listens beyond loopback.** 6 of 6 sessions carry `ccy=true`, so the partial rollout F20 recorded is over. Five are on one GitHub account (account A) and one is on another (account B). Five sessions have no listening sockets. One, `container-B`, listens on port 5432 on IPv4 and IPv6 loopback only. Loopback reaches only that session itself, so by P4's own legend it is harmless. F22's conclusion holds for this snapshot: nothing is bound for a bridge neighbour to reach.
- **F27 (P5): one project network is busy.** There are 5 networks. One project network holds 9 members (`project B`'s compose stack, which this repo does not own), `podman` holds 6, and the other three hold none.
- **F28 (P13): `--connect` is live.** 3 projects have a recorded network preference. None of them names the default bridge and all 3 name a non-default network (two distinct networks between them). So projects do join project networks, and **Option 4 (delete the override, revert to pasta) would break real use**. Any option that removes the shared bridge must preserve `--connect`. This is input (b) to Task 2.2, which was the input it lacked.

Where this leaves the decision: input (a) says nothing is exposed now (F26), and input (b) says the bridge is buying `--connect` for three projects (F28). The remaining work on H1 and H4, and on whether the fix would actually work, is the active run: `triage.bash --reachability` on the HOST. Task 2.1 (threat model) can be written now from F22 to F28. Task 2.2 is the owner's decision.
