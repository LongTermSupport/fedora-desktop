# Plan 00113 — Journal 26-09-14

> **Append-only activity log** for plan 00113. One file per day
> (`00113-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:03 · action · — — plan scaffolded

Plan 00113 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:10 · action · — — PLAN.md authored from 00068's specification, by reference not by paraphrase

Plan 00068 stopped at a specification on purpose; this plan is its Task 4.1. The authoring rule
was that nothing here restates 00068 — the Overview links `DECISIONS.md`, `reports/ci-flow.md`
and `reports/ci-tool-surface.md`, and states that **where the two disagree, 00068 wins** and the
disagreement is a defect in this plan. A plan that paraphrases its own specification is a second
copy, and the second copy is the one that drifts.

The dedupe scout checked 59 live plans before the folder was created and found no overlap: 00089
is a subset (token-first `ssh-handling.bash`), and 00080, 00092, 00093 and 00072 are different
subsystems. They are recorded as **do not duplicate** rather than as dependencies.

Phase 0 exists because three of this plan's inputs are derivations, not measurements: the
prompt-site count (`ci-flow.md` says so of itself), the security-carrying flags (ccy auto-updates
the CLI daily, so the binary is not the one E9 measured), and the host facts, last probed before
Plan 00089 changed the launcher.

## 11:50 · finding · P0 — the review of 00068 landed four defects on this plan, and Phase 0 was the shallow one

`CLAUDE/Plan/00068-ccy-ci-runner-variant/subagent-reports/260914-review-00068-opus-5.md`,
verdict BLOCK. Four of its findings are about this file, and the first is the one that matters.

**Task 0.1 was carrying six obligations it cannot discharge.** It said running 00068's
`triage.bash` covered §9's B1–B4 and C1/C2. `probe-network.bash:224` states in its own output
that **C1 and C2 are deliberately not probed** — both need a listener on the host, and a probe
that opens host sockets stops being read-only. B1–B4 are in no probe at all: they need a real
container and a real session on the owner's machine, burning quota. Naming a method that the
source says will not answer the question is worse than naming none, because it closes the item.
Task 0.1 now claims only E1/E6/C3; Tasks 0.4 and 0.5 carry the rest, each stating the method
that would actually settle it. Task 0.1's "never been run" was also wrong — run
`20260731-225344` is authoritative; what is missing is a run *since* 00089.

**Plan 00089 was described as landed and settled.** Its code landed. Its proof did not: 00089 is
In Progress and its success criterion — `--no-ssh` with `GH_TOKEN` pre-exported, live on HOST —
is unticked. This plan's credential story rests on it, so Task 0.6 discharges it in Phase 0
rather than assuming it.

**Compose and networking were dropped entirely.** `DECISIONS.md:279-299` records it as an owner
requirement in the owner's words, with a keep/drop table and an explicit "pick one deliberately"
on resolving the network before `podman run`. This plan had no task, no criterion and no
Non-Goal covering it — a silent drop at a hand-off, which is the failure mode a hand-off document
is least able to show. Phase 2b now covers it, plus the untested no-SSH-key entrypoint path.

**Task 2.2's assertion 2 changed shape** with the source document: it is a diff against a
declared expected set, not an absence check, because a renamed write primitive is absent-by-name
and would pass green. That makes Task 0.3 a prerequisite — it must now capture the tool
vocabulary *by name*, since the 2026-08-10 measurement recorded counts only.

## 11:55 · handoff · — — ready to start at Phase 0, on HOST

Nothing in this plan has been executed; every task is unticked and that is accurate. It is not
startable from a CCY container: Tasks 0.1 and 0.4–0.6 are all HOST work, and 0.4 needs real
quota on the owner's machine.

Start at Task 0.1 and work Phase 0 in order. Nothing in Phase 1 should begin before 0.2 has
counted the prompt sites — six versus fourteen changes that phase's shape — and nothing in
Phase 2 before 0.3 has captured the tool names, which assertion 2 cannot be written without.
