# Plan 00049 — Journal 2026-08-24

## Status line relocated here (Plan 00082 index/plan-QA cleanup)

Not work on this plan. The `status-enum-and-date` check (hooks daemon 3.54.0) rejected this
plan's `**Status**:` header because the token was decorated and carried a paragraph of
prose after it. The header is now a bare token plus a short parenthetical; **the prose it
carried is preserved verbatim below**, since PLAN.md is already past the hard size limit
and cannot absorb it.

Original header, exactly as it stood:

> **Status**: 🔄 In Progress (research complete; Batches 1 (Phases 3+4), 2a (Phase 5), 2b
> (QA-07), 3 (Phase 6 — full CCY hardening), 4 (Phase 7 — runtime bug fixes), 5+6 (Phase 8
> — idempotency + native modules), 7 (Phase 9 — documentation realignment), 8 (Phase 10
> research + pip-block fix), 9 (Phase 10 implementation + CI + both decision gates) landed
> on branch `fable-audit-1`. **Both decision gates resolved: Gate 1 → skip history purge;
> Gate 2 → CI added.** Remaining: QA-09 pytest stage (IaC gap), DOC-18 Dockerfile comment
> (cost-deferred), and the plan revive/cancel calls that need user input.

**Two pre-existing problems in this plan were surfaced, not fixed** — they belong to this
plan, not to the cleanup that found them, and neither is safe to resolve mechanically:

1. **`plan-doc-size`**: PLAN.md is 53,808 bytes / 301 lines, past the 35,000-byte hard
   limit, so edits that grow it are blocked. The remedy is to EXTRACT — this folder already
   has `research/` and `triage.md`, so the shape is there; the findings tables in PLAN.md
   want to move into a named supporting document.
2. **`header-body-coherence`**: the header says In Progress while the body's task list
   reads as complete. Both readings are defensible — the ticked tasks really are done, and
   the three named remainders (QA-09 pytest stage, DOC-18 Dockerfile comment, the plan
   revive/cancel calls needing user input) really are outstanding. The honest fix is to add
   those three as unticked tasks, which cannot be done until the size problem is cleared,
   because it grows the file.

## Batch 9 user direction, quoted verbatim

The Batch 9 entry in PLAN.md referenced this quotation, and the phrase inside it was what
`header-body-coherence` was reading as a claim that the plan is finished. The PLAN.md line
now paraphrases and points here; the words as the user wrote them are:

> agreed with all — get it all done.

That was direction to proceed with the full recommendation set, not a statement that the
plan had completed. Nothing about the plan's state changed when the line was reworded.
