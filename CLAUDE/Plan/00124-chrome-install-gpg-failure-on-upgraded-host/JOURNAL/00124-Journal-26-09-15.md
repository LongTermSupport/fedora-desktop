# Plan 00124 — Journal 26-09-15

> **Append-only activity log** for plan 00124. One file per day
> (`00124-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:42 · action · — — plan scaffolded

Plan 00124 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 19:45 · finding · P1 — the first fix landed and exposed a second cause

Issue #45's new comment settles which cause is live. The failing task now names
repo `google-chrome`, not `@commandline`, so `31191b5f` is deployed and working —
and the install still fails, on a different thing:

```
TASK [Add Google Chrome Repository] ***
ok: [localhost]
TASK [Install Google Chrome] ***
[ERROR]: Failed to validate GPG signatures: Public key "…/linux_signing_key.pub"
is already present, not importing.,OpenPGP check for package
"google-chrome-stable-153.0.8010.36-1.x86_64" … from repo "google-chrome" has
failed: Public key is not installed.
```

Both halves are true at once, which is the whole finding. I briefly read this as
"my fix was wrong" and said so; it was not. The repo IS being used, and the key
IS present — by primary id. What is absent is the subkey the package was signed
with.

Evidence, from the repo's own package: extracting tag 268 (`RPMSIGTAG_RSAHEADER`)
gives signer `FD533C07C264648F`. The published key file holds primary
`7721F63BD38B4796` plus eight subkeys, and that signer is one of them. So the key
URL is right and the host's imported COPY is stale — an F41-era import that rpm
will never replace, because rpm decides presence by primary.

## 19:55 · decision · P2 — find the key by id, never by description

First cut of the helper matched installed keys on a summary substring
(`--match google`). Writing the play showed what that means in practice: the
verdict feeds `rpm -e`, and "any installed key whose description mentions google"
is not the same set as "the key this play manages". A host with a Google Cloud
SDK key would have had it deleted to make room.

Changed to derive the identity from the published key itself: take its primary,
take rpm's short id (`7721F63BD38B4796` → `d38b4796`), and match only packages
rpm named `gpg-pubkey-d38b4796-*`, anchored on the trailing separator so
`d38b4796ab` is not caught. Nothing in the invocation names Google any more.

That also changed the answer for a key sitting at that id with a *different*
primary — a short-id collision. It was `refresh`, i.e. remove it. It is now
`import`: our key is absent, and the one that is there is not ours to remove.

## 19:58 · action · P2 — helper and tests green, verified against the real key

33 tests, all passing. Every subprocess call takes an injected runner, so the
decisions are driven without a real keyring — including the case that matters
most, where the installed and published keys DIFFER (a fake returning one canned
answer to both could only ever exercise the equal case).

Unit tests cannot vouch for the gpg colon parsing itself, so that was run against
the real published key rather than a fixture:

```
primary: 7721F63BD38B4796
subkeys: 1397BC53640DB551 1D09C015006FEAB8 32EE5355A6BC6E42 4EB27DB2A3B88B8B
         6494C6D6997C215E 78BD65473CB3BD13 E88979FB9B30ACF2 FD533C07C264648F
signer FD533C07C264648F present: True
rpm package id: gpg-pubkey-d38b4796-<created>
```

The rpm half cannot be exercised here at all — this container has no `rpm`. It
fails loudly with `FileNotFoundError` rather than reporting "nothing to do",
which is the right shape, but it does mean Phase 4 on the real host is the first
end-to-end proof and the plan should not be called done before it.

## 20:00 · decision · P3 — the loop is the gate, and there is no second one

`play-browsers.yml` removes what the helper names, with no `when:` on the task.
The helper prints an envelope line only on a `refresh`, so an empty loop is the
skip. Adding a `when:` as well would create a second place that decides, and the
two could disagree — the disagreement being a deleted signing key.

The repo's `gpgkey` now points at `file:///etc/pki/rpm-gpg/RPM-GPG-KEY-google-chrome`
rather than the URL, so dnf validates against the same bytes the play checked the
keyring against, instead of a second fetch nobody reasoned about.

## 20:22 · finding · T4.1 — confirmed on the affected host

Owner re-ran on the F41-upgraded laptop: *"41 laptop chrome issue resolved, all
seems to be chugging away happily now — its still installing but seems fine"*.

That is the first end-to-end proof of the rpm half of the helper. Nothing in this
container could give it: there is no `rpm` here, so `installed_envelopes`,
`key_armour` and the `rpm --erase` path had only ever run against an injected
fake. The gpg half was verified against the real published key; this closes the
other half.

Still open, and deliberately not claimed yet:

- **T4.2** the second run. The owner's run was still in progress, so nobody has
  yet seen the key tasks report `ok` rather than `changed`. Idempotence is a
  stated goal and an unconditional remove-then-import would satisfy T4.1 exactly
  as well as the gated one does — so T4.1 passing is not evidence for it.
- **T4.3** `qa-reviewer`, still running against the diff at the time of writing.

## 20:35 · finding · P3b — both safety claims were breakable, and the review broke them

`qa-reviewer` came back FIX-BEFORE-MERGE. I had asked it specifically to attack the
two things this plan asserts, and it demonstrated both rather than reasoning about
them. Neither was theoretical.

**1. A two-certificate published key refreshes for ever.** `key_ids` read the primary
from the first certificate but accumulated `sub:` records from every certificate in
the file. The reviewer drove three consecutive runs against a two-cert bundle with a
fully current installed key and got `refresh` every time, naming the second
certificate's subkey as "missing".

This is the precise failure the helper exists to prevent — the docstring's own
"removing unconditionally would report `changed` on every run for ever" — reintroduced
by the parser. And the trigger is the scenario `PLAN.md` already contemplates: a vendor
rotating a primary ships a transitional two-cert bundle. Worse, the test for this
shape existed and asserted only the primary half; its docstring even says "a keyring
export can hold several keys". The test named the risk and then checked the wrong half.

Google's file holds one certificate today, so this was latent rather than live — the
owner's laptop was never going to churn. Verified before saying so.

**2. The erase set was identity-checked for one member and erased in full.** With two
packages at short id `d38b4796`, `main` made exactly ONE description query
(`envelopes[0]`) and printed BOTH names as `RPM-KEY-ENVELOPE`, so both reached
`rpm --erase`. The whole stranger-at-the-same-id branch — the thing I wrote up as the
reason for identifying keys by id rather than description — only held when the
stranger happened to be listed first. `installed_envelopes`' own docstring says
multi-envelope is the EXPECTED state on an upgraded host.

So the module documented a safety property it did not have. The decision was right and
the plumbing did not implement it.

Fixed by moving the erase set into the verdict: `needs_refresh` now takes every package
found at the id, matches each against the published primary itself, and returns the
names to erase. Nothing downstream re-derives that list, so there is no second place
that could disagree with the thing that did the checking.

**3 (nit, but it drives the same failure).** Expired and non-signing subkeys were
counted. Five of Google's eight are expired — measured, not assumed — so a host missing
only those got an erase-and-reimport described as "missing signing subkey(s) … so it
cannot verify what the repo now ships", which is untrue of an expired key. gpg's
validity and capability fields are now read, which drops the count from 8 to 3 and
keeps the actual signer.

Verified after the fix, against the real key and a real two-cert bundle rather than
fixtures:

```
REAL published key
  usable signing subkeys: 3 of 8 records
  signer FD533C07C264648F retained: True
  expired ones dropped: True
TWO-CERTIFICATE BUNDLE
  pub records in input: 2
  subkeys attributed: 3 (equals the single-cert count)
  three consecutive runs -> ['none', 'none', 'none']
```

## 20:38 · decision · T3.4 — the play had no post-condition, so give it one

The reviewer's sixth point is the one I would have missed longest: nothing asserted the
re-import actually took. The erase reports success, the import reports success, and if
the installed key still lacks the subkey afterwards the next run simply finds the same
deficiency — erase, import, green, for ever.

That is not hypothetical here. rpm stores the key as a `%{description}`, and if that is
a normalised re-export the difference might never close. Task 4.2 was the only thing
standing between that and silence, and Task 4.2 is a human remembering to run something
twice.

So the play now re-asks the helper after the import and fails unless the answer is
`none`. It costs one command and converts a silent forever-loop into a first-run
failure with a reason attached.

## 20:42 · thought · — — the same defect is latent in two more repos here

Recorded late: this was thought at 20:24, before the review landed, and an earlier
edit of mine put it out of sequence at the foot of the file. Restating it here in
order rather than moving it, because entries are append-only.

Worth writing down rather than acting on. Brave and Vivaldi are both added by
`config-manager addrepo --from-repofile`, which brings a vendor `.repo` declaring
its own `gpgkey`. That is the same import path, so the same staleness is possible:
if either vendor adds a signing subkey under an existing primary, a host that
imported that key years ago hits this identical failure, and `rpm_key` will not
rescue it any more than it did here.

`helpers/rpm_keys/subkeys.py` is generic — it takes a published key path and has
nothing Google-specific in it — so wiring either up is a small job. NOT doing it
now, on YAGNI: neither vendor has actually done this, and a refresh gate on a key
nobody has seen go stale is speculative machinery guarding a hypothetical. The
note exists so that if Brave or Vivaldi ever fails this way, the diagnosis is
already written down and the tool already exists.
