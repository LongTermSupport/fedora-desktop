# Plan 00082 — Journal 26-09-17

> Continues `00082-Journal-26-08-24.md`. Append-only; corrections are new entries.
>
> Times here are **UTC**, the CCY container's clock. Host-written entries are BST,
> an hour ahead.

## 08:39 · finding · P5 — Phase 5 has been dischargeable since 2026-09-13 and the plan never learned it

Phase 5 asked for a full `RUN_BASH_GITHUB_ACCOUNTS=none` provision on a real host or
disposable VM. It was written expecting a downstream repo to supply the proof, and so
sat open while the proof was produced in-repo, four days earlier, by a different plan.

`vmtest:920` and `:1229` hardcode `RUN_BASH_GITHUB_ACCOUNTS=none` as the provisioning
default; only the opt-in `SCENARIO_HOST_ONLY` branch (`:1230-1234`) substitutes an
account. So **every** `server-fast-provision` run is this plan's scenario. Plan 00110's
run `20260913T170901Z-server-fast-provision`, repeated through the container-to-host
bridge as `20260913T181420Z`, completed with `RUN-BASH-EXIT 0` and 13 green guest checks.

Task 5.1 wanted `--list-tasks` to show the ordering was right. What exists is stronger:
`guest-acceptance-server.bash:158` asserts `gh-cli-installed` in a guest with no GitHub
identity at all, and a wrong static trace would have aborted the run before that check.
Ordering proven by execution rather than by a listing.

Both load-bearing claims above were read directly from the files before ticking, not
taken from the assessment that proposed the closure.

## 08:39 · finding · — — the Delivery section described a branch that merged three weeks ago

Delivery said the branch was "local, not yet pushed — per operator instruction". It
merged as PR #33, `68f1596f`. The line was true when written and was never revised, so
the plan carried a stale delivery state for the whole of Phase 5. Corrected in place
with the merge recorded, since a Delivery section describes current state.

Success criterion 1 is ticked with an explicit limit: **HTTPS is inferred, not
asserted.** The clone succeeds in a guest where SSH could not work, which is good
evidence and is not the same as a check reading the remote URL scheme. Nothing in the
guest suite does that. Recording it rather than letting a tick imply more than was
measured.

Criterion 2's two residues are owned by Plan 00063 Phase 3, not by this plan.

## 08:40 · handoff · — — closed

Status → Complete, folder moved to `Completed/`, README updated. No host action is
owed by this plan.
