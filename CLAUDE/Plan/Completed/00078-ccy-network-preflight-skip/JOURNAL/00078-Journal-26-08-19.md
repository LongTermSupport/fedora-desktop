# Plan 00078 — Journal 26-08-19

> **Append-only activity log** for plan 00078. One file per day
> (`00078-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:45 · action · — — plan scaffolded

Plan 00078 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:46 · action · T1.1/T1.2 — the patch, applied

Guarded `claude-yolo:2669-2762`'s network-reachability preflight on
`CCY_SKIP_NETWORK_PREFLIGHT` (unset by default — no behaviour change for any
existing caller), following the exact idiom `lib/ssh-handling.bash` already
uses for `CCY_SKIP_TOKEN_OWNER_CHECK`: a named opt-out for one specific
preflight, not a general trust switch. Added the escape hatch as item 6 in
the existing failure-path debugging list, a troubleshooting-table row in
`docs/ccy.md`, and a `docs/ccy-changelog.md` entry. `CCY_VERSION` bumped
3.38.0 → 3.39.0 with a short pointer comment — deliberately did NOT chain
another "Previously (3.38.0): ..." block onto the existing one, which is
already the exact bloat `docs/ccy-changelog.md`'s own header says it exists
to stop (its intro records the comment once reached 5,645 characters).

## 13:50 · finding · T1.3 — qa-all.bash could not complete; verified another way

`./scripts/qa-all.bash` failed at its `qa-bash.bash` stage: "file discovery
found 0 bash files under /workspace/untracked/repos/fedora-desktop". Checked
whether this was caused by the patch — it is not: `git ls-files '*.bash'`
from the same working directory finds 74 tracked files, and
`qa_discover_shell_files` (`scripts/qa-shell-discovery.bash`) uses plain
`find`, not a missing external tool, so the "Missing required tools"
message qa-all.bash prints is generic rather than diagnostic here. Did not
chase the root cause further — out of scope for this plan, and pre-existing
in this environment regardless of this patch. Verified the actual change
directly instead: `bash -n files/var/local/claude-yolo/claude-yolo` clean;
`shellcheck -s bash` and `shellcheck -x -s bash` both clean, with every
reported finding (SC1091 sourcing-info notices, one pre-existing SC2086 at
line 2960, SC2178/SC2128 pairs at lines 2202-2206 and 2715-2718) confirmed
by line number to be unrelated to the edited region. Zero new findings from
this change.

## 13:52 · handoff · —

Patch complete and verified by direct tool invocation (not the full QA
suite, which has an unrelated pre-existing environment issue — see above).
Not yet exercised against a live host: no fenced runner currently runs
`ccy` at all (`lts-infra` Plan 00030 Phase 1/2 unbuilt), so
`CCY_SKIP_NETWORK_PREFLIGHT=1` has been verified by reading the guarded
control flow, not by a live launch. That live proof is `lts-infra` Plan
00030's to obtain once it actually deploys `ccy` to `dc-runner-vm`. Closing
this plan now — narrowly scoped, done. The broader question this plan
deliberately did NOT answer — what a *running* ccy session should be
allowed to reach on the runner (fedora-desktop Plan 00068's Decision 8,
still unmerged on `plan-00066-ccy-ci-runner`) — remains open.
