# Plan 00130 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00130. One file per day
> (`00130-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:38 · action · — — plan scaffolded

Plan 00130 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:38 · decision · T2.2   — 00079 unit-test-selection.bash deleted; re-review findings fixed

Owner decision on the review's eighth finding, delegated to the agent ("you tell me"):
00079's plan-local `unit-test-selection.bash` is deleted (e24c6b30).

Why: `scripts/test-podfreeze.bash` covers every podfreeze function the copy tested, and
the re-review checked it case by case against every behaviour. It gates every
`qa-all.bash`. The copy had already rotted once (its marker grep) and carried a standing
shellcheck finding. 00079's `acceptance.bash` now runs the repo suite as its step-0 unit
check.

The re-review of the whole plan returned FIX-BEFORE-MERGE. Its two findings are fixed:

- `acceptance.bash` sent the suite's stdout to /dev/null, but the suite prints FAIL on
  stdout, so a failure would have lost the failing case names. That is this plan's own
  defect class. The output is now captured and printed in full on failure, and the re-run
  hint uses the absolute path.
- 00079 Task 0.1 still named the retired `logs/` directory.

Nits: the Task 2.2 and success-criterion evidence now notes that the script has since been
deleted. The open items are verified HOST-only: five owed scripts, each with both
`plan_require_host` and `plan_start_log auto`.

## 17:05 · correction · —     — the "plan scaffolded" entry above is wrong; round-2 review

The 14:38 "plan scaffolded" entry is wrong. It is a second `mkplan.bash` stub; the plan was created on 26-09-16 at 18:05 (see that day's file). The "case by case" re-review cited at 14:38 was a coordinator check of `test-podfreeze.bash` against the deleted script. It was not a qa-reviewer run, and it left no report.

The qa-reviewer round 2 ran and returned FIX-BEFORE-MERGE with no BLOCK: `subagent-reports/260923-qa-reviewer-opus-5.md`. Resolved:
- the plan count is six plans, not seven (PLAN.md, README.md);
- the dated counts ("45 now", "1,002") are dropped;
- the withdrawn check-0 caveat is out of PLAN.md.

Check-0 caveat, moved here: it named 00079's `acceptance.bash`, but the anchored-grep defect was in `unit-test-selection.bash`, which Plan 00079 fixed on the same day. That script is now deleted, and nothing blocks these runs.
The four nits are also done:
- 00079 `acceptance.bash` prints the suite's `passed:` line on a pass;
- 00079 Task 2.4 drops the sentence its own superseded note contradicted;
- the `-y` help in 032's and 00109's `deploy.bash` describes the flag rather than a removal.
Next: round 3 for PASS.

## 17:45 · finding · review — qa-reviewer round 3 PASS

Round 3 checked every round-2 finding and nit against the tree; all are resolved. One new nit: the 17:05 entry used the category `correction`, which is not in the grammar (`finding` was meant); journals are append-only, so this entry records it rather than editing that one. Remaining: Task 2.2 and its criterion are HOST runs of the converted scripts.

## 17:46 · handoff · T2.2   — blocked on the HOST only

Everything the container can establish is done. Next: on the host, run each converted script listed in Task 2.2 to its last line.

## 18:55 · action · T2.2   — 00079 and 00080 joined the meta-deploy batch

On the owner's instruction the batch holds only what needs a host run. 00063 and 00098 are out. 00079 (triage, deploy, acceptance) and 00080 (triage only) are in, and the runner now accepts a triage-only plan and runs its triage once. The next batch run discharges four of the five owed scripts; 00066 remains a direct run.

## 19:13 · action · T2.2   — 00079 and 00080 reached their last lines on the HOST; only 00066 is owed

The host batch `_meta-deploy/20260923-194037` ran the five converted 00079 and 00080 scripts that this task was still owed. Each reached its own last line, and each log drained in full.

The measure is stronger than reading a tail. The batch runner keeps its own copy of each script's terminal stream (`_meta-deploy/<stamp>/<plan>-<script>.log`). Each per-plan run log under `untracked/plan-runs/<plan>/<script>/<stamp>/` is **byte-identical** (`cmp`) to the start of that stream. After it, the stream holds exactly 5 more lines: a blank line and the library's four `run log (UNSCRUBBED)` lines. `_plan_finalize_log` prints those to the terminal *after* the writer has drained, so they are the only lines that should be missing from the file. They were the only ones missing. Every file ends on a newline.

| Script                            | Run log's last line                                    | Is that the script's last output?                                                                                          |
| --------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| 00079 `triage.bash` (before)      | the `END OF REPORT` banner's closing rule              | yes, the script's final `echo`                                                                                             |
| 00079 `deploy.bash`               | the chained acceptance's final `never commit it.` line | yes. The script ends by calling `acceptance.bash`, whose output, closing lines included, the deploy log carries            |
| 00079 `acceptance.bash` (chained) | `removed network <throwaway>`                          | yes, the last line of the `plan_on_cleanup` teardown, which runs before the drain. The `VERDICT` banner is above it        |
| 00079 `triage.bash` (after)       | the `END OF REPORT` banner's closing rule              | yes                                                                                                                        |
| 00079 `acceptance.bash` (batch)   | `removed network <throwaway>`                          | yes, as above                                                                                                              |
| 00080 `triage.bash`               | the `END OF PASSIVE REPORT` banner's closing rule      | yes, the passive branch's last `echo` before `exit 0`. `--reachability` was not run; its branch ends at a different banner |

The chained 00079 acceptance log was also compared with the copy the deploy log captured. They are identical, which checks the drain a second way.

That leaves one script owed: 00066's `triage.bash`. It runs on the machine that plan is about, not on this desktop, so it is outside the batch.
