# Plan 00108 — Journal 26-09-09

> **Append-only activity log** for plan 00108. One file per day
> (`00108-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 16:42 · action · — — plan scaffolded

Plan 00108 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:50 · finding · T1.1 — headless boxes carry `export PS1_COLOUR=`

Three headless boxes provisioned by a consuming estate all hold an empty colour line: the
interactive `pause` returned nothing, `default('lightblueBold')` does not treat empty as
missing, and the prompt script's `${PS1_COLOUR:-lightblue}` hid it as a plain lightblue prompt.

## 16:55 · action · T1.1 — RUN_BASH_PS1_COLOUR

`run.bash` 1.19.0: validated against the colour function names, forwarded as `-e PS1_Colour=`;
usage text; `play-basic-configs.yml` priority-0 comment and `default(..., true)` on the prompt
answer; headless-provisioning table row, install example, changelog, configuration page.

## 17:45 · handoff · — — complete; proven by the consuming estate

Delivered in `ad0ca02e`. The consuming estate (its Plan 00119) re-provisioned a headless box that
follows this branch's tip with `RUN_BASH_PS1_COLOUR=purpleBold`; the box read back
`export PS1_COLOUR=purpleBold`. Its other boxes stay on an older pinned run.bash and keep the
empty line until that pin moves. QA: green except seven pre-existing `.claude/rules/*` pointers
at an untracked daemon path, untouched by this plan.
