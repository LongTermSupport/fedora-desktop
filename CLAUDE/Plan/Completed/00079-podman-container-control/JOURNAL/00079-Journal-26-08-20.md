# Plan 00079 — Journal 26-08-20

> **Append-only activity log** for plan 00079. One file per day
> (`00079-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> tasks, decisions); this tracks what actually happened.

______________________________________________________________________

## Renumbered 00078 → 00079, after a plan-number collision

`git fetch` showed `origin/F44` two commits ahead with a **different** Plan
00078 — `Completed/00078-ccy-network-preflight-skip`, a `CCY_SKIP_NETWORK_PREFLIGHT`
opt-out for an egress-fenced CI runner — while this clone had been building
`00078-podman-container-control` under the same number. Branches were 13 ahead
/ 2 behind.

### Root cause, and why it will happen again

The authoritative plan counter is `hooksdaemon.latestPlanNumber`, held in
**`--local` git config**. Local config is not a tracked file and is never
pushed, so it does not travel with a branch. Two clones of the same repo each
read `78` and each handed out `00078` — neither did anything wrong, and neither
could have seen the other's allocation. `mkplan.bash`'s lock makes the
allocation atomic *within one clone*; nothing makes it atomic *across* clones.

The collision is therefore structural, not a slip, and it is worth recording
here rather than quietly renumbering: the next multi-clone session hits it
again. A durable fix would have to put the counter somewhere that syncs (a
tracked file, or a git ref that is pushed), which is a hooks-daemon change and
not this plan's business.

### Which one moved, and why

Theirs. Not mine — the other way round:

- Theirs is **pushed** and **Complete**, archived under `Completed/`. Renumbering
  it would rewrite a published, finished plan and break any reference to it.
- Mine is **local-only**, unpushed, and **In Progress**. Its number appears in
  six plan-local files and one README row, all of which I own.

So the plan that had not left this machine is the one that moves. Renamed the
folder and the day-1 journal file with `git mv`, replaced all 20 in-file
occurrences, and updated the README index row with a one-line note saying it was
renumbered and why — so anyone who finds a `Plan 00078:` commit message in this
branch's history can tell which plan it meant.

The three commits already on this branch keep their `Plan 00078:` subjects. They
are pushed nowhere yet, but rewriting them would mean an interactive rebase over
a merge, which trades a real risk for a cosmetic gain. The README note and this
entry cover it.

### The merge

`git merge --no-ff origin/F44` — a merge commit, never a squash (see
`CLAUDE/AgentNotes.md`). One conflict, in `CLAUDE/Plan/README.md`, and both
sides were pure additions: they added their Completed row for
`00078-ccy-network-preflight-skip`; this side had moved 00077 from Active to
Completed. Kept both Completed rows and dropped the stale Active row for 00077.

Worth noting for the next person who resolves a conflict in a `.md` file here:
the `markdown_table_formatter` hook reformatted the file **while the conflict
markers were still in it**, escaping them into `\<<\<<\<<< HEAD`, `\=======`
and `> > > > > > > origin/F44`. A `grep '^<<<<<<<'` then reports zero markers on
a file that is still conflicted — an absence of evidence read as evidence of
absence, which is this repo's own recurring defect class showing up in the
merge tooling. Found by grepping for `origin/F44` instead.

______________________________________________________________________

## Session state

Failsafe recovery cron `c557c06c` created (hourly at :23, non-durable —
it dies with the session). It is a safety net for an externally-interrupted
resume, never a pacing signal.

______________________________________________________________________

## Phase 1: the run-time labels, and then dropping the image label

Phase 1 was the fix for F16 — `--ccy` selecting a CCY-*derived* throwaway that
was not a session. `claude-yolo` now sets `--label ccy=true --label
ccy-project=<project>` at `podman run`; CCY_VERSION 3.39.0 → 3.40.0, with the
changelog entry.

Then the part that was not in the task list. D6 had said the tool should
"prefer `ccy=true` and keep the inherited label as the fallback for pre-Phase-1
containers", and writing it revealed that the fallback covers **nothing**:

- a session from CCY ≥ 3.40.0 has `ccy=true`;
- every older session is named `<project>_yolo[_N]`, because
  `get_next_container_name` constructs it that way and there is no other path;
- so the image label's only remaining contribution is the false positive F16
  demonstrated.

So it is dropped outright rather than kept. That is a narrowing, which is
normally the dangerous direction here — an over-match is visible in the printed
set, an under-match is silent — but the asymmetry argument only bites when the
narrowed selector might miss something, and this one provably cannot. D6 is
rewritten to say that rather than left implying a fallback that does not exist.

Knock-on: `acceptance.bash` check 9 had been enumerating the expected set with
`--filter label=claude-yolo-version`, which is now the *wrong* contract — it
would demand the tool include exactly the containers it is supposed to exclude.
Re-aimed at `label=ccy=true`. The elaborate fixture-image partitioning added
yesterday (prefer unlabelled images, SKIP the exclusion assertion when only
labelled ones exist) went with it: which image the throwaway comes from stopped
being load-bearing the moment the tool stopped reading image labels. Both the
comment and the code shrank, and the assertion is now unconditional.

## Then the user asked for identity labels

> *"i wonder if we can label ccy containers by key label and anthropic token as
> well? so could easily freeze all containers with github ID XXX"*

Yes, and cheaply — the values are all already in scope at the run site:
`GITHUB_USERNAME`, `TOKEN_NAME_FOR_CONFIG` (set for `save_launch_config` a few
lines earlier), and `SSH_KEYS`. Three more labels: `ccy-github`, `ccy-token`,
`ccy-ssh-keys`.

Three things worth recording about the shape:

**`none` rather than an empty value.** An empty label value is
indistinguishable from an unlabelled container at the filter, so "this session
has no GitHub identity" would read as "this container is not a CCY session".
Stating it costs four characters.

**`ccy-ssh-keys` is genuinely multi-valued** — more than one key can be
mounted — so membership is a word match on the label value, while the
single-valued axes compare whole. That asymmetry is a real bug surface: a
substring match would make `--ssh-key github` select a session whose keys are
`github_work github_personal`, silently widening the freeze. `identity_matches`
keeps the two modes explicit and the unit test asserts both directions,
including that a prefix is not a match.

**Rows are derived from the inventory, and only when they discriminate.** An
axis is offered only when it has ≥ 2 distinct values — with one GitHub account
on the machine, "github: <id>" and "all CCY containers" are the same button,
and a menu that says the same thing twice reads as more capable than it is.
Same rule the network rows already follow: never offer a group with no members.

Implementation note: the first cut used a bash **nameref** to pick which
associative array an axis reads (`local -n _lookup="$(...)"`). shellcheck
immediately flagged `INV_GITHUB`/`INV_TOKEN`/`INV_SSHKEYS` as unused (SC2034)
— correctly, because the indirection hides every read from static analysis,
which is exactly how a typo'd array name becomes a silently empty group. It
also tripped SC2155. Replaced with a `case` that reads the arrays by name; both
findings went, and the code got easier to follow rather than harder.

Unit test now at **37 assertions**, all passing, including the three that drive
`select_identity`'s failure path in a subshell — an unknown value must exit
non-zero, name the value, and list the known ones, never resolve to an empty
set and exit 0.

`acceptance.bash` gains checks 13 and 14. 13 asserts `--github` against podman
rather than against the tool's own idea of the set. It deliberately exercises
**only** the GitHub axis: all three share one code path, and the token label is
a private identifier that has no business being written into a log file, even a
gitignored one.

______________________________________________________________________

## Host run 2: PASS, 16 passed / 2 skipped

`deploy.bash` ran both plays clean — `play-claude-yolo.yml` `ok=43 changed=4
failed=0`, `play-podfreeze.yml` `ok=4 changed=1 failed=0` — then handed over to
acceptance, which returned **PASS (16 passed, 2 skipped)**.

The two skips are 9 and 13, both wanting a session started by the NEW launcher;
nothing had been relaunched yet, so they correctly declined to assert rather
than passing on a vacant set.

**The fixture came from `localhost/claude-yolo:<project>`** — a CCY-labelled
image. Yesterday that combination *failed* the gate: check 9 asserted the
throwaway was excluded from `--ccy`, and a container built from a CCY image
carried the inherited label, so it genuinely was in the set. Today the same
fixture passes untouched, because the tool no longer reads image labels at all.
The elaborate image-partitioning workaround was deleted this morning and the
run that would have needed it went green without it — which is the better kind
of confirmation than re-running the workaround would have been.

One thing the log does NOT establish: the pre-rename cleanup block printed
nothing, meaning `~/.local/bin/podman-freeze` was absent. Absent is the desired
end state, but silence here cannot distinguish "already clean" from "we looked
in the wrong place" — this repo's own defect class. Not yet probed.

## H7: `podman` is one network, not one per container

User question: *"we have network podman listed — I suspect this isnt one single
network is it? does each container have a default network called podman but is
actually different? or are all ccy containers launching networked together
which would seem weird?"*

Answered from the existing triage log rather than from memory. `podman network
ls` lists it once, with a single NETWORK ID and the `bridge` driver — so it is
**one** shared bridge, not a per-container default. Seven CCY sessions were
attached to it at once, and an eighth (`<project>_yolo`) was on the app network
`mkt` instead, because that one was launched with `--network`.

So yes — every session launched without `--network` lands on the same bridge
together. That is podman's default, not something CCY chooses. Recorded as F17
because it changes how the menu reads: "network: podman" is approximately
"every session that did not join a project network".

What the log does **not** establish is whether they can actually address each
other, and I am not asserting it. Added an H7 probe — `network inspect` for the
subnet, plus the per-container attachments — so the next run settles it with a
subnet rather than with reasoning about rootless netavark from memory.

## D9: choosing a group now opens it

> *"can we have a way to drill into a grouping - eg select all ccy and drill
> into list - first option is "all" or its then possible to pick specific ones.
> i think this will be easiest and clearest"*

Built. A group row leads to a second screen listing its members with the verb
each will get, and the **first row acts on all of it**.

The thing to be careful about was not undoing D7. Yesterday's feedback was that
per-container tab-selection was *forced* and that group targets were
unreachable without knowing flag names. A drill-down could easily reintroduce
exactly that. It does not, because "all" is row 1 and bare ENTER takes it: the
whole group is still one keypress, and picking specific containers is now the
option rather than the obligation. What it adds is the one thing a group row
structurally cannot show — **its members** — which matters precisely because
F15/F17 mean "network: podman — FREEZE 5" can include a live Claude session
without saying so.

The standalone "pick individual containers…" entry is gone: drilling into "all
containers" *is* that screen, and two routes to one screen is worse than one.

Two implementation notes:

- The "all" row is keyed by a sentinel `*all*`, not the word "all". Podman
  container names must begin with an alphanumeric, so no container can collide
  with it; keying on the word would have been a latent bug waiting for someone
  to name a container `all`.
- `row_verb` was first written as `[ test ] && printf A || printf B`.
  shellcheck did not flag it (both printfs succeed), but that shape runs the
  third branch whenever the second one fails, which is the discarded-failure
  class the repo gates on. Rewritten as explicit `if`s — the linter's silence
  is not the standard.

Unit test 37 -> 43 assertions, all passing, including `row_verb` under an
explicit verb in both directions (an explicit `freeze` must render `-` against
an already-frozen row rather than claiming it will act).

---

## Self-review pass over the plan diff — two drifts, both in the docs

Ahead of Task 3.3 (the `qa-reviewer` agent proper), walked the plan's own diff
against the checklist the agent uses. Two findings, both real, both in the
documentation rather than the code.

**1. PLAN.md said `Not Started`** above a task tree that is 13 ticked, 2 open,
with the tool built, deployed, and passing acceptance on the host. Task 3.2 also
still read *"Awaiting run 2"* when run 2 had already returned PASS (16 passed,
2 skipped) and was journaled here. The journal was ahead of the plan — which is
the wrong way round for the file that gets read in full every session. Both
fixed; 3.2 now records both runs and names exactly what is left (the 2 skips
need a session started by the 3.40.0 launcher).

**2. The 3.40.0 changelog entry documented two of the five labels it shipped.**
It covered `ccy=true` and `ccy-project` and stopped, omitting the identity trio
— the half that was added because the user asked for it. `docs/ccy.md` carried
the complete table, so the two user-facing documents disagreed about the content
of one released version. Entry rewritten to cover all five, plus two facts the
table does not carry: the `:-none` fallback (so a filter never matches nothing
merely because an identity was absent) and that the values are identifiers
rather than secrets.

Both claims were checked **against the code**, not against my own summary of it
— `claude-yolo:2977-2986` for the fallbacks and the basename-only key list.
Given that today already produced one wrong fact from a truncated `grep`,
re-deriving from source rather than from memory is the cheap part.

**Deliberately not done:** bumping `CCY_VERSION` for a comment reword. The
version comment is a summary that already delegates to the changelog, so the
gap was in the changelog, not the script. A bump would have meant a new version
and a host redeploy for zero behaviour change — churn dressed up as rigour. The
script is byte-identical to what is deployed.

QA green throughout: 526 files, exit 0. Task 3.3 is still open — a self-review
is not the `qa-reviewer` agent and does not get to tick its box.

---

## acceptance.bash refused, and it was right — I gave the wrong command

Told the user `acceptance.bash` alone was enough because "nothing deployed has
changed since run 2". It refused to run:

```
ERROR: the deployed podfreeze differs from the repo copy.
  Run this plan's deploy.bash — verifying a stale binary proves nothing.
```

The timestamps settle it. Run 2's deploy finished at **10:31:59**. `a678e31` —
the D9 drill-down, the feature the user specifically asked for — was committed
at **10:42:17**, ten minutes later. So the host has never run the drill-down,
and run 2's "16 passed, 2 skipped" verdict, which I have been quoting as the
plan's evidence of health, **never exercised it**.

My claim was not a guess about something unknowable; it was checkable in one
`git log` and I did not check it. The gate did.

This is the Plan 00099 defect class exactly — repo says fixed, host runs the old
build — caught by the guard written after that incident. It is the first time
that guard has fired in anger, and it fired on me rather than on drift caused by
someone else, which is the more useful test of it.

Recorded as F18. The operational consequence is a rule worth stating plainly in
the task: **`deploy.bash` is the entry point whenever the tool itself changed**;
`acceptance.bash` alone is only valid when the host copy is already current, and
the honest way to know that is to let the gate tell you rather than to reason
about it from memory.

---

## Run 3: PASS 21/0 — and the green tick hid a gap in check 9

`deploy.bash` redeployed and chained into acceptance: **PASS, 21 checks, 0
skipped**, up from 16 passed / 2 skipped. Checks 9 and 13 are real assertions
now — `--ccy` resolves the live session set, `--github` resolves the identity
axis, and the unlabelled throwaway is excluded from both. The D9 drill-down is
finally on the host and exercised.

Then read the log rather than just the verdict, and found F19.

**Check 9 builds its expected set from `label=ccy=true` alone.** It asserts that
every *labelled* running session appears in `--ccy`, and that the throwaway does
not. It never asserts that the **name-pattern fallback** works. On this host
that fallback is not an edge case: Plan 00080's triage just established that
**4 of the 6 live sessions are unlabelled**, so the majority of the real
population reaches `--ccy` through the exact path the gate says nothing about.
Break `CCY_NAME_PATTERN` tomorrow and check 9 still prints OK.

What makes this worth writing down is the timing. This is the *third* instance
of one shape in a single day — the truncated `grep`, triage's P4, and now check
9 — and the third was found only because the first two had trained me to go and
look. The tell is always the same: a check that verifies the path you were
thinking about, and is silent on the path that carries the load. The verdict
line reads identically either way, which is precisely the problem with trusting
a verdict line.

Also worth noting the check's own comment reasons carefully about *which* label
to use (run-time `ccy=true`, not the inherited image label, with the history of
why). It thought hard about label-vs-label and never asked about
label-vs-no-label. Careful reasoning inside too narrow a frame.

**Check 9b** now asserts the fallback directly: enumerate running containers
matching podfreeze's own pattern, subtract the labelled ones, and require every
remainder to appear in `--ccy --dry-run`. When every session happens to be
labelled it reports that state explicitly rather than skipping — a skip there
would be indistinguishable from the gap it exists to close.

One deliberate detail: 9b recomputes the labelled set instead of reusing check
9's `$ccy_live`. That variable holds podman's *error text* when its call failed,
and matching container names against an error string would silently misclassify
every session — the discarded-failure-signal class, one line away.

**9b has never run.** A gate that has never executed is not evidence of
anything, so it is tracked as Task 3.2b rather than counted as a fix.

---

## Facts F1-F14 relocated from PLAN.md (verbatim snapshot)

PLAN.md crossed the escalated size tier. F1-F14 are the Phase 0 research and
host-triage findings; every one is settled and none is still being argued, so
the full text is preserved here and PLAN.md keeps a compact statement of each.
Relocation, not deletion — the claims are unchanged, only the prose is shorter.

- **F1** — `podman pause`/`unpause` use the cgroup freezer and take `--all` and
  `--filter` (`network=`, `label=`, `name=`, `status=`, `ancestor=`).
  *podman-pause(1), docs.podman.io, 2026-08-19*
- **F2** — `podman container checkpoint`/`restore` (CRIU) is **not supported
  rootless**; CRIU needs elevated capabilities. *podman.io/docs/checkpoint,
  criu.org/Podman, 2026-08-19; research logged in JOURNAL*
- **F3** — this repo's default engine is **rootless Podman**, and CCY runs
  under it. *`CLAUDE/ContainerEngines.md`*
- **F4** — CCY containers are named `<project>_<suffix>[_N]`, suffix `yolo` or
  `browser`. *`claude-yolo/lib/common.bash`, `get_next_container_name()` 598–631*
- **F5** — the CCY `run` invocation passes **no `--label`**; the image carries
  `claude-yolo-version` and `claude-yolo-dockerfile-hash`.
  *`claude-yolo:2944`; `Dockerfile:36,326`*
- **F6** — CCY containers run with `--rm`. *`claude-yolo:2944`*
- **F7** — fzf is already deployed here, with a numbered-menu fallback when
  absent. *`play-open-command.yml:53`; `files/home/.local/bin/open`*
- **F8** — no container-management UI tool is deployed by any play.
  *grep of `playbooks/`, 2026-08-19*
- **F9** — podman-tui is packaged in Fedora and needs the user `podman.socket`.
  *packages.fedoraproject.org; podman-tui README, 2026-08-19*
- **F10** — the user's four verbs map to **one** rootless-capable mechanism,
  pause/unpause; checkpoint is the only other and is root-only. *F1 + F2 + F3*

**From the Phase 0 host triage** (raw log in this plan's gitignored `logs/`,
write-up in the journal):

- **F11** — podman 5.8.4, `rootless=true`, cgroups v2, `cgroupManager=systemd`,
  `runtime=crun` — **rootless pause is viable, so the Task 0.3 gate passes** (H1)
- **F12** — `ps --filter label=claude-yolo-version` matched exactly the running
  CCY containers, so the group is selectable with no launcher change (H2)
- **F13** — `podman pause` accepts `-f, --filter`; `ps --filter network=`
  partitions every network correctly, reporting empty sets not errors (H3)
- **F14** — `podman-tui 1.11.3-1.fc44` is in `updates`, its `podman.socket` is
  already active, and `fzf` is at `/usr/bin/fzf` (H4 packaging; D3 dependency)
---

## Run 4: PASS 22/0, and then the same shape a fourth time (F20)

`deploy.bash` again: **PASS, 22 checks, 0 skipped**. 9b did exactly what it was
written for —

```
OK — unlabelled sessions resolve via the name fallback: <project-a>_yolo
     <project-b>_yolo <project-c>_yolo <project-d>_yolo
```

— so `--ccy` provably covers all six sessions, not just the two labelled ones.
The fallback was always *intended* to work; now it is *shown* to.

Then, checking whether the same blind spot existed on the identity axes, found
that it did.

**F20.** `identity_values()` skips any container with an empty identity, which
is precisely every pre-3.40.0 session. So an unlabelled session is in `--ccy`
but in **no** identity group, and `podfreeze freeze --github X` answers "every
session for X" with a strict subset. The user's original ask was *"so could
easily freeze all containers with github ID XXX"* — a silent partial answer
defeats exactly that.

And `select_identity` already had a guard. It refuses when **nothing** carries
the axis label, with a helpful message about relaunching. It says nothing when
**some** do — which is the state this machine is in, and will stay in until
every session has been relaunched.

That is the fourth instance of one shape today:

| #   | Where                    | Guarded            | Blind to                     |
| --- | ------------------------ | ------------------ | ---------------------------- |
| 1   | my truncated `grep` read | —                  | output cut at the line limit |
| 2   | 00080 triage P4          | zero labelled      | *some* labelled              |
| 3   | acceptance check 9       | wrong label chosen | the non-label path entirely  |
| 4   | `select_identity`        | zero labelled      | *some* labelled              |

Rows 2 and 4 are the same guard written twice, by me, in two different files,
both times stopping one case short. Each was found only because the previous one
taught where to look — which is the useful part, but also means the count is a
lower bound on how many more exist.

**The fix is disclosure, not blocking.** An unlabelled session's account is
genuinely unknowable — the labels are the only evidence there is — so refusing
the selection would be wrong, and guessing would be worse. Being *quiet* was the
defect. `select_identity` now prints a NOTE naming the sessions it could not
consider, and gives both remedies (`--ccy` covers everything; relaunching makes
them selectable by identity).

On **stderr**, per StderrHygiene: a caller doing
`names=$(podfreeze freeze --github X --dry-run)` must get names, not prose.
Check 13b asserts both halves — that the NOTE appears, and that it does *not*
appear on stdout.

Two details in 13b worth recording, both mistakes caught in the writing:

- It first reused check 9b's `$unlabelled`. If 9b takes an early failure branch
  that variable is unset, and `${unlabelled:-}` would then read as "nothing to
  disclose" and report a PASS the check never earned. Recomputed independently,
  and `CCY_NAME_PATTERN` moved to a single top-level definition so no check
  depends on another having run.
- It first used a discard-stderr redirect to isolate stdout. That is the
  error-hiding pattern this repo blocks, and doubly wrong here because **stderr
  is the thing under test**. Split with a temp file instead.

Neither has run on the host yet — tracked as Task 3.2c, for the same reason 9b
was: a gate that has never executed proves nothing.

### Correction, same session: real container names scrubbed before commit

The run-4 entry above originally quoted check 9b's output verbatim, which named
four live containers. Those names derive from private project identifiers, and
the pre-commit secret scanner rejected the commit against `github_accounts` in
`localhost.yml`. Replaced with `<project-a..d>_yolo` placeholders.

This is a rewrite of an entry appended minutes earlier and never committed, not
an edit to journal history — the append-only rule protects the record, and
leaving a public-repo leak in place to honour it would invert its purpose. The
real names remain in this plan's gitignored `logs/`, which is exactly the split
`CLAUDE/PlanTriage.md` describes: host state readable beside the plan, never
committed.

Worth naming the miss. `CLAUDE/AgentNotes.md` says to treat `CLAUDE/Plan/` as
public and scrub before writing, and I pasted tool output straight in instead.
The scanner is the backstop, not the control — and it only fires on `git
commit`, so the same paste into a `gh issue` body would have gone straight out.

---

## Run 5: PASS 24/0 — and the class gets written down instead of just fixed

`deploy.bash` again: **PASS, 24 checks, 0 skipped**. Check 13b holds on both
halves —

```
OK — the NOTE names the sessions the identity axis cannot cover
OK — the NOTE is on stderr only; stdout stays the payload
```

— so the F20 disclosure works and does not pollute a captured dry-run. Tasks
3.2b and 3.2c are closed, and the gate has grown 15 → 24 checks across the run
sequence without a single one being loosened to get there.

**Then stopped fixing instances and went looking for the class.** Four in one
day is not four accidents. Grepped this repo's own docs for prior art, and found
it — in `CLAUDE/QA.md`, from **Plan 00076**:

> "Zero coverage was already treated as a broken gate; *partial* coverage was
> not."

That is the identical sentence about the identical mistake, written months ago
about `qa-bash`/`qa-patterns` discovery, plus a second instance in the same
section (`.paths.scanned` lists a file semgrep could not *parse*). So the count
today is not four, it is **six**, and the class was already documented — as a
local quirk of two QA gates, in a file you read when working on QA gates.

That is why it kept recurring. A defect class filed under one of its instances
is findable only by someone already looking at that instance.

**So it is now written down as a class**, per the repo's own convention that a
defect getting past the reviewer should be added to the reviewer rather than
only fixed:

- `CLAUDE/AgentNotes.md` gains *"A partial result read as a complete one — guard
  the empty case, miss the partial"*, sited next to its sibling (the discarded
  failure signal) with the asymmetry argument, all six instances in a table, and
  five specific "how to apply" rules.
- `CLAUDE/QA.md`'s Plan 00076 note now points up at the general class, so the
  local instance stops looking local.
- `.claude/agents/qa-reviewer.md` §C gains the three concrete things to look
  for: a guard on the zero case with nothing for the partial case; a selector
  with a documented fallback where only the primary path is asserted; and
  coverage implied by the length of a list rather than stated as a number.

The rule I most want the next session to have is the last of those. **A set you
never enumerate cannot look short.** Every instance in the table would have been
visible immediately if the check had printed `n of m` instead of a list — which
is exactly what triage's COVERAGE line now does, and what turned run 1's
confident "no listening sockets" into a visible 1-of-5.

Second-most: **an under-match never appears as a failure.** It cannot be waited
for. You have to go and ask what population the filter missed, which is a
deliberate act, which is why it needs to be on a checklist rather than left to
notice itself.

---

## qa-reviewer: FIX-BEFORE-MERGE, 9 findings — and four were this same class again

Ran the mandated review (Task 3.3). Verdict **FIX-BEFORE-MERGE**. Nothing broke
another user or leaked an identifier, but the class documented *in this very
diff* had four more live instances, one of them in the shipped tool.

**Finding 1 — the identity menu row was suppressed exactly during the state it
exists for.** The comment said the rule correctly ("offered only when a value
does not cover every CCY session"); the code tested `${#values[@]} -lt 2`, which
is only a proxy for it. With one labelled account and four unlabelled sessions
— this machine's actual state — that is one distinct value covering 2 of 6, so
the row vanished from the menu while `--github X` kept working on the CLI. Three
consequences, and the second is the bad one: menu and CLI disagreed; the menu's
only remaining route was the *wider* "all CCY containers", silently steering
toward freezing four sessions the user never asked for; and `select_identity`
was never called, so the F20 disclosure was unreachable from the menu. Fixed by
gating on coverage — the thing the comment always claimed.

**Finding 2 — `none` and "unlabelled" were collapsed.** The inventory header
asserted they stayed distinguishable; the code dropped `none` into the same
empty-map state as an absent label. A 3.40.0 session with no gh identity, no
token and `--no-ssh` was therefore reported as predating 3.40.0 and its owner
told to relaunch it — advice that cannot work, plus an inflated count in the
NOTE. Fixed with `INV_HAS_LABELS`, keyed on what came back from the `ccy=true`
query, and the header comment rewritten to say what is actually true.

**Finding 3 — acceptance check 13 could pass having asserted nothing.** `gh_one`
is chosen from a query including paused sessions; the expectation narrows to
running. So if the only labelled session is paused — which is *normal*, pausing
them being this tool's entire job — the expected set is empty, the loop body
never runs, and it prints "resolves every session for that account" over a
population of zero. Check 9 guards its upstream emptiness; 13 guarded neither
end. Now skips explicitly.

**Finding 4 — the `COVERAGE: n of m` rule added to AgentNotes in this diff was
not applied to the gate in this diff.** Plan 00080's triage got it; Plan 00079's
acceptance did not. The same rule landing in one file of a pair is the precise
recurrence shape the note describes. The gate now prints the fleet size and how
much of it carries labels, above the verdict.

Counting properly, that makes **ten** instances of this class in one day.

Also fixed: the changelog told consumers to fall back to the inherited
`claude-yolo-version` label (finding 5) — the exact thing D6 dropped because it
produced F16; it now names the session-name pattern instead. The `CCY_VERSION`
comment still described two of five labels (finding 6) — I had declined that
earlier to avoid a redeploy for a comment, and that reasoning expired the moment
`podfreeze` itself changed, since `deploy.bash` runs both plays anyway; bumped
3.40.1, comment-only. The README row promised a `[y/N]` confirmation and `-y`
that D7.3 deliberately removed (finding 7), and its 00080 row still carried the
"row dissolves" claim I had already corrected in PLAN.md but never propagated.
The unit test wrote its generated fragment into `logs/` inside the repo, where
shebang-based discovery then linted a gitignored artifact (finding 8) — now
`mktemp` outside the tree with an EXIT trap, and QA's file count dropped
accordingly, which is the confirmation.

Two nits taken seriously rather than noted: `FIELD_SEP='|'` is justified for
container and network names but the identity query's fields are *label values*,
and an SSH key basename is a filename that may legally contain `|` — a
mis-split would silently group a session under the wrong identity, so the read
now takes a fifth field and fails loudly if one appears. And `select_network`
called `exit` from inside the interactive loop, so a network vanishing between
menu-draw and choice killed the session; it now returns, the menu re-prompts,
and the CLI path exits non-zero explicitly.

Unit test 43 → **49 assertions**, including regressions that fail on both
finding 1 and finding 2 specifically. QA green.

What I want to remember from this: the reviewer found four instances of a class
I had just spent the day documenting, in the diff where I documented it. Writing
the rule down does not make you apply it — and the two places it was *not*
applied were the gate and the tool, i.e. the two places that report to a human.

---

### Correction: a real project name scrubbed from the 08-19 day-file

A second `qa-reviewer` pass found a live leak the first one missed:
`00079-Journal-26-08-19.md:283` recorded acceptance output naming a container
image as `localhost/claude-yolo:<project>`, and `claude-yolo:1539` sets
`PROJECT_IMAGE_NAME="claude-yolo:${PROJECT_NAME}"` — so the tag is a real
project directory name from the host. Introduced by `31f66d2`, replaced with a
`<project>` placeholder.

This is the same scrub I performed on the 08-20 file earlier today, and I did
not check its sibling — a fix applied to one file of a pair, which is the exact
recurrence shape this plan spent the day documenting. Third time today.

Two things the reviewer was right to state plainly, and which I am not going to
soften:

- **A follow-up commit does not undo it.** The string is in `31f66d2` on this
  branch. Scrubbing it now keeps the working tree clean but the commit still
  carries it, so pushing publishes it. That is the user's call, and it has to
  be made *before* a push, not before a merge.
- **The same identifier is already published** on `origin/F44` in an earlier
  plan. That is a larger, pre-existing instance, out of this diff, and also the
  user's decision. Flagged rather than quietly cleaned, because a silent scrub
  of published content would misrepresent what is actually public.

The rewrite of a previous day's journal entry is deliberate and narrow: a
public-repo leak overrides append-only, the edit is a redaction rather than a
revision, and this entry records it.
