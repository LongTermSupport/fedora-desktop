# Plan 00079 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00079. One file per day
> (`00079-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.
>
> This day-file's preamble was written without the template's "plan scaffolded" seed
> entry: the plan was created on 26-08-19, and the seed would have said otherwise.

## 19:12 · action · T3.3b   — HOST acceptance PASS against the reviewed code

The host batch `_meta-deploy/20260923-194037` ran `triage.bash`, `deploy.bash`, `triage.bash` again, and `acceptance.bash`. The deploy chains into its own acceptance run, so there are two acceptance runs. Both are **`VERDICT: PASS — 22 check(s) passed, 1 skipped`**.

- **Against the reviewed code.** The deployed `podfreeze` was compared byte for byte with the repo copy (`acceptance.bash:144`), and the gate passed. That copy's last change (`3a3e8b93`) postdates Task 3.3d, so for the first time an acceptance run has executed against the code the re-review asked for. This is Task 3.3b.
- **Deploy.** `play-claude-yolo.yml` ran with `changed=2` and `play-podfreeze.yml` with `changed=0`.
- **Check 0.** `scripts/test-podfreeze.bash` ran on the host: 187 passed, 0 failed.
- **COVERAGE.** 6 running CCY sessions, all 6 carrying `ccy=true`. Triage (after) shows every session with all five labels (`ccy`, `ccy-project`, `ccy-github`, `ccy-token`, `ccy-ssh-keys`), none empty. Five sessions are on one GitHub account and one is on another.
- **Check 13 ran.** `--github` resolves every session for that account and excludes the unlabelled throwaway.
- **Check 13b is SKIP**, as designed: no unlabelled running session exists, so there is nothing to disclose. Check 9b says the same about the legacy-name fallback. Both paths were exercised live in the Task 3.2b and 3.2c runs, and the name-pattern selection is in check 0's suite.

Ticked: Task 3.3b, and the success criteria for `--network`, `--ccy`, the in-container refusal and `--dry-run`, the host `VERDICT: PASS`, and the identity labels.

**Not ticked, and why the plan does not complete:**

- "Interactive picker works with and without fzf". No run exercises it. `test-podfreeze.bash` states that `pick_target` and `drill_into_group` die without a TTY, and acceptance has no interactive check. This is a human check: at a HOST terminal, run `podfreeze` once with `fzf` on `PATH` and once with it hidden, and open a group, act and quit in each.
- "`qa-reviewer` verdict is PASS". The last review (Task 3.3c) was FIX-BEFORE-MERGE. Task 3.3d addressed its findings, but no review has confirmed them. That needs a confirming `qa-reviewer` run over the plan's diff.

Task 3.4 waits on both.

## 19:12 · finding · —   — triage probe for the deployed ccy version fails with rc=127

Both triage runs reached their closing banner. One probe inside them fails, and it failed the same way in both of today's runs: `deployed ccy version` returns `rc=127, ccy: command not found` (`triage.bash:87`). The label rows above it still show what H6 asks, so no conclusion rests on it. The probe calls `ccy` by name from a non-interactive script, where it is not on `PATH`. A fix would read `CCY_VERSION` from the deployed launcher file instead, the way `deploy.bash` already does. It has not been changed here.

## 20:30 · finding · T3.5   — confirming review: FIX-BEFORE-MERGE; corrections to this day's entries

The confirming qa-reviewer returned FIX-BEFORE-MERGE (`subagent-reports/260923-qa-reviewer-confirm-opus-5.md`). Corrections to the entries above, which stay as written because the journal is append-only:

- **The count.** Today's runs read "22 passed, 1 skipped", but check 9b's no-unlabelled branch called `ok()` without running the tool. With every session labelled, the real count was 21 passed and 2 skipped. Check 9b now calls `skip()` in that branch, following the rule checks 13 and 13b already use.
- **The commit.** The 19:xx entry names `3a3e8b93` as podfreeze's last change. That commit is the freeze library's; podfreeze's own last change is `cfe8afbe`. Both postdate the Task 3.3d fix `bb75d40d`, so the conclusion, that the runs exercised the reviewed code, holds.
- **The probe line.** The failing `deployed ccy version` probe is at `triage.bash:147`, not `:87`.
- **Half the tool was compared.** "Byte-checked deployed copy" covered the entry script only. Half of podfreeze lives in `~/.local/lib/freeze/freeze-common.bash`, and nothing compared it. `acceptance.bash` now compares the deployed library too, and refuses when it is missing or differs.

## 20:35 · action · T3.5   — review fixes: criteria reworded, Tasks 3.6 and 3.7

- **The `--network` criterion** now says what was shown. The live run proves the preview, the pause and the printed set on a one-member throwaway network. Exclusivity is proven by the unit suite (check 0), not live.
- **The `--ccy` criterion is un-ticked.** Resolution is shown, by check 9's dry run and check 0's name-pattern cases. A real `freeze --ccy`, and any `thaw --ccy`, has never run, because acceptance deliberately dry-runs against live sessions. New Task 3.6 is that proof, by a human at a quiet moment.
- **The probe.** `deployed ccy version` called the `ccy` alias, which a non-interactive script cannot see, hence rc=127. It now calls `/var/local/claude-yolo/claude-yolo --version`. Task 3.7 confirms this at the next host triage.
- **Stale comments.** Two `acceptance.bash` comments are corrected: the podman-freeze removal is the play's job, not `deploy.bash`'s, and the "4 of 6 sessions predate 3.40.0" figure is gone.

Task 3.5(b), the confirming PASS, is still owed: the review is re-run over these fixes.

## 19:46 · finding · T3.5   — second confirming review: FIX-BEFORE-MERGE, fixed; corrections to the two entries above

This entry is out of order on purpose. The two entries above are stamped 20:30 and 20:35, but they were committed in `880154a6` at 19:32:15 UTC, so both were written before 19:32. Their times are wrong; they stay as written, because the journal is append-only. The same entry's reference to "the 19:xx entry" means the 19:12 entry.

The second confirming qa-reviewer returned FIX-BEFORE-MERGE (`subagent-reports/260923-qa-reviewer-confirm2-opus-5.md`). Every finding from the first confirming review was fixed in the code, but the plan text around the fixes had drifted. Fixed here:

- **Acceptance criterion un-ticked.** Its PASS came from the gate before both confirming reviews. New Task 3.8 is the HOST re-run with the fixed gate.
- **Check 9's dry-run assertion.** "nothing was frozen by the --ccy dry run" was an unconditional `ok()`, so it counted a pass after the skip and after the listing failure, when nothing ran. It is now judged only when the dry run was invoked.
- **The version probe.** It ran the launcher with `--version`, but the launcher reaches `--version` only after checking for `.git` in the cwd and asking a migration question. From anywhere but a main checkout's root it exits 1. It now reads `CCY_VERSION` from the deployed file with awk, and reports a missing file or line as a probe failure.
- **PLAN.md.** The status line now lists the four open criteria and Tasks 3.5 to 3.8. Two stale "last verdict (Task 3.3c)" references are gone. Task 3.5(b) links both confirming reports. The `--ccy` criterion now says "pauses / resumes", matching Task 3.6.

Task 3.5(b) still needs a third review, over these fixes.
