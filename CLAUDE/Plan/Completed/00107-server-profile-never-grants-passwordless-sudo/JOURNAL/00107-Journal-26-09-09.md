# Plan 00107 — Journal 26-09-09

## 08:46 · finding · T2.2 — full headless run green at 1.18.1

On a real headless server box (password sudo, `RUN_BASH_SUDO_PASSWORD_FILE`): main playbook
under the server profile, then every optional playbook with the become password now passed
through, completed unattended. `sudo -k -n true` as the user exits non-zero afterwards; no
`passwordless sudo for` marker in `/etc/sudoers`. A second downstream converge changed nothing.

## 08:52 · finding · T2.2 — the removal branch, on a planted block

Planted the play's own marker-wrapped block by hand, then ran `play-basic-configs.yml`:

- `--check --diff`, profile auto-detected as server: the sudoers task diffs exactly the three
  planted lines out; `changed=1`, nothing else.
- live: the task reports `changed`, and the next `become` task fails
  `sudo: a password is required` — the invocation had no credential of its own once the block
  went, which is the consequence `docs/headless-provisioning.md` now spells out.
- afterwards: marker absent, `visudo -c` OK, sudo refuses the user without a password.

The desktop branch (`state: present`) is byte-identical to the previous task; no desktop box was
to hand for a live run, so it is asserted by reading, not by running.

## 08:56 · handoff · — — plan complete

Archived to Completed/. Nothing open.
