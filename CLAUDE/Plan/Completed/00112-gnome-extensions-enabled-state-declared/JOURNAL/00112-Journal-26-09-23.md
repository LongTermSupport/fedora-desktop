# Plan 00112 journal — 2026-09-23

## 18:30 · action · T2.2   — desktop-fresh-install passes 16/16; plan complete

The run went through the bridge from the container: `20260923T173406Z-desktop-fresh-install`, verdict `pass`, planned 16, passed 16, skipped 0, against `bbc3f151`. The two earlier green runs did not certify this plan because the host's deployed checker was stale. That cause is gone: the 09-17 batch's lab leg converged with `changed=0`, and `guest-acceptance-desktop.bash` has no commit since. The COVERAGE line itself stays in the host transcript. To read it: `grep COVERAGE ~/.local/share/vmtest/runs/20260923T173406Z-desktop-fresh-install/*transcript* | cat`. Every task and criterion is now ticked.
