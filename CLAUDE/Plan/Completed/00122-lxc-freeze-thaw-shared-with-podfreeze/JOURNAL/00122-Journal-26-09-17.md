# Plan 00122 — Journal 26-09-17

> Continues `00122-Journal-26-09-16.md`. Append-only; corrections are new entries.
>
> Times here are **UTC**, the CCY container's clock. Host-written entries are BST,
> an hour ahead — the run referenced below is stamped 09:02 BST, which is 08:02 here.

## 08:45 · finding · T4.6 — the host run discharged the last executable task

`deploy.bash` then `acceptance.bash` ran on the host as part of the batch:
12 of 12 checks executed, 33 assertions passed, 0 failed, ACCEPTED.

What that settles, check by check: freeze and thaw verified against `lxc-info -s` rather
than the tool's own report ([7], [8]); the same command toggling the container back ([8]);
a refused `sudo` naming itself and printing no list at all ([10]); `lxc` absent reported as
a missing dependency that names the play which installs it, distinguishable from zero
containers ([11]); all three deployed artefacts byte-identical to their repo copies at the
declared mode ([1]); and both tools resolving the shared library by the same relative hop
([2]) and starting against it ([4]).

**What it does not settle, and the tick says so.** Check [3] asserts only that `fzf` is
installed — `podfreeze`'s picker path is still unexercised by anything, so Task 4.6's
"both tools still behave as before" is measured for the start, library-resolution and list
paths and inferred for the picker.

## 08:45 · decision · — — the ssh-after-a-long-freeze criterion is closed on an argument

The one criterion never measured as written: a first-attempt ssh after a freeze longer
than the one-hour DHCP lease. What was measured is a real freeze/thaw whose
`journalctl -t dnsmasq-dhcp` shows `DHCPDISCOVER` through `DHCPACK` in the same second as
the thaw. The renewal is unconditional, so a longer freeze reaches the identical code
path; the only untested variable is whether the address had already been dropped.

The owner declined to stage an hour-long freeze for it, so the real overnight case
confirms itself the next time it happens. Recording it as an accepted gap rather than
letting the tick imply an experiment nobody ran — if a first-attempt ssh ever fails after
a long freeze, that criterion is where the gap was accepted, not the renewal code.

## 08:46 · handoff · — — closed

Status → Complete, folder moved to `Completed/`, README updated. No host action owed.
