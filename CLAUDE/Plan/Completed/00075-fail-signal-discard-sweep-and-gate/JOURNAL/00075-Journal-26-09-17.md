# Plan 00075 — Journal 26-09-17

> Continues `00075-Journal-26-08-18.md`. Append-only; corrections are new entries.
>
> Times here are **UTC**, the CCY container's clock. Host-written entries are BST.

## 08:47 · finding · T4.4 — the two defects worth reporting upstream no longer exist

Task 4.4 was the plan's last open item: report two hooks-daemon defects that could not be
patched locally, since a daemon upgrade overwrites both files. Re-checked before writing
anything, because filing a report about a defect that has since been fixed costs a
maintainer's time on a public tracker and teaches them to read this project's reports
more slowly.

Both are gone.

**`lint_on_edit` exclude paths.** The handler's own guidance now documents
`options.exclude_paths` and says the project-wide `daemon.exclude_paths` applies as well.
Measured rather than taken from the documentation: `.claude/hooks-daemon.yaml` already
excludes `.semgrep/bash-conventions.bash`, so I appended a comment line to that
deliberately-invalid fixture and watched what the edit drew. Nothing — where the task
recorded "a wall of findings on every edit". The probe line was removed immediately and
`git status .semgrep/` confirms the file is unchanged.

**`resolve_venv_python` discarding its failure.** The task cited `.claude/init.sh:310`.
The call now sits at `:445-451` and is the correct form:

```bash
if PYTHON_CMD="$(resolve_venv_python "$HOOKS_DAEMON_ROOT_DIR")"; then
    return 0
fi

local rv=$?
PYTHON_CMD=""
return "$rv"
```

`$?` after the `fi` is the failed condition's status, so the failure propagates. Worth
noting that this is a *correct* instance of exactly the shape this plan was built to
catch — the class is about the status being read wrong, not about the construct.

The stale line number is its own small lesson: a task that names a file and a line in
someone else's tree is a claim with a shelf life, and this one had expired.

## 08:48 · handoff · — — closed

Every task and every success criterion is discharged. Status → Complete, folder moved to
`Completed/`, README updated. No host action and no upstream report owed.
