# Plan 00066 — Journal 26-07-29

> **Append-only activity log** for plan 00066. One file per day
> (`00066-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:45 · action · — — plan scaffolded

Plan 00066 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 19:48 · finding · — — the plan counter was stale; reconciled before scaffolding

`hooksdaemon.latestPlanNumber` read `63` while `00064` and `00065` already
existed on disk — they arrived in this clone by `git pull` from another agent's
clone, and the counter is per-clone git config, so it never advanced here.
`mkplan.bash` refused correctly rather than allocating a colliding `00064`:

```
counter (63) is behind the highest plan on disk (65). The counter is stale;
reconcile with: git -C '<repo>' config --local hooksdaemon.latestPlanNumber 65
```

Applied that exact remediation, then scaffolded → `00066`. Worth noting the
drift guard did its job; a folder scan would have silently collided.

## 19:52 · finding · P1 — `--headless` is not the flag it sounds like

The single most important finding, and it inverts the obvious design. `ccy`
already has `--headless` (`claude-yolo:155`), so the natural assumption is that
unattended operation is mostly solved. It is not. `--headless` changes exactly
two things:

- `claude -p "$PROMPT"` instead of an interactive invocation (`2626-2628`)
- `-i` instead of `-it` on the container (`2694-2699`)

Both are about how *Claude Code* is invoked. The **launcher** still prompts:
**35** `read -rp` sites across `claude-yolo` and the libs, of which
`HEADLESS_MODE` guards exactly **one** (`lib/ssh-handling.bash:357`).

So `--headless` describes the payload, not the harness. Recorded as E1/E2 in
`PLAN.md`, and it is why Decision 2 makes `--non-interactive` a *separate* flag
rather than widening this one.

## 20:01 · finding · P1 — the prompt loops do not just prompt, they HANG

Escalates E2 from annoyance to blocker. At least ten of the 35 sites are
`while true` menu loops of this shape (`claude-yolo:2011` shown):

```bash
while true; do
    read -rp "Select option [1-4]: " cleanup_choice
    case "$cleanup_choice" in
        1) ... ;;
        4) ... ;;
        *) echo "Invalid choice. Please select 1-4" ;;
    esac
done
```

With no TTY, `read` returns non-zero **immediately** with an empty value, the
`case` falls to `*`, and the loop repeats — unbounded. I replicated the exact
shape with an injected counter and fed it `/dev/null`:

```
Invalid choice. Please select 1-4     (x4)
SPUN 5 TIMES ON EOF -> INFINITE LOOP CONFIRMED
```

Sites: `claude-yolo:1104`, `claude-yolo:2011`, `lib/docker-health.bash:162,369,486`,
`lib/network-management.bash:271`, `lib/dockerfile-custom.bash:37,117,157,718`.

Consequence: an unattended `ccy` does not fail, it **hangs** — which in CI burns
a runner slot until the job timeout and produces no diagnostic. This is why
Phase 2 (`--non-interactive`) is ordered before everything else: nothing later
can be verified unattended while this is true.

Note the contrast: `lib/ssh-handling.bash:357` and the `stty susp` guard at
`claude-yolo:2732` (`[[ -t 0 ]]`) show the codebase *does* know how to handle
absent-TTY correctly. The gap is coverage, not capability.

## 20:09 · finding · P4/P5 — two capabilities are genuinely absent, not merely weak

- **MCP: zero support.** Searched `mcp`/`MCP` across `claude-yolo`, all 7 libs,
  all 4 Dockerfiles and `entrypoint.sh` → **no matches at all**. So "inject MCP
  into a ccy session" is net-new capability, not a repair of something partial.
- **Egress: the flag that exists does the opposite.** `--network NETWORK`
  *attaches* the container to an additional podman network so it can reach
  compose services (`148`, `1801-1812`); default is `--network podman` (`2516`);
  and there is a **positive** connectivity probe fetching `http://google.com`
  that warns when it fails (`2529`). No proxy, no allowlist, no restriction
  anywhere.

The second is a naming hazard worth flagging early: `--network` and a future
`--egress` would sit on what looks like the same axis and act in opposite
directions. Task 5.1 has to resolve that rather than stack a second flag on top.

## 20:15 · decision · D1 — a variant IMAGE is justified; a variant LAUNCHER is not

The request floated "a `ccy-ci-runner`, a special version of ccy". Taken
literally that is a second launcher, and I do not think that is right — it would
be re-implementation #5 in this estate, and the two copies would drift across
all 35 prompt sites, the token path, the version gate and the podman argv.

What *is* justified is a variant **image**: CI needs MCP server binaries baked
in, and baking those into the desktop image is weight every workstation user
carries and never runs. The repo's existing `Dockerfile` already layers
`base` → `full`, so a `ci` stage is a third step in an established pattern
rather than a new mechanism.

So: one launcher, orthogonal opt-in flags, layered images. Recorded as
Decision 1 in `PLAN.md` with the rejected option and its reasoning, because the
rejection is the load-bearing part.

Corollary for the three options as posed: they are not alternatives to choose
between. A CI image, MCP injection and egress restriction are three of the
**four** things a CI mode needs — the fourth (non-interactivity) was not named
and is the prerequisite for verifying any of the others.

## 20:22 · blocker · T1.1 — cannot answer the `/dev/dri` question from in here

`claude-yolo:2773` passes `--device /dev/dri:/dev/dri` **unconditionally** — one
occurrence, no guard — while the GUI socket mounts right above it (`2704-2727`)
*are* properly guarded. On a headless server with no render node that looks like
a day-one failure.

I tried to settle it by running podman. That was a mistake and it produced a
false signal:

```
podman run --rm --device /dev/definitely-not-here:/dev/x alpine true
Error: copying system image from manifest list: ... potentially insufficient
UIDs or GIDs available in user namespace (requested 0:42 for /etc/shadow) ...
exit=125
```

Non-zero, and **nothing to do with the device check** — it died pulling the
image under this container's userns before argument validation. Had I read
"exit 125" as "podman rejects the missing device" I would have written a proven
finding that was not one. This is the same failure mode this estate keeps
hitting: a true statement about a check standing in for a stronger statement
about the world.

Owner confirmed the general rule: **do not test podman from inside a podman
container; write `triage.bash` and have it run on the host.** Done — probe D in
`CLAUDE/Plan/00066-ccy-ci-runner-variant/triage.bash` decides it either way, and
E6 stays marked UNVERIFIED in `PLAN.md` until it runs.

## 20:31 · finding · — — "✓ bash: 0 files OK" was the gate scanning NOTHING

Nearly banked a second false green. `./scripts/qa-all.bash` reported:

```
✓ bash: 0 files OK
✗ python: ruff not installed (sudo dnf install ruff)
ERROR: Missing required tools. Install them and re-run.
```

"0 files OK" in a repo full of bash is not a pass. Cause: this clone lives at
`/workspace/untracked/repos/fedora-desktop`, and `qa-bash.bash`'s discovery
carries `! -path "*/untracked/*"` (`scripts/qa-bash.bash:43`) to skip the repo's
own scratch tree — but the repo's **own path** contains an `untracked/`
component, so the exclusion matches every file in it. Confirmed directly: the
same `find` returns `0`.

This is an artifact of **where I cloned it**, not a defect in fedora-desktop —
at the normal `~/Projects/fedora-desktop` the exclusion behaves as intended. And
`ruff` is genuinely absent in this container, so the gate hard-fails at stage 2
by design regardless.

Net: **the project QA gate could not run here, and I am not claiming it passed.**
What I can claim, having run them directly, is that the one script this plan
adds is clean:

```
bash -n CLAUDE/Plan/00066-ccy-ci-runner-variant/triage.bash   -> syntax OK
shellcheck -f gcc ...                                          -> clean (0.9.0)
```

## 20:38 · handoff · — — design drafted; needs a host run and the audit loop

State: `PLAN.md` written with 9 cited proven findings (E1-E9), 3 decisions, 6
phases; `triage.bash` written for the host; README index row added. On branch
`plan-00066-ccy-ci-runner` — deliberately NOT `F44`, because another agent is
active in this repo on Plan 00065 and this must not collide with it. No file
outside `CLAUDE/Plan/00066-*/` and the README row is touched. Zero code changes,
per the owner's instruction.

Two things gate progress, in this order:

1. **Owner runs `triage.bash` on the host.** It settles T1.1 (`/dev/dri`), tells
   us whether the deployed launcher matches this checkout (if it does not, every
   line citation in `PLAN.md` describes something that is not running), and
   censuses the prompt sites on the deployed tree.
2. **The audit/fix loop (Phase 6)** — hostile review then independent deep scan,
   each round its own file under `reports/`. The scan brief must say: re-read the
   source, do not trust this plan's citations.

Recovery cron `ffc583d1` (hourly, :23, non-durable).

## 21:10 · finding · T6.1/T6.2 — round 1 dismantled the thesis, and three of my own claims

Both reviewers delivered. `reports/fable-review-1.md`: 2 BLOCKER, 4 MAJOR, 2 MINOR.
`reports/sonnet-scan-1.md`: 5 CRITICAL, 2 HIGH, 2 MEDIUM. I re-verified every
load-bearing finding against source before accepting it — including the ones
that make me look bad, and one where the hostile reviewer had accepted a wrong
claim of mine that the scan then caught.

**The two that change the plan's direction:**

1. **I missed the most consequential axis entirely.** `claude-yolo:2792` runs
   `claude --dangerously-skip-permissions` unconditionally; there is no
   `--permission-mode`/`--allowedTools`/`--disallowedTools` surface anywhere in
   the launcher, the 7 libs, or the entrypoint. Stronger still:
   `entrypoint.sh:245` writes `"bypassPermissionsModeAccepted": true`, so the
   image *pre-accepts* the bypass dialog. The consumer's design is the exact
   inverse — `--permission-mode default`, allowlist-first, ungranted tool
   REFUSED. ccy is YOLO **by construction**; that is its whole name and purpose.
   I wrote a plan about making it CI-ready and never mentioned it.

2. **My thesis mischaracterised the consumer.** I wrote "~1,737 lines
   re-implementing ccy". It is not re-implementation. `ccy-baseline/Dockerfile:61`
   is literally `FROM claude-yolo:latest` — it **reuses** the image — and
   `run-sandbox.sh:375-402` documents at length why it **must** discard ccy's
   entrypoint, including the honest note that `--entrypoint /bin/bash` drops tini
   and why that trade is accepted. So it reuses what is reusable and replaces
   what is not. My "fourth re-implementation" framing was rhetoric I had carried
   in from the other repo's audit, and it does not survive contact with the file.

The design consequence is real, not cosmetic: the shared surface is the **image**;
the launcher/entrypoint diverge because the *trust models* diverge, and no
quantity of `--non-interactive`/`--mcp`/`--egress` flags converges a
YOLO-by-construction launcher with a fail-closed sandbox. Decision 1 rejected
"a second launcher" and never considered the option the evidence supports —
**a second small CI entrypoint sharing the base image**, which is approximately
what the consumer already has.

## 21:18 · finding · T6.3 — the meta-bug, three times, in the plan written to avoid it

Task 1.1 exists *because* I caught myself reading `exit=125` as "podman rejects
the device". Round 1 found I then committed the same error twice more in the same
document:

- **E2's census.** I searched `read -rp`, found 35, and wrote "35 interactive
  prompts across the launcher and libs" — implying all seven libs. Re-ran it:
  **37** `read -rp` **+ 9** `read -r -p` = **46**. All nine of the missed ones are
  in `lib/token-management.bash` (878 lines — the entire credential subsystem),
  because that file separates the flags. Among them, `select_token` at
  `token-management.bash:611`, which `claude-yolo:1004` calls **on the default
  launch path**. So the earliest, most certain unattended blocker is credential
  resolution — and my evidence table has zero citations into that file.
  A true statement about what one grep pattern found, presented as a statement
  about how many places ccy can hang.

- **E3's "spins forever".** `claude-yolo:41` sets `set -e`. My replication script
  did not. So I proved a property of my test harness and attributed it to the
  launcher. Re-tested with `set -e` present:

  ```
  exit=1   # aborts at the FIRST read — does not spin
  ```

  Only prompts whose ancestor is invoked as an `if`/`while` *condition* spin,
  because bash suspends errexit for that whole subtree — i.e. the two
  `check_*_containers_startup` TUIs, not the six sites I cited. The hang is real
  but narrower; the abort is still a defect (undiagnosable `exit 1` mid-banner).

Both were caught by re-verifying, not by the reviewers being trusted. The scan
also caught E3 *after the hostile reviewer had accepted it* — which is the whole
argument for running two independent reviewers with different briefs rather than
one.

## 21:24 · finding · T6.3 — five defects that have nothing to do with CI

Round 1 surfaced real bugs in ccy that exist today for desktop users:

- **Concurrency TOCTOU.** `get_next_container_name` (`lib/common.bash:583`) picks
  a free name by `ps -a` + increment, no lock, from a `PROJECT_NAME` that is just
  `basename $PWD` + parent. Then `claude-yolo:2747` unconditionally
  `container_cmd rm -f "$CONTAINER_NAME"` — which force-removes a **running**
  container. Two sessions on one repo: the second can kill the first. The consumer
  salts with `GITHUB_RUN_ID` (`run-sandbox.sh:222`) precisely to avoid it.
- **Compose services leak on failure.** The teardown at `claude-yolo:2794-2846`
  is unreachable when the run exits non-zero, because `set -e` already ended the
  script. Exit-code propagation itself is correct — verified.
- **An unrelated hard dependency.** With podman and without `--no-network`, ccy
  pulls `alpine` and fetches plain `http://google.com` in a separate container,
  exiting 1 on failure (`2524-2599`) — outside any egress policy, for a service
  unrelated to the workload.
- **No token-by-value path.** `--token NAME` selects among *files*; nothing
  accepts `CLAUDE_CODE_OAUTH_TOKEN` directly. And `create_token` is an inherent
  human-browser OAuth flow whose recorded expiry its own comment admits is a
  90-day guess.
- **No registry push/pull anywhere**, so "pre-built by Ansible" only answers the
  persistent-host case.

Worth stating plainly: several of these justify themselves without the CI story
at all. That is a better outcome than the plan I set out to write.

## 21:30 · decision · T6.3 — corrections APPENDED, body left intact

Wrote C1-C11 as a block at the head of `PLAN.md` and left everything from
`## Overview` down byte-identical to what the reviewers read. If I had rewritten
the body, both reports would now cite sections that no longer exist and the audit
trail would be worthless. Same discipline as this journal's append-only rule, for
the same reason. Added Phase 7 (Tasks 7.1-7.5) to carry the corrections into a
round-2 restatement, marked 6.1-6.3 ✅.

## 21:34 · handoff · — — round 1 closed; round 2 and the host probe both outstanding

`PLAN.md` now carries 9 original findings (E1-E9, of which **E2 and E3 are
corrected** and E6 remains unverified pending the host probe), 11 corrections
(C1-C11), 3 decisions of which **Decision 1 is superseded** by C2, and 7 phases.
Both round-1 reports are on disk. Still zero code changes; still on branch
`plan-00066-ccy-ci-runner`, nothing outside this plan folder + the README row.

Outstanding, in order:

1. **Owner runs `triage.bash` on the host** — settles E6 (`/dev/dri`), confirms
   whether the deployed launcher matches this checkout, and re-censuses prompts
   on the deployed tree. Everything else is design; this is the only thing that
   needs hardware.
2. **Round 2** (Phase 7) — restate the thesis per C1/C2, then re-review. Round 2
   should NOT reuse round 1's reviewers' conclusions as settled; the value came
   from independence.

The open design question for the owner, stated plainly because it is a judgement
call and not mine to make: given ccy is `--dangerously-skip-permissions` by
construction and the untrusted-PR case needs the opposite, is the goal (a) ccy
grows a fail-closed mode and absorbs the sandbox, or (b) ccy stays the trusted-
human tool, gains the five fixes above, and the untrusted-PR sandbox stays a
separate artifact sharing only the image? Round 1's evidence points at (b).

Recovery cron `ffc583d1` still hourly at :23.

______________________________________________________________________

## 23:10 · finding · T1.1 — the triage script I shipped was defective, and the guidance it followed was too

The owner ran the first `triage.bash` and it failed in the most instructive way possible:

```
sha256sum: /home/<user>/Projects/LTS/lts-infra/files/var/local/claude-yolo/claude-yolo:
           No such file or directory
```

then printed `Could not checksum both files` and carried on. Their reply — *"FFS … what you
just made me do was stupid"* — was fair.

Root cause: `REPO_ROOT="$(git rev-parse --show-toplevel)"`. `git rev-parse` answers about the
**current working directory**, not about the script. Run by path from `lts-infra`'s root
(this repo is checked out at `untracked/repos/fedora-desktop` inside it) it resolved to
lts-infra, wrote the report there, and compared the deployed launcher against a path that does
not exist in that tree.

Two things make this worse than a one-line slip, and both matter more than the bug:

1. **This repo's own `CLAUDE/PlanWorkflow.md:69` told me to do it** — *"Resolve the repo root
   with `git rev-parse --show-toplevel`"* — and `CLAUDE/Plan/CLAUDE.md` said it a second time.
   The guidance was followed faithfully. Fixing my script would have left the next author to
   walk into the same hole.
2. **The probe degraded into a shrug instead of failing.** `Could not checksum both files` and
   exit 0 is the same defect class as `|| true`, which this repo bans outright: a control that
   silently becomes a no-op. Even with the path bug fixed, that shrug was independently wrong.

## 23:14 · action · T1.1 — rebuilt on a shared library; both copies of the bad advice corrected

The owner's instruction was to lift ballicom-infra's plan-script system into this repo and
lts-infra rather than patch one script. Done as **lts-infra Plan 00023**; landing here:

- `CLAUDE/Plan/_planlib.inc.bash` — the tested library (112 assertions in
  `scripts/test-planlib.bash`). Repo root now comes from a script-relative, filesystem-only
  marker walk **bounded at the `.git` boundary** — which matters specifically here, because
  this repo sits inside lts-infra and *both* have an `ansible.cfg` at their root, so an
  unbounded walk finds the outer repo and appears to work.
- `CLAUDE/PlanScriptStandards.md` — R1–R14.
- `CLAUDE/PlanWorkflow.md` **and** `CLAUDE/Plan/CLAUDE.md` both corrected. Fixing only one
  would have left two vocabularies, which is the mistake Plans 00020 and 00022 already paid
  for. Both corrections keep the original *reasoning* (must survive the move into `Completed/`,
  must not be a fixed `../` hop) and replace only the mechanism.
- `triage.bash` rebuilt as bootstrap + mode + legs, with probes in `probe-engine.bash` and
  `probe-launcher.bash`.

## 23:18 · decision · T1.1 — the host-only rule is now a guard, not a comment

This plan's Phase-1 preamble already said a nested podman result is not evidence about the
host. It was a comment asking nicely, and I had already proven it was not enough by running a
nested probe that died on a userns/subuid error during an image **pull** — before it ever
reached the device check — and returning a non-zero exit that said nothing about the question.

`plan_require_host` now enforces it. Proven from `/tmp` inside the container:

```
EXIT=1
[FATAL] refusing to run inside a container (found /run/.containerenv): it probes the host
container engine, host device nodes, and the deployed /var/local/claude-yolo tree. A result
obtained here would not be evidence about the host — it would be a confident wrong answer.
```

No run directory is created — the guard fires *before* `plan_start_log`, so there is no
half-written report to be mistaken for evidence later.

The other two behavioural fixes: "could not determine" is now a **non-zero exit** rather than a
printed shrug, and the prompt census counts **both** `read -rp` and `read -r -p` (C3 — the
earlier census searched one spelling, missed nine sites, and reported a total as if complete).

## 23:22 · finding · — — `./scripts/qa-all.bash` proves nothing in this checkout

`./scripts/qa-bash.bash` reports `✓ bash: 0 files OK`. It scanned **nothing**:
`REPO_ROOT` is `/workspace/untracked/repos/fedora-desktop`, and line 43 excludes
`*/untracked/*` against the **absolute** path, so every file in the repo is filtered out.

So the three scripts here were verified with `bash -n` and `shellcheck -x` run directly on each
file (all clean) — which is what the gate would have run. Not fixing `qa-bash.bash` from this
plan: it is a shared QA gate in another agent's active area, and the fix (match the exclusion
against the repo-relative path) is not plan tooling. Recorded as a finding in lts-infra Plan
00023 so it survives.

## 23:26 · handoff · T1.1 — one thing needs the human

**Owner: please run `./CLAUDE/Plan/00066-ccy-ci-runner-variant/triage.bash` on the HOST.**
It is read-only, needs no arguments, works from any directory, and prints the report path when
it finishes. The container guard deliberately withholds these facts from me, so this cannot be
self-served.

E6 itself is already settled from the earlier host run (`EXIT 125` — a missing `--device` is
fatal, so the unconditional flag is a day-one failure on any headless host). What the re-run
adds is the deployed-vs-checkout comparison and the corrected prompt census, both of which the
first script got wrong.

Round 2 (Phase 7) and the open (a)/(b) design question from the previous handoff are unchanged.
