# Plan 00089 — Journal 26-08-25

> **Append-only activity log** for plan 00089. One file per day
> (`00089-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:28 · action · — — plan scaffolded

Plan 00089 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:35 · finding · T1.1 — confirmed single-file fix, no entrypoint.sh change needed

Before implementing, read `entrypoint.sh` directly rather than assuming: it only
cross-checks `GITHUB_USERNAME` against the authenticated token when that variable is
non-empty (`if [ -n "$GITHUB_USERNAME" ]; then …`, line 64) and otherwise just requires
`GH_TOKEN` to be set (line 31). `GITHUB_USERNAME` is empty by construction on the
no-SSH-key path this fix targets, so the container side already accepts a bare token.
Confirms the fix is `lib/ssh-handling.bash` alone.

## 10:40 · action · T1.1 — guard implemented, version bumped

Added an `elif [ -n "${GH_TOKEN:-}" ]` branch in `build_ssh_mounts_and_validate()`
before the existing `gh auth token` fallback, so a caller-supplied token is honoured
as-is (unverified — there is no SSH identity to cross-check it against) instead of
being overwritten. Bumped `CCY_VERSION` 3.43.0 → 3.43.1 in `claude-yolo` (required
whenever `lib/*.bash` changes, per `CLAUDE/ContainerRules.md`) and added the 3.43.1
entry to `docs/ccy-changelog.md`.

## 10:45 · action · T1.1 — QA green

`./scripts/qa-all.bash` → `✓ QA passed: 537 files checked`. The 137 shellcheck
diagnostics reported are pre-existing `info`-level findings across the whole repo;
7 fall in the two touched files, all pre-existing (`SC1091` "not following" library
sources, one pre-existing `SC2086` at line 3035 unrelated to this change) — none are
`error`/`warning` (the gating tiers) and none are on lines this fix added. Dispatched
the `qa-reviewer` (infra-reviewer-equivalent) agent over the diff per this repo's
required-before-commit workflow step.

## 11:15 · finding · T1.1 — the premise was wrong: no live clobbering bug existed

`qa-reviewer`'s verdict: **FIX-BEFORE-MERGE**. It empirically tested the claim rather
than trusting it: `GH_TOKEN=dummy_test_value_12345 gh auth token` returns
`dummy_test_value_12345` (rc=0, clean). `gh` gives an exported `GH_TOKEN` env var
precedence over its own stored credentials — a fact neither I nor the earlier
scoping fork (from the prior session) had verified before concluding the fallback
branch clobbered a caller-supplied token. It did not; the runner path already worked
before any change here.

Two secondary findings: the `command_exists gh` gate earlier in the same function
still requires the `gh` binary to be installed even on this path, so "purely by
token" doesn't mean "no `gh` CLI dependency at all" — only "no `gh auth login` state
needed". And the working tree at review time had a `CLAUDE/Plan/README.md` row for
Plan 00090 whose folder wasn't yet tracked — expected mid-session, resolves once
committed together.

Reviewer's recommendation: keep the code (it makes token precedence explicit in our
own code instead of resting on an internal, unasserted `gh` behaviour, and drops the
`gh auth token` dependency for this path) but correct every place that asserted the
defect as fact. Rewrote: this file's Overview/Goals, `lib/ssh-handling.bash`'s two
comments (the elif block and the per-file Version header), the `CCY_VERSION` line in
`claude-yolo`, and the `docs/ccy-changelog.md` 3.43.1 entry — all now state the
corrected framing (explicit precedence / dropped dependency, not a bug fix) and note
the `gh`-binary caveat. Re-running QA and dispatching a second `qa-reviewer` pass
before commit.

**Lesson for Plan 00090**: verify claims about `gh`/CI-runner behaviour empirically
before writing them into a merge or a plan document — this is the second time in
this general area (lts-infra Plan 00045's own history records a prior instance of an
agent asserting an owner decision that was never made) that an unverified premise
almost shipped as fact.

## 11:50 · finding · T1.1 — second pass caught a second unverified claim, in the same paragraph

Second `qa-reviewer` pass: still FIX-BEFORE-MERGE. My own correction paragraph had
claimed the branch "touches no file under `files/var/local/claude-yolo/`" — false,
verified now by actually reading the diff rather than trusting a prior session's
summary: `git diff --stat origin/F44 origin/plan-00066-ccy-ci-runner --
files/var/local/claude-yolo/` shows 10 files differ, `lib/ssh-handling.bash` itself
among them (128 lines). Read the file's full diff this time, not just the stat: every
hunk is the branch's copy *missing* F44-only robustness work added since the fork
(`resolve_token_owner_login()`'s retry/validate logic, CCY 3.36.0) — stale, not a
competing implementation. The narrower claim that actually matters holds, but I'd
written the broader, unverified one. Also caught: an overclaim in the opposite
direction, calling gh's GH_TOKEN precedence an "internal" behaviour — `gh help
environment` documents it explicitly. Fixed both across every touched file
(this plan, `CLAUDE/Plan/README.md`, Plan 00090's PLAN.md, `ssh-handling.bash`,
`docs/ccy-changelog.md`), and flagged in Plan 00090 that `ssh-handling.bash` — absent
from the original 18-file `git merge-tree` conflict list, computed before this
plan's edit existed — may newly conflict there once this edit lands.

## 12:05 · finding+action · T1.1 — third pass: PASS WITH NITS, committing

Third `qa-reviewer` pass, reading every touched file in full: no remaining trace of
the refuted premise anywhere in the repo; the `elif` confirmed reachable on the
`--no-ssh` path with no early return ahead of it; `./scripts/qa-all.bash`
independently re-verified green; the `CLAUDE/Plan/README.md` row confirmed under the
500-char index-row-length limit. Two nits, both fixed: a missing `---` section
separator in `docs/ccy-changelog.md`, and a reminder to stage only the 5 intended
paths — this working tree also carries pre-existing, unrelated untracked/modified
content (a mode-only change on `scripts/git-hooks/README.md`, a 708 MB
`scripts/qa-ccy/` node_modules directory) that must not be swept into this commit.

**Separately flagged to the user, not part of this plan's own scope**: the second
pass's own empirical test of `gh auth token` echoed a live, unmasked `gho_…` token
(broad scopes: admin:org/repo/workflow) into that subagent's transcript on local
disk. Not read further by me. Rotation and any local transcript cleanup are the
user's call — a real operational tradeoff (breaks the live `gh` session) that isn't
mine to make unilaterally.

Committing now: `files/var/local/claude-yolo/lib/ssh-handling.bash`,
`files/var/local/claude-yolo/claude-yolo`, `docs/ccy-changelog.md`,
`CLAUDE/Plan/00089-ssh-handling-runner-token-guard/`, `CLAUDE/Plan/README.md` —
explicitly, not `git add -A`.
