# Plan 00048 — Journal 2026-09-12

> **Append-only activity log** for plan 00048. One file per day
> (`00048-Journal-YY-MM-DD.md`). Entry grammar and the append-only discipline
> are documented at the top of `00048-Journal-26-09-02.md` and in
> `CLAUDE/PlanJournalling.md`.

## 15:40 · finding · T4.3 — `cc` silently switches account when no token passes the guessed expiry stamp

Reported symptom, user's words: "the cc command on desktop does not seem to be
using ccy tokens for some reason", on a laptop freshly upgraded F43 → F44 with
`play-claude-code.yml` and `play-claude-yolo.yml` re-run.

Traced in the checkout and verified line by line. The two launchers answer
"no valid token" in opposite ways:

```
token-management.bash select_token()
  1043-1050  host mode, token dir missing  -> banner, SELECTED_TOKEN="", return 0
             container mode, same          -> return 1
  1093-1102  host mode, zero VALID tokens  -> banner, SELECTED_TOKEN="", return 0
             container mode, same          -> return 1
```

`cc:162-165` reads an empty `SELECTED_TOKEN` as "Desktop" and launches plain
`claude` against host `~/.claude/` OAuth. `claude-yolo:1099-1115` treats the
non-zero return as a hard stop ("You must create a new token") and exits 1.

So **`ccy` can never run without a named token; `cc` silently runs as a
different account.** No error, no non-zero exit, no prompt — only a banner that
scrolls past. That is the reported symptom exactly.

### Why it surfaced now

Validity is decided by a **guessed** date baked into the filename, not by the
token's real expiry:

```bash
# token-management.bash:741-746
# Use conservative 90-day expiry estimate
# NOTE: claude setup-token doesn't tell us when the token actually expires,
expiry_date=$(date -d "+90 days" +%Y-%m-%d)
```

and `is_token_valid` (`common-pure.bash:113-119`) treats `expiry_date < today`
**or `== today`** as expired. Tokens minted during this plan's June host work
(2026-06-10 / 2026-06-18) stamp out at 2026-09-08 … 2026-09-16. Today is
2026-09-12, so some or all of the pool has just crossed the stamp — while very
possibly still authenticating fine. `ccy` reacts by demanding a fresh token, so
it always ends up on a valid one; `cc` drops them from the menu and falls
through to Desktop.

A rebuilt `$HOME` on the new F44 install reaches the same branch by the other
route: `play-claude-yolo.yml:408-424` creates the token tree but never
populates it, so the pool is empty.

### The defect is a conflation, not the Desktop fall-through itself

An empty pool falling through to Desktop **is** designed — PLAN.md Goals ask
for exactly that ("Empty pool: Desktop is the only option and a one-shot banner
explains `ccy --create-token`"). The bug is that `1093-1098` cannot tell
"pool is empty" from "pool has tokens, all past a guessed stamp". The first
deserves the short-circuit; the second must not silently change account, and
the same Goals require Desktop to be "explicit, not silent".

Task 4.3 — empty-pool banner and Desktop fall-through — is the one Phase 4
check never executed. It is precisely this branch.

## 15:40 · action · — — usage-cache task fixed (unrelated defect found in passing)

`play-claude-yolo.yml:352` addressed the cache with
`ansible_facts['env']['HOME']`, the trap Plan 00088 fixed and which
`play-claude-code.yml:20-28` documents at length: under `gathering = smart`
with jsonfile fact caching, a cached fact from an elevated play resolves it to
`/root`, so a full `playbook-main.yml` run deleted
`/root/.claude-tokens/ccy/usage-cache` and left the user's stale cache in
place. Now `/home/{{ user_login }}/…`, matching every other path in the play.
Cosmetic in effect (a stale usage column), but the same class of silent wrong
target. `qa-all.bash` green, 696 files.

## 15:40 · handoff · T4.3 — awaiting a decision on the fix shape, immediate workaround available

**Immediate unblock for the user, no code change:** `ccy --create-token` mints a
freshly stamped token and the symptom disappears.

The code fix is a real choice and touches `select_token`, which **both**
launchers call — so a mistake breaks `ccy` too, and it cannot be tested in the
CCY container (no `claude` binary, no token pool). Deliberately not guessed.
Two shapes, both satisfying the Fail Fast rule:

- **A — hard stop (minimal).** In host mode, short-circuit to Desktop only when
  the pool holds no token files at all; when files exist but none pass the
  stamp, `return 1` as container mode does. `cc:86` calls `select_token` bare
  under `set -e`, so cc exits with the expired list and renew instruction
  already printed by `1071-1091`. ~6 lines, no menu changes, container mode
  untouched. Cost: Desktop becomes unreachable via `cc` while the pool is
  stamped out (plain `claude` still works).
- **B — Desktop as a keypress (better UX, more risk).** Render the menu with
  stamped-out tokens listed and selectable, plus Desktop as `d`. Because the
  stamp is a guess, a working token stays usable. Needs changes in the redraw
  loop from `1108` down, which is shared with container mode.

Either way: `CCY_VERSION` bump required in the same commit (lib change), then a
host deploy of `play-claude-yolo.yml` and `play-claude-code.yml`.

Three further hardening items found in the same read, none of them today's
symptom, all in `cc`: `$HOME/.claude` is hardcoded at `cc:56`/`cc:128` so a set
`CLAUDE_CONFIG_DIR` makes the credential parking target a file Claude Code is
not reading; the park at `cc:155-161` is never asserted, so a failed park
degrades silently to Desktop auth; and the self-heal at `cc:64-66` skips when
both files exist, after which `cc:156`'s `mv -f` destroys the parked Desktop
credential. Worth folding into whichever fix is chosen.

## 15:52 · decision · T4.3 — shape A chosen, with all three hardening items

User picked shape A (hard stop) over B (Desktop as a keypress), and asked for
all three `cc` hardening items in the same change. A was recommended on risk:
`select_token` is shared with container-mode `ccy`, and neither shape can be
tested in the CCY container, so the minimal change that leaves the redraw loop
alone is the one that cannot break the user's primary launcher.

B is not rejected on merit — listing stamped-out tokens as selectable is the
better UX precisely because the stamp is a guess. It stays available as a later
task if the hard stop proves annoying in practice.

## 15:52 · action · T4.3 — fix landed as CCY 3.50.0, awaiting host deploy

`token-management.bash` host mode now requires BOTH `valid_tokens` and
`expired_tokens` to be empty before short-circuiting to Desktop. With expired
tokens present it falls through to the pre-existing `return 1`, which container
mode already used. Container mode is byte-for-byte unchanged in behaviour.

`cc` no longer leaves that return to bare `set -e` — it reports it explicitly
and exits 1, because an abort with no closing message is poor form in a script
a human is sitting in front of, and because Desktop must not be offered as a
consolation prize. The three hardening fixes:

- `CLAUDE_CONFIG_HOME="${CLAUDE_CONFIG_DIR:-$HOME/.claude}"` now anchors
  `CRED_FILE`. Scope confirmed from the documented wording captured in Plan
  00098 `research/raw-findings.md:240`, which also settles Decision 7's
  rejection of the variable as "undocumented in scope" — it is documented.
- The park is asserted with `[ -e "$CRED_FILE" ]` after the move and fails fast
  if the file is still there.
- The both-files-present state is refused up front, with the two recovery
  commands printed, and the park now uses `mv` rather than `mv -f`.

Two paths were deliberately NOT relocated, both commented in place so a future
reader does not "fix" them: `.last-launch.conf` stays under `$HOME/.claude`
because its reader is the hooks daemon, which resolves the fixed pair
`(".claude", ".last-launch.conf")` under `$HOME` in
`status_line/account_display.py` — pointing it at a relocated config dir would
silently blank the status line. And `$HOME/.claude.json` stays put because it
is a *sibling* of `~/.claude`, so the documented "every `~/.claude` path"
wording does not decide it; relocating on a guess would seed the onboarding
flag into a file `claude` may not read and reintroduce the login-wizard bypass.

Verification done here: `bash -n` and `shellcheck -x` clean on both files,
`qa-all.bash` green at 696 files. NOT verified: any runtime behaviour — there
is no `claude` binary and no token pool in this container, which is the whole
reason Task 4.3 is still open.

## 15:52 · handoff · P4 — host deploy then re-verify 4.3, 4.6, 4.7

Deploy on the host (never from the container):

```
./playbooks/imports/play-claude-yolo.yml     # ships the lib fix
./playbooks/imports/play-claude-code.yml     # ships the cc wrapper
```

Then the three open Phase 4 checks, 4.3 against the FIXED behaviour: empty pool
still banners through to Desktop; a pool of only expired tokens now refuses
with the renew instruction rather than switching account; 4.6 Desktop selection
from a populated pool; 4.7 `ccy` chooser still offers create/renew unchanged.
4.7 matters more than usual this time — the edit is in the function `ccy` also
calls, even though container mode's branches were not touched.

## 18:38 · action · T4.3 — the changed branches ARE runtime-testable here after all

The earlier handoff said runtime behaviour could not be verified in the
container. That was too pessimistic and worth correcting: **every branch this
fix touches returns before `select_token` reaches its `read -p` menu**, so all
of them can be driven with no terminal, no `claude` binary and no real token.
`is_token_valid` only parses the date out of the FILENAME and never opens the
file, so a fixture holding a placeholder string exercises the real code path.

Wired in as a permanent gate rather than a throwaway check, following the
`test-ccy-rootless-guard.bash` precedent exactly (a ccy pure-function suite in
`scripts/`, run by `qa-all.bash`, reporting `passed: N`):

- `scripts/test-ccy-token-mode.bash` — 10 cases. Container mode across all three
  unusable-pool shapes (must stay `return 1`), host mode's designed Desktop
  short-circuit (missing dir, empty pool), host mode's fixed refusal
  (expired-only), the `is_token_valid` stamp boundary including "expires today
  counts as expired", and a discrimination check asserting host mode answers an
  empty pool and an expired-only pool DIFFERENTLY — that conflation is the bug.
- Wired into `scripts/qa-all.bash` beside the rootless guard, so it reaches CI
  for free (`.github/workflows/qa.yml` runs `qa-all.bash`, and that file states
  the convention outright).
- `CLAUDE/QA.md` gate table updated: eight further gates -> nine, four `test-*`
  suites -> five.

**Control fixture, per the qa-gates rule that a gate change must be proved with
something that could have failed.** Swapped the pre-fix library in from `HEAD~1`
and re-ran the full gate:

```
qa-all exit=1
  PASS  expired tokens only                       -> rc=1 sel=EMPTY      (container, unchanged)
  FAIL  expired tokens only -> refuse             -> rc=0 (want 1)       (the bug)
        the two cases are conflated — that IS the bug this test guards
✗ QA FAILED: ccy token-mode unit tests
```

So the gate discriminates rather than merely passing: the container cases stayed
green while exactly the fixed case went red. The library was then restored and
confirmed byte-identical to the committed version before anything else ran.

Green state now: `qa-all.bash` exit 0, `ccy-token-mode: passed: 10`, 698 files.
`shellcheck -x` clean on both the new suite and the edited `qa-all.bash`.

This narrows the handoff below: Task 4.3's LOGIC is now covered by an automated
gate. What still needs the host is the end-to-end path the gate cannot reach —
that `cc` actually launches, that a chosen token authenticates, and 4.6/4.7.

## 18:40 · finding · T4.3 — correction: shape B was cheaper than the decision entry said

The 15:52 decision entry framed shape B as needing "Desktop as a keypress"
built. **It already exists.** Reading the menu code below the redraw loop:

```bash
# token-management.bash ~1201
if [ "$mode" = "host" ]; then
    prompt_hint="1-${#valid_tokens[@]}, d${usage_hint}"
...
# ~1240
if [ "$mode" = "host" ] && [ "$selection" = "d" ]; then
    return 0
```

So whenever the menu renders at all, host mode already advertises `d` and
already handles it. The only thing B would actually add is putting
stamped-out tokens into the selectable list — the index maths and the `d`
handler are done.

This does not change the choice: A is still the lower-risk change, it is what
the user picked, and it is what shipped. But B is a cheaper follow-up than the
earlier entry implied, and worth reaching for if the hard stop proves annoying
in practice rather than treating it as a big rework.

It also sharpens what A actually costs. Desktop is NOT unreachable from `cc`
generally — it is one keypress away whenever at least one token is valid. The
cost is narrower than stated: Desktop is unreachable from `cc` only in the
specific state where every token has lapsed, and there the remedy is
`ccy --create-token` or plain `claude`.

## 19:05 · finding · T4.3 — the 3.50.0 fix introduced an unbounded loop at the menu

The `qa-reviewer` pass on this branch returned BLOCK, and it is right.

3.50.0 made host mode able to return non-zero, so `cc` grew
`if ! select_token "$TOKEN_DIR" "host"; then`. **Bash suppresses `errexit` for
the entire dynamic extent of a command in an `if`/`!` condition.** That switched
`set -e` off inside `select_token` — including its `read -r -p` menu, whose
failure on EOF used to be fatal. With it off: `read` fails on EOF, `selection`
is empty, the empty branch `continue`s, `read` fails again. The menu spins.

Reproduced here against the committed blobs, one valid fixture token, stdin
`</dev/null`:

```
PRE-diff shape (bare call)         rc=1    lines=13
POST-diff shape (if ! call)        rc=124  lines=1548446
```

A human pressing Ctrl-D at the chooser got a hang at ~250k lines/second where
they previously got a clean exit. Violates `CLAUDE/InteractiveScripts.md` rule
02 (bounded retries) and rule 03 (EOF aborts cleanly).

`ccy` calls the same function through `|| { … }` — also a condition context — so
container mode had this hole ALREADY, pre-dating 3.50.0. Fixed once, in
`select_token`, which closes it for both launchers.

Two independent defences, because either alone still leaves a way to spin:
EOF returns 2 with a "cancelled" message, and invalid selections are capped at
three before giving up. Return codes are now a documented contract — 0 chose,
1 usage error, 2 cancelled, 3 no usable token — and `cc` reports each
separately, replacing a single message that asserted "every token is past its
expiry date" for causes that were nothing of the kind.

## 19:10 · action · T4.3 — the new gate had to be made to discriminate, twice

First attempt at gating the loop asserted only the exit status. The control run
**passed**, which is the failure mode a control exists to expose: the retry cap
alone terminates an EOF run, so the test still went green with the EOF check
deleted. It was testing the property, not the mechanism.

Each menu case now also asserts the message naming which defence fired. Both
controls then behaved:

```
# EOF check removed:
FAIL  EOF at the prompt -> cancelled      -> rc=2 but never said "Cancelled"
# retry cap removed:
FAIL  runaway invalid input -> gives up   -> rc=2 but never said "Giving up"
```

Library restored byte-identical after each. Suite is 13 cases, green, and the
menu cases run under a hard `timeout` so a future unbounded loop fails the gate
instead of hanging CI.

## 19:15 · finding · T4.3 — three claims in the 15:52 entry were overstated

Also from the review, and worth recording against the entries that made them:

1. **The park assertion's rationale was wrong.** The 15:52 entry (and the
   changelog, and the code comment) said a failed park "previously left the
   credential in place and launched anyway". It did not: the `mv` is in an `if`
   BODY, where `set -e` applies, so a failing `mv` already aborted with its own
   message. The assert is belt-and-braces for the narrower case of an `mv` that
   exits 0 and still leaves the source. Code comment and changelog corrected;
   the assert stays.
2. **The `CLAUDE_CONFIG_DIR` symptom was narrated in the past tense** ("the
   session ran on the Desktop account while `/status` reported…") when nothing
   in this repo sets the variable and the state has never been reached here. The
   original journal wording was correctly conditional; it hardened into observed
   history on the way into the code and the changelog. Both restated as the
   deduction they are.
3. **"Cannot be tested in the CCY container" was asserted, not established** —
   already corrected at 18:38, and the review independently found the same
   thing by writing the test in about two minutes. Worth naming as a pattern:
   the untaken test was the one that would have caught this regression.

`DECISIONS.md` Decision 7 also still recorded `CLAUDE_CONFIG_DIR` as rejected
for "undocumented scope" while the code now honours it; a superseding note was
added there rather than editing the original decision.

## 19:40 · finding · T4.3 — second review pass: my own evidence block was spliced

The `qa-reviewer` second pass independently reproduced the RED run and found my
18:38 entry misrepresents it. Correcting here rather than editing that entry.

**The 18:38 RED excerpt spliced two non-adjacent lines** and reads as one
discriminating failure. The real pre-fix run produced **two** failures,
`passed: 8   failed: 2` — the fixed case AND the separate "discrimination
check", firing together.

**That discrimination check was tautological**, which is why it fired in
lockstep. It re-invoked the empty-pool and expired-only fixtures that the cases
above it already assert at rc=0 and rc=3. While both of those pass it cannot
fail; when one breaks it adds a second failure line for one defect. Its comment
claimed it guarded against "host mode returning non-zero unconditionally" — but
the `empty pool -> Desktop` case already catches exactly that. It has been
removed rather than reworded, and the case count is honest at 14 without it.

A control that FIRES is not the same as a control that DISCRIMINATES, which was
the very thing that entry set out to demonstrate, and I got it wrong in the
demonstration itself.

**Coverage is now stated as a number, not implied.** `select_token` has 12
return points; the suite exercises 9. The two cheaply reachable gaps were closed
in this pass — the `d` Desktop keypress (load-bearing: it is the whole reason
the 3.50.0 hard stop is acceptable) and the invalid-mode usage error. The three
still uncovered need something the container cannot provide: container
create/renew call `create_token`, which needs a real `claude` binary and an
interactive OAuth round trip, and the post-redraw `return 1` is reachable only
after a usage fetch, which costs a billed API request. The summary line now says
so, because a green run must not read as "all of this code is good".

Also corrected: line 131's "Container mode is byte-for-byte unchanged in
behaviour" — the bytes did change, the behaviour did not. And the fixture dir
now uses `mktemp -d` instead of `$$`, which collides across PID namespaces
sharing this bind-mounted repo.
