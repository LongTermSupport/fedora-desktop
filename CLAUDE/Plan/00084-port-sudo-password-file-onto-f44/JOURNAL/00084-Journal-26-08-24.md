# Plan 00084 — Journal 26-08-24

> **Append-only activity log** for plan 00084. One file per day
> (`00084-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:30 · action · — — plan scaffolded

Plan 00084 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:45 · action · T1.1–T1.4 — mechanism ported, QA clean

Diffed `86ba6ae` (Plan 00073) against its parent to isolate the sudo-password-file
feature (202 lines in `run.bash`). A raw `git cherry-pick -n 86ba6ae` onto a fresh
`F44` branch conflicted on two things: the plan-folder path (`CLAUDE/Plan/00073-…` now
belongs to a different, unrelated plan on `F44` — `Cancelled/00073-ccy-token-usage-limits`
— since each branch's `--local` plan counter allocated 00073 independently) and
`run.bash` content (PR #33's own nearby preflight/help edits). Dropped the stale
plan-folder files from the cherry-pick entirely (this port gets its own plan, 00084,
not a resurrection of the old one) and abandoned the cherry-pick in favour of manually
applying the isolated patch onto current `F44` `run.bash` — cleaner than fighting a
conflicted 3-way merge across 90 commits of divergence for a patch this well-contained.

Ported: version header + changelog entry, the new `_sudo`/`hl_sudo_askpass_start`/
`hl_sudo_probe_password` helpers (inserted after `hl_resolve_secret`, before
`headless_preflight`), the sudo-credential decision block inside `headless_preflight`
(composed with PR #33's existing `GITHUB_ACCOUNTS` branch just above it, untouched),
`main()`'s `HL_SUDO_OPTS=()`/`HL_SUDO_PW_FILE=""` init, `--help`/`--help-run-headless`
doc text (short usage line, PRECONDITIONS bullet, SECRETS table row, CANONICAL
INVOCATIONS addendum), `run_playbook_with_issue_option()`'s and the final main-playbook
invocation's become-password-file branch, and all 14 bare `sudo` → `_sudo` call-site
swaps (dnf install, grubby ×4, chown, hostnamectl ×2, gh-cli dnf ×3, reboot ×2) —
verified via `grep '[^_]sudo '` that zero bare invocations remain outside `_sudo`'s own
definition, `hl_sudo_probe_password`, and prose/comments.

`docs/headless-provisioning.md`/`docs/headless-server-install.md`: Plan 00073's own doc
diff (`git diff 86ba6ae^..86ba6ae -- docs/...`) applied cleanly via `git apply` —
`git apply --check` reported no conflict, so F44's doc drift since that commit's parent
didn't touch the same lines. While there, found and fixed a stale claim sitting right
next to the new sudo-precondition bullet in `headless-provisioning.md`: it still said
`GITHUB_ACCOUNTS=none` was "a planned follow-up" that "currently fails fast" — false
since PR #33 merged — and its secrets-required table still marked
`GITHUB_TOKEN_FILE`/`GITHUB_SSH_PASSPHRASE_FILE` unconditionally required and
`VAULT_PASSWORD_FILE` conditional-on-vault-use, the reverse of what `run.bash
--help-run-headless` (this doc's own stated authority — "if the two ever disagree, the
--help output wins") actually requires. Fixed both to match the script.

Verification: `bash -n run.bash` clean; `shellcheck -x run.bash` — 0 findings; full
`./scripts/qa-all.bash` — QA passed, 537 files checked (the repo-wide 137-issue
shellcheck advisory count is pre-existing baseline noise unrelated to this change —
`run.bash` alone shellchecks clean standalone).

Not yet committed. Next: commit on `plan-00082-port-sudo-password-file` (branched off
`F44`@`d7ccd83`), push, open a PR — not a direct push, per this plan's Goals: a
cross-repo mechanism port onto the default branch warrants review, unlike the
plan-local scripts this whole investigation started from.

## 15:20 · finding · T2.1–T2.2 — committed, PR #34 opened, independent review returned
MERGE-READY

Committed `f38db91`, pushed `plan-00082-port-sudo-password-file`, opened PR #34 against
`F44`. Dispatched a general-purpose agent standing in for this repo's own required
`qa-reviewer` pass (no such agent type is registered in this session's launcher),
instructed to be adversarial specifically about the manual-port risk: a dropped hunk, a
misplaced conditional, or bad composition with the already-merged `GITHUB_ACCOUNTS=none`
code.

**Verdict: MERGE-READY**, conditional on one fix. Findings:

- **F1 (fix-before-merge)**: `docs/headless-server-install.md`'s prerequisites table
  still said "GitHub account + scoped PAT … mandatory in headless v1" on a row this
  commit's own table-reflow re-emitted — contradicted the script's own
  `--help-run-headless`. Fixed: the row now states the account-or-`none` choice.
- **F2 (non-blocking, verified independently)**: Plan 00073's `_sudo` argv acceptance
  harness (`_acceptance-cases.inc.bash`/`acceptance.bash`) wasn't ported — it lived in
  the old 00073 plan folder, dropped from this port along with the rest of that folder
  (Task 1.2's plan-number-collision note). The reviewer re-derived the harness's core
  property by hand — the shipped `_sudo` must build argv with NO empty leading
  argument when `HL_SUDO_OPTS` is empty (the exact defect a naive `"${arr[@]:-}"`
  rewrite would introduce, invisible to `bash -n`/`shellcheck`/`qa-all.bash`, and would
  break every pre-existing sudo call site) — extracted the shipped function from this
  PR's `run.bash`, ran it under both empty and `-A` option sets, and separately proved
  discrimination by perturbing a copy to the `:-` form and confirming shellcheck's
  SC2068 fires on the perturbation but not the original. Property holds; recommended
  as a follow-up standing check, not a merge blocker.
- **F3 (fix-before-merge)**: this plan's own `PLAN.md` still said Task 2.1 was
  un-started and the port "not yet committed" — stale the moment the commit landed.
  Fixed both the task checkbox and the Delivery & Milestones section.

Also independently reverified, not merely trusted from the earlier journal entry: all 14
`sudo`→`_sudo` call sites present with exact line numbers, zero bare `sudo` survives
anywhere in the file outside `_sudo`'s own body/prose, the sudo-credential branch sits
unconditionally after the `GITHUB_ACCOUNTS` block closes (both GitHub paths reach it),
`HL_SUDO_OPTS`/`HL_SUDO_PW_FILE` are initialized before every read site under `set -u`,
both Ansible invocation sites check `HL_SUDO_OPTS` before the NOPASSWD probe (so the
preflight-established timestamp is never discarded), `hl_cleanup`'s EXIT trap shreds
both new secret files, `bash -n`/`shellcheck -x` still clean, and `qa-all.bash` still
reports the same 537-files-checked pass.

Next: merge PR #34, then notify `lts-infra` Plan 00045 (Task 2.3) so its Task 3.1b can
re-measure the merged commit and bump the pin.
