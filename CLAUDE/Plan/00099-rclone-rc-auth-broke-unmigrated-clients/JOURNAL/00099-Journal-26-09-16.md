# Plan 00099 — Journal 26-09-16

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 13:40 · finding · T5.6 — this plan's own diff was nearly unfindable

Picked up because Task 5.6 is the only open task and closing it closes the plan. Locating
the diff took longer than it should have, and the reason is worth writing down rather than
just working around.

`git log --grep='00099'` returns **one** commit, and it is unrelated (`d1f0529c`, a
tree-wide comment renumbering). Nothing in the repository references this plan number.
`PLAN.md`'s Delivery section records no hash — only the line "Plan opened; root cause
confirmed by triage".

The delivery commit is **`942fb724`**, found by asking git which commit introduced
`files/home/.local/bin/rclone-rc-auth.bash`. Its message reads *"Plan 00072: authenticate
every rclone RC client, not just one"*.

**That is not a mislabel pointing at an unrelated plan.** This plan was created as 00072,
collided with the real Plan 00072 (`ccy-assert-rootless-engine`), and was renumbered
afterwards — `git show --stat 942fb724` shows the folder it created carrying a
`00072-Journal-26-08-16.md`. It is the collision `CLAUDE/Plan/CLAUDE.md` documents, where
the counter is `--local` git config and is never pushed, so two clones allocate
independently. Plan 00079 is recorded there as a second instance of the same thing.

The commit is a single one covering the whole plan: the library, all four migrated
clients, both plays, the drift gate and its `qa-all.bash` wiring.

Recorded in the Delivery section, with the renumber explained, so the next reader does not
repeat the search. Also noted there that `scripts/qa-deployed-drift.bash` has been edited
by Plans 00081, 00110 and 00122 since — this plan owns its introduction, not its current
state, and a review of "the full diff" has to keep those apart.

## 13:42 · action · T5.6 — qa-reviewer dispatched

`qa-reviewer` running over `942fb724` with the renumber trap stated up front, so it does
not lose the same time I did.

One thing handed to it that I noticed while reading `PLAN.md` and could not resolve from
the plan text: Task 5.2 records `acceptance.bash` failing **9 failed / 1 passed** before
deploy, and Task 5.4 records it passing **ACCEPTED 8/8** after. Nine plus one is ten;
eight plus zero is eight. **Two assertions are unaccounted for between the two runs**, and
a case that silently stops running is this repo's recurring defect rather than a rounding
difference. The script is HOST-only and refuses to run in this container, so it can be
read but not driven from here.

Also flagged for it: `qa-deployed-drift.bash` reports its skips as
`✓ deployed-drift: skipped (…)` — a tick, which `helpers/qa_environment/verdicts.py`
parses as a stage symbol. Whether that is a defect in its own right is the reviewer's
call; it was found while working Plan 00128 on an unrelated gate and it belongs to this
plan's deliverable.
