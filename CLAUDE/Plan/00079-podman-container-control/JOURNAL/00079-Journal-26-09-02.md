# Plan 00079 — Journal 2026-09-02

## 22:10 · action · — — PLAN.md slimmed below the size threshold

PLAN.md had grown past the daemon's 25 KB warning tier (26.5 KB) because the
Facts (F1–F23), the nine decisions (D1–D9) and the risk table were all inline.
The document was restructured, nothing was destroyed:

- The old PLAN.md was renamed to `PLAN_archive.md` and kept verbatim (one
  archive banner line added at the top), so every fact, decision and task
  note remains readable in its original form and in git history. The staged
  cross-reference update in F18 (Plan 00072 → Plan 00099, from today's
  sibling-plan renumbering) is preserved there.
- Facts F1–F23, the F15/F17 safety-case note and the Risks & Mitigations
  table moved to `RESEARCH-facts.md`.
- Decisions D1–D9 moved to `DECISIONS.md`, text unchanged.
- The new lean PLAN.md keeps the header, Overview, Goals, Non-Goals, a short
  Context pointing at the two supporting documents, the task tree with every
  tick exactly as it was (Tasks 0.1–3.3 ✅, 3.3b and 3.4 ⬜), Dependencies,
  Success Criteria (all still unticked) and Delivery & Milestones, with two
  milestone lines added for Phase 1 landing and the acceptance runs 3–5.

Nothing about the plan's state changed: Task 3.3b (re-run `deploy.bash` on
the HOST against `podfreeze` and CCY 3.40.1) is still the next action, then
Task 3.4 closes the plan.
