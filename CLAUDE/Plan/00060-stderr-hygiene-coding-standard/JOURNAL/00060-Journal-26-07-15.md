# Plan 00060 — Journal 26-07-15

> **Append-only activity log** for plan 00060. One file per day
> (`00060-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:49 · action · — — plan scaffolded

Plan 00060 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:52 · action · T1.1 — gh-<alias> wrapper status line → stderr

Root cause confirmed in `playbooks/imports/play-github-cli-multi.yml`: the
generated `gh-{{ alias }}()` wrapper (the `gh-<alias>` consumer path) did
`echo "Switching to {{ username }}..."` on **stdout** at line 781, before
`gh "$@"`. Any `$(gh-<alias> … --json)` capture got that chatter mixed into the
JSON — but only when the account wasn't the box default (the `current_active`
branch). One-word fix: appended `>&2` (plus a WHY comment). Note the sibling
`gh-switch` line uses a different message format (`Switching to <alias>
(<user>)...`) and is a manual command, not a gh passthrough — left for the
audit to classify rather than blanket-fixing.

## 12:55 · action · P2 — repo-wide audit dispatched

Launched a background general-purpose audit agent over bash executables,
generated bash includes, playbook shell blocks, `scripts/`, and `helpers/`,
classifying candidates HIGH/MED/NOT (help/status/report commands whose stdout
is meant for humans are explicitly NOT bugs). Awaiting its report to drive
Phase 2 fixes.

## 12:56 · finding · T1.2 — QA green on the gh fix

`./scripts/qa-all.bash` → exit 0 (355 files; 74 playbooks syntax-OK). The 51
shellcheck items are advisory `warning`/`info`, pre-existing, non-gating.
