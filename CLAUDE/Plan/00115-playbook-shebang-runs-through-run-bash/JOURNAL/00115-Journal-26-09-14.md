# Plan 00115 — Journal 26-09-14

> **Append-only activity log** for plan 00115. One file per day
> (`00115-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:45 · action · — — plan scaffolded

Plan 00115 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:10 · action · T1.1–T1.3 — shebang rerouted, executors made explicit, docs back to paths

Prototype first: a temp repo with a stub run.bash proved the `${p%/playbooks/*}/run.bash`
form resolves the root from the repo root, from `/` by absolute path, and from inside
`playbooks/` — the earlier `${0%playbooks/*}` form failed the last shape because `$0` had no
`playbooks/` prefix there; `realpath` first fixes it. New line is 93 bytes.

`make-playbooks-executable.bash` writes the new line and migrates the previous
cd-and-exec-ansible-playbook form as its legacy line; run across `playbooks/`, 78 files now
carry it byte-identically and `qa-ansible` (updated to the same string) reports 77 playbooks
correct. run.bash's five play-file executions — runner ×3, headless optional loop, main
playbook ×3 branches — now call `ansible-playbook <file>`; grep confirms none remain. The 73
doc lines went back to `./playbooks/…`; `docs/playbooks.md` and README explain the routing.
The estate never executes a play file on a guest directly, so its headless path is only
affected through run.bash's own explicit calls.

The mechanical doc revert briefly touched Plan 00114's PLAN.md and helper script; both were
restored from HEAD — a plan's documents describe what that plan did.
