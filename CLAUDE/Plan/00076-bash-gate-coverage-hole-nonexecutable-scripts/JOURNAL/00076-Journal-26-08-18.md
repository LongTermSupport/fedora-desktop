# Plan 00076 Journal — 2026-08-18

## origin · found by refusing to accept a zero

This plan exists because of a zero I did not believe.

Widening Plan 00075's `bash-capture-discards-status` rule to `files/**` reported
**0 new findings**. That is a suspiciously clean result for a tree containing 30
raw `$(… 2>/dev/null)` captures, and "a gate reporting clean because its own
machinery is broken" is the exact defect Plan 00075 is about — so the zero got
tested rather than banked.

The first control I wrote was itself wrong twice, which is worth recording:

1. I scanned the fixture `.semgrep/bash-conventions.bash` with the widened
   config and got 0 hits, and briefly read that as "the rule is broken". It was
   not: `paths.include` applies to a *scan*, and the fixture is not under
   `files/`. `semgrep --test` does **not** apply path filters — confirmed
   separately, `5/5: ✓ All tests passed` — so the fixture assertions are real.
2. My second control copied the launcher to `/tmp` — also outside
   `paths.include`. Another invalid zero.

The valid control was a canary planted **inside** the tree, which did fire. So
the rule worked and the tree was genuinely clean… except `.paths.scanned` from
the directory-wide run listed every `lib/*.bash` beside the launcher and
`docker-in-lxc`, and **not `claude-yolo` itself**.

## finding · the execute bit decides whether a file is linted at all

`semgrep/target_manager.py:1024`:

```python
elif not path_has_permissions(path, stat.S_IRUSR | stat.S_IXUSR):
    return None
```

`get_shebang_line()` refuses to read a file that is not owner-executable. For a
file with no recognised extension the shebang is the *only* language signal, so
no execute bit ⇒ no language ⇒ never scanned. `git ls-files -s` gives the mode:

| File                                       | Mode     | Scanned |
| ------------------------------------------ | -------- | ------- |
| `files/var/local/docker-in-lxc`            | `100755` | yes     |
| `files/var/local/claude-yolo/claude-yolo`  | `100644` | **no**  |
| `files/var/local/claude-yolo/entrypoint.sh`| `100644` | yes — the `.sh` extension carries it |

`qa-bash.bash` has the same hole from the other direction: its two `find` passes
are "named `*.sh`/`*.bash`" **or** "`-type f -executable` with a shell shebang".
A mode-644 extensionless script is in neither, so it gets no `bash -n` either.

27 tracked scripts are in that state. The census (`shellcheck -S style`) is in
PLAN.md; the number that matters is **SC2155 ×19**, because Task 4.1 of Plan
00075 raised the shellcheck bar to `warning` *for that code specifically* and
then fixed 12 instances. The raise was real; its reach was not.

## note · no new recovery cron

The scaffolder advises creating an hourly failsafe recovery cron. This session
already has one firing — several ticks are in the transcript — so a second would
be duplicate coverage, not extra safety. Not created deliberately, recorded here
so the absence reads as a decision rather than an omission.

## progress · first 15 of the 34, and a census that was incomplete

Working the findings BEFORE widening discovery, so the tree never sits red.

**Four were false positives on correct code**, and the repo blocks
`# shellcheck disable=`, so each was expressed unambiguously instead of
suppressed:

- `SC2054 ×3` — "use spaces, not commas, in array elements" on
  `-o PreferredAuthentications=password,keyboard-interactive`. That comma is
  part of an **ssh option value**. Quoting the element states that and silences
  the check; verified with a two-case probe that the unquoted form reports and
  the quoted one does not.
- `SC1007 ×1` — `GQ_NEW_INSTANCE= setsid …`. The empty value is deliberate and
  per-command. `GQ_NEW_INSTANCE=''` is identical to bash and distinguishable
  from a typo to everything else.

**`SC1090 ×4`** — `source "$SWA_LIB"`, where the path is
`<script dir>/ssh-with-agent.bash` and therefore knowable. A
`# shellcheck source=…` directive is not a suppression: shellcheck then actually
**follows** the file, so the analysis gets stronger, not quieter. Confirmed with
a probe before applying it to the four.

**`SC2120 ×2`** — `sort_uploads` and `prune_orphan_raws` took
`local dir="${1:-$UPLOAD_DIR}"` and no call site has ever passed anything.
Checked the whole function body of each for other positional use (none — the
`local file="$1"` nearby belongs to `sort_one_file`, which starts at 1390, past
`prune_orphan_raws`'s close at 1354), then deleted the unused parameter. YAGNI
is a stated repo principle; dead flexibility is the thing it names.

### The census in PLAN.md measured only shellcheck

Found while reading `ftp-camera`: line 2141 was `sort_uploads || true`. That is
`bash-error-hiding-or-true`, a **semgrep** rule — and one with *no*
`FAIL-FAST-OK` escape by design, because it mirrors the write-time blocker. So
widening coverage will surface semgrep findings too, which the plan's 34 does not
count. Rewritten as the explicit reporting form the rule's own comment
prescribes, keeping the best-effort behaviour while saying so on stderr rather
than discarding it.

Two sub-findings recorded, not yet acted on: several plays deploy these paths
with no explicit `mode:`, and **five of the 27 have no deploying play reference
at all** (`lan-scan`, `qnap-finder`, `tmate-full`, `tmate-share`, `colours`) —
which needs checking before Phase 2 sets any execute bit.

`qa-all.bash` green (446); the six edited files are 0-gating under direct
`shellcheck -x` and all parse.

## progress · shellcheck side clear — 34 to 0 across the 27

**One was a live bug, and it is the one this plan predicted.** `nord` read the VPN
credentials with `local username=$(head -1 "$CREDENTIALS_FILE")`. `local` is the
command whose status is reported, so an unreadable or empty credentials file
yielded an **empty username and password** — which were then written into the
NetworkManager profile, with the write's own failure swallowed by a trailing
`|| true`. The result is a VPN profile configured with blank credentials that
fails to authenticate later, with nothing anywhere saying why. Now: both reads
checked, both values checked non-empty, and the `nmcli modify` failure reported
with what nmcli said instead of discarded.

The other 18 SC2155 were mechanical splits — each read in context first, and
each is followed immediately by an emptiness check or wraps a command that
cannot meaningfully fail (`basename`, `date`, `echo | sed`). One got a small
upgrade on the way past: `ip=$(curl … || echo "unknown")` became
`ip=$(curl …) || ip="unknown"`, so a curl that fails *after* printing cannot
leave partial output glued to the fallback. Checked that an unreachable host
still yields `unknown`.

### The launcher's three write-only variables

`CONFIG_LOADED`, `HOST_GID`, `PODMAN_DEFAULT_DETECTED` — six assignments between
them, zero reads. Each was traced before deletion rather than deleted on the
strength of the warning:

- `CONFIG_LOADED` is not a dropped behaviour. Accepting quick-launch takes effect
  by populating `SPECIFIED_TOKEN`/`SSH_KEYS`/`SPECIFIED_NETWORK`, and declining
  takes effect by leaving them empty. The flag duplicated that state and nothing
  consulted it.
- `HOST_GID` sat under a comment about "proper file ownership" and was used by
  nothing. `HOST_UID` is genuinely read, for the never-run-as-root check.
- `PODMAN_DEFAULT_DETECTED` — the inner `if` in the network scan existed only to
  set it.

### SC1090: a directive is not a suppression

The two remaining findings were `source` calls. They got opposite treatments,
because they are opposite situations:

- The library loop sourced `$SCRIPT_DIR/lib/$lib.bash` for five **repo-owned**
  files. A computed filename is unresolvable, so shellcheck reported five
  unfollowable sources rather than checking the libraries every function in the
  launcher comes from. Unrolled into five explicit `source` lines with
  `# shellcheck source=` directives — the presence check stays a loop, so a
  missing library still fails naming the file. Tested against a stub tree: all
  five load in the original order, and a missing third library fails with rc=1
  having loaded **nothing** (the old loop would have sourced the first two
  before erroring — a partial load that is no longer possible).
- `.last-launch.conf` is user runtime state, not a repo file, so there is
  genuinely nothing to follow. `source=/dev/null` states that fact. Its failure
  is already handled by the `||` branch that reports and resets the file.

CCY 3.37.0 → **3.37.1** with a changelog entry, per the repo's bump rule.

**Shellcheck across all 27: 34 → 0.** `qa-all.bash` green (446). Still to do:
the 58 semgrep findings, then the discovery widening itself.

## progress · `|| true` cleared: 15 → 0, and two more wrong answers found

`bash-error-hiding-or-true` has **no `FAIL-FAST-OK` escape by design**, so every
one had to be rewritten rather than annotated. Several already carried careful
comments explaining why the status was safe to drop — and those comments were
right about the *expected* case and silent about every other one, which is the
whole problem. `|| true` cannot tell "there was nothing to do" from "I could not
do it".

**Two were live wrong answers**, both in `nord remove-all`:

```bash
echo "$connections" | while read conn; do
    nmcli connection delete "$conn" 2>/dev/null || true
    count=$((count + 1))
done
echo "Removed all nordvpn connections"
```

The loop body ran in a **pipeline subshell**, so `count` never survived it — and
nothing read it anyway. Each delete's status was discarded. So the closing line
announced *"Removed all nordvpn connections"* whether every delete succeeded,
some failed, or all of them did. Now a here-string keeps the loop in this shell,
each failure is counted and named with what nmcli said, and the summary reports
the real numbers and returns non-zero when any removal failed.

**The teardown cases got something better than compliance.** `pkill -P` returning
1 when a process has no children was the documented reason for `|| true` in
`ftp-camera`'s cleanup — the bug that once left the FTP server running after
Ctrl+C. Asking `pgrep -P` **first** keeps that fixed *and* leaves a genuine pkill
failure fatal, which `|| true` could not: it discarded both outcomes identically.
Verified with a live process tree that the no-children path skips cleanly under
`set -e`.

The rest: `read … || true` on two delete-confirmation prompts became explicit
EOF-declines (`reply=""`, same safe answer, now stated); `shift || true` became
`if [ "$#" -gt 0 ]`, which also retired a `2>/dev/null` that was hiding shift's
own message; `wait` statuses are captured and logged rather than dropped; and
`exiftool … | awk … || true` — a function whose stdout **is** its return value —
now says in a branch that an empty result is the answer.

### Caught myself inventing an API

Writing the `ftp-camera` teardown I called `_dbg "..."` to log the wait status.
There is no `_dbg` in that script — I had imported a habit from `wsi`. In a
`set -e` cleanup path a command-not-found would have aborted teardown: precisely
the failure the surrounding comment exists to prevent. Found by grepping for the
function instead of assuming it, before it was committed. The line is gone; the
branch now says what it means without inventing output.

**Semgrep: 56 → 41.** `bash-error-hiding-or-true` is at 0. Shellcheck across the
27 stays at 0; `qa-all.bash` green (446).

## progress · `bash-capture-discards-status` cleared: 14 → 0

Eleven of the fourteen genuinely handle the empty case and now say so on the
line, which is the point of the annotation: the reasoning stops being something
a reader has to reconstruct. The best of them was already right and only *looked*
wrong — `wsi` suspends errexit around the transcription call deliberately and
reads `$?` on the very next line, which is textbook-correct; the rule matched the
line's shape, not its behaviour.

**Three were genuinely unhandled**, and one is the plan's own subject matter
turning up in the launcher:

- `claude-yolo` read the Docker context with the status discarded. A query that
  **could not run** — daemon down, socket unreachable — produced an empty string,
  which compares unequal to `rootless`, so ccy announced *"requires rootless
  Docker / Current Docker context: default"* and sent the user to reconfigure a
  context that may have been perfectly fine. That is the **same shape as the
  GitHub-outage misreport fixed in 3.36.0**, in the same file, found only because
  this pass is the first time the file has ever been linted. → CCY 3.37.2.
- `ftp-camera` read a mount's source without checking it. Empty does not fail
  there — it silently produces the **wrong** offset: the prefix strip removes
  nothing and the "relative" path is quietly the full remote path.
- `wsi` could not tell an unreadable audio duration from a duration of zero. Both
  left the variable empty and the `-n` test then skipped the minimum-length pad
  in silence, disabling a correctness step without a word. Skipping is still the
  right fallback — better to attempt transcription than abort — but it now says
  why.

### A non-bug, checked rather than assumed

`find . -maxdepth 2 -name "*compose*.yml" -o -name "*compose*.yaml"` looks like
the classic `-o` precedence trap, where the implicit `-print` binds only to the
last term and `.yml` matches vanish. Built a fixture with both extensions across
two depths: GNU find applies the implicit action to the whole expression, and the
grouped and ungrouped forms produce **identical** output. Left alone. Worth
recording, because "fixing" it would have been a confident change to correct code
— the mirror image of the defect this plan is about.

**Semgrep: 41 → 27.** `bash-capture-discards-status` and
`bash-error-hiding-or-true` are both at 0; `bash-error-hiding-pipe-echo` (21) and
`bash-test-discards-status` (6) remain. Shellcheck stays 0. `qa-all.bash` green.
---

## The gates now see the whole repo — and the mode bit was never the fix

Phases 1 and 2 landed together, because doing Phase 1 properly made Phase 2's
plan wrong.

### Phase 1 — discovery keys on what a file IS

`qa-bash.bash` had two discovery branches: `*.sh`/`*.bash` by name, and
`-type f -executable` plus a shebang sniff. A mode-0644 extensionless script is
in neither. Dropped `-executable` from the second branch, added `! -name "*.j2"`
(a Jinja template is not a shell script), and the count went **125 → 152**.

The naive version cost 87 seconds, up from ~60. Measured rather than guessed
where it went: the sniff loop forked `grep -Iq .` + `head -n1` per candidate,
**8,980 ms** for 992 files. Replaced both with the `read` builtin — **127 ms**,
a 70× drop — with an explicit `[ -r ]` check first, so an unreadable file stops
the gate instead of being quietly filed as "not a shell script". Whole run is
now 43 s, faster than before the widening.

### Phase 1 — the assertion, proved in both directions

Zero-discovery was already guarded ("a discovery that finds nothing is a BROKEN
GATE"). Nothing guarded *partial* discovery, which is the case that actually
happened. Added a coverage assertion: every TRACKED file that is a shell script,
by the same predicate discovery uses, must appear in the discovered set.

`find` stays the discovery source so a brand-new, not-yet-`git add`ed script is
still linted; git is the independent second opinion, not the source.

- **Positive control**: dropped a mode-0644 extensionless script with a known
  defect into the repo root → discovered count 152 → 153. It is seen.
- **Negative control**: copied the gate, restored `-executable` on the second
  branch, ran it → `rc=2`, naming all 27 hidden files plus the canary. Had this
  assertion existed, the hole would have been reported the day it opened.

### Phase 2 — the execute bit was the wrong fix, and Task 2.1 is why

Task 2.1 said derive the deployed mode from the playbooks before touching
anything. Doing that killed the plan's own Decision 1.

Twenty-five of the 27 deploy `0755`. **Two do not**: `/var/local/colours` and
`/var/local/ps1-prompt` are deployed `0644` by `play-basic-configs.yml` because
they are **sourced libraries** — `ps1-prompt` opens with
`source /var/local/colours`. An execute bit on those would be a lie told to a
linter, and semgrep would still never see any future sourced library. A fix that
cannot cover its own category is not a fix.

A correction to record while I am at it: four files (`lan-scan`, `qnap-finder`,
`tmate-full`, `tmate-share`) were noted mid-session as having **no deploying
play at all**. They do — `play-network-tools.yml` and `play-collaboration.yml`
deploy them `0755` through `{{ item }}` loops. The gap was in my grep, which
looked for the literal `src:` path; the loop form never contains it. Worth
writing down because it was one grep away from being filed as an IaC gap that
does not exist.

**So no file modes were changed.** Instead `qa-patterns.bash` copies every
discovered script into a temp mirror at its own repo-relative path, appending
`.bash` where it has no shell extension, and scans that. Verified first that the
relative path is preserved well enough for each rule's `paths.include` to still
apply — it is. Findings and `.paths.scanned` are mapped back through a
mirror→repo path map, so every path the gate prints is one a human can open.

Both gates now share `scripts/qa-shell-discovery.bash`. One exclusion list, one
shebang predicate. Two gates quietly disagreeing about what counts as a repo
script is how a file ends up outside both while each prints a plausible number.

`qa-patterns.bash` got the same coverage assertion: every file handed to semgrep
must come back in `.paths.scanned`. Negative control: planted a 2 MB script
(over semgrep's default `--max-target-bytes`) → `rc=2` naming it.

### Phase 3 — 153 files scanned, 17 findings, one of them live

The census in `PLAN.md` predicted 28 remaining (22 pipe-echo + 6 test-discards).
The real gate found **17**. That census was measured by extracting each rule's
regex and applying it by hand; the shipped rules are stricter. Recording the
correction rather than restating the number, same as the 34 → 92 correction
above.

**The live one, `claude-yolo` — the daily Claude Code update check.** Both
version probes ended `… | grep -oP … | head -1` with a fallback to `"unknown"`.
The file sets `set -e` but never `pipefail`, so the pipeline's status is
`head`'s — and `head -1` succeeds on empty input. **The fallback never fired.**
A container that would not start produced an *empty* version, not `"unknown"`,
which walked straight past the offline guard on the very next line:

- both empty → they compare equal → `✓ Claude Code is up to date ()`, and the
  daily stamp is refreshed so it does not re-check that day;
- one empty → `📦 Updating Claude Code: → 2.x.y` and an in-place package update
  driven by a comparison against a version nothing had read.

Proved it with a stub `container_cmd` that fails: old form → *"up to date ()"*,
new form → offline guard hit. This is the plan's thesis in one site — a failure
laundered into a confident wrong answer — and it was invisible because the file
is extensionless and mode 0644.

Six more in the same file, each now naming what actually failed: `--networks`
listing, `claude --help` in the image, network-prune inspect (its failure read
as "no containers attached", so nothing was stopped and the removal then failed
for a reason the user never saw), token discovery (now a glob, whose no-match is
a fact about the directory rather than `ls`'s ambiguous empty string), the
update-check stamp, and the base-image version. → **CCY 3.38.0**.

`nord` and `wsi` shared an identical log-rotation site where a failing `stat`
became the literal `0`, which reads as "well under the limit" and so silently
disables rotation on a log that may be enormous. Both now report it. Probed all
three states with a stub `stat`. `wsi` also had a second whisper-server probe
that collapsed "could not connect" into "not 200" — the *first* one, twelve
lines up, was already correct, so this was a copy that missed a fix.

One annotation only, on a debug line that prints `N/A` for a token size: the
fallback names the failure rather than passing off a size nothing measured.

**Both gates green over 153 files each** (`✓ QA passed: 502 files checked`),
where they reported 125 before and 27 files had never been opened by either.

---

## The gate I had just written was making the same claim it was built to catch

`ftp-camera` came back from the widened semgrep scan with **zero findings**, and
zero is the number this whole plan exists to distrust. It has three
`numfmt … 2>/dev/null || echo "?"` lines; an isolated file containing exactly
those lines is flagged. So the file was not clean, it was **unread**.

The reason: semgrep lists a file it could not **parse** in `.paths.scanned`,
returns no findings for it, and exits **0**. The reason appears only in
`.errors[]` — which my brand-new coverage assertion did not look at. I had
asserted `.paths.scanned` contains every discovered file and called that
coverage. It is not: it is the analyser's *intent* to read the file, not
evidence that it did.

That is this plan's defect class, committed by me, inside the gate written to
catch it, within the hour. Recorded rather than quietly fixed, because the shape
is the point: the check was one level too shallow, and the number it produced
looked exactly like success.

Checking `.errors[]` turned up **14** affected files in two distinct classes:

- **`Syntax error` ×3** — the whole file failed to parse, so **no rule ran on any
  line**: `ftp-camera` (2,475 lines), `rclone-cache-status`, `rclone-tail`. The
  last two are executable, so the **old** gate scanned them too and reported them
  passing. This hole is older than the one this plan opened with, and independent
  of it.
- **`PartialParsing` ×11** — parsed apart from named ranges. The recurring
  construct is `${var:-(text)}`, parentheses inside a default value, which is
  **my own idiom** from Plan 00075's error-reporting fixes. Also extglob
  (`${str##+([[:space:]])}`) and some arithmetic. Worst of these:
  `scripts/git-hooks/pre-commit` has an unparsed span of ~86 lines — a secret
  scanner, partly unanalysed.

### Calibration, and why not simply "make it fatal"

All 14 pass `bash -n`. Whether semgrep's grammar accepts a given piece of *valid*
bash is a property of semgrep, not of this repo, so "rewrite until the parser
copes" is not automatically the right answer — it would mean changing working
production code to suit a tool.

So the two classes are treated differently, on what they actually mean:

- **`Syntax error` → gating.** 0% analysed is indistinguishable from not scanning
  the file, and calling that a pass is the lie.
- **`PartialParsing` → reported, not gating.** Every rule ran on everything
  outside the named ranges, and the affected files are printed, so the gate
  claims nothing it did not check.

The three current whole-file failures are listed in `SEMGREP_CANNOT_PARSE` with
their reason, and the list is **self-expiring in both directions**: an unlisted
file that cannot be parsed fails the gate, *and* a listed file that starts
parsing fails the gate until it is removed. A stale exception would hide the next
regression behind it.

Every run now prints its coverage — `N NOT ANALYSED`, `N analysed EXCEPT for some
ranges` — above the `✓ patterns: 153 files OK` line, pass or fail. A summary
consisting only of a green tick and a file count is the format that let all of
this sit unnoticed.

Finding the actual constructs is Phase 4.3/4.4. One method already ruled out:
**bisecting by truncation is invalid** — a prefix cut mid-function is unparseable
in its own right, so the search converges on where truncation first hurts rather
than where the parser breaks. It produced three confident, confounded answers
before I noticed the isolated `numfmt` probe had already contradicted them.

### One consequence for the earlier ftp-camera annotation

The three `numfmt` lines had picked up a `# FAIL-FAST-OK (all three):` comment on
the line *above* them. Two things wrong with it: the rules match the annotation
only on the **same line**, and the file is not analysed at all, so it suppressed
nothing and never had. Replaced with three real same-line annotations — correct
now, and correct on the day the file becomes parseable.

---

## One `case` on one line cost 2,475 lines of coverage

Task 4.3, with a method that is actually valid. Bisecting by truncation was
wrong; **leave-one-out on complete top-level chunks** is not. Split each file
into whole function-sized segments, write each variant as its own file, and scan
them all in **one** semgrep run — the parser's verdict is then per file, with no
mid-construct cuts to confound it.

`ftp-camera` reduced to a single 45-line function, and from there to one line:

```bash
case "$src" in "${remote_name}:"*) echo "$mnt"; break;; esac
```

Not the `case` by itself — that parses. Not the quoted-expansion pattern — that
parses. It is a **one-line `case … esac` nested inside a `while` inside a command
substitution**; written across lines, the identical bash parses fine. One line,
and 2,475 lines of a script that runs a camera workflow were invisible to every
pattern rule.

The first thing the newly-readable file reported was a real defect, at line 704:

```bash
if [ "$(nmcli -g ipv4.method connection show "$n" 2>/dev/null)" = shared ]; then
```

That loop finds the active hotspot connection. "nmcli could not be asked about
this connection" and "this connection is not shared" were the same empty string,
so a failed query silently skipped the connection and the hotspot was reported
as not found. Now it asks, checks the status, and says so.

**`SEMGREP_CANNOT_PARSE` is down to two**, which is the list behaving as designed.

### The other two: cause NOT established

`rclone-tail` and `rclone-cache-status` each reduced to *two* segments, either of
which fixes the file when removed — an interaction, not a single construct.

The obvious suspect was the embedded Python heredoc inside an `if` condition
(`if ! python3 - "$vfs_json" << 'PY' 2>/dev/null`) that both share. **Wrong**: it
parses in isolation. Recording the dead end rather than the guess — a plausible
cause asserted without a probe is precisely what this plan is about. Task 4.3
stays open for those two, and the exception list now carries what is known and
does not pretend to know the rest.

---

## Correction: the PartialParsing cause was asserted, not established

Two entries above I wrote that the recurring `PartialParsing` construct "is
`${var:-(text)}`, parentheses inside a default value, which is my own idiom" —
and named extglob and arithmetic alongside it. **That was a guess dressed as a
finding.** Probed all three in isolation just now: every one of them parses.

What is actually true is weaker and worth stating precisely: the ranges semgrep
reports *sit on* those constructs, which is where the inference came from, but
the constructs are not sufficient to cause the failure. Like the `rclone-*` pair
in 4.3b, the cause is contextual and remains unestablished.

This is the second theory in one session that survived until it was probed and
then died — the embedded-Python-heredoc suspect being the first. Both were
plausible, both were written down as though settled, and both were wrong. The
`case`-in-`while`-in-`$( )` finding is the one that held, and it held because it
came out of leave-one-out reduction rather than out of reading the code and
recognising something.

Note for 4.4 when it is picked up: the worst instance is
`scripts/git-hooks/pre-commit`, which has an unparsed span of roughly 86 lines
(266–352). That is a secret scanner on a public repository, partly outside the
analysed region.

---

## Correction: the 86-line span in the secret scanner is Python, not bash

The entry above flagged `scripts/git-hooks/pre-commit` 266–352 as "a secret
scanner on a public repository, partly outside the analysed region", which reads
as far worse than it is. Checked the span: 274–352 is the **body of an embedded
Python heredoc** (`if ! python3 - … <<'PYEOF' … PYEOF then`). Bash pattern rules
would never apply to Python source, so the practical loss is a handful of bash
lines, not 86.

Still unexplained, and still not guessed at: the same heredoc-inside-an-`if`
shape parses in isolation, with the redirect before *or* after the marker. Third
theory to die on contact with a probe. The line-count-to-significance inference
was the sloppy part — a span is not a loss until you look at what is in it.

---

## 4.3b has a shape, and my analysis jq had a bug

Reduced `rclone-tail` and `rclone-cache-status` to two functions each, and tested
the two functions **alone** and **together**:

- `only_2` — parses
- `only_5` (or `only_4`) — parses
- `pair_2_5` (and `pair_2_4`) — **Syntax error**

So it is not a construct. Neither function is individually unparseable; the
grammar fails on the two of them in combination. That points at a parser
complexity or error-recovery budget rather than a missing rule, which is the next
hypothesis to test — and it explains why three construct-shaped theories all died
on isolated probes.

**A wrong reading on the way there, worth recording.** My first pass said each
segment failed individually, which contradicted the earlier chunk run. It was a
bug in my throwaway analysis `jq`:

```jq
.paths.scanned[] | "\(.): \(if ($bad|index(.)) then ...)"
```

After `$bad |`, `.` rebinds to `$bad` — so `index(.)` asks whether `$bad`
contains itself, which is truthy for every file. Every scanned file came back
"SYNTAX ERROR" and I believed it long enough to write it down.

Checked immediately whether the **shipped** gate shares the defect. It does not:
`qa-patterns.bash` reads `.path` off the error object inside `select(...)`, and
its `$seen[.]` lookup is inside a `map`, where `.` is correctly the element. The
bug was confined to a scratch command. Verified rather than assumed, because a
gate that names the wrong file is worse than one that names none.

---

## Fourth theory dead: it is not a size or complexity budget

Took the function that parses alone, made eight distinct copies of it in one
file, and scanned: parses. So the pair failure is not the parser running out of
budget on volume — it is something specific to those two functions together.

Four hypotheses tested and killed today (embedded Python heredoc,
`${var:-(text)}`, the 86-line span's significance, complexity budget) against one
that held (`case` inside `while` inside `$( )`). The one that held came from
mechanical reduction; all four that died came from reading the code and
recognising something plausible. Stopping the guessing here: 4.3b needs
delta-debugging within the failing pair, and that is its own sitting.
