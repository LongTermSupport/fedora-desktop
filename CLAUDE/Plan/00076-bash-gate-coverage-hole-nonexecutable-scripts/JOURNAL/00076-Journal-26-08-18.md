# Plan 00076 Journal — 2026-08-18

## origin · found by refusing to accept a zero

This plan exists because of a zero I did not believe.

Widening Plan 00075's `bash-capture-discards-status` rule to `files/**` reported
**0 new findings**. That is a suspiciously clean result for a tree containing 30
raw `$(… 2>/dev/null)` captures, and "a gate reporting clean because its own
machinery is broken" is the exact defect Plan 00075 is about — so the zero got
tested rather than banked.

The first control I wrote was itself wrong twice, which is worth recording:

1. I scanned the fixture `.semgrep/bash-conventions.bash` with the widened
   config and got 0 hits, and briefly read that as "the rule is broken". It was
   not: `paths.include` applies to a *scan*, and the fixture is not under
   `files/`. `semgrep --test` does **not** apply path filters — confirmed
   separately, `5/5: ✓ All tests passed` — so the fixture assertions are real.
2. My second control copied the launcher to `/tmp` — also outside
   `paths.include`. Another invalid zero.

The valid control was a canary planted **inside** the tree, which did fire. So
the rule worked and the tree was genuinely clean… except `.paths.scanned` from
the directory-wide run listed every `lib/*.bash` beside the launcher and
`docker-in-lxc`, and **not `claude-yolo` itself**.

## finding · the execute bit decides whether a file is linted at all

`semgrep/target_manager.py:1024`:

```python
elif not path_has_permissions(path, stat.S_IRUSR | stat.S_IXUSR):
    return None
```

`get_shebang_line()` refuses to read a file that is not owner-executable. For a
file with no recognised extension the shebang is the *only* language signal, so
no execute bit ⇒ no language ⇒ never scanned. `git ls-files -s` gives the mode:

| File                                       | Mode     | Scanned |
| ------------------------------------------ | -------- | ------- |
| `files/var/local/docker-in-lxc`            | `100755` | yes     |
| `files/var/local/claude-yolo/claude-yolo`  | `100644` | **no**  |
| `files/var/local/claude-yolo/entrypoint.sh`| `100644` | yes — the `.sh` extension carries it |

`qa-bash.bash` has the same hole from the other direction: its two `find` passes
are "named `*.sh`/`*.bash`" **or** "`-type f -executable` with a shell shebang".
A mode-644 extensionless script is in neither, so it gets no `bash -n` either.

27 tracked scripts are in that state. The census (`shellcheck -S style`) is in
PLAN.md; the number that matters is **SC2155 ×19**, because Task 4.1 of Plan
00075 raised the shellcheck bar to `warning` *for that code specifically* and
then fixed 12 instances. The raise was real; its reach was not.

## note · no new recovery cron

The scaffolder advises creating an hourly failsafe recovery cron. This session
already has one firing — several ticks are in the transcript — so a second would
be duplicate coverage, not extra safety. Not created deliberately, recorded here
so the absence reads as a decision rather than an omission.

## progress · first 15 of the 34, and a census that was incomplete

Working the findings BEFORE widening discovery, so the tree never sits red.

**Four were false positives on correct code**, and the repo blocks
`# shellcheck disable=`, so each was expressed unambiguously instead of
suppressed:

- `SC2054 ×3` — "use spaces, not commas, in array elements" on
  `-o PreferredAuthentications=password,keyboard-interactive`. That comma is
  part of an **ssh option value**. Quoting the element states that and silences
  the check; verified with a two-case probe that the unquoted form reports and
  the quoted one does not.
- `SC1007 ×1` — `GQ_NEW_INSTANCE= setsid …`. The empty value is deliberate and
  per-command. `GQ_NEW_INSTANCE=''` is identical to bash and distinguishable
  from a typo to everything else.

**`SC1090 ×4`** — `source "$SWA_LIB"`, where the path is
`<script dir>/ssh-with-agent.bash` and therefore knowable. A
`# shellcheck source=…` directive is not a suppression: shellcheck then actually
**follows** the file, so the analysis gets stronger, not quieter. Confirmed with
a probe before applying it to the four.

**`SC2120 ×2`** — `sort_uploads` and `prune_orphan_raws` took
`local dir="${1:-$UPLOAD_DIR}"` and no call site has ever passed anything.
Checked the whole function body of each for other positional use (none — the
`local file="$1"` nearby belongs to `sort_one_file`, which starts at 1390, past
`prune_orphan_raws`'s close at 1354), then deleted the unused parameter. YAGNI
is a stated repo principle; dead flexibility is the thing it names.

### The census in PLAN.md measured only shellcheck

Found while reading `ftp-camera`: line 2141 was `sort_uploads || true`. That is
`bash-error-hiding-or-true`, a **semgrep** rule — and one with *no*
`FAIL-FAST-OK` escape by design, because it mirrors the write-time blocker. So
widening coverage will surface semgrep findings too, which the plan's 34 does not
count. Rewritten as the explicit reporting form the rule's own comment
prescribes, keeping the best-effort behaviour while saying so on stderr rather
than discarding it.

Two sub-findings recorded, not yet acted on: several plays deploy these paths
with no explicit `mode:`, and **five of the 27 have no deploying play reference
at all** (`lan-scan`, `qnap-finder`, `tmate-full`, `tmate-share`, `colours`) —
which needs checking before Phase 2 sets any execute bit.

`qa-all.bash` green (446); the six edited files are 0-gating under direct
`shellcheck -x` and all parse.
