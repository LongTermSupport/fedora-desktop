# Plan 00063 — Journal 26-09-14

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 09:12 · finding · — — three statements in PLAN.md were no longer true

Read while working out what of this plan the Plan 00110 lab can still discharge. All three
verified at source before changing anything:

1. **Non-Goals said the `RUN_BASH_GITHUB_ACCOUNTS=none` path "is deferred to a follow-up
   plan".** It shipped — Plan 00082, `run.bash:293`, with its own guards against
   `RUN_BASH_CONFIG_SOURCE` and `RUN_BASH_RESTORE_PROJECTS`. Every VM acceptance scenario
   runs on it. The line now records what this plan did not do, instead of describing a
   feature that exists as missing.
2. **Task 1.6 said `none` "fails fast in `headless_preflight` naming the follow-up".** It is
   an accepted value now. The tick stays — that decision was made and was implemented as
   described — with a superseding note, because a reader working forwards from the plan would
   otherwise write code against a fail-fast that is gone.
3. **The Overview said "host verification is outstanding"**, contradicting the Status header
   two paragraphs above it, which already records Phase 3 partly discharged by the lab.

No task state changed; nothing here was open work. This is drift between a plan and the code
it tracks, which is the exact thing the Plan Commit Rule exists to stop.

## 09:14 · thought · T2.2 T2.3 T2.4 T3.2 — what is actually left, and why the lab cannot reach it

Tasks 2.2, 2.3 and 2.4 are code-complete and wait on a run that carries a **real GitHub
token and a real SSH passphrase**. All six allowlisted bridge scenarios run
`RUN_BASH_GITHUB_ACCOUNTS=none`, so no PAT and no passphrase exist in the guest and those
assertions would pass by absence — a check that cannot fail, which is worse than no check.
Discharging them needs a seventh scenario (`server-github-token`, specified in Plan 00110
DESIGN.md §10 but deliberately never built or put on the bridge) plus a throwaway PAT the
owner provides.

Task 3.2 — "confirm the desktop interactive path is unchanged" — is interactive by
definition. No VM proves a human's prompt experience; 00110 §8 says so in as many words.

So this plan is blocked on a human for everything that remains, and honestly so.
