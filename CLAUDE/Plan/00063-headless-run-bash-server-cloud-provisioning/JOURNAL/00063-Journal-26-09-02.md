# Plan 00063 — Journal 26-09-02

> Append-only activity log for plan 00063. Entry grammar:
> `## HH:MM · category · REF — optional short title`; new entries at the bottom,
> earlier entries never edited.

## 22:10 · action · — — PLAN.md slimmed to the lean house structure

PLAN.md had grown to 48,612 bytes, past the 35,000-byte hard limit. It was
renamed to `PLAN_archive.md` (verbatim, with an archive banner) and replaced by
a lean PLAN.md holding only the header, overview, goals, non-goals, the task
tree with its tick state unchanged, dependencies, success criteria and
milestones.

Durable content moved out of the old prose rather than deleted:

- `DESIGN.md`: the context line-map, the round-1 premise correction, Design v2
  (D1–D11), the v3 deltas (V3.1–V3.9), the round-3 hardening (V3.10–V3.15), the
  canonical invocation (collapsed to the v1 token-required form) and the risks
  table.
- `DECISIONS.md`: Technical Decisions 1–6 with their round-3 refinements (the
  Task 1.6 deferral under Decision 2, the required passphrase file under
  Decision 6).

The dated narrative in the old PLAN.md (round-1/2/3 audit judgements, the
open-decision list marked resolved, the slice-by-slice implementation notes in
the task bodies) was already recorded entry-by-entry in
`00063-Journal-26-07-23.md`, so nothing new was journalled from it; the old
wording remains readable in `PLAN_archive.md`.

State is unchanged by this slimming: Phase 2 code-complete at run.bash v1.10.0;
Tasks 2.2–2.5 and 2.8 stay 🔄 pending Phase 3 HOST verification (3.1, 3.2 ⬜).
