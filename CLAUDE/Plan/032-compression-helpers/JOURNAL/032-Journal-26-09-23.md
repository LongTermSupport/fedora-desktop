# Plan 032 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> Continues `032-Journal-26-09-17.md`. Append-only; corrections are new entries here,
> never edits to an earlier day's file. This day-file was written by hand, because
> `mkplan.bash --journal` pads the number to five digits and cannot find a three-digit
> plan folder. Its time was read from `date -u`.

## 19:10 · action · P4 — HOST run: deploy idempotent, acceptance ACCEPTED

The host batch `_meta-deploy/20260923-194037` ran `deploy.bash` and then `acceptance.bash`.

- **Deploy**: both play legs ended `changed=0`. Leg 2 is the idempotency evidence the
  script names, so the "Playbook is idempotent" criterion is met.
- **Acceptance**: `ACCEPTED`, 11 of 11 checks executed, 14 assertions passed, 0 failed.
  - Checks 0, 1 and 8 are the preconditions: ouch reports 0.8.1, matching the pin; the
    deployed wrappers are byte-identical to the repo copies; and `PATH` resolves both to
    `/usr/local/bin`.
  - Checks 2 to 7 are Phase 4's six items, one each, all PASS.
  - Checks 9 and 10 are the `--gz`/`--7z` and conflicting-flag criteria.

Ticked from that: the HOST RUN item and its six sub-items, plus every success criterion
the run exercises.

Two criteria are **not** exercised by this acceptance, so they stay open:

- "`uncompress anything.{tar.xz,zip,tar.gz,7z,tar.zst}` creates a single new folder".
  Checks 5 and 6 cover `.tar.xz` and `.zip` only. Check 9 builds a `.tar.gz` and a `.7z`
  but never uncompresses them, and nothing builds a `.tar.zst`.
- "Running either command twice refuses to overwrite unless `--force`". Check 7 proves the
  refusal on both wrappers. Nothing runs `--force`.

Closing them is container work plus one host run. Extend `acceptance.bash`: uncompress the
two archives check 9 already builds plus a `.tar.zst`, asserting one new entry each, and
run `--force` on both wrappers, asserting replacement. Then re-run it on the HOST. The plan
stays In Progress until then.
