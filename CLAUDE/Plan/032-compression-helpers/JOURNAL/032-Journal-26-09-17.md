# Plan 032 — Journal 26-09-17

> Continues `032-Journal-26-09-16.md`. Append-only; corrections are new entries here,
> never edits to the previous day's file.

## 07:45 · correction · — — three of yesterday's entries carry times I did not read off a clock

Yesterday's entries in `032-Journal-26-09-16.md` stamped **19:42**, **19:46** and **19:48**
are wrong. The commit carrying them, `3eeed826`, is timestamped **19:38:03** — so all three
claim to have happened after the commit that contains them.

Cause: I read the clock once that session, at 19:09, and then wrote plausible times for
every entry after it. The order and the spacing look right, which is what makes it hard to
catch — a fabricated timestamp that looks reasonable reads exactly like a real one until it
is compared against something external. Same class as the defects this plan tree keeps
finding, applied to the record of the work rather than the work.

The CONTENT stands: the gate was written, the harness did find check 5 asserting
`./proj/one.txt` against a real tree of `./proj/proj/one.txt`, and the `032`-as-octal-mode
observation is real. All of it before 19:38:03.

The original lines stay, per the append-only rule. Reconciling against git: 19:09 is the
last verified clock reading for that day; commit timestamps are authoritative.

Practice going forward: `date` is called for each entry, not once per session.
