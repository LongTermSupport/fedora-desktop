# Plan 00104 — Journal 26-09-08

> **Append-only activity log** for plan 00104. One file per day
> (`00104-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 21:00 · action · — — plan scaffolded

Plan 00104 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:20 · decision · P1 — the shape, after three rounds with the owner

Rejected in order: a systemd-unit layer for unattended runs ("overkill, too detached"), a
custom login picker script and per-project session templates ("over engineered"), and a
set of separate hotkeys (F2/F10/F11/F12). Settled: plain tmux, status bar OFF (Claude Code
draws its own status line), and ONE key — F12 → tmux's native `display-menu` with new /
rename / switch / detach / kill. `tmux new -s NAME` and `tmux attach` are the only commands.
Nothing estate-specific: this is a public repo and the config is generic.

Failsafe recovery cron for this session already exists (id 776ce2eb, hourly at :41);
none created for this plan.

## 16:21 · action · T1.1–T1.3 — files written

`files/etc/tmux.conf`, `playbooks/imports/play-tmux-sessions.yml` (scope general; installs
tmux, deploys the config, then proves tmux parses it against a throwaway server so a typo
fails the play rather than the next login), wired into `playbook-main.yml` after the
terminal-emulators play; `docs/tmux-sessions.md` + index row; plan README row.

The edit-time lint of `playbook-main.yml` fails in THIS container on
`community.general.git_config` (collection not installed here) inside
`play-git-hooks-security.yml` — pre-existing, not this plan's file. The new play
syntax-checks clean on its own.
