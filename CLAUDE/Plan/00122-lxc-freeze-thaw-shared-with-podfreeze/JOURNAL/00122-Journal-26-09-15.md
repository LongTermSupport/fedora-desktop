# Plan 00122 — Journal 26-09-15

> **Append-only activity log** for plan 00122. One file per day
> (`00122-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:58 · action · — — plan scaffolded

Plan 00122 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 17:06 · decision · — — a sibling tool, not a flag, and the owner picked the axis

Asked for by the owner mid-session: "i want a way to freeze/thaw lxc containers — exact
same concept as podfreeze — could even try to combine into podfreeze as another group
maybe?"

Established before offering options, rather than assumed:

- This repo runs **classic LXC 4.x** from the `ganto/lxc4` Copr
  (`playbooks/imports/play-lxc-install-config.yml`), **not** LXD or Incus. So the verbs
  are `lxc-freeze` / `lxc-unfreeze`, not `lxc pause`.
- It is **rootful**. `files/var/local/docker-in-lxc` — this repo's existing LXC tool —
  uses `sudo lxc-info`, `sudo lxc-start`, `sudo lxc-stop` throughout, and the install
  play's own `lxc-ls` sanity check runs under `become: true`. Podman here is rootless and
  `podfreeze` never escalates.
- **Both** `play-lxc-install-config.yml` and `play-podman.yml` are core imports of
  `playbook-main.yml`, so on any host that has run main, both engines are present. That
  is what decides the deployment question in Task 3.1: neither tool needs a guard the
  other does not, so one play owns both rather than two plays copying four tasks each.

Three options went to the owner with the trade-offs named: combine and rename to
`cfreeze`; combine and keep the `podfreeze` name; or a separate `lxcfreeze` sharing a
library. **Chosen: the separate tool.**

The deciding cost was not the name but the `sudo`: a group row cannot print `FREEZE 3`
without first reading `/var/lib/lxc`, so a combined tool would prompt for root on every
menu open — including for someone who only wanted Podman. Keeping them apart keeps
`podfreeze` rootless and keeps the name honest.

## 17:07 · action · — — recovery cron

Reusing the session's existing failsafe recovery cron `b4fdd77b` (hourly at :23) rather
than creating a second. One per session, not one per plan.

## 17:08 · finding · T1.1 — the extraction has no safety net today

`podfreeze` is 1,261 lines, has no test file anywhere in the repo, and is used daily. So
the test suite is Task 1.1 and comes **before** the extraction, not after it: a suite
written against the refactored code proves the refactor's own idea of correct, which is
exactly the failure Plan 00109's `groupIn` helper turned out to be — a test that agreed
with the thing it was meant to check.

The seam looks clean from the outside: only `load_inventory` and a single
`podman "$podman_verb" "$name"` call in `do_action` touch the engine, plus
`assert_podman`. Everything else — `infer_action`, `print_table`, `pick_target`,
`drill_into_group`, `interactive_loop`, the bounded retry — reads the shared `INV_*`
arrays and `SELECTED`. Mapping that precisely before touching anything.

## 17:34 · decision · T3.1 — a new play, reversing the plan's own first answer

The plan filed an hour earlier said one play should own both tools, and derived that from
the repo's own test: a play earns its own file when it has "a lifecycle of its own"
(`docs/playbooks.md`), and `lxcfreeze` has none — same `hosts`, same `become`, same
`scope`, and both engines are core imports of `playbook-main.yml`.

That derivation is still correct and the answer changed anyway, because of the owner's
*"for now dont touch podfreeze"*. Extending `play-podfreeze.yml` leaves two options and
both cost more than the duplication: its name stops describing what it does, or it gets
renamed — and a rename churns the catalogue heading, the `#play-podfreezeyml` anchor
`docs/ccy.md` links, and Plan 00079's `PLAN.md` and `deploy.bash`, all for a plan that
was told not to touch that tool.

So `play-lxcfreeze.yml`, with three deploy tasks duplicated and the reason written into
the play's own header. Phase 4 reconciles both plays and both tools together, which is
the right unit of work for it anyway.

## 17:38 · finding · T1.2 — errexit arrives by sourcing, and the suite passed by luck

`scripts/test-lxcfreeze.bash` sources the shipped tool to reach its functions. The tool
carries `set -euo pipefail`, and sourcing applies that to the test's shell — so the suite
runs under errexit regardless of its own `set -uo pipefail`.

It passed anyway, and that is the part worth recording: every negative case happens to
sit inside an `if` condition or a `$( )` substitution, both of which errexit exempts. So
the suite was green for a reason nobody had chosen. Plan 00109 lost a day to the same
exemption in the other direction — a function moved into a command substitution and its
failure stopped being fatal.

Turning errexit back off was the obvious fix, and `R-ERROR-HIDING` blocks it, correctly.
The better fix was the opposite one: keep errexit, check that it arrived — which also
catches the tool losing its fail-fast prelude — and write the constraint down where the
next case gets added. The mutant "lxcfreeze loses its fail-fast prelude" is killed by
that check and by nothing else.

## 17:44 · finding · T2.3 — a test caught my own label, not the code

`lxcf_bridge_label` carries three answers: a bridge name, `(no network)`, and
`(could not read)`. `bridge_names` filters the last two out of the group menu by exact
string, and that filter is only safe if neither label can be a real interface name. The
suite checks the property rather than the wording — a Linux interface name cannot
contain whitespace — and it FAILED on the first attempt, because the label was
`(unreadable)`, which has none. The label changed; the test stayed.

The distinction itself is the Plan 00109 shape again, third instance in two days: a
config that declares no network and a config that could not be read are different facts,
and folding the second into the first files a container under a claim about the machine
nobody established — and puts it beyond the reach of every bridge group.

## 17:50 · action · P1–P3 — 69 cases, 18 mutants, all killed

`untracked/scratch/mutate_lxcfreeze.py` (untracked; the suite is the deliverable). Each
mutant is a defect the tool could plausibly have had, and a mutation whose search text
does not match is reported as a SURVIVOR rather than skipped — Plan 00109 had two
"mutants" that were never applied and read as kills.

Killed, with the case that caught each: substring name matching (the prefix case),
`infer_action` reading only the first selected name (the running-behind-two-frozen case),
`count_in_state` counting the inventory instead of its arguments, one shared refusal for
both explicit verbs, the label counting the selection instead of the acted-on, vanished
folded into skipped, vanished dropped silently, the partition not resetting between
passes, the wrong state for thaw, `parse_state` accepting any word, `parse_state` reading
silence as STOPPED, `parse_bridge` matching a commented key, honouring the pre-2.1 key,
taking the last interface instead of the first, `bridge_label` ignoring the read status,
reporting unreadable as no-network, the two labels collapsing to one string, and the
prelude going missing.

Also confirmed: QA's own inventory gate works. Wiring the suite into `qa-all.bash`
without a `CLAUDE/QA.md` row failed TWO independent gates — `qa-docs.bash` and
`tests/helpers/docs/test_link_check.py` — which is the derived-inventory design from
Plan 00070 doing exactly what it was built for.

## 17:52 · handoff · T3.4 — what a host still has to settle

The owner needs this to freeze real containers, so the honest statement of what is and
is not established:

- Proven here: every decision, against fabricated output, falsified by 18 mutants.
- NOT proven here, and unprovable here: that `sudo lxc-ls -1` and
  `sudo lxc-info -n NAME -s` emit what the parsers are fed. This container has no `lxc`,
  and the tool refuses to run inside a container by design. `lxc-ls -1` and
  `lxc-info -s` were chosen over `lxc-ls -f` precisely because the fancy format's
  aligned columns are presentation rather than interface — but that is a judgement about
  which surface is stabler, not a measurement.
- Bounded risk on first run: `lxcfreeze list` changes nothing and `-n` previews, so a
  format surprise shows up as an empty or wrong-looking list rather than as a wrong verb
  sent to a container.

## 20:05 · finding · T4.1 — podfreeze cannot be sourced, and cannot be run either

Both obvious routes into `podfreeze` are closed, which is why this suite looks different
from `test-lxcfreeze.bash`:

- **Sourcing it whole** is out. It has no `BASH_SOURCE[0] == $0` dispatch guard, so its
  argument loop and main body run at top level — a `source` would parse the SUITE's
  arguments, fall into `assert_on_host` and exit.
- **Running it as a subprocess** is out too. `assert_on_host` dies when
  `/run/.containerenv` exists, which it does in here. Black-box CLI testing is not
  available on this machine at all.

So only the definitions are sourced: everything above the first executing statement, which
is the `while [ "$#" -gt 0 ]; do` line. The cut is found by matching that line's own text
with awk, never by line number — a number slides onto the wrong side of the boundary as the
tool grows, and the failure mode of that is sourcing real execution, silently. The extracted
prefix is run through `bash -n` before it is sourced, so a cut that landed mid-function is a
named failure rather than a syntax error hundreds of lines into a temp file.

## 20:18 · decision · T4.1 — stub podman rather than leave the loader untested

`load_inventory` and `select_network` are the two functions with no pure-function seam, and
the lxcfreeze suite's "nothing shells out" rule would have left both uncovered. But they own
two of the decisions this task exists to pin — the mis-split guard on a label value holding
the field separator, and an unknown network RETURNING rather than exiting — so leaving them
out would have meant not doing the task.

A `podman` shell function shadows the binary for the last third of the file. That is the
same collaborator-stubbing `scripts/test-vmtest-reboot-dispatch.bash` already does. The stub
fails loudly (exit 99, a named message) on any query it was not primed for, because a stub
that answered "no containers" to an unexpected call would be telling the suite the exact
confident lie the tool refuses to tell from inside a container.

What this does NOT establish, stated rather than glossed: that `podman ps --format` really
emits the record shape the stub feeds the parser. Only a host can say that, and Task 4.6 is
where it gets said.

## 20:31 · finding · T4.1 — two defects in the suite, both found by mutants not by the run

183 cases went green on the first run, which proves nothing on its own. 15 mutants were
built against a scratch copy of the tool, and two of them exposed defects in the SUITE:

- **`select_network` mutated to `exit 1` killed the run silently.** The probe that
  distinguishes return-from-exit was written as `net_probe="$(…)"`, and a bare assignment
  whose command substitution fails takes its status — so errexit aborted before any report
  printed. The `$( )` exemption the header warns about applies to an ARGUMENT, not to an
  assignment. Fixed by wrapping it in `if !`, and by gating the cases that call the function
  in *this* shell behind the probe's result, so a tool that exits produces a named failure
  and a printed summary instead of a dead run.
- **`inventory_index_of` mutated to substring matching corrupted its own report.** `notok`
  let the predicate's stdout reach the terminal, so a failing case printed
  `0  FAIL  a prefix of a real name does not match` — a failure report that breaks its own
  format, and would break any parser reading it. Stdout now goes to a file and is reported
  as a detail line.

The other 13 were killed cleanly, each by cases that name the right thing: the cardinality
proxy `identity_axis_discriminates` warns about, `unlabelled_ccy_names` keyed on empty
identity maps instead of `INV_HAS_LABELS`, the mis-split guard removed, `infer_action`
reading only the first selected name, `select_names` dropping unknown names, `count_in_state`
counting the inventory, `build_network_map` accumulating across rebuilds, `identity_matches`
comparing ssh-key whole instead of by word, `row_verb` reporting an unknown container as `-`,
`load_inventory` keeping the literal `none`, `target_effect` collapsing its two explicit
refusals, `load_inventory` inventorying exited containers, `${nets:-none}` dropped,
`select_identity` resolving an unknown value to an empty set, and `identity_values` losing
its `sort -u`.

## 20:36 · finding · T4.1 — two things pinned that look like bugs, and are not fixed

Recorded as current behaviour with the case commented as pinned-not-endorsed, because this
task is a statement of what the tool does today and Task 4.2 is the place to change it:

- **`select_network`'s existence check is a REGEX match.** `grep -qx -- "$net"` with no
  `-F`, so `podfreeze --network 'podma.'` matches a network called `podman` and passes the
  check. `select_identity` does the same job three hundred lines later with `grep -qxF`. The
  user-visible effect is that a typo'd network with a metacharacter in it reaches the filter
  query and comes back empty, so they get "Nothing in that group" instead of the "no such
  network" error with the list of real ones.
- **`identity_matches` word-splits the ssh-key haystack unquoted.** `for word in $have` is
  pathname expansion as well as word splitting, so a label value of `*` expands against the
  working directory and every file there becomes a key that session appears to hold. Pinned
  in a controlled temp directory so the case does not depend on where the suite is run.

## 20:39 · handoff · T4.1 — done; what Task 4.2 must not break

`scripts/test-podfreeze.bash`, 183 cases, wired into `qa-all.bash` and rowed in
`CLAUDE/QA.md` (gate counts updated — that omission has failed two gates before). Full
`./scripts/qa-all.bash` green. `git diff` touches no line of `podfreeze`, which was the
point.

The three distinctions most likely to be lost silently by the extraction, each with cases
that name them:

1. `INV_HAS_LABELS` separating "a CCY session with no identity on any axis" from "a session
   predating the labels". Both leave the identity maps empty; only the first is current, and
   telling its owner to relaunch is advice that cannot work.
2. A label value containing `FIELD_SEP` is FATAL, not mis-grouped. The fifth-field check is
   the only thing standing between that and a session filed under someone else's identity.
3. An unknown network RETURNS non-zero; it does not exit. The menu depends on that to
   re-prompt when a network disappears between being drawn and being chosen.

Not covered, deliberately: `pick_target` and `drill_into_group` (both die without a TTY),
and `do_action` (the half that actually calls `podman pause` — its act/skip/vanished split
deserves its own suite). Task 4.6 on the host still owes the fzf path and the real
`podman ps` record shape.

## 20:55 · decision · T4.1b — fix the two defects before the extraction, not after

Both bugs the pin found sit in functions Phase 4 is about to move. Fixing them first
costs nothing and buys a much stronger guarantee for Task 4.2: the extraction becomes a
pure move, whose claim is "no behaviour change at all" rather than "no behaviour change
except these two, which changed for unrelated reasons". Mixing a behaviour fix into a
refactor is how a refactor stops being reviewable.

Reproduced both before touching anything, rather than taking the report on trust:

```
'podma.' matched 'podman'            (grep -qx, no -F -> regex)
-F correctly did not match
one label value of '*' became 2 'keys'  (unquoted for-loop, in a dir of 2 files)
```

`grep -qx` -> `grep -qxF`, matching what `select_identity` already did. The unquoted
`for word in $have` -> `read -r -a words`, which splits on IFS without pathname
expansion. Checked the replacement against an empty haystack under errexit before
committing to it — `read` returns 0 on a here-string, so the function does not abort
the shell on a session with no keys.

**The pins were flipped, not deleted.** Each case now asserts the fixed behaviour, and
each gained a companion proving the fix tightened the check rather than broke it: the
literal network name is still accepted, and a genuine multi-key label still matches its
first and last members and still refuses one it does not hold. A fix that made
`identity_matches` always return 1 would pass the glob case and fail these.

187 green, up from 183.

## 20:58 · thought · P4 — the "no line of podfreeze" criterion had to go

It was a Phase 2–3 constraint and it contradicted Phase 4 outright: a library both
tools **source** cannot exist without editing `podfreeze`. Left standing it would have
been a success criterion the plan's own next task must violate.

Scoped it to Phases 2–3, where it was met and where it earned something real — it is
exactly why Task 4.1's suite pins the tool as shipped rather than a version adjusted to
be testable. Task 4.4 replaces it: the suite passing is what protects `podfreeze` from
here, not the absence of a diff.

## 21:05 · decision · T4.2 — where the library goes, and the one hard constraint on it

`files/home/.local/lib/freeze/freeze-common.bash`, deployed to
`~/.local/lib/freeze/freeze-common.bash` at mode 0644.

The obvious model was `files/var/local/claude-yolo/lib/`, and it is the wrong one: that
is root-owned system scope for a root-deployed tool, while these two are user tools
`copy`d into `~/.local/bin` as the login user. `files/home/.local/lib/` already exists
here — `clean-paste` keeps its helper in a per-tool subdirectory under it — so a
user-scope library under a directory named for the shared thing follows what the repo
already does rather than inventing a fourth convention.

It also pays for itself twice. Because `files/` MIRRORS the target filesystem, the same
relative path finds the library from a repo checkout and from a deployed
`~/.local/bin`: `<dir of the tool>/../lib/freeze/freeze-common.bash`. One resolution
rule, two layouts, no environment variable and no build step.

**The constraint that shaped the resolver**, and it is not hypothetical:
`scripts/test-podfreeze.bash` cuts the definitions above `podfreeze`'s argument loop
into a temp file and sources THAT. By then `BASH_SOURCE[0]` is a path under `/tmp`, so
the rule above resolves into the temp directory and finds nothing. The suite may not be
edited — it is the safety net the whole extraction is measured against — so the
resolver tries the same two layouts beside the ENTRY POINT (`$0`) as well, which for
that suite is `scripts/test-podfreeze.bash` and lands back in the checkout. Written
down here because it looks like an odd second candidate until you know why it is there.

## 21:12 · decision · T4.2 — the menu layer moved, and the hooks are the whole design

Seven hooks and five declared settings, and **no `if` on an engine name anywhere in the
shared half** — the plan's explicit constraint, and the reason it matters is that the
first such `if` is where the second one gets written.

| Hook                          | What it abstracts                                            |
| ----------------------------- | ------------------------------------------------------------ |
| `freeze_hook_preflight`       | engine present, privilege available (`assert_podman` vs `assert_lxc` + `assert_sudo`) |
| `freeze_hook_refresh`         | the inventory query and any derived map (podfreeze also rebuilds its network map) |
| `freeze_hook_menu_rows`       | which groups exist — CCY/network/identity, or bridge         |
| `freeze_hook_select`          | a menu key → `SELECTED`; non-zero is RECOVERABLE and re-prompts |
| `freeze_hook_act`             | one container, one call (`podman pause` vs `sudo lxc-freeze -n`) |
| `freeze_hook_table_header`    | the columns after NAME and STATE                             |
| `freeze_hook_table_row`       | those columns for one inventory entry, by index              |

Settings: `FREEZE_TOOL`, `FREEZE_STATE_RUNNING`, `FREEZE_STATE_FROZEN`,
`FREEZE_HOST_ONLY_NOTE`, `FREEZE_TARGET_HINT`, and the optional `FREEZE_LIST_NOTE`. The
library refuses to load without the first five, and refuses two state words that are the
same string — collapse those and every container is simultaneously a freeze target and
a thaw target, the derived verb always says freeze, and the partition never skips.

`freeze_hook_table_row` takes an INDEX rather than a name deliberately. Every caller has
already resolved the index, and a hook handed a name would need its own not-found
branch — an answer nobody asked for, printed into a table row.

## 21:18 · finding · T4.4 — 187/187, byte-identical, and the one text I did normalise

`scripts/test-podfreeze.bash` passes 187 of 187 with `git diff` touching not one line of
it. That is the claim the whole ordering of Phase 4 was built to be able to make.

One user-visible string DID change, and it is stated rather than buried: `podfreeze` had
TWO different "no terminal to ask on" messages for the identical condition — one in
`pick_target` listing `NAME..., --ccy, --network NET, or --all`, and one in
`drill_into_group` saying the same alternatives in different words. The library has one,
built from `FREEZE_TARGET_HINT`, so the drill-down's phrasing is now the picker's.
Neither is covered by any case (both functions die without a TTY, which the suite
records as out of scope), and one message for one condition is the honest shape. No
other behaviour, wording, ordering or exit status differs.

## 21:22 · finding · T4.3 — lxcfreeze's own help was already describing the tool it now is

Its `usage()` has said *"Choosing one opens the group: the first row acts on all of it,
the rest are its containers if you want only some"* since Phase 2 — carried over from
podfreeze's wording while the menu underneath was a flat list with no drill-down at all.
Nobody noticed because nothing compares help text to behaviour.

So the adoption made the documentation true rather than requiring it to change, which is
a pleasant way to find out that the divergence was never intended in the first place.

What it actually gained: `fzf` when present, the two-level drill-down, `TAB` (or
`2,4,5`) member selection, `ENTER`/`1` for all, `b` to go back, and a retry budget of 3
WRONG ANSWERS rather than 3 prompts. The top-level menu lost its per-container rows —
those are the drill-down now, where a container shows its state and bridge and several
can be chosen at once.

## 21:26 · decision · T4.3 — cases MOVED, and the move made them stronger

`test-lxcfreeze.bash` went from 69 cases to 74, and that is not "5 new": roughly 25
moved out and roughly 30 came in.

Moved to `scripts/test-freezelib.bash`, because the code they drove moved:
`lxcf_index_of`, `lxcf_count_in_state`, `lxcf_infer_action`, `lxcf_target_effect` and
`lxcf_partition`. Every assertion survived intact — and each now runs TWICE, once under
podman's `running`/`paused` and once under LXC's `RUNNING`/`FROZEN`. That is strictly
more than either tool's suite could say alone: a library that hardcoded one engine's
state word passes one pass and fails the other, and a single-vocabulary suite cannot
kill that mutant at all.

New in `test-lxcfreeze.bash`, covering what is genuinely LXC's and what was never
covered before: `bridge_names` (the two non-bridge labels must not be offered as
groups), `select_bridge` (members in inventory order; an unknown bridge RETURNS; a
machine with no bridges at all gets a different message), and all four hooks — including
that the freeze branch is the one calling `lxc-freeze`, read out of the function's own
text, since an inverted mapping would thaw everything a user asked to freeze and nothing
runnable in this container would see it.

## 21:29 · finding · T4.2 — 20 mutants, 0 survivors, and do_action is covered at last

`untracked/scratch/mutate_freezelib.py` (untracked; the suite is the deliverable). Each
mutant runs against a temp tree mirroring the repo layout rather than an edit in place,
and a mutation whose search text does not match is reported as a SURVIVOR rather than
skipped.

Killed, each by a named case: `infer_action` hardcoding podman's state word (caught only
by the LXC pass), `count_in_state` counting the inventory, `inventory_index_of` matching
a substring, vanished folded into skipped, the partition not resetting, one shared
refusal for both explicit verbs, `row_verb` reporting an unknown container as `-`,
`freeze_menu_row` counting the machine instead of its members, a dry run that acts, a
batch abandoned after the first failure, the undo hint naming the whole selection, the
undo hint printed after a thaw, the loop treating a vanished group as a selection,
`select_names` dropping an unknown name, two identical state words accepted, a missing
setting accepted, `drill_into_group` dying on an empty group, the library imposing
errexit on its caller, `freeze_assert_contract` accepting a missing hook, and the
library being runnable as a program.

One of those found a real weakness in the SUITE rather than the library: the empty-group
case called `drill_into_group` directly, so a mutant that died there took the whole run
down and the kill read as `(died before reporting)`. It is driven through a command
substitution now, and the mutant is killed by a case that names itself.

**`do_action` is now covered**, which it never was. Task 4.1 put it out of scope
explicitly — "the half that actually calls `podman pause`" — and it is exactly the code
a defect in would hit both tools at once. Its act/skip/vanished reporting, its dry run,
its per-target failure accounting and its undo hint are driven against a recording hook
that logs every call, so a case can assert what the engine WAS asked, not only what the
tool printed.

## 21:33 · decision · T4.5 — the plays were reconciled by NOT merging them

Task 3.1 deferred this here, and the deferred argument was about duplicated deploy
tasks. With the library there are three artefacts and two plays, so the shapes on offer
were: merge into one play, give the library its own play, or share a task file.

**A third play is disqualified outright**, and this is the part worth recording:
`ansible-playbook play-podfreeze.yml` on a fresh host must produce a WORKING podfreeze,
and the tool sources the library at startup — deploy one without the other and it does
not run at all. A dependency that must be satisfied by a play the user was not told to
run is not a deployment, it is a trap.

**Merging was not needed**, once the question was asked precisely: what do the two plays
actually share? One artefact. Not the `fzf` package (podfreeze's alone), not the tool
copy, not the name, not the docs anchor, not the pre-rename cleanup task podfreeze still
carries. So `tasks/deploy-freeze-lib.yml`, included by both — the pattern
`tasks/ensure-jq.yml` already establishes in three plays here. Every plan document's
worry about renaming `play-podfreeze.yml` (the catalogue heading, the
`#play-podfreezeyml` anchor `docs/ccy.md` links, Plan 00079's `deploy.bash`) evaporates
because nothing needs renaming.

`qa-deployed-drift.bash` gained the library too. Its loop walks `.local/bin` only, so a
deployed tool whose own bytes match the repo could still be sourcing a stale menu — the
exact invisible drift that gate exists for, one directory over. Its lab-pairs array grew
a third field naming the play to run, since the pair list now spans two plans and a
hardcoded remedy would have sent half its readers to the wrong play.

## 21:36 · finding · P4 — the ansible lint hook rejects any task file, including the repo's existing one

Writing `tasks/deploy-freeze-lib.yml` was blocked by the hooks daemon's
`ansible-playbook --syntax-check`, which reads any `.yml` as a playbook and reports
`'ansible.builtin.file' is not a valid attribute for a Play`.

Checked before working around it rather than after: the same command rejects
`tasks/ensure-jq.yml`, which has been in the repo since February and is included by
three plays. So it is the hook reading a task file as a playbook, not a defect in the
new file. `qa-ansible-syntax.bash` derives its own population by CONTENT — a top-level
`- hosts:` or `- import_playbook:` — so neither task file is in it, and the full
`qa-all.bash` is green with both plays syntax-checking clean.

## 21:40 · handoff · P4 — done bar the host, and what the host must do first

`./scripts/qa-all.bash` green: 906 files, freezelib 204, lxcfreeze 74, podfreeze 187.
`scripts/test-freezelib.bash` is wired into `qa-all.bash` with a row in `CLAUDE/QA.md`
and both gate counts bumped (thirty-four → thirty-five, twenty-seven → twenty-eight) —
the omission that has failed two independent gates before.

**Not verified here, and not verifiable here**: neither tool was RUN. This container has
no reachable podman and no `lxc`, and both tools refuse to start inside a container by
design — so every statement above is about decisions driven directly, never about a
container that was actually frozen. Task 4.6 is where a host says otherwise.

**The host must deploy before it tests.** The library is a NEW file and both tools
source it at startup, so a `~/.local/bin/podfreeze` refreshed without
`~/.local/lib/freeze/freeze-common.bash` beside it does not start. Run
`play-podfreeze.yml` and `play-lxcfreeze.yml` — either one deploys the library, both are
safe to run twice.

Task 3.5 (`qa-reviewer` over the diff) is still open and is the plan's last non-host
step.
