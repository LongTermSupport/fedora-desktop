# Plan 00065 — Journal 26-07-29

> **Append-only activity log** for plan 00065. One file per day
> (`00065-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:22 · action · — — plan scaffolded

Plan 00065 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 18:40 · finding · — — three whole-run BLOCKERS + two silent misbehaves on Cloud Base

`PLAN.md` filled from a downstream Cloud Base provisioning audit, re-verified against
this repo's `F44` HEAD. Under `ansible.cfg any_errors_fatal = true`, three `general` core
plays each abort the whole run on minimal Cloud Base: fwupd `get-devices` (import 3, bare
`set -e`, fwupd never installed / no firmware surface); `play-lxc-install-config` (import
18 — hard-deps `firewalld`/`dnsmasq`/`iptables-nft`/`NetworkManager` it never installs,
plus a vault + `git@`-SSH clone of the **public** `lxc-bash`); `play-markless` `cd
~/Downloads` (import 26, XDG dir absent headless). Two more leave a container host silently
wrong (no `loginctl enable-linger` ⇒ oomd override inert, `podman.socket` skipped). Missing:
a preflight edition/flavour fail-fast (`VERSION_ID` can't distinguish edition per Plan
00063's own journal).

## 18:44 · decision · — — fix deps at the layer that needs them

Per this repo's "Missing Dependencies — Fix in IaC" rule: a play that needs a package
`ansible.builtin.dnf`-installs it (never probe/assume); a pre-Ansible dep is installed by
`run.bash` itself. No `failed_when: false`/`ignore_errors` papering. LXC stays a first-class
`general` play (load-bearing) — fix is gating + dep-declaration, never removal.

## 18:47 · action · T1.1,T1.2 — first two blockers fixed (Phase 1)

- **T1.1** `play-markless.yml`: `cd ~/Downloads` → `mktemp -d` scratch dir + `trap 'rm -rf
  "$work"' EXIT`; also removes the `rm -rf markless*` shared-dir hazard. Play stays general.
- **T1.2** `play-basic-configs.yml`: gated the fwupd task `when: provisioning_profile != 'server'` with a WHY comment (same pattern as the USB-audio task ~11 lines above).
- Validation: `ansible-playbook --syntax-check` rc=0 on BOTH (throwaway gitignored
  `vault-pass.secret`). Full `qa-all.bash` can't run in this CCY container (`ruff` not
  provisioned — a HOST tool); edits touch no Python, so HOST QA is deferred to the live test.

## 18:49 · handoff · P1 — Phase-1 remainder is the LXC surgery + design-laden items

Committed T1.1/T1.2 (edit+commit only; NOT pushed — human pushes + runs the HOST test).
Next entry point: **T1.3** (LXC dep-declaration) + **T1.4** (LXC `lxc-bash` → HTTPS, ~65
lines removed) — bigger, careful edits to a ~400-line play, warrant an adversarial review
pass before the human HOST-tests. **Design-laden items needing a steer** before I make
opinionated changes to this public repo: LXC-F1 opt-in var shape, F7 SELinux-permissive
gating, F8 DOCKER-USER reboot-persistence mechanism (firewalld permanent vs oneshot unit),
and the Phase-5 server-recommended bundle mechanism.

## 18:52 · action · T1.3,T1.4 — LXC play unblocked (dep-declare + HTTPS clone)

Both edits to `play-lxc-install-config.yml` landed; play is now `general`-safe on Cloud Base.

- **T1.3** — added to the `Install Packages` task: `firewalld`, `python3-firewall`, `dnsmasq`,
  `iptables-nft`, `NetworkManager`, each with a WHY comment naming the downstream task that
  hard-requires it (zone-bind / DHCP assert / `iptables -I` / `nmcli` probe). Also inserted an
  `Ensure firewalld is running` systemd task immediately before the zone-bind — Cloud Base does
  not start firewalld by default and the `immediate: true` half of the bind needs the live
  daemon. Did NOT touch SELinux-permissive (F7) or the DOCKER-USER reboot-persistence (F8) —
  both are design-laden Phase-3 items.
- **T1.4** — replaced the ~65-line vault + `ssh -T git@github.com` + `git@` SSH clone +
  `always:` cleanup block with a ~13-line `ansible.builtin.git` HTTPS clone (public repo,
  `update: false` to match the old `creates:` once-only semantics) + a `lineinfile` sourcing
  the completion from `.bashrc`. lxc-bash provides shell aliases/completion only — not an LXC
  prerequisite — so a GitHub SSH identity was never actually needed.
- Net: play 431 → 401 lines (−33 insertions / +… ; `git diff --stat` = 33 ins / 62 del).

**Validation blocker resolved.** First `--syntax-check` returned rc=4:
`couldn't resolve module/action 'community.general.copr'` at line 48 — an UNCHANGED task, not
my edit. Root cause: `community.general` is not installed in this container's ansible-venv
(the markless/basic-configs plays syntax-checked fine because they use only
`ansible.builtin`/`ansible.posix`). Fixed by `ansible-galaxy collection install -r
requirements.yml` (installs `community.general` 13.2.0 + `ansible.posix` 2.2.2 to
`~/.ansible/collections`). Re-ran: **rc=0, clean**. This is a local dev-env provisioning step
(the repo's own CI does the same `ansible-galaxy install -r requirements.yml`), not a
manual-fix workaround of a real defect.

## 18:53 · handoff · P1 — T1.5 QA (host) + Phase-4 adversarial review next

T1.3/T1.4 ready to commit (edit+commit only; human pushes + HOST-tests). Phase-1 code is
done; T1.5 is the HOST `qa-all.bash` run (ruff absent here). Next: an adversarial review pass
(Phase 4 / Task 4.1) over ALL four Phase-1 play edits before the human HOST-tests, persisted
as a tracked review file in this folder. Then Phase 2 (linger / systemd-user-tweaks /
podman.socket).

## 18:58 · finding · T4.1 — Phase-1 adversarial review (Fable): SOUND-WITH-FIXES, all applied

Dispatched a Fable adversarial reviewer over all four Phase-1 edits (T1.1–T1.4). Verdict
**SOUND-WITH-FIXES** — no blocker; 1 MUST-FIX + 2 NITs. Full findings persisted at
`reviews/2026-07-29-phase1-play-edits-review-fable.md`. Categories it cleared with no issue:
dep completeness (all 5 declared packages map to a real consumer; `python3-firewall` is the
correct bindings package; `git` is a safe pre-existing assumption), ordering, HTTPS-clone
ownership/parent-dir-creation/`update: false` semantics + no orphaned vault var, fail-fast
annotations, 2.19 parser hazards, scope, mktemp/trap, fwupd-gate precedent.

Applied all three:

- **MUST-FIX** (`play-lxc-install-config.yml`) — this is Cloud Base's first-ever firewalld
  start; nothing guaranteed the default zone still permits SSH before enabling it, a
  self-lockout path on exactly the remote-headless-VM class this plan targets. Added an
  idempotent `ansible.posix.firewalld` task (`service: ssh`, no `zone:` → resolved default
  zone, `permanent: true`, `immediate: true`) right after the firewalld-start task. No-op on
  stock Fedora (public zone already allows ssh); load-bearing on any non-stock cloud image.
- **NIT-2** — added a NOTE by the `iptables-nft` dep about the since-reverted Fedora 42
  packaging bug (missing `/usr/bin/iptables` alternatives link) so a future command-not-found
  on the DOCKER-USER assert is diagnosed as a distro regression, not an IaC bug.
- **NIT-3** (`play-markless.yml`) — brought the T1.1-touched shell block into house style:
  `set -ex` → `set -euxo pipefail` (AnsibleStyle "External Repository Integration").

`ansible-playbook --syntax-check` rc=0 on both edited plays.

## 18:59 · handoff · P2 — awaiting Fable design verdict on linger home + oomd hardening

Phase-1 review fixes committed (edit+commit only; human pushes). Phase 2 (T2.1 linger, T2.2
oomd reload/verify hardening, T2.3 podman.socket) is blocked on a second Fable agent
(`phase2-design`) resolving two design calls the user explicitly deferred to me: (1) where to
home `loginctl enable-linger` (new `play-AC-user-linger.yml` vs. fold into an existing play);
(2) how aggressively to harden the oomd reload/verify given this repo has NO existing
precedent for a *working* headless `systemctl --user` (prevent-ssh-suspend sidesteps it by
skipping on server). Verdict doc will land at `reviews/2026-07-29-phase2-design-decision-fable.md`.

## 19:10 · decision · P2 — Fable design verdict: fold into systemd-user-tweaks + FULL-HARDEN-NOW

`phase2-design` (Fable) returned. **D1**: home linger in `play-systemd-user-tweaks.yml` (NOT a
new play — no independent lifecycle; the `AA/AB/ZZ` prefixes are reserved bookend anchors, not
a convention to extend; it already precedes import #23 by living in import #11). **D2**:
FULL-HARDEN-NOW — the decisive finding is that `loginctl enable-linger` only QUEUES the
`user@UID.service` start async (real cold-start race for a never-logged-in Cloud Base user;
`play-rclone.yml` is not counter-evidence — it only ever runs against a user with a live
session), but pairing it with an explicit `ansible.builtin.systemd: state: started` of
`user@UID.service` closes the race deterministically (the module blocks on the Type=notify job
until READY=1). And `XDG_RUNTIME_DIR` — not `DBUS_SESSION_BUS_ADDRESS` — is the var
`systemctl --user` actually needs (private socket). Full writeup with file:line + checklist in
`reviews/2026-07-29-phase2-design-decision-fable.md`.

## 19:12 · action · T2.1,T2.2,T2.3 — Phase 2 implemented (headless user-manager correct)

Implemented all three per the checklist + one refinement of my own:

- **T2.1** `play-systemd-user-tweaks.yml` first 3 tasks: `getent` UID → `enable-linger`
  (`creates:`) → explicit `systemd: user@UID.service state: started` (the deterministic start).
- **T2.2** same play: added the `XDG_RUNTIME_DIR`+`DBUS_SESSION_BUS_ADDRESS` env to the reload
  handler + verify; **deleted the core tree's only `ignore_errors: true`**; converted the
  verify's `failed_when: false` into a real `assert` on `ManagedOOMMemoryPressure=auto`.
  **Refinement (not in the design checklist):** inserted a `meta: flush_handlers` BEFORE the
  verify — handlers otherwise flush only at play end, so the assert would read the pre-reload
  user.slice and trip on a first run. Now it reads the reloaded value.
- **T2.3** `play-podman.yml`: deleted the `dbus_session_check` probe-then-`when` anti-pattern;
  gave the `podman.socket` enable its own `become_user` + env block + a defensive `getent`;
  dropped the `when:` so the Docker-API socket is enabled unconditionally, relying on T2.1.
  Known edge (documented in-play): standalone `play-podman.yml` on a box that has NEVER run
  the full playbook (no prior linger) fails loud on the enable rather than silently skipping —
  fail-fast-acceptable; the full run.bash flow (the plan's target) is deterministic.

Validation: `--syntax-check` rc=0 on both; grep confirms no unannotated
`failed_when: false`/`ignore_errors` remain and `dbus_session_check` is fully gone.

## 19:22 · decision · P3 — Fable design verdict: /run/ostree-booted gate + PartOf=docker unit

`phase3-design` (Fable) returned. **T3.1**: gate on `/run/ostree-booted` (blocklist-of-atomic),
NOT a `VARIANT_ID` allowlist — the marker is boot-truth for "ostree-managed root" (the actual
dnf/`/etc`-write failure mechanism), can't false-reject Workstation/Server/Cloud, and needs no
per-spin maintenance as Fedora ships new Atomic spins. **T3.2/F8**: a systemd oneshot
`lxc-docker-user-iptables-reconcile.service` (`After=/Requires=/PartOf=docker.service`,
`WantedBy=multi-user.target`) driven by ONE shared script both Ansible and the unit call —
firewalld direct rules rejected (can't resync after docker flushes DOCKER-USER = only fixes the
reboot half), iptables-save/restore rejected (snapshots Docker's own dynamic chains). Full doc
w/ exact YAML + script/unit content + HOST-test checklist:
`reviews/2026-07-29-phase3-design-decision-fable.md`.

## 19:24 · action · T3.1,T3.2 — Phase 3 implemented (edition fail-fast + DOCKER-USER persistence)

- **T3.1** `play-AA-preflight-sanity.yml`: added `stat /run/ostree-booted` + `assert not exists`
  (with an actionable multi-line fail_msg naming the supported/rejected sets), placed between
  "Check Fedora" and "Check provisioning_profile". Plain modules — no 2.19 parser exposure.
- **T3.2/F8**: new `files/usr/local/bin/lxc-docker-user-iptables-reconcile.bash` (0755,
  `set -euo pipefail`, probe-then-insert via `iptables -C`, stable `LXC-IPTABLES-RECONCILE-*`
  marker) + new `files/etc/systemd/system/lxc-docker-user-iptables-reconcile.service`. Replaced
  the LXC play's inline derive/validate/3×probe-insert block with deploy-script → apply-now
  (changed_when on the marker) → deploy-unit → enable+start; updated the now-stale "runtime-only"
  caveat comment. One script = single source of truth for the rule set (no live-vs-persist drift).
- **NOT done**: T3.3 (optional multimedia/dev-header gating — deferred, enhancement only).

Validation: `--syntax-check` rc=0 on both plays; `bash -n` + shellcheck (present here) PASS on
the script. **T3.2 is HOST-test-warranting** (design-flagged): the `PartOf=` restart-propagation
onto a `Type=oneshot` unit and the "docker creates its chains before docker.service is active"
ordering assumption are correct-on-paper but must be exercised live (reboot + `systemctl restart
docker` + container-egress checks) — added to Phase 4 T4.2 HOST criteria.

## 19:25 · handoff · P3 — Phase 3 core done; next = Phase-2/3 impl review + Phase 5 bundle

Phases 1-3 code complete + committed (edit+commit only). Remaining: an adversarial review over
the Phase 2 + Phase 3 *implementations* (Phase 1 already got one; Phases 2/3 had Fable *design*
reviews pre-implementation, not post-impl adversarial passes) → Phase 4 T4.1; then Phase 5
(server-recommended optional-play bundle). Design-laden Phase 5 mechanism (bundle keyword vs
per-play marker) is the next steer/Fable candidate.

## 19:40 · finding · T4.1 — Phase-2/3 impl review (Fable): SOUND-WITH-FIXES, all applied

Dispatched a Fable adversarial reviewer over the ACTUAL committed Phase-2/3 code (`8ae3106`,
`eb14ba2`), not the design docs. Verdict **SOUND-WITH-FIXES** — no blocker (nothing breaks the
untagged full `playbook-main.yml`/run.bash run the human HOST-tests). Broad checked-clean surface:
getent fact scope/index + cross-play persistence, `state: started` genuinely blocking on
`user@.service` Type=notify READY=1, flush_handlers placement + re-run non-false-pass, ignore_errors
fully gone, podman standalone fail-loud + become pattern, T3.1 ostree assert (no 2.19 hazard), F8
script idempotency + `PartOf=` restart-propagation (verified vs systemd.unit(5)), zero dangling
`lxc_subnet_cidr` refs, file modes, no state:started staleness bug. Review:
`reviews/2026-07-29-phase23-impl-review-fable.md`.

Applied all actionable findings:

- **MUST-FIX 1** `play-systemd-user-tweaks.yml`: the getent/linger/`user@`-start tasks were tagged
  `[systemd, linger]` but the oomd verify/assert/handler that CONSUME the getent fact are
  `[systemd, oomd]` — so `--tags oomd` would skip the fact producers and fail on undefined var
  under `any_errors_fatal`. Fixed by tagging the 3 bring-up tasks `always` (unconditional
  prerequisites). Untagged HOST run was never affected (shared `systemd` tag).
- **MUST-FIX 2** reconcile script: the `iptables -n -L DOCKER-USER` chain dump landed on stdout
  ahead of the parsed marker (StderrHygiene violation). Fixed via capture-the-probe
  (`chain_dump="$(… 2>&1)"` → re-emit to stderr on failure), so stdout is only the marker.
- **NIT (SIGPIPE)** reconcile script: `| awk '{print $1}' | head -n1` → `| awk 'NR==1{print $1}'`.
  Chose read-all-input over the reviewer's `awk '{print $1; exit}'` — early awk-exit could still
  SIGPIPE `ip` under pipefail on a multi-route bridge; `NR==1` consumes all input so it can't.
- **NIT (one-failure-surface)** `play-systemd-user-tweaks.yml`: the oomd verify `shell … | grep`
  → `command: systemctl --user show user.slice --property=ManagedOOMMemoryPressure` (no pipe/grep),
  so the only failure surface is "manager unreachable"; the value check is the assert.
- **NIT (consistency)**: aligned the `user@`-start task to bracket `ansible_facts['getent_passwd']`
  access, matching the handler/verify/podman.

Correction (review NIT): the Phase-2 design doc + earlier journal entries say `play-podman.yml` is
import "#23" — the actual count of `playbook-main.yml` is **#19** (docker=17, lxc=18, podman=19).
Does not change any correctness claim (systemd-user-tweaks #7 still precedes podman #19); recorded
here rather than rewriting the shipped design artifact.

Validation: `--syntax-check` rc=0; `bash -n` + shellcheck PASS; all 3 bring-up tasks carry `always`.

## 19:50 · decision · P5 — Fable design: manifest file + 12-play curated bundle

`phase5-design` (Fable) returned (its deliverable landed before a transient API-500 killed the
final message — recovered from disk). **Mechanism**: a tracked manifest
`playbooks/imports/optional/server-recommended.bundle` (one basename/line) that `run.bash`
expands the reserved `server-recommended` token into — chosen over a hardcoded array (couples
curation to RUN_BASH_VERSION churn) and a per-play marker (scatters membership). **Curation**: 12
plays, each read + verified `scope: general` + headless-safe; `play-lastpass` EXCLUDED as
NOT-headless-safe (unconditional `pause` prompts). Full per-play table + exact run.bash diff:
`reviews/2026-07-29-phase5-bundle-design-fable.md`.

## 19:55 · action · T5.1,T5.2,T5.3 — Phase 5 implemented (server-recommended bundle)

- Created `playbooks/imports/optional/server-recommended.bundle` (header + 12 basenames).
- `run.bash`: inserted the expansion+de-dup block in `hl_run_optional_playbooks()` before the
  per-token resolver (unknown-token abort + per-play failure handling inherited unchanged);
  updated the function comment; bumped `RUN_BASH_VERSION` 1.10.0 → 1.11.0 + changelog line;
  added the `--help-run-headless` entry. Interactive menu deliberately NOT touched (YAGNI —
  its `A) Run all` already covers Common Optional).
- Docs: `docs/headless-server-install.md` example + `docs/headless-provisioning.md` table row.

Validation: `bash -n` OK, `shellcheck -S error` clean on `run.bash`. The `.bundle` file is plain
text — not matched by any `find -name "*.yml"`, so no play-enumeration picks it up as a play.
Next: an adversarial review over the run.bash change (state-changing installer — per
`CLAUDE/Plan/CLAUDE.md`) before the human HOST-tests; then commit fixes.
