# Plan 00065 — Journal 26-07-29

> **Append-only activity log** for plan 00065. One file per day
> (`00065-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:22 · action · — — plan scaffolded

Plan 00065 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 18:40 · finding · — — three whole-run BLOCKERS + two silent misbehaves on Cloud Base

`PLAN.md` filled from a downstream Cloud Base provisioning audit, re-verified against
this repo's `F44` HEAD. Under `ansible.cfg any_errors_fatal = true`, three `general` core
plays each abort the whole run on minimal Cloud Base: fwupd `get-devices` (import 3, bare
`set -e`, fwupd never installed / no firmware surface); `play-lxc-install-config` (import
18 — hard-deps `firewalld`/`dnsmasq`/`iptables-nft`/`NetworkManager` it never installs,
plus a vault + `git@`-SSH clone of the **public** `lxc-bash`); `play-markless` `cd
~/Downloads` (import 26, XDG dir absent headless). Two more leave a container host silently
wrong (no `loginctl enable-linger` ⇒ oomd override inert, `podman.socket` skipped). Missing:
a preflight edition/flavour fail-fast (`VERSION_ID` can't distinguish edition per Plan
00063's own journal).

## 18:44 · decision · — — fix deps at the layer that needs them

Per this repo's "Missing Dependencies — Fix in IaC" rule: a play that needs a package
`ansible.builtin.dnf`-installs it (never probe/assume); a pre-Ansible dep is installed by
`run.bash` itself. No `failed_when: false`/`ignore_errors` papering. LXC stays a first-class
`general` play (load-bearing) — fix is gating + dep-declaration, never removal.

## 18:47 · action · T1.1,T1.2 — first two blockers fixed (Phase 1)

- **T1.1** `play-markless.yml`: `cd ~/Downloads` → `mktemp -d` scratch dir + `trap 'rm -rf
  "$work"' EXIT`; also removes the `rm -rf markless*` shared-dir hazard. Play stays general.
- **T1.2** `play-basic-configs.yml`: gated the fwupd task `when: provisioning_profile != 'server'` with a WHY comment (same pattern as the USB-audio task ~11 lines above).
- Validation: `ansible-playbook --syntax-check` rc=0 on BOTH (throwaway gitignored
  `vault-pass.secret`). Full `qa-all.bash` can't run in this CCY container (`ruff` not
  provisioned — a HOST tool); edits touch no Python, so HOST QA is deferred to the live test.

## 18:49 · handoff · P1 — Phase-1 remainder is the LXC surgery + design-laden items

Committed T1.1/T1.2 (edit+commit only; NOT pushed — human pushes + runs the HOST test).
Next entry point: **T1.3** (LXC dep-declaration) + **T1.4** (LXC `lxc-bash` → HTTPS, ~65
lines removed) — bigger, careful edits to a ~400-line play, warrant an adversarial review
pass before the human HOST-tests. **Design-laden items needing a steer** before I make
opinionated changes to this public repo: LXC-F1 opt-in var shape, F7 SELinux-permissive
gating, F8 DOCKER-USER reboot-persistence mechanism (firewalld permanent vs oneshot unit),
and the Phase-5 server-recommended bundle mechanism.

## 18:52 · action · T1.3,T1.4 — LXC play unblocked (dep-declare + HTTPS clone)

Both edits to `play-lxc-install-config.yml` landed; play is now `general`-safe on Cloud Base.

- **T1.3** — added to the `Install Packages` task: `firewalld`, `python3-firewall`, `dnsmasq`,
  `iptables-nft`, `NetworkManager`, each with a WHY comment naming the downstream task that
  hard-requires it (zone-bind / DHCP assert / `iptables -I` / `nmcli` probe). Also inserted an
  `Ensure firewalld is running` systemd task immediately before the zone-bind — Cloud Base does
  not start firewalld by default and the `immediate: true` half of the bind needs the live
  daemon. Did NOT touch SELinux-permissive (F7) or the DOCKER-USER reboot-persistence (F8) —
  both are design-laden Phase-3 items.
- **T1.4** — replaced the ~65-line vault + `ssh -T git@github.com` + `git@` SSH clone +
  `always:` cleanup block with a ~13-line `ansible.builtin.git` HTTPS clone (public repo,
  `update: false` to match the old `creates:` once-only semantics) + a `lineinfile` sourcing
  the completion from `.bashrc`. lxc-bash provides shell aliases/completion only — not an LXC
  prerequisite — so a GitHub SSH identity was never actually needed.
- Net: play 431 → 401 lines (−33 insertions / +… ; `git diff --stat` = 33 ins / 62 del).

**Validation blocker resolved.** First `--syntax-check` returned rc=4:
`couldn't resolve module/action 'community.general.copr'` at line 48 — an UNCHANGED task, not
my edit. Root cause: `community.general` is not installed in this container's ansible-venv
(the markless/basic-configs plays syntax-checked fine because they use only
`ansible.builtin`/`ansible.posix`). Fixed by `ansible-galaxy collection install -r
requirements.yml` (installs `community.general` 13.2.0 + `ansible.posix` 2.2.2 to
`~/.ansible/collections`). Re-ran: **rc=0, clean**. This is a local dev-env provisioning step
(the repo's own CI does the same `ansible-galaxy install -r requirements.yml`), not a
manual-fix workaround of a real defect.

## 18:53 · handoff · P1 — T1.5 QA (host) + Phase-4 adversarial review next

T1.3/T1.4 ready to commit (edit+commit only; human pushes + HOST-tests). Phase-1 code is
done; T1.5 is the HOST `qa-all.bash` run (ruff absent here). Next: an adversarial review pass
(Phase 4 / Task 4.1) over ALL four Phase-1 play edits before the human HOST-tests, persisted
as a tracked review file in this folder. Then Phase 2 (linger / systemd-user-tweaks /
podman.socket).

## 18:58 · finding · T4.1 — Phase-1 adversarial review (Fable): SOUND-WITH-FIXES, all applied

Dispatched a Fable adversarial reviewer over all four Phase-1 edits (T1.1–T1.4). Verdict
**SOUND-WITH-FIXES** — no blocker; 1 MUST-FIX + 2 NITs. Full findings persisted at
`reviews/2026-07-29-phase1-play-edits-review-fable.md`. Categories it cleared with no issue:
dep completeness (all 5 declared packages map to a real consumer; `python3-firewall` is the
correct bindings package; `git` is a safe pre-existing assumption), ordering, HTTPS-clone
ownership/parent-dir-creation/`update: false` semantics + no orphaned vault var, fail-fast
annotations, 2.19 parser hazards, scope, mktemp/trap, fwupd-gate precedent.

Applied all three:

- **MUST-FIX** (`play-lxc-install-config.yml`) — this is Cloud Base's first-ever firewalld
  start; nothing guaranteed the default zone still permits SSH before enabling it, a
  self-lockout path on exactly the remote-headless-VM class this plan targets. Added an
  idempotent `ansible.posix.firewalld` task (`service: ssh`, no `zone:` → resolved default
  zone, `permanent: true`, `immediate: true`) right after the firewalld-start task. No-op on
  stock Fedora (public zone already allows ssh); load-bearing on any non-stock cloud image.
- **NIT-2** — added a NOTE by the `iptables-nft` dep about the since-reverted Fedora 42
  packaging bug (missing `/usr/bin/iptables` alternatives link) so a future command-not-found
  on the DOCKER-USER assert is diagnosed as a distro regression, not an IaC bug.
- **NIT-3** (`play-markless.yml`) — brought the T1.1-touched shell block into house style:
  `set -ex` → `set -euxo pipefail` (AnsibleStyle "External Repository Integration").

`ansible-playbook --syntax-check` rc=0 on both edited plays.

## 18:59 · handoff · P2 — awaiting Fable design verdict on linger home + oomd hardening

Phase-1 review fixes committed (edit+commit only; human pushes). Phase 2 (T2.1 linger, T2.2
oomd reload/verify hardening, T2.3 podman.socket) is blocked on a second Fable agent
(`phase2-design`) resolving two design calls the user explicitly deferred to me: (1) where to
home `loginctl enable-linger` (new `play-AC-user-linger.yml` vs. fold into an existing play);
(2) how aggressively to harden the oomd reload/verify given this repo has NO existing
precedent for a *working* headless `systemctl --user` (prevent-ssh-suspend sidesteps it by
skipping on server). Verdict doc will land at `reviews/2026-07-29-phase2-design-decision-fable.md`.
