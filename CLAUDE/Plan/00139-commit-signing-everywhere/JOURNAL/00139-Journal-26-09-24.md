# Plan 00139 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00139. One file per day
> (`00139-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 16:49 · action · — — plan scaffolded

Plan 00139 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:50 · decision · —   — Why, and the owner's ruling on agents signing

The owner asked for commit signing to be rolled in, because another system of theirs is
starting to assume it for this repo. Their reference clone was fetched and is current on
all 8 branches. At its pushed state, nothing there checks commit signatures on this repo:
it pins a commit and digest-checks `run.bash`. This repo's GitHub rulesets do not require
signatures either. The owner chose to build signing anyway, ahead of need.

On the key question the owner answered: "any agent = my any agent / not ANY agent / this
is still a huge security win - we get commits verified by ME or something from MY MACHINE /
the goal is not to prevent MY OWN AGENTS from being able to push stuff". That overrules
Plan 00137 D3, and it gives this plan's D2 to D4.

The session's single failsafe recovery cron, 6adbf689, covers this plan. No new one was
created.

## 17:10 · action · 78d33e43   — Implementation merged; Task 1.3 is an owner step

The implementation fork's commit `6001a540` was merged as `78d33e43`. Report:
`subagent-reports/260924-fork-impl-opus-5.md`.

What it did:
- `play-git-configure-and-tools.yml` generates `~/.ssh/id_ed25519_git_signing` with no
  passphrase when it is absent. It asserts the key is 0600 with its `.pub`, and that it
  loads with an empty passphrase. It sets `gpg.format`, `user.signingkey`,
  `commit.gpgsign` and `tag.gpgsign` globally, and removes the XDG keys Plan 00137 wrote.
- CCY 3.66.0. `stage_git_signing_key` (in `lib/ssh-handling.bash` 1.5.0) copies the key
  into the gitconfig copy's directory. That directory is already mounted read-only and
  relabelled, and it is removed by the launcher's EXIT trap. The function repoints
  `user.signingkey`, and refuses a launch that has signing on but no usable key.
- A new gate, `ccy-git-signing`, passes 19 of 19. It fails against the old library.

Task 1.3 is decided as an owner step. The play does not register the key through `gh`,
for two reasons. Adding `admin:ssh_signing_key` to the shared required scopes would fail
every account's scope audit until each was refreshed. And the key belongs on one account
only, the one whose verified email is `user_email`. Acceptance reads the public
`users/<login>/ssh_signing_keys` endpoint, which needs no scope.

QA in the main checkout after the merge: `ccy-git-signing` passes 19. Three gates fail, all
because of this container's tools, not the code:
- ruff is 0.16.4 here, against the 0.16.8 pin in `.qa-versions` (another session raised
  it today);
- `helper-counts-reader` fails on that same mismatch;
- `bash-history-search` fails because gawk is missing.

The Dockerfile already installs both, so a container rebuild clears all three.

For the owner:
- With a self-updating server that trusts this key, every push is a release.
- A host_vars `git_signing_key` still pointing at the old passphrase key stops the play,
  and the message gives the remedy.

## 18:04 · handoff · —   — Two review rounds done; code complete, host steps remain

Review round 1 (`subagent-reports/260924-qa-reviewer-opus-5.md`) was FAIL. With signing on
globally, the self-update-cycle test's "unsigned" fixtures came out signed, and 149 of 163
cases passed. Four should-fix findings and five nits came with it. A worktree fork fixed
all ten (`d8a1af89`, merged as `8003868e`):
- `test-self-update-cycle.bash` and `qa-helper-tests.bash` ignore the machine's global and
  system git config;
- the self-update test starts with a probe proving an unsigned commit stays unsigned;
- `deploy.bash` installs the new launcher before turning signing on;
- `docs/ccy.md` names the signing key among the risks;
- the play tells a corrupt key apart from one with a passphrase;
- acceptance checks 8 and 11 are more honest about what they can see.

Round 2 (`subagent-reports/260924-qa-reviewer-round2-opus-5.md`) was PASS WITH NITS. The
coordinator fixed its should-fix and nits directly:
- the isolation probe fails when it could not make a commit, where it used to read "no
  commit" as "unsigned";
- `test-ccy-git-signing.bash` and the two self-update helper tests drop `GIT_CONFIG_COUNT`
  and `GIT_CONFIG_PARAMETERS`, which outrank `GIT_CONFIG_GLOBAL`;
- `helpers/CLAUDE.md` states that rule.

Re-run under the reviewer's scenarios, with signing forced on through `GIT_CONFIG_COUNT`,
once with a real throwaway key and once with a missing one:
- `ccy-git-signing`: 23 of 23 both times (it was 21 of 23);
- `self-update-cycle`: 163 of 163;
- `test_update` and `test_affected_plays`: OK.

Handoff. The code is complete and reviewed. What is left needs the host and the owner:
1. Deploy through the desktop agent's mailbox, or by hand: `./deploy.bash`, then
   `./acceptance.bash`.
2. The owner registers the key on the GitHub account whose verified email is `user_email`
   (Task 1.3).
3. A commit inside a ccy session started after the deploy (Task 4.3).
4. The self-updating server gets the new key's `.pub` line (Task 3.1).
5. The owner's call on a ruleset that requires signatures (Task 4.2).

## 20:53 · finding · —   — Deployed on the host; acceptance 10 of 11, only GitHub registration left

The desktop `cc` agent took mailbox request 0002. Deploy capture:
`untracked/plan-runs/00139-commit-signing-everywhere/deploy/20260924-214608`, exit 0.
Acceptance capture: `acceptance/20260924-214719`, exit 1.

Acceptance was REJECTED on one check out of 11, and all 11 ran:
- checks 1–4: `gpg.format` is ssh, commit and tag signing are on, and `user.signingkey`
  names the key;
- checks 5–7: the key is 0600 and the owner's, needs no passphrase, and matches its `.pub`;
- check 8: the XDG config holds none of Plan 00137's settings;
- checks 9 and 10: a real commit and a real tag are signed, and verify against the key;
- check 11 fails: none of the 5 GitHub accounts read lists the key as a signing key. That
  is expected: registration is the owner's step (Task 1.3).

The play did not stop at the passphrase assert, so host_vars did not name the old key.

The owner asked the desktop agent whether the signing key is vaulted and pushed to the
config repo. It is neither. The key is generated per machine and nothing copies it
anywhere; a reinstall makes a new one, which has to be registered and trusted again. The
owner has not asked for a change.

The mailbox now has a `to-ccy/` folder and `watch.bash`, a background watcher for each
side that exits when there is something to handle. That replaces hourly polling.

## 20:59 · decision · cd50ffc1   — The public key is recorded in host_vars (desktop agent, owner-approved)

The desktop agent (message D-0001) merged and pushed `cd50ffc1`, with the owner's approval. It adds commit `a594063e`: the git play writes the machine's public signing key into `localhost.yml` as `git_signing_public_key`, in a managed block, so run.bash's "Save local config to repo" keeps it. Reviewed here. Four other plays already use blockinfile on that file, and its metadata shows mode 0600 and plain YAML (it holds inline vault values, so it is not one encrypted blob). The name is kept apart from `self_update_signing_public_key` on purpose: a server must never be made to trust its own commits. Recorded as Task 1.4. Mailbox request 0003 asks the desktop agent to redeploy and re-accept.
