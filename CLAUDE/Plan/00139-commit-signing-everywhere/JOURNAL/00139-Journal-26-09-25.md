# Plan 00139 — Journal 26-09-25

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00139. One file per day
> (`00139-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:18 · action · — — plan scaffolded

Plan 00139 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 07:18 · decision · —   — Owner: a signing key per GitHub account, registered by IaC

The owner ruled again on Task 1.3: "GH key signing IS NOT JUST FOR THIS REPO / if we are doing it we can do it PROPERLY" and "you can make different keys / WE ALREADY DO THIS". The global `commit.gpgsign` signs every repo on the machine, and `gh-switch <alias> --update-git` sets a repo's email to that account's address. So one key registered on one account leaves most commits Unverified. Task 1.3 is rewritten to follow the existing `github_<alias>` auth-key pattern: `play-github-cli-multi.yml` generates and uploads a signing key per account, git picks it with `includeIf` on the `github.com-<alias>` remote, ccy stages the resolved key, and `admin:ssh_signing_key` joins the shared scope audit. A worktree fork is implementing it. The earlier fork's reasoning (one account only, keep the scope out of the shared list) is withdrawn.
