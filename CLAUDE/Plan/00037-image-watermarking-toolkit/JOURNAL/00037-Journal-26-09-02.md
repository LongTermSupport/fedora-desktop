# Plan 00037 — Journal 2026-09-02

> **Append-only activity log** for plan 00037. One file per day
> (`00037-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 22:12 · action · — — plan document slimmed; history archived

The original `PLAN.md` had grown past the plan-size warning threshold. It was
renamed verbatim to [PLAN_archive.md](../PLAN_archive.md) and replaced by a
lean `PLAN.md` carrying only the header, overview, goals, non-goals, the task
tree (tick state preserved unchanged), success criteria and milestones.

Durable content was extracted into named supporting documents:

- [DECISIONS.md](../DECISIONS.md) — Decisions 1–6 and the risk table
- [RESEARCH-watermarking.md](../RESEARCH-watermarking.md) — tooling,
  metadata field set, sentinel namespace, visible-mark technique,
  idempotency channels, composability surface, delivered artefacts

The dated progress notes from 2026-04-28 (plan creation, the Task 1.4 recipe
lock including the `XMP-x:` dead-end and the exiftool date-format gotcha,
and the Phase 2/3/4 container delivery with its smoke-test results) were not
previously journalled. They remain readable in full under
`## Notes & Updates` in `PLAN_archive.md`; the reusable facts from them now
live in the two supporting documents above.

State of the work is unchanged by this slimming: container side of Phases
2–4 is delivered; Phase 5 (host deploy and host smoke test) is next.
