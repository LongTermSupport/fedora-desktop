# Plan 00073 — Journal 26-08-02

> **Append-only activity log** for plan 00073. One file per day
> (`00073-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:36 · action · — — plan scaffolded

Plan 00073 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 07:50 · finding · — — password sudo is already supported; only HEADLESS refuses it

The trigger is external: lts-infra Plan 00031 needs this runbook to provision a CI runner host
whose provisioning user must NOT keep `NOPASSWD:ALL`. The owner asked whether passwordless sudo is
*necessarily* required rather than letting it be assumed. Measured at `e67abde`:

- **Interactive** (`run.bash:1203`, `:2213`): probes `sudo -k -n true` and falls back to
  `--ask-become-pass`. Password sudo works today.
- **Headless** (`run.bash:193-195`): `headless_fail "Passwordless (NOPASSWD:ALL) sudo is
  required"`. A hard stop — because `--ask-become-pass` prompts and there is no TTY.

So the constraint belongs to the headless PATH, not to fedora-desktop. That reframes the work from
"remove a requirement" to "add the missing non-interactive equivalent of a branch that already
exists".

## 07:50 · decision · — — this EXTENDS Plan 00063, it does not correct it

Worth stating, because the easy framing is "00063 got this wrong". It did not. D4 and Decision 5
argue the NOPASSWD requirement deliberately, and 00063's own risk table already carries
*"Password-sudo box: first direct `sudo` hangs headless … mitigated by a startup NOPASSWD probe →
fail fast"*. Failing fast on an unsupported configuration was correct for v1; this plan supplies
the support. `docs/headless-server-install.md:249` and `run.bash:503-504` already name it as a
limitation, so this closes a gap that was acknowledged, not reverses a judgement.

## 07:50 · finding · — — the change is far smaller than the naive count suggests

`rg -c "sudo " run.bash` → 32, which reads like a large refactor. Reading the hits instead of
counting them:

| Category                                                           | Count  | Where                                    |
| ------------------------------------------------------------------ | ------ | ---------------------------------------- |
| Real privileged commands (`dnf`, `grubby`, `hostnamectl`, `chown`) | **12** | `:1485`–`:1620`, one contiguous region   |
| `sudo reboot`                                                      | 2      | already skipped when `RUN_BASH_REBOOT=0` |
| The probes themselves                                              | 3      | `:193`, `:1203`, `:2213`                 |
| Comments / help text                                               | 15     | —                                        |

Generalisable: a `-c` count measures *matches*, not *work*, and here the difference was nearly a
factor of three. Taken at face value it would have argued for a much heavier plan than the code
needs.

## 07:50 · finding · — — the askpass idiom is already in this script

`hl_ssh_agent_start` (`:222-248`) writes a `0600` secret file plus a `0700` helper from a **quoted**
heredoc — so the secret never enters the helper's own text, only the non-secret path does — then
registers both in `HL_SECRET_FILES` for the EXIT-trap shred and exports the path.

`SUDO_ASKPASS` consumes a helper exactly as `SSH_ASKPASS` does, so the sudo version is a near-copy
of a pattern this file already trusts. That matters for review: reuse of a local idiom, not a new
secret-handling mechanism to be argued from scratch.

Verified rather than assumed: `--become-password-file` / `--become-pass-file` is a **native**
ansible-core flag, present in 2.19.11 (`ansible-playbook --help`). The Ansible half needs no
wrapper at all.

## 07:50 · decision · D5 — carry V3.7's weak probe forward EXPLICITLY

Plan 00063 V3.7: *"a command-scoped NOPASSWD passes `true` but fails `dnf`"*. So the existing
`sudo -k -n true` gate can pass on a box where the first real privileged command dies.

This plan does **not** fix that, and the temptation is to say nothing — the new code inherits the
weakness quietly and no reviewer would notice. Stated instead: the password probe proves *the
password authenticates*, not *that this user may run `dnf`*, and `ALL` scope stays the documented
requirement. An unfixed limitation that is written down is a known cost; the same limitation
unmentioned is a trap for whoever reads the new preflight and assumes it is complete.

## 07:50 · thought · P3 — in-container green will NOT be evidence

Recording the trap before walking into it. Plan 00063's own note: *"END-TO-END execution is
HOST-verified on a real server (Phase 3) — in-container this is `bash -n` + shellcheck + preflight
acceptance only."*

A container cannot exercise `sudo` against a real sudoers file. `acceptance.bash` passing will
prove the preflight branches behave, and nothing about whether the twelve rewritten call sites
actually obtain privilege. Task 3.3 exists so that gap is a task rather than an assumption, and the
risk table carries it at H/H.
