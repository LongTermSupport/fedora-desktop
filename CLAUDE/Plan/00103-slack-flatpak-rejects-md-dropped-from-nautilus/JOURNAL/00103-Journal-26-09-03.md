# Plan 00103 — Journal 26-09-03

> **Append-only activity log** for plan 00103. One file per day
> (`00103-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 16:36 · action · — — plan scaffolded

Plan 00103 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:40 · finding · P1 — repo-side facts and the working hypotheses

Written from the CCY container, so nothing here is a host observation.

- Slack is the Flathub Flatpak, installed system-wide by `play-comms.yml`; the repo
  sets no `flatpak override` for it, so the sandbox runs on the manifest's finish-args.
- The Slack message is its catch-all for a dropped `File` with an empty type or
  unreadable content; the same text appears for an admin file-type restriction.
- Four hypotheses tabled in `PLAN.md`: H1 path outside the sandbox's filesystem grants,
  H2 the runtime cannot MIME-type `.md`, H3 admin restriction, H4 Slack on XWayland.
  H1 and H2 are the ones a probe can settle; H3 needs the paperclip test by hand.

## 16:45 · action · T1.1 — triage script written, awaiting a host run

`triage.bash --file <the .md>` + `probe-slack.bash` (planlib gather mode, host-only,
read-only). Sandbox probes run `stat`, `wc`, `grep`, `xdg-mime` and `file` INSIDE
`com.slack.Slack` via `flatpak run --command=` so they see exactly what Slack sees.
The Slack log section redacts `xox*-` tokens by substitution and verifies before
writing. The report lands in `triage-runs/<stamp>/`.

## 16:46 · handoff · T1.2

Next: user runs on the host
`./CLAUDE/Plan/00103-slack-flatpak-rejects-md-dropped-from-nautilus/triage.bash --file ~/path/to/mapping-clarifications-business-questions.md`
and notes the three manual observations from `PLAN.md` (paperclip upload, a `.png`
drop, copy-paste). Agent reads the report, settles H1–H4, then Phase 2.

## 16:55 · action · T1.1 — fixture added, --file now optional

The user pointed out that asking for a path to the original file is needless: any
`.md` reproduces it. `sample-drop.md` now sits next to `triage.bash` and is the default
file the probes test, so the manual drag test and the sandbox probes share one path.
`--file` remains for probing a different file. Supersedes the 16:46 hand-off command:
run `triage.bash` with no arguments, then drag `sample-drop.md` into Slack.
