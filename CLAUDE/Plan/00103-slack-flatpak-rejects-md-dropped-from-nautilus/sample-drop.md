# Plan 00103 drop fixture

This file exists to be dragged from Nautilus into Slack. It is the default
file `triage.bash` probes when `--file` is not given, so the probes and the
manual drop test exercise the same path.

Reproduce: open this plan folder in Nautilus, drag this file into a Slack
message, and note whether Slack accepts it or reports it as unsupported.
