# Plan 00104 — Journal 26-09-08

> **Append-only activity log** for plan 00104. One file per day
> (`00104-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:25 · action · — — plan scaffolded

Plan 00104 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:30 · finding · T1.1 — the suspend aborted; nothing recovered

Incident triage off the host journal. `PM: suspend entry (s2idle)` at 11:40:49,
then a USB disconnect storm at 11:40:52 as the dock came out, and no
`PM: suspend exit` anywhere in the boot. Journal volume then held at ~110–260
lines/min for 25 minutes with no idle gap — the machine was awake the whole
time it was in the bag.

Full sanitised transcription with numbered facts: `TRIAGE-EVIDENCE.md`.

## 12:34 · finding · T1.1 — logind handles the lid as an EVENT, not a state

`man logind.conf`: "Only input devices with the power-switch udev tag will be
watched for key/lid switch **events**". The lid had been closed since the
previous day, so the abort produced no new transition and `HandleLidSwitch`
never fired. This is the actual gap — the abort itself is unremarkable.

The same man page gives the precedence as docked → external-power → plain, with
`HandleLidSwitchDocked=` defaulting to `ignore` and **not** set by our playbook.
So the branch that applied on the earlier lid close was probably `Docked`, not
the `HandleLidSwitchExternalPower=ignore` line we deploy. Logged as premise P1 —
it changes which knob a fix should target, so Task 2.2 settles it on the host.

## 12:38 · finding · T1.1 — ruled out: GNOME is not overriding logind

The man page warns that desktop environments commonly take a `handle-lid-switch`
low-level inhibitor, which would make our `laptop-lid.conf` inert. Checked:

```
gsd-media-keys  handle-power-key:handle-suspend-key:handle-hibernate-key  block
```

`gsd-media-keys` blocks the power/suspend/hibernate **keys** only; nothing holds
`handle-lid-switch`. So logind does own the lid and the drop-in is live. Every
`sleep` inhibitor present was `delay`, not `block`, so none of them blocked the
11:40 suspend either.

## 12:41 · finding · T1.1 — the battery safety net is unmanaged drift

`sleep-inactive-battery-type` is `'nothing'`, and a repo-wide grep finds it in
**no** playbook, template or var file. GNOME's own default is `suspend`, so this
was set outside IaC and nothing here would ever restore it. Its AC sibling *is*
managed — deliberately, by `play-prevent-ssh-suspend.yml:51-67` — which is what
made the absence easy to miss.

## 12:46 · decision · — — primary fix is the setting, not a resume hook

First instinct in session was a `system-sleep` post-resume hook that re-suspends
when the lid is still closed. Rejected as the primary: it is more code than
restoring the GNOME default, and it covers strictly **less**, because it only
fires after a resume. If H1 holds (undock a lid-closed machine, never request
suspend at all) the hook never runs and the bag scenario returns unchanged.

Recorded as Decision 1 in `PLAN.md` with the option that was rejected, since it
is the tempting one. The AC→battery udev approach is parked behind a decision
gate in Phase 3 rather than built now — H1 is not yet demonstrated.

## 12:52 · handoff · — — plan filed, host work pending

Committed: `PLAN.md`, `TRIAGE-EVIDENCE.md` (sanitised), `triage.bash` +
`probe-suspend.bash`. Nothing deployed and no host state changed — this session
was read-only diagnosis.

Next agent starts at Task 1.2: run `./triage.bash` **on the HOST** (it enforces
`plan_require_host`) and reconcile its report against `TRIAGE-EVIDENCE.md`. Then
Phase 2 settles H1 and P1, which gates the Decision 2 work.

Failsafe recovery cron for this session: `c3bd8dd6` (hourly at :23, session-only).

## 13:05 · finding · T2.2 — my own probe would have written a misleading empty section

Went to verify P1 rather than leave it as an inference. Two results.

The good one: logind exposes a readable `Docked` property, so the lid branch
does not have to be inferred at all. With the dock detached:

```
Docked: b false
connected output count: 1   (eDP-1 only)
```

The bad one: the probe I had written for this used

```
systemctl show systemd-logind --property=HandleLidSwitch ...
```

which returns **nothing** — logind does not publish the `Handle*` settings as
unit properties. That probe would have emitted a silently empty section, and an
empty section reads as evidence of absence. This is precisely the failure
`CLAUDE/PlanTriage.md` names ("never write a misleading empty result"), written
into the triage script by the same session that quoted the rule.

Fixed: `show_logind_config` now states the limitation in the report instead of
implying a reading, and a new `show_lid_branch_inputs` probe captures the two
values that actually answer the question. P1 drops from "needs investigation" to
"needs one observation with the dock plugged in".

Recorded as F14 in `TRIAGE-EVIDENCE.md`, including the negative result, since
"this command cannot answer it" is itself worth not rediscovering.

## 13:40 · finding · T2.1 — power is direct, so the two variables are separable

Operator reports mains power goes **direct** to the laptop in both home and
away setups, never through the dock, so the dock can be powered down
independently. Recorded as F15.

This reframes the incident: it was **two** disconnections, not one. Unplugging
the dock alone would not have changed AC state, yet the journal shows the USB
storm *and* an NVRM power-source event a second later — packing up, so dock out
then mains out.

Usefully, it also means the AC branch and the display-count branch can be tested
**separately by hand**, which is what makes a mains-only test legitimate rather
than partial. No dock is needed to settle the AC half.

Built `probe-power-transition.bash` + `triage.bash --watch-power` for it: polls
for a live AC transition, snapshots lid/dock/display/power state either side, and
captures the logind/upower/kernel journal delta across it. Polling rather than
prompting, so it can be run mid-work — unplug and replug whenever. Verified the
timeout path fails loudly and writes no section, rather than emitting an empty
one.

## 13:52 · finding · T3.1 — qa-reviewer: the obvious playbook is never deployed

Reviewer verdict FIX-BEFORE-MERGE, 5 should-fix, no blockers. Leak check clean.

The one that changes the plan: `play-laptop-lid-power-management.yml` — the
topically obvious home for the battery setting, and the file that already owns
the lid config — **is imported by nothing**. `playbook-main.yml` does not pull it
in. `play-prevent-ssh-suspend.yml` is imported at `playbook-main.yml:8`.

So Decision 2's deferral was wrong: this is decidable now and the answer is
`play-prevent-ssh-suspend.yml`. Putting a drift-prevention setting in a playbook
that never runs would recreate the exact failure being fixed. Recorded as
Decision 3, and Task 3.1's first subtask closed.

Noted as a follow-on: the unimported lid playbook is a latent drift source in its
own right — the lid config on this host would not be restored by a rebuild.
Out of scope here.

## 13:58 · decision · — — F2's histogram was not reproducible; replaced

Reviewer also caught that F2's per-minute figures matched neither plain
`journalctl -b -1` nor kernel-only. Correct: they spliced a `-p notice` capture
of 11:40–11:51 with a `-p info` capture of 11:52–12:05, then cited the result as
plain `-b -1`.

Replaced with a single unfiltered command and its real output: **~1500–2500
lines/min**, not ~110–260. The conclusion is unchanged and stronger, but the
provenance was wrong, so the correction is stated in-place in F2 rather than the
old numbers being quietly swapped.

## 14:20 · finding · T2.2 — busctl DOES expose the lid settings; my fix was worse

Earlier today I found the `systemctl show` probe returned nothing and "fixed" it
by having the report state the limitation. The reviewer found the better answer:
the settings are logind **manager** properties, so `busctl` reads them fine:

```
busctl get-property org.freedesktop.login1 /org/freedesktop/login1 \
  org.freedesktop.login1.Manager \
  HandleLidSwitch HandleLidSwitchDocked HandleLidSwitchExternalPower
s "suspend"
s "ignore"
s "ignore"
```

So `HandleLidSwitchDocked=ignore` is now **measured**, not inferred from the man
page default. F7 upgraded accordingly. My version documented the blind spot; this
one removes it.

## 14:28 · finding · T1.2 — the hardcoded `-1` boot selector was a trap

Reviewer finding 3, and the sharpest of them. `probe-suspend.bash` hardcoded
`-b -1` for the timeline, density and thermal probes. `-1` is **relative**: one
more reboot and it points at an innocent boot, and all three probes would come
back looking *clean* rather than *blind* — while `PLAN.md` Task 1.2 instructs the
next agent to "note any divergence from TRIAGE-EVIDENCE.md". That is a probe
artefact dressed up as a system finding.

Fixed: the selector is a parameter (`triage.bash --boot N`, default -1), every
section title names the boot it measured, and the report now leads with
`journalctl --list-boots` so the reader can see which boot that was.

## 14:33 · decision · — — three evidence corrections, stated not swapped

- F7 was quoting `laptop-lid.conf` while silently dropping its two comment lines,
  under a `Source:` label. Now verbatim.
- F6 claimed a "complete set of matches" for a pattern **narrower** than the
  plan's own probe, which returns a fifth line. Now says which pattern, and shows
  the extra line.
- F2's "boot 0 begins 12:06:49" — the machine says 12:06:53.

None are load-bearing, but all three were stated as fact and contradicted by the
host. Corrected in place with the correction visible, per the same discipline
applied to F2's histogram.

## 14:50 · blocker · T1.2 — operator reported the first leg hanging; my defect

Operator ran `--watch-power` and it sat silent at the first leg header. Not hung
— `show_journal_density` was scanning the **whole** incident boot, which on this
host is **7,767,480 lines**, piped through `grep -oE` and `uniq -c`.

Two defects in one:

- **Unusable output.** A 48-hour boot is ~2880 histogram rows. The question is
  only ever "is there a gap after the suspend attempt", so the whole-boot scan
  was expensive *and* worse than a scoped one.
- **Looks identical to a hang.** Probes write their payload to the report file
  and print nothing, so a slow probe gives the operator no signal at all.

Fixed both. `show_journal_density` now locates the last `PM: suspend entry` in
the boot (kernel-only search, cheap) and measures from there to the end of the
boot — self-locating on the interesting window. If the boot contains no suspend
attempt it says so explicitly and falls back to the final 30 minutes, naming that
this answers a different question rather than quietly emitting a lookalike table.
`probe()` now echoes each probe label to stderr as it starts.

Full leg: **24s**, 26 histogram rows, and the numbers agree with the corrected
F2. Was: minutes of silence.

Lesson for the plan's own standards: I verified this script by redirecting to
`/dev/null` and checking the exit code, which is precisely a verification that
does not exercise what it vouches for — it could not have caught either defect.

## 15:05 · blocker · T2.1 — the probe's null result was the QUERY, not the system

Operator ran `--watch-power` to completion. Result: `-- No entries --` on both
transitions. Nearly reported that as H1 confirmed.

It was a **third** instance of the same class of defect. The probe ran:

```
journalctl -u systemd-logind -u upower -k --grep '...'
```

`-u` and `-k` are **mutually exclusive** — journalctl ANDs them, and no record is
both a unit's message and a kernel message. Measured: that invocation returns
**0 lines for an entire boot**, whatever happened. The confident emptiness was a
property of the query.

Fixed by splitting into two invocations, and the unit query is now unfiltered —
logind+upower produce ~238 lines per boot, so a `--grep` there risks hiding the
very message being sought for no meaningful saving.

## 15:12 · finding · T2.1 — H1 mechanism CONFIRMED, with a positive control

Re-ran the corrected queries retroactively against the operator's real 13:12
transition window — the journal still held it, so no second unplug was needed.

```
systemd-logind + upower, unfiltered, across unplug AND replug:   (nothing)
kernel, power/lid/battery terms, same windows:                   (nothing)
```

Positive control, because three false negatives is enough: the same query shape
over boot -1 at 12:03 returns `Lid opened.` correctly. So the query works and the
emptiness belongs to the system.

**logind takes no action and logs nothing at all on a power-source change.** Only
`AC online` moved; `Docked`, `LidClosed` and the output count were unchanged.
That is exactly the step H1 depends on: a lid-closed machine moving to battery
gets no re-evaluation, so it stays awake indefinitely. Recorded as F16.

H1 updated to "mechanism confirmed, end-to-end behaviour still unobserved" rather
than plain confirmed — the lid-closed run needs an idle moment and has not
happened. Not going to let a confirmed mechanism quietly become a confirmed
hypothesis.

## 15:18 · decision · T3.2 — what F16 does and does not settle

F16 settles that **nothing recovers**. It does not settle **how fast** recovery
must be, which is the actual content of Decision 2 (900s idle timeout vs a
near-immediate udev rule on the AC transition). That is a judgement about
acceptable bag time and heat, not a fact triage can produce, so it goes back to
the operator rather than being decided here.

## 15:40 · decision · — — operator rejected the premise, and was right

Asked the operator to pick a recovery speed. They rejected the question:

> "there is no acceptable bag time — if i say suspend it should go for suspend.
> something as trivial as unplugging the power should not derail that."

That is a better frame than mine and it invalidates how Decisions 1 and 2 were
reasoned. I had been designing a *bounded failure*: let the suspend break, then
catch it within N minutes. The actual defect is that **an explicit request was
silently not honoured**. A timeout makes the failure shorter; it does not make
the request hold.

Recorded as Decision 4, which supersedes Decision 1's priority ordering. Note
what this reverses: Decision 1 rejected the `system-sleep` hook as "narrowest,
covers strictly less". Under the recovery framing that was true. Under the
correct framing the hook is the layer that delivers the requirement, and the idle
timeout is the one covering less — it cannot honour a request, only curtail the
consequences of dropping one. Decision 1's choice of setting survives as layer 3;
its reasoning about priority does not.

## 15:48 · finding · T3.2 — the power ports are armed wakeup sources (F17)

Chased the operator's "unplugging power should not derail it" to a mechanism.
`/sys/kernel/debug/wakeup_sources` — every active source is power or input:

```
ucsi-source-psy-USBC000:001   active 9   events 12
AC                            active 3   events 3
ucsi-source-psy-USBC000:002   active 1   events 1
```

And all three carry `power/wakeup = enabled`. So a mains unplug fires a wakeup
event **by design**, and one landing during the s2idle transition aborts the
suspend. Exactly the complaint.

Disarmable on stable udev keys (`SUBSYSTEM=="power_supply"`, `KERNEL=="AC"` /
`ucsi-source-psy-*`) — deliberately not sysfs paths, since F5 already showed USB
paths get reused (`3-6` was a hub, then a keyboard).

Honest limit recorded in F17: `wakeup_count` is 0 for every device because this
boot has not suspended, so this establishes *that they are armed*, not that one
of them caused the 11:40:52 abort. P2 stays a premise.

## 15:55 · action · T3.1 — moved the playbook, because adding to it would have been a no-op

Implemented Decision 4. The placement forced a move: layers 1 and 2 need a play
that owns suspend policy, and `play-prevent-ssh-suspend.yml` (Decision 3's
answer) is the wrong home for a udev rule and a sleep hook. But the topically
right file was the unimported lid play, and `playbook-main.yml:3-4` explicitly
forbids importing anything from `imports/optional/`.

So: `git mv` to `imports/play-suspend-and-lid-policy.yml`, imported from main
next to `play-prevent-ssh-suspend.yml`. That also closes the latent bug flagged
in Decision 3 — the lid config was live on this host but would not have survived
a rebuild.

Deployed nothing. `ansible-playbook --syntax-check` on the full main playbook
passes; `qa-all.bash` is red only on the two pre-existing items (00079's script,
the ruff pin), and all three of my shell files pass shellcheck individually.

## 16:30 · blocker · T4.1 — qa-reviewer BLOCK, and the operator gated deploy on it

Operator authorised deploying, then immediately added "but clearly only once we
confirm its safe!". Correct instinct — the review came back **BLOCK**, and the
blocking finding is breakage I introduced by moving the play.

**Blocking**: the play's UPower `lineinfile` has no `create:` and no profile
guard. It was harmless under `imports/optional/` because nothing imported it.
Importing it from `playbook-main.yml` put it on the headless-server path, where
`/etc/UPower/UPower.conf` does not exist (no playbook in this repo installs
upower) — and with `any_errors_fatal = true` that aborts the **entire**
provisioning run, not just this play. The play already carried exactly this guard
on its gsettings neighbour; I moved the file without generalising it.

Verified the fix rather than assuming: `--check -e provisioning_profile=server`
now skips the task, exit 0.

## 16:38 · finding · T3.3 — two defects that would have silently disabled the fix

**`systemd-run` with a fixed `--unit=` and no `--collect`.** Without it a failed
run stays loaded in failed state, and every later `systemd-run` with that name
then fails outright — permanently disabling layer 2. The likely trigger is in
this plan's own evidence: F12's `ssh-suspend-guard` block inhibitor makes logind
refuse the suspend. I documented that interaction under Dependencies and then
wrote a hook that ignored it.

**The udev rule was reloaded but never triggered.** `udevadm control
--reload-rules` only affects future device events, and `AC`/`ucsi-source-psy-*`
are never re-added on a running system — so layer 1 did nothing until reboot,
and the play papered over it with a "REBOOT REQUIRED" banner. The correct
reload → trigger handler chain already existed in `play-displaylink.yml`.

## 16:45 · decision · T3.3 — narrowed the window, and stopped the cap erasing itself

Two of my own parameters were wrong on inspection:

- `RESUSPEND_WINDOW=30` was not derived from anything. F1 measures the abort at
  **~3 seconds**. Thirty seconds is an order of magnitude wider than the failure
  it targets, and every extra second is false-positive surface on the docked,
  lid-shut configuration used daily. Narrowed to 10 with the derivation written
  into the file.
- The `MAX_ATTEMPTS` give-up branch deleted the counter, so "giving up" lasted
  exactly one cycle. Removed; the counter is now cleared only by a normal resume,
  which is the only event meaning the problem actually stopped.

Also fixed: the lid probe could not distinguish "lid open" from "no lid interface
on this host" and logged the former — while this play is now imported for every
host, including ones with no lid. Now queries logind (F13's interface),
falls back to `/proc`, and logs an explicit "could not determine" rather than
guessing.

## 17:20 · decision · T3.1 — operator: "dont leave a landmine here"

Operator asked for real sanity checks in the play rather than the profile guard.
Correct: `provisioning_profile != 'server'` is a **proxy** for "upower is
installed". A desktop without upower, or a misdetected profile, walks onto the
same mine — the guard only happened to cover the case the reviewer tested.

Replaced proxies with measured preconditions, gathered in a preflight block:

| Precondition | Established by | Consumed by |
| --- | --- | --- |
| host can sleep | `slurp /sys/power/state` | `meta: end_play` |
| upower present | `stat /etc/UPower/UPower.conf` | UPower task |
| sleep hook dir | `stat /usr/lib/systemd/system-sleep` | hard `fail` |
| GNOME schema | `gsettings get` probe (`failed_when: false`) | layer 3 |

Both skip paths now print WHY they skipped, so a skip is never silent. The
sleep-hook-directory case FAILS rather than skipping: systemd scans exactly that
path, so its absence means the hook would be installed where nothing runs it —
a silent no-op reporting success, which is the failure class this repo bans.

## 17:32 · finding · T3.1 — my own guard test found a bool trap

Verified all five paths in `--check` rather than reasoning about them:

- desktop: 12 ok / 4 changed
- `-e provisioning_profile=server`: layer 3 skips **and says so**, exit 0
- `-e '{"upower_conf":{"stat":{"exists":false}}}'`: UPower task skips with its
  explanation, exit 0, changed drops 4 -> 2 — the landmine is defused
- `-e '{"system_sleep_dir":{"stat":{"exists":false}}}'`: exit 2 with the intended
  message
- `-e host_can_suspend=false`: **did not fire.**

That last one was a real bug, not a test artefact. `when: not host_can_suspend`
against the string `"False"` is False in Jinja, because a non-empty string is
truthy — so the guard would silently never trigger. A Jinja-templated `set_fact`
can absolutely yield that string. Fixed with `not (host_can_suspend | bool)`;
retested, the play now stops after 7 preflight tasks with `changed=0`.

Worth noting how it was caught: only by *running* the negative case. Inspection
would not have found it, and it is the same shape as the `-u`/`-k` journalctl
defect earlier today — a condition that looks right and is structurally incapable
of being true.

## 18:10 · finding · T4.3 — round 2: all ten fixed, five new, DO NOT DEPLOY

Reviewer verified every round-1 fix mechanically rather than by reading the diff
— including proving the udev trigger is not a no-op, via `udevadm test
--action=change` against the real devices, showing the `ATTR{power/wakeup}` write
would fire on `AC` and both UCSI ports and not on `BAT0`.

**My sanity-check work was itself a HARD RULE violation.** The two "Report that X
was skipped" `debug` tasks are the prohibited skip-and-warn pattern. The sharp
version of the objection: the gsettings one printed a reassuring "layers 1 and 2
are unaffected" for **any** non-zero rc, so a transient D-Bus or session failure
on a desktop would silently drop layer 3 and report it as expected.

Resolved by separating the two cases that I had collapsed into one:

- **Genuine absence** needs no branch at all. On a server the probe is skipped by
  its own `when:`, `rc` is undefined, `default(1)` makes the set task's condition
  false, and it simply does not run. Absence is handled by *not asking*.
- **Failure** now **fails**, with the rc and stderr in the message. On a
  non-server host that schema is expected to be readable, so a failing probe is a
  real fault.

The upower `debug` is simply gone — a bare `when:` skip is ordinary Ansible and
already visible in the output; the `debug` added nothing but the prohibited shape.

## 18:22 · finding · T3.3 — N4/N5 were the two that would have bitten in practice

**N4 — the sticky give-up could disable layer 2 for the exact incident.** COUNT
was cleared only by a resume outside the window or an observed OPEN lid. Docked
lid-shut, neither happens for hours. So: suspend, change your mind, wake from the
keyboard, 3x -> cap reached -> hours later the real bag incident arrives and the
hook logs "giving up" and does nothing. Round 1 asked for stickiness and was
right; the missing half was expiry. The `pre` branch now clears COUNT when the
previous stamp is older than 600s — a suspend requested long after the last one
is a fresh incident, while the retry's own `pre` fires ~5-8s later and does not
clear its own counter.

**N5 — the lid probe had a stale-read race that could suspend a machine I had
just opened.** I asked logind first; `LidClosed` is a *cached* property updated
from an evdev SW_LID event, and there is no guarantee it has been processed by
the time the `post` hooks run. If not: LidClosed still true, elapsed a few
seconds, hook re-suspends the laptop the user just opened. Reversed to read
`/proc/acpi/button/lid` first — a live hardware read — with logind as fallback,
plus `--timeout=2` on busctl, which defaults to 25s inside a hook
systemd-suspend.service blocks on.

## 18:30 · action · T3.2 — the play now verifies layer 1 instead of asserting it

N3: `udevadm trigger` ran without `--settle` and nothing read the result back,
while the handler stated "so it applies immediately" as fact. Added `--settle`
and an `assert-wakeup-disarmed` handler that reads `power/wakeup` back on all
three devices and fails if any still says `enabled`. Also documented that the
handler *ordering* is load-bearing — a handler can only notify one defined after
it, so a tidy-up that reordered them would break layer 1 silently.

Re-ran all six guard scenarios after the rework:

```
desktop            exit=0  ok=12 changed=4 failed=0
server             exit=0  ok=11 changed=4 failed=0
no-upower          exit=0  ok=10 changed=2 failed=0
no-sleep-dir       exit=2  fails loudly
cannot-suspend     exit=0  ok=6  changed=0   (end_play fires)
gsettings-broken   exit=2  fails loudly       (was: warn and continue)
```

## 19:05 · blocker · T4.3 — round 3: BLOCK, and the QA claims in this journal were false

Correction to the 15:55, 17:32 and 18:30 entries above, and to the commit messages
that repeated it. Every line of the form *"`qa-all.bash` red only on the two
pre-existing items"* described a run that **structurally could not have looked at
a playbook**.

`scripts/qa-all.bash:40-43` runs `qa-python.bash` third and `exit 2`s on rc=2.
The pre-existing ruff pin mismatch (0.16.0 vs 0.16.3) returns exactly rc=2. So
qa-all has been short-circuiting there and never reaching **qa-patterns,
qa-ansible, qa-ansible-syntax, qa-js or qa-docs**. Run by hand:

```
qa-patterns        rc=0
qa-ansible         rc=1   <- RED, on this plan's own playbook
qa-ansible-syntax  rc=0
qa-docs            rc=0
```

This plan's entire change surface is a playbook. `CLAUDE/AgentNotes.md`: *"a gate
that is not running is indistinguishable from a gate that passes"* — and I quoted
its green output six times as evidence.

The ansible violation was mine: a bare `failed_when: false` with the
`# FAIL-FAST-OK:` justification on the line **above** instead of trailing, which
is the form `qa-ansible.bash` actually parses. Fixed, along with the same defect
in `play-podman.yml:127` (pre-existing, 2026-09-07, a second casualty of the same
short-circuit). `qa-ansible` is now green.

**The ruff pin is therefore not a nuisance — it is blinding five gates
repo-wide.** That needs its own plan; noted for the operator, not fixed here.

## 19:18 · finding · T3.6 — my "simplification" was a regression, and the journal misdescribed it

The 18:30 entry above says the assert handler *"reads `power/wakeup` back on all
three devices and fails if any still says `enabled`"*. That describes the **loop
form**, which `80024ce` had already deleted four minutes later. The grep that
replaced it behaves differently and worse:

| scenario | grep rc | verdict |
| --- | --- | --- |
| AC disarmed, ucsi absent — **policy fully applied** | 2 | **fail** |
| no AC and no ucsi at all (a desktop PC) | 2 | **fail** |
| all present and disarmed | 1 | pass |

`grep` exits 2 on a missing path, and `failed_when: rc != 1` turned "this machine
does not have that hardware" into a fatal error. The udev copy has no `when:`, so
the chain fires on any host's first run — it would have aborted provisioning on
every machine that is not this laptop. I introduced that while fixing a *style*
violation, and `ansible-lint` passed it clean because the rule I broke is a repo
rule, not a linter rule.

Both forms shared one omission: **neither stated its population.** The loop
printed "all disarmed" on a host with zero devices; the grep failed on one. So
the replacement is a tested helper that always reports the set it checked:

```
COVERAGE: 2 of 2 power-delivery devices disarmed
COVERAGE: 0 of 0 — no power-delivery wakeup devices on this host
COVERAGE: 0 of 3 ... — STILL ARMED: AC, ucsi-source-psy-USBC000:001, ...
```

`helpers/suspend_wakeup/` (core + cli, stdlib only, 15 unit tests, test file
written first). Exercised against real sysfs and two fixture hosts, including the
empty-host case that was the regression.

## 19:30 · action · T3.5/T3.6 — the rest of round 3

- **S5**: the assertion was a handler three links down a notify chain, so it only
  ran on the run that changed the rules file — and never in check mode, because
  its notifier is skipped there. It had therefore never executed once. Now a task
  after `meta: flush_handlers`, running every time, so the COVERAGE line is
  present on every run rather than only on failure.
- **S4**: the preflight `slurp` of `/sys/power/state` was itself unguarded — a
  fatal on the very first task of a host that simply cannot sleep. `stat` first.
- **S3**: `meta: end_play` -> `end_host`. `end_play` "affects all hosts", so one
  non-suspending machine would have cancelled the policy for the whole group.
- **S9**: the system-sleep-directory `fail` sat between mutating tasks, so firing
  it left a partial apply. Moved into preflight.
- **S1/S2**: `deploy.bash --help` still claimed a reboot is always required, and
  its header overstated what the assertion checks ("all three devices").
- **S6/S8**: PLAN.md Task 3.4 was ticked with a description that no longer matched
  the guard; `docs/playbooks.md` did not mention the two new ways this play can
  abort a provisioning run.

Also extracted the four Technical Decisions into `DECISIONS.md` — PLAN.md had
crossed the size advisory (18.6 KB); now 12.5 KB.

Gates, run individually because qa-all cannot reach them: ansible ✓, docs ✓,
patterns ✓, helper tests ✓ (240), ruff ✓ on the new helper, shellcheck ✓ on hook
and deploy.bash. All six guard scenarios re-verified. Still not deployed.

## 20:15 · decision · — — "no obvious stopping point" was wrong

Asked the operator to choose between a fourth review and deploying, framing it as
a cost question because each round had found something. They rejected the framing:

> "no obvious stopping point? is it clean. what does clean mean? clean means its
> good and safe to run and is free of defects"

Correct, and it is the more useful test. There IS an obvious stopping point — a
round that returns nothing. Rounds 1-3 each returned real defects, which is not
diminishing returns, it is *not clean yet*. Pricing my own review time against a
binary condition was the error.

Also closed a gap before round 4 reported: the verification task is skipped under
`--check`, so it had **never once executed through Ansible** — I had only ever run
check mode. Ran it ad-hoc: rc 1, stdout carries the COVERAGE line with the em-dash
intact, and on failure the line lands in Ansible's own error output.

## 20:40 · finding · T3.6 — round 4: the verification could have blocked the fix

All of round 3 (B1, B2, S1-S10, both nits) verified fixed. Four new defects, and
the first is the one that mattered:

**Verification sat between layer 1 and layers 2/3.** Under `any_errors_fatal` a
failed wakeup check aborts the run *at that point* — so a check failure would have
left the machine with neither the recovery hook nor the idle-suspend backstop
installed. The bag scenario fully live, because a *verification* failed. Moved to
the end of `tasks:`, with the principle written into the play: a verification must
never be able to prevent the fix it is verifying.

**My helper had B2's defect one level down.** `read_wakeup_states` caught bare
`OSError`, so a device whose `power/wakeup` attribute is simply ABSENT — not
wakeup-capable, entirely healthy, `BAT0` is exactly this — was reported
`UNREADABLE` and hard-failed the whole run. Same conflation of "absent" with
"broken" that made the `grep -l` wrong, reintroduced inside the module written to
fix it. Now `FileNotFoundError` skips the device and any other `OSError` is a real
fault. Ten new tests in `test_cli.py` against real tmp fixtures rather than mocks,
including the not-wakeup-capable case.

Two smaller ones: the reboot banner claimed the COVERAGE line was "above" when
`flush_handlers` makes the banner print first; and PLAN.md said "both hard
preconditions moved into preflight" when the GNOME one deliberately stays beside
the task it guards.

Gates after: ansible ✓, docs ✓, patterns ✓, helper tests ✓ (250, up from 240),
ruff ✓, ansible-lint ✓, syntax ✓. Six scenarios re-verified. Task order confirmed
by listing it, not assumed.

## 15:40 · finding · pre-deploy baseline captured, and BAT0 confirms the round-4 fix was not theoretical

Recorded the host's wakeup state BEFORE deploying, because the deploy destroys it
and the before/after is the only evidence the udev rule did anything:

```
AC                            = enabled
ucsi-source-psy-USBC000:001   = enabled
ucsi-source-psy-USBC000:002   = enabled
COVERAGE: 0 of 3 power-delivery devices disarmed — STILL ARMED: AC, ucsi-source-psy-USBC000:001, ucsi-source-psy-USBC000:002
exit=1
```

So the post-deploy `3 of 3` is a real state change, not a tautology.

The incidental finding is the more useful one. `/sys/class/power_supply/` on this
host holds four devices, and `BAT0` has **no `power/wakeup` attribute at all** —
it is the not-wakeup-capable case exactly. The pre-round-4 helper caught bare
`OSError`, so it would have mapped `BAT0` to `None`, reported it UNREADABLE, and
hard-failed the entire provisioning run **on this very machine**, on the first
real deploy. That defect was found by review rather than by a test run, and it
would have fired on the first thing I did after the review.

Reinforces the standing lesson from B2: "absent" is not "broken", and every layer
that touches the filesystem has to be asked which of the two it is reporting.

## 16:05 · finding · T3.6 — round 5: no blocking findings, and a deny-list that called garbage clean

Round 5 returned **zero blocking findings** and verified all four round-4 fixes
mechanically — task order proven by `--list-tasks`, and the
`FileNotFoundError`/`OSError` split held under four hostile cases (missing
`power/` dir, EACCES, dangling symlink, `IsADirectoryError`). The reviewer also
confirmed 4 of the 10 new tests fail when the pre-fix implementation is
reinstated, so they discriminate rather than merely pass.

Both real findings were `PLAN.md` drift, neither affecting the deploy:

- Task 3.5 claimed the GNOME probe "also runs" in preflight. It is task 15 of 19,
  under `# ---- Layer 3 ----`, after four mutating tasks. The *fail* placement is
  deliberate and was described correctly; the *probe* placement was not — the same
  bullet round 4 corrected, wrong in a new way. Now states it plainly: the probe
  sits with the task it gates, not in preflight.
- Test counts still said "15 unit tests … (240 tests)" when the same commit's
  journal said 250. A document contradicting its own journal inside one commit.

**The nit that was really a defect.** `core.evaluate` used a deny-list: anything
that was not `enabled` counted as disarmed. Measured against fixtures, `''`,
`'   \n'`, `'potato\n'` and `'ENABLED\n'` all printed
`COVERAGE: 1 of 1 … disarmed`, exit 0. That is this module's own stated failure
mode — reporting blind as clean — surviving at per-device granularity, in the
module written to prevent it. Now an allow-list: `disabled` disarmed, `enabled`
armed, **anything else unverifiable**.

Renamed the bucket `unreadable` -> `unverifiable` rather than folding garbage into
the existing name as suggested. `UNREADABLE: AC` when `AC` read *fine* and said
`potato` misdescribes the measurement, which is the defect class this entire
review chain has been about.

Also fixed: a dangling device symlink was silently dropped from the population
(the device is now resolved before its attribute, so it counts as unverifiable
rather than shrinking the denominator); `TestMain` now captures stdout, so the
gate no longer prints four stray `COVERAGE:` lines after `OK` that read like
findings; the `flush_handlers` task name said "udev changes" when it flushes
`restart-upower` too; the reboot banner promised a COVERAGE line that `--check`
never prints; and a bare commit hash came out of `core.py` (`R-COMMENT-CHANGELOG`).

Not fixed, with reasons: `force_handlers` is an ansible.cfg-wide property not made
worse by this range; the two unconditional `debug` tasks are the payload, by
design. `ruff format` disagrees with three files — it is **not** a gate here
(`qa-python.bash` runs `ruff check` only, E5 deliberately off with 209
pre-existing findings), so reformatting would have churned untouched lines.

Gates: helper tests 255 (was 250), ruff ✓, ansible ✓, ansible-syntax ✓, docs ✓,
patterns ✓ (200 files), ansible-lint production profile ✓, check-mode
`ok=13 changed=4 failed=0 skipped=6` unchanged, task order re-listed after the
rename — verification still 18-19 of 19.

## 16:35 · finding · T3.6 — round 6: allow-list settled against the kernel ABI, and a count that went stale on its own fix

Round 6: **no blocking findings.** The important verification is that the
allow-list was checked against the **kernel ABI documentation**, not against this
machine's current values — `power/wakeup` holds exactly `enabled\n` or
`disabled\n`, and is absent for devices that are not wakeup-capable. That was the
one way the round-5 fix could have been a disaster: a legitimate third value would
have turned a healthy host into a fatal abort of the whole provisioning run. There
isn't one. 75 attributes measured on the host agree.

The `unreadable` -> `unverifiable` rename was confirmed the right call, complete
repo-wide, with no consumer parsing the COVERAGE line. New tests proven
discriminating by mutation: 8 fail under the deny-list, 1 under the no-guard
variant.

**S1 is the one worth learning from.** Round 5 caught the test counts stale at
15/240 and I corrected them to 25/250 — and the same commit made them 30/255. Its
own journal said 255. Third instance of the same finding. A hardcoded count on a
✅ line goes stale on the very commit that fixes it, so the count is now **gone**
rather than corrected again: the line describes what the tests cover and points at
the gate for the number.

S2: the docs' third abort case still said "exists but cannot be read", which the
allow-list and the device guard had both widened past. Now names all three
conditions.

Nits fixed: a `round-3 finding B2` reference survived in `test_core.py` after the
same cleanup removed it from two sibling files — "fixed the instance, never asked
what else has the same shape", which is the recurring shape of this whole plan's
defects; the `TestMain` docstring described output that no longer exists; and the
`device.exists()` comment named only the dangling-symlink case when EACCES and
ELOOP land there too.

Recorded rather than changed, per the reviewer: a `ucsi-source-psy-*` unplugged
between `iterdir()` and `stat()` now fails the run instead of silently shrinking
the population. That is the correct trade — a loud re-runnable failure beats a
quiet under-count, and the check is the last task, so the abort lands after all
three layers are installed. Written into the docstring so it reads as deliberate.

Gates: helper tests 255 ✓, ruff ✓, docs ✓, ansible ✓. No round-number references
remain in any source file.
