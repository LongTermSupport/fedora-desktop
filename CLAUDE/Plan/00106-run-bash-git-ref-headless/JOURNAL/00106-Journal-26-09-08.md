# Plan 00106 — Journal 26-09-08

> **Append-only activity log** for plan 00106. One file per day
> (`00106-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 21:35 · action · — — plan scaffolded

Plan 00106 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 17:55 · decision · P1 — composer semantics, headless path only

Measured first: the headless HTTPS path clones without `-b` (default branch) and `git pull`s
it; no `RUN_BASH_*` variable names a ref. A downstream consumer that pins run.bash by digest
therefore pins the script and nothing else. `RUN_BASH_GIT_REF` = branch name (track tip via
`checkout -B <b> origin/<b>` after a fetch) or 40-hex sha (detached). When set it replaces
`git pull`, which would fail on a detached checkout. Interactive path untouched. Version
1.18.0. Consumer-side proof (Task 1.3) will be observed from the consuming project's runbook.
