# Plan 00068 — Journal 2026-08-01

## Phase 3 designed; two of its three requirements did not survive

Applied the working rule to each remaining requirement.

- **MCP (3.1) — retired.** `grep -rn -i mcp` over the deployed tree: zero hits. Nothing to
  extend, and nothing needed — `$PWD` is mounted at `/workspace` (`:1771`, `-w` at `:2784`),
  so a committed `.mcp.json` is already in front of Claude Code. Could not write a truthful
  statement of why that fails, so the requirement goes. Residuals logged, not built: no env
  passthrough for an MCP secret; whether Claude Code still prompts to approve a project-scoped
  server is **unverified** and would be a prompt-site question, not an MCP one.
- **`--egress` (3.2) — out of scope.** The runner VM's squid + nftables layers already own
  egress and sit outside the job's control; a launcher flag sits inside it. C3 kept as the
  constraint on any later build.
- **Unattended-launch (3.3) — specified**, four cited defects. The one that would have hurt:
  `get_next_container_name` is check-then-act, and `:2741` `rm -f`s the chosen name, so two
  concurrent jobs for one repo can converge on `<repo>_yolo` and the second **kills the
  first's running container**.

## The thing I was not looking for

Checking the `--network` collision surfaced the internet preflight at `:2518-2593`:
`podman run --rm --network <net> alpine wget http://google.com`, then a bare `exit 1`. It
needs an unpinned image pull and egress to `google.com` — neither of which a squid-allowlisted
runner grants — and it is on the path of every launch. Same defect class as `--device
/dev/dri`, so requirement 2 now names both, and the risk table records that this class has
materialised twice.

Recorded as **E7, read not measured**. `triage.bash` cannot settle it: the desktop has open
egress and a warm cache, so it passes there and says nothing about the runner. Filed against
lts-infra Plan 00030 as open question 2, where the runner can actually be measured.

Phase 4 left open on purpose — Task 4.1 is gated on the owner decisions, two of which change
what gets built.
