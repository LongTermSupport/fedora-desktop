# Plan 00068 — Journal 26-07-30

> **Append-only activity log** for plan 00068. Entries are added at the bottom, never edited.
> Corrections are new entries, not rewrites of old ones.

______________________________________________________________________

## 09:05 — Renumbered 00066 → 00068 to clear a cross-branch collision

The plan number collided. This plan was scaffolded as **00066** on branch
`plan-00066-ccy-ci-runner`; independently, `F44` took **00066** for
`00066-ftp-camera-airbnb-wifi-and-hotspot-triage`. Both are real plans with real history, and
the collision is invisible while the branches are apart — it surfaces the moment they meet, where
plan-QA's `no-new-collisions` and `row-folder-bijection` checks reject it.

**This one moved, not the other, for two reasons.** The ftp-camera plan is already merged into
the default branch, so renumbering it would rewrite shared history; and the owner asked for this
one to move, since this agent owns it. 00067 was unavailable —
`Completed/00067-qa-gates-inert-in-nested-checkout` holds it — so **00068**.

**What the counter did and did not do.** `hooksdaemon.latestPlanNumber` read 67, which is
consistent with both 00066 and 00067 having been handed out. The counter is per-clone git config;
it serialises allocation *within* a checkout and cannot see a number another branch took. So this
is not a case of the counter being ignored — it is the counter working as designed and the design
not covering concurrent branches. Worth recording rather than filing as operator error.

**What was changed, and what deliberately was not:**

| Changed                                                    | Left alone                                                     |
| ---------------------------------------------------------- | -------------------------------------------------------------- |
| folder `00066-…` → `00068-…` (`git mv`, history preserved) | branch name `plan-00066-ccy-ci-runner` — a ref, not an artefact |
| `JOURNAL/00066-Journal-26-07-29.md` → `00068-…` (renamed)  | that file's **body**, which still says 00066                    |
| PLAN.md title, self-references, journal-name hint          |                                                                 |
| `triage.bash`, `probe-engine.bash` path/report references  |                                                                 |
| both `reports/*.md` headings (+ a renumber note)           |                                                                 |
| README index row; counter set to 68                        |                                                                 |

The journal body is the important "left alone". Day-files are append-only by rule, and renaming a
file preserves its content while rewriting it would destroy the record of what was actually
written at the time. The correction is this entry plus the note in PLAN.md's header — never a
retroactive edit. Anyone reading `00068-Journal-26-07-29.md` will see 00066 throughout and should
read it as this plan under its old number.

```
git mv folder + journal   -> history preserved
bash -n + shellcheck -x   -> clean on all three scripts (stderr empty, not just rc 0)
residual 00066 refs       -> only the deliberate renumber notes + the append-only journal body
```

**Not touched: the ftp-camera plan, or anything else on `F44`.** This branch is 20 behind F44 and
was not merged or rebased as part of this — mixing a renumber with a merge would make both harder
to review.

______________________________________________________________________

## 09:35 — Task 7.2: the census was blind to an entire file, not merely undercounting

Re-ran the prompt census with a pattern matching `read -r -p` as well as `read -rp`, per review
finding C3. C3 predicted ~46; measured **46**, up from 37.

The count is the boring half. **All nine missed prompts are in `lib/token-management.bash`, and
the Round-1 pattern found ZERO there** — the file was not undercounted, it was unseen. It uses
`read -r -p` exclusively, so a pattern requiring `-rp` matched nothing and the census reported a
clean file. Nine blocking prompts on the default path rendered indistinguishable from none.

That is the same shape this estate keeps meeting, now inside the measuring instrument: a control
that silently becomes a no-op, and whose null result reads as a good result. A census reporting
"token-management: not scanned" would have been fine. Reporting `0` was not.

Then the worse finding, from reading the control flow at `:322`:

```bash
while true; do
    read -r -p "Token: " manual_token          # EOF -> returns non-zero, leaves it EMPTY
    if [ -z "$manual_token" ]; then
        read -r -p "Try again? (Y/n): " retry  # EOF -> retry is EMPTY
        if [ "$retry" = "n" ] || [ "$retry" = "N" ]; then return 1; fi
        continue                                # empty is neither -> spin
```

The loop's only exits are a valid token or an explicit `n`. EOF is neither, and EOF is the one
thing guaranteed on a runner. So it spins forever, emitting output, burning a JIT slot until the
job times out with no diagnosable cause. The `while true` also suspends `errexit`, so `set -e`
cannot save it. A hang is worse than a crash.

**This reorders a Task 7.4 item.** `create_token` is an irreducibly human OAuth flow and it sits
on the default path whenever no token is pre-provisioned. So accepting `CLAUDE_CODE_OAUTH_TOKEN`
**by value** is not one capability among six — it is the only thing that makes the default path
survivable unattended. Promoted to prerequisite.

A note to whoever re-runs this: the census pattern is now a known-fragile control. Assert it
against a file known to contain `read -r -p` before trusting a clean sweep, or it will lie the
same way twice.

```
grep -rcE 'read[[:space:]]+-rp' ...                                  -> 37  (Round 1)
grep -rcE 'read([[:space:]]+-[a-zA-Z]+)*[[:space:]]+-[a-zA-Z]*p' ... -> 46  (corrected)
every 'read -<flag>' call                                            -> 74  (the extra 28 are
                                                                        pipe/heredoc reads, not
                                                                        blockers — counted to
                                                                        confirm 46 is not itself
                                                                        an undercount)
```

Claimed by inspection only: that the prompts exist and that `:322` spins on EOF by its control
flow. Nothing executed — `ccy` needs a real engine and the earlier nested-podman attempt failed
for an unrelated userns reason. Empirical confirmation belongs to the HOST triage run.

**Process note, recorded because it nearly cost the entry.** This journal append was written in
the same shell command as the commit, and the shell's cwd had been reset to the outer repo
between calls — so the heredoc wrote nowhere and the commit went out WITHOUT the journal, while
still reporting success. Absolute paths for journal appends from now on, and never batch an
append with the commit that is supposed to contain it.

______________________________________________________________________

## 10:05 — CORRECTION to the 09:35 entry: I had the spin mechanism wrong

The 09:35 entry, the Task 7.2 report and its commit message all said the `while true` ancestor
"suspends `errexit`", and attributed the EOF spin to `create_token:322`. **Both wrong.** Found
while starting Task 7.3, by executing bash instead of reasoning about it:

```
f() { while true; do read -r -p "x: " v; ...; done; }
( f )      < /dev/null   -> rc=1, ZERO iterations   ABORTS
f || { … } < /dev/null   -> "SPUN 5x"               SPINS
```

A loop **body** does not suspend errexit — only a **condition** context does. What actually
decides spin-vs-abort is the **call context of the enclosing function**.

So the real spinner is **`select_token:610`**: `claude-yolo:1004` and `:1117` invoke it as
`select_token "$TOKEN_DIR" "container" || { … }`, and that `||` suspends errexit through the whole
function body. An EOF-failed `read` leaves `selection` empty, the loop prints
`Invalid selection: (empty)` and `continue`s, forever.

`create_token` called bare (`claude-yolo:897/:907/:970/:1000/:1015/:1111`) does the opposite — it
**aborts**, and aborts with no message about why, which is C4's *other* class. Reached via
`select_token` (`token-management.bash:639/:656`) it inherits the suspension and spins.

**The same source line is a spin or an abort depending on the path that reached it.** That is the
real Task 7.3 finding, and it means "classify each site" cannot be done by looking at the site —
the classification is a property of the call graph. Sharper than what I first wrote, and it only
surfaced because I distrusted my own sentence enough to test it.

What survives unchanged: the census numbers (37 → 46), that all nine missed prompts are in
`token-management.bash`, that the file was invisible to the Round-1 pattern, and that there IS an
EOF spin on the default path inside that file. The headline conclusion was right; the mechanism
and the line were not.

Corrected in `reports/prompt-census-round2.md` (with an explicit CORRECTION block, since the
original text was already committed and pushed) and in PLAN.md's Task 7.2 entry. This journal
entry is the append-only record — the 09:35 entry above is left exactly as written.

Lesson, and it is the same one this estate keeps relearning: I asserted a bash semantic from
memory in a report, a plan and a commit message before testing it. The test took one command.

______________________________________________________________________

## 09:20 — Task 7.3: the classification, and a parser bug that would have silently corrupted it

Classified all 46 prompt sites. Report:
[reports/prompt-classification-round3.md](../reports/prompt-classification-round3.md).
Reproducible: [analysis/classify-prompts.bash](../analysis/classify-prompts.bash).

**The task's own premise was wrong.** Task 7.3 said the discriminator is "errexit suspended by
an `if`/`while` ancestor". A loop *body* does not suspend errexit — only a *condition* does. The
discriminator is the **call context of the enclosing function**, and it propagates transitively:

```
outer() { inner; }            # bare call
( outer )   < /dev/null  ->  rc=1, ZERO iterations   ABORTS
outer || …  < /dev/null  ->  "SPUN 5x"               SPINS
```

The `||` suspends errexit through `outer`'s whole body *and* through the bare `inner` beneath
it. That is exactly `claude-yolo:1004` → `select_token` → `create_token`. Corrected the task
text in PLAN.md rather than leaving a wrong premise in tracked config.

**Verdicts**: 32 abort-only, 6 spin-only, 6 spin-or-abort *by path*, 1 falls through, 1 guarded.

**Three things I did not expect:**

1. **All 20 `claude-yolo` prompts are at TOP LEVEL** — the file is 2848 lines with 9 functions
   and 2592 top-level lines. So for 20 of 46 sites the phrase "call context of the enclosing
   function" has no referent at all; they are governed by the script's own `set -e` (line 41).
2. **Five spin paths across four functions, not "the two spinning TUIs"** the task assumed.
   The extra ones came from `check_zombie_containers_startup` and
   `check_project_containers_startup` being invoked as `if ! …` (suspended), and
   `_do_compose_start` reached through two different `if`-guarded callers.
3. **`ssh-handling.bash:357` is the ONLY guarded prompt of all 46.** It detects
   non-interactivity, announces the inference, and proceeds. The correct pattern is already in
   the codebase, used once out of 46 opportunities. So "never *silently* infer" needs no new
   mechanism — just application of the existing one.

**The parser bug is the part worth remembering.** My first block tracker reported an unclosed
`if@2626` in `claude-yolo`. I could have shrugged at one leaked block; instead I looked, because
site `:2818` sits after it and would have been misclassified. Cause: `claude-yolo` builds its
startup banners as multi-line double-quoted strings, and the PROSE inside contains the words
`if`, `for` and `done` —

```
    STARTUP_INFO+="
    …
The container will automatically rebuild if the Dockerfile changes.
```

A per-line stripper reads that `if` as a keyword. Same shape as the `grep`-has-no-`\t` defect:
syntactically fine, silently wrong answer, and no tool reports it. Only an invariant that could
fail (block stack must balance at EOF) exposed it.

So every measurement in the driver now carries a check that can fail, and I mutation-tested the
checks themselves rather than trusting green:

- 63/63 function bodies re-parse under `bash -n`; truncating each by one line makes 63/63
  *reject*, so the check has teeth. (Extending by one does NOT break it — the next line is
  usually blank — so the end is pinned from the other side by asserting it is a closing brace.)
- Reintroducing the multi-line-string bug in a copy makes the driver exit 1. Removing it makes
  it exit 0. Both directions confirmed, mutation copy deleted.

Also caught the driver contradicting itself: it printed "all 9 files balance at EOF" immediately
after reporting that one did not. Fixed to print the actual count — the exit code and the text
must agree (bash-standards §9).

**Task 7.3 is NOT complete.** The classification is done; the *fixes* it also calls for are not,
and they touch the launcher (CCY version bump + HOST QA). Left as 🔄 with explicit subtasks
rather than ticked.

**QA**: `qa-all.bash` exits 2 in this container because `ruff` is absent. `ruff` **is** declared
in IaC (`playbooks/imports/play-python.yml:20`) — the host has it, the CCY container does not,
so this is environmental and not a gap to fix. Rather than record a blocker, I got the same
verdict from ruff 0.16.0 in a throwaway venv: it found 2 real defects (leaked file handles,
SIM115) in the new tooling, which I fixed properly rather than suppressing. `shellcheck -x`
clean on the driver; `bash -n` clean; classification re-run green after the refactor.

______________________________________________________________________

## 09:35 — Reconciling Round 3 with C3/C4: the corrections block disagreed with itself

Went back to check my classification against the Round-1 corrections block, and found C3 and C4
cannot both be complete:

- **C3** says `select_token` on the default launch path is "the earliest and most certain
  unattended blocker".
- **C4** says only the two `check_*_containers_startup` TUIs spin.

Both were written from the same evidence. The resolution from the call graph: `select_token`
**does** spin — `claude-yolo:1004` and `:1117` both guard it with `||` — so C3's blocker is a
hang too, and C4 simply had not walked that path. C4's count of 2 should read **5**.

Worth being precise about what C4 got right, because it is easy to misread this as C4 being
wrong: **C4's mechanism is correct** — "an ancestor invoked as an `if`/`while` *condition*". It
is Task 7.3's paraphrase that broke it, dropping the word *condition* and leaving "errexit
suspended by an `if`/`while` ancestor", which inverts the meaning. My Round-3 finding refines C4
(adds `&&`/`||`/`!` and transitivity, extends 2 → 5); it does not overturn it. All of C4's
negative claims hold — `claude-yolo:1104`, `:2011`, `network-management.bash:271`,
`dockerfile-custom.bash:37/117/157` genuinely do not spin.

Recorded as a nested REFINED note under C4 rather than rewriting C4's text: the corrections
block is the record of what was believed and when, and a wrong count left unflagged in tracked
config is the thing this project keeps warning about.

**Runner status re-checked while here** (primary goal, unchanged): no new lts-infra runs since
08:38. The latest failure is still exactly the image-staleness guard at `44090d9c` —
`lts-infra-ccy:latest` built from `c3268aca…`, commit tracks `ecc808b8…`. The runner DID execute
the job, so runners are up; this is the last gate, and it is the operator's one command.

## 23:00 — Owner steer received; it settles Decision 1 and reframes Round 2

Verbatim, so it is not paraphrased away later:

> "DO THE FEDORA-DESKTOP plan for the ccy ci runner optoins - this was the right approach -
> allow each project to have its OWN ccy runner in the NORMAL WAY - dockerfile customisation,
> custom tooling etc etc - its perfect
> BUT - CI need safety and MCP etc - it needs either adhoc or full blown customisation"

Two statements, and they do different work.

**Statement 1 settles a question Round 1 left open.** Task 3.3 asked whether a `ccy`-owned
platform overlay applied *on top of* a project's Dockerfile is warranted, and told me to argue
both sides. The owner has answered: the per-project `.claude/ccy/Dockerfile` seam is the right
mechanism and is not to be replaced. A project's CI runner image is just a project image. That
kills the "mandatory platform overlay for the general case" branch — it survives, if at all,
only for the untrusted-checkout case, which is a different plan.

**Statement 2 is where the work is, and it collides with C1.** "CI need safety" cannot be
delivered by the mechanism statement 1 endorses, and the reason is structural rather than a
matter of effort. Recording the spine now, before I have finished verifying it, and flagging
which parts are still unverified:

- `ccy` has **three** layers, not two: the **image**, the **launcher** (`claude-yolo` + 7 libs,
  on the host), and the **entrypoint** (`entrypoint.sh`, which is `COPY`d into the image at
  `Dockerfile:184` and set as `ENTRYPOINT` at `:215`).
- The owner's "normal way" is an **image** mechanism. It therefore delivers **tooling**, which
  is exactly what it was built for, and what it is genuinely perfect at.
- The permission posture (C1) lives in the other two layers: the launcher hardcodes
  `--dangerously-skip-permissions` (`claude-yolo:2792`) and the entrypoint pre-accepts the
  bypass dialog (`entrypoint.sh:245`). Neither is reachable from a project Dockerfile.
- **The sharp bit, which I do not think Round 1 stated:** the entrypoint is *inside* the image.
  So taking the base image "in the normal way" also takes `entrypoint.sh` — the `GH_TOKEN` hard
  requirement, the `gh auth login`, the `/root/.claude` → checkout symlink, the trust flag —
  whether or not the caller wants them. That is not a thing a project Dockerfile opts out of by
  adding packages. It is why the consumer had to `--entrypoint`-override rather than configure
  (fable §2, quoting `run-sandbox.sh:375-402`).

So "adhoc or full blown customisation" resolves per-capability, not globally:

| Capability             | Ad-hoc (stock image + runtime config) | Full (project Dockerfile) | Verdict                             |
| ---------------------- | ------------------------------------- | ------------------------- | ----------------------------------- |
| Tooling                | partly (`CCY_EXTRA_MOUNTS`)           | **yes — already works**   | owner is right; nothing to build    |
| MCP                    | wiring is net-new (E4: zero matches)  | binary can be baked       | both routes viable, both need E4 work |
| Safety (permissions)   | **no**                                | **no**                    | needs a launcher/entrypoint change  |
| Safety (egress)        | plausible                             | no (runtime property)     | ad-hoc only                         |

That table is the Round-2 thesis in miniature: **two of the four rows are unreachable from the
mechanism the steer endorses**, and saying so is the honest answer rather than stretching the
steer to cover them.

**Still unverified at the time of writing** — four read-only agents are out gathering citations
on: the image-resolution and `REQUIRED_CONTAINER_VERSION` path (does the version gate inspect
the project image or the base?), the full permission/config-injection surface, the container
argv and network surface, and what the consumer actually built and why. Nothing above that
carries a bare assertion should be promoted into `PLAN.md` until those land. Specifically I have
**not** yet confirmed that a project image inherits the base `ENTRYPOINT` unoverridden — it is
the load-bearing claim of the whole section and I have only read `Dockerfile.project-template`,
which is silent on `ENTRYPOINT` (silence is consistent with inheritance, but silence is not
proof, and this plan has been bitten twice by exactly that inference).

## 23:20 — Task 7.1 delivered; and the ENTRYPOINT claim came back three ways

[reports/round2-restatement.md](reports/round2-restatement.md) written; Task 7.1 and both its
sub-items ticked; R1–R11 recorded as a ROUND 2 block at the head of `PLAN.md`, appended rather
than rewriting Round 1 — same discipline as Task 6.3.

**Three of the four agents did not transmit their reports.** Each stopped with a sentence saying
the report had been "delivered above", which is a statement about its own context, not about
what reached me. One (the network/argv sweep) did return a substantive six-point summary and it
was good. I re-gathered the rest myself rather than re-prompting three times — image resolution,
the version gate, registry absence, the entrypoint, the container naming and the preflight are
all now read directly by me. Recording it because it changes how the evidence should be read:
**every citation in the Round-2 report is one I read myself**, and the one agent result I did use
(the `--no-network` finding) I then spot-verified at `claude-yolo:2514-2517` before building R11
on it. That was worth doing — the agent's phrasing was "a genuine isolation gap", and the code
shows something more specific and more useful: the flag does not pass `--network` at all, so
podman's default applies.

**The ENTRYPOINT inheritance claim I flagged as unverified at 23:00 is now the best-evidenced
claim in the document**, and it arrived from three directions that had never spoken to each
other:

1. My own check — `ENTRYPOINT` declared once at `Dockerfile:215`, zero occurrences across all
   three project-facing templates.
2. The consumer's `run-sandbox.sh:375-402`, which found it by having a container run the wrong
   script and hard-fail on `GH_TOKEN`.
3. `lts-infra`'s `tasks/runner-ccy-project-image.yml:287-300`, which found it the same way and
   whose comment says `--entrypoint /bin/bash` "IS LOAD-BEARING — do not remove it" — and then
   records that the fix was applied to its CI workflow but *not* to its provisioning copy, so
   the play aborted mid-converge after building the image but before installing the units.

That third one is the reason Decision 6 exists. I had been treating "a second CI entrypoint" as
an architectural nicety to be argued on taste. It is not: three codebases have independently
hand-rolled the same override and two of them got it wrong. Shipping the entrypoint is the
correction for a defect with a measured recurrence rate, which is a much stronger argument than
the one Task 7.1 asked me to make.

**The finding I did not expect: `ccy.env` is an execution vector, and it makes E10 coherent
rather than sloppy.** `entrypoint.sh:269-274` sources `/workspace/.claude/ccy/ccy.env` — a file
*in the mounted workspace* — as shell, immediately before the exec, and `:280-282` then `exec`s
whatever `CCY_CLAUDE_WRAPPER` it set. The code's own comment is scrupulous about the property it
claims ("never on the host — so a project cannot execute code on the host via it") and that claim
is true. But it means the checked-out tree chooses the command that runs.

Read alongside `--dangerously-skip-permissions` (`:2792`), `bypassPermissionsModeAccepted`
(`entrypoint.sh:245`) and the unconditional `hasTrustDialogAccepted` (`:257-263`), that is four
independent assertions of one proposition: *the operator owns this workspace*. C1 read the first
of those as a missing flag. It is not a missing flag — it is a design, executed consistently in
four places, and that is exactly why bolting a permission surface onto it would be wrong rather
than merely expensive. Declining it (Decision 4) is the answer C1 said had to be given out loud,
and the price — trusted automation only — is stated in the same breath rather than buried.

**Where this leaves the plan.** Task 7.1 is the keystone and it is done. Phases 3, 4 and 5 are
*not* designed by this document and the report says so in its own §7, because the failure this
plan keeps catching is a partial result being read as a complete one. Next unit of work is
Phase 3 (image layering / `Dockerfile.ci`), which R9/R10 have already constrained enough that it
should go quickly: the build belongs at provision time, the identity belongs in a `LABEL`, and
the registry question is closed.

## 23:45 — Phase 3 done, and trying to specify against the plan's own words broke two of them

[reports/phase3-image-layering.md](reports/phase3-image-layering.md); Tasks 3.1–3.4 and the 3.3
sub-item ticked.

I expected this to be quick. It was not, because the first thing I did was try to write
"`Dockerfile.ci`, `FROM claude-yolo:full`" as Task 3.1 instructed, and went to check what
`claude-yolo:full` actually contains.

**There is no `claude-yolo:full`.** `full` is a *stage* (`Dockerfile:231`), and the stage is
published under the tag `claude-yolo:latest` (`common.bash:564-565`). Zero matches for the string
across `files/`, `playbooks/` and `docs/` — the only places it appears in this repo are
`PLAN.md:335` (Decision 1, where I invented it), `PLAN.md:475` (Task 3.1, where I repeated it),
`fable-review-1.md:424` (where the hostile review quoted it back approvingly), and this plan's own
`probe-engine.bash`, which — to its credit — probes for it as one of three *candidates* rather
than assuming.

So a name that never existed survived being written into a Decision, copied into a Task, and
endorsed by a hostile review whose explicit brief was to verify every citation against source. It
survived because it is *plausible*: there is a `base` tag and a `base` stage, so a `full` stage
implies a `full` tag. Nobody checked, including the reviewer told to check, including me twice.
The probe script is the only artefact that behaved correctly, and it did so by not knowing.

**The second finding is worse, because it is live and it lands on the mechanism the owner just
called perfect.** `playbooks/imports/play-claude-yolo.yml:338-343` builds exactly one tag,
`claude-yolo:latest`, with no `--target` anywhere in the play. `claude-yolo:base` is produced only
by `build_container_with_hash`'s optional fourth argument (`common.bash:555-562`), passed only at
`claude-yolo:1431`/`:1438` — both inside `if ! validate_container_version …`. An Ansible-provisioned
box has current labels, so that gate passes, so no launcher build runs, so **`claude-yolo:base` is
never created**. Meanwhile `Dockerfile.project-template:12`, `docs/CUSTOM-DOCKERFILES.txt:48` and
`Dockerfile:224` all offer `FROM claude-yolo:base` as a supported choice.

A project following the documented normal way and picking the lean base can therefore fail with
`image not known` — on a machine that was provisioned correctly. I have scoped the claim carefully
in the report: what is *proven* is the code path; whether a given box has `:base` depends on its
rebuild history, and **that dependency is itself the defect**. One host command settles it and it
belongs in the Task 1.1 triage run.

The reason I am pleased to have found it rather than annoyed is that it is the exact template for
how `claude-yolo:ci` would have gone wrong. I was about to introduce a third tag with the same
provenance — built by one path, documented for another. Step 5 of Task 3.4 now fixes `:base` as a
precondition of adding `:ci`, because adding a third history-dependent tag while leaving the second
broken would be indefensible.

**One thing I nearly got wrong on E8.** Task 3.4 names the daily npm auto-update as needing egress
CI must not have, and my first instinct was "set `CCY_AUTO_UPDATE=0` in CI". That is the
plausible-looking answer and it is wrong in an instructive way: the auto-update lives in the
*launcher*, and the whole point of R9's by-time split is that the launcher does not run at job
time. So E8 cannot fire on the CI path at all. Reaching for the environment variable would have
papered over the fact that the launcher was running — which is the actual defect it would have
hidden. Written into the report explicitly for that reason rather than left as an implication.

Committed and pushed. Phases 4 (MCP) and 5 (egress) remain, then the Round-2 audit (Task 6.4).

## 00:10 — Phases 4 and 5: Task 5.1 turned out not to be a naming problem

[reports/phase45-mcp-and-egress.md](reports/phase45-mcp-and-egress.md); Tasks 4.1–4.3 and 5.1–5.4
ticked. Designed together because both are runtime properties, so neither is reachable from the
image seam and both are launcher/entrypoint surface or nothing.

**Task 5.1 under-scoped its own problem twice, and the second one is serious.** It was written as
"two flags whose names suggest the same axis and act oppositely — propose a rename". R11 had
already made it three-way. But the real finding is that **it is not a naming problem at all**: the
consumer measured that `--network pasta:…` and `--network <name>` are mutually exclusive
(`pasta-loopback-forward-probe.sh:42`), because both occupy podman's single `--network` argument.
So a ccy session cannot have both compose-service attachment and proxy-forwarded egress. Any
design that renames the flags and stops there ships a launcher that silently drops one of the two
— which is the failure mode that would be hardest to notice, because both flags would appear to
have been accepted.

Specified as a hard error rather than a precedence rule. A precedence rule is exactly the "one
decision, two paths, one of them quietly wrong" shape this plan keeps finding.

**Phase 4's most useful borrowing is a defensive pattern, not a mechanism.** The consumer verifies
the CLI actually advertises `--permission-mode`, `--strict-mcp-config` etc. before relying on them
(`entrypoint.sh:133-137`), stating at `:114-115` that a CLI silently lacking them "would run with
a weaker boundary than the design states". That is this estate's signature bug, anticipated and
defended against by someone else.

**ccy needs it more than the consumer does, and the reason is self-inflicted**: ccy auto-updates
Claude Code daily, in place (E8). Its CLI is a moving target *by its own design*. So a
`--mcp-config` that silently stopped being recognised would produce a session with no MCP servers
and no error — the control degrading to a no-op through a mechanism ccy built for itself. Written
in as a required assertion.

**Two small payoffs I did not expect to find.**

- Task 4.2 ("does tool restriction belong in ccy?") stopped being a judgement call once Decision 4
  landed. Without fail-closed permissions, a tool allowlist narrows the MCP server's tools while
  `--dangerously-skip-permissions` leaves everything else open — a control that *fires* without
  *discriminating*. That phrase is from lts-infra's own rules and it applies exactly.
- Task 5.3's answer has two halves and the second is new: under the desktop entrypoint the minimum
  boot allowlist is `api.github.com` (three fatal touchpoints), but **under the Decision 6 CI
  entrypoint it is EMPTY**. Decision 6 was adopted for a completely different reason — stopping
  three codebases hand-rolling `--entrypoint /bin/bash` — and it turns out to also take GitHub off
  the boot path entirely, so egress policy is decided by what the workload needs rather than by
  what session-prep demands.

**What I have deliberately NOT done**: re-measured the pasta numbers under ccy. They are the
consumer's measurements on its own runner, reused as Task 5.2 instructed, and ccy's container
shape differs (writable `/workspace` bind mount, desktop entrypoint). The report says so in its
own closing section. Task 5.4's battery is specified, not executed — this plan remains design-only
by explicit owner instruction.

Remaining: Task 7.4's capability list, Task 7.5's reordering, Task 2.x (`--non-interactive`, whose
classification work Round 3 already did), Tasks 1.2/1.3, and then the Round-2 audit loop (6.4/6.5)
— which now has three new documents to attack.

## 00:35 — Task 7.4 + 7.5; and C5's evidence is a trap for whoever reads it next

[reports/task74-capabilities.md](reports/task74-capabilities.md); Tasks 7.4 (all six sub-items)
and 7.5 ticked. Round-2 hostile review dispatched against all four new documents in parallel.

Two of the six sub-items turned out to be stated *less* precisely than the source supports, and in
both cases acting on the sub-item as written would have made things worse.

**C5 — `CLAUDE_CODE_OAUTH_TOKEN` has twelve occurrences, one of them on the `podman run` argv.**
So a grep says ccy already supports taking the token from the environment, and C5 looks refuted.
It is not. The variable is an **output** channel: `claude-yolo:2756` sets it from
`$CLAUDE_OAUTH_TOKEN`, which is assigned only at `:1033` and `:1135`, both
`$(cat "$SELECTED_TOKEN")` — from a **file**. `:2777` then passes it into the container. Nothing
lets a *caller* fill it.

This is the recurring failure mode running backwards, and it is the first time I have seen it in
that direction. Usually the trap is a check that passes vacuously and gets read as proof. Here the
trap is evidence that *looks* like proof of a capability, where the arrow points the other way. I
have written the direction table into the report explicitly, because "C5 is already solved, look
at `:2777`" is exactly the conclusion the next reader reaches, and it is wrong.

**C10 — "trap-based, since `set -e` makes the current trailing block unreachable".** The
unreachability is confirmed. But *a trap already exists*: `trap cleanup EXIT` at
`claude-yolo:1716`, the only one in the tree, and it fires reliably. `cleanup()` (`:1695-1715`)
just does not touch compose — stty restore, `rm -rf "$CONFIG_TEMP"`, debug log.

So following the sub-item literally — add a trap — would install a **second** `trap … EXIT`, and
in bash that **replaces** the first. The fix as written would have silently discarded the
temp-directory cleanup and the terminal restore, to fix a compose leak. The correct fix is to move
the teardown *into* `cleanup()`. Worth the two minutes it took to read the trap's body rather than
trusting the correction that raised it — which is the same lesson as C5, arriving by a different
route on the same afternoon.

**C7 needs two fixes, not one.** The sub-item says "run-ID-salted names, and scope the `rm -f`" —
which reads as one change with two parts, but they repair independent faults. Salting alone leaves
the reaper armed for any caller that does not pass an identifier; scoping alone leaves two
concurrent jobs contending for one name. Recorded as two.

**Task 7.5 I could tick honestly rather than by assertion**, which is unusual for a reordering
task. C11's claim was that Phases 3/4/5 do not need Phase 2. I designed all three this session
without Phase 2 existing, so the claim is demonstrated rather than argued. The one real change to
the order: token-by-value moves *ahead* of Phase 2 instead of being one of its 46 sites, because
`select_token` spins on the default path and Phase 2 cannot prove anything unattended until a
caller can supply a token without touching it.

## 01:00 — Phase 2, and Task 2.3's gate turns out to be already written

[reports/phase2-non-interactive.md](reports/phase2-non-interactive.md); Tasks 2.1–2.3 ticked, plus
Task 1.2 (discharged by Round 3) and Task 7.3's "soften the design text" sub-item.

**Task 2.1's unit of work is wrong and Round 3 already proved it.** The task says "for every site,
specify which of the three outcomes applies". That is unanswerable: `create_token`'s seven prompts
ABORT reached bare and SPIN reached via `select_token` — same lines, two verdicts, decided by the
call path. Round 3 stated the consequence in its own text and I had still written Phase 2's tasks
as a per-site table, because they predate the measurement. Restated as **entry-point decisions**,
of which there are five rather than forty-six.

**The nicest find: Task 2.3 asks for a QA gate that already exists in this plan folder.**
`analysis/classify-prompts.bash` + `bashctx.py` + `fnmap.py` — 397 lines, exits 1 on any failed
invariant, mutation-tested in both directions, and it already carries the Task 7.2 lesson as a
live assertion: the census pattern is checked against a file *known* to contain `read -r -p`, so a
pattern matching nothing can never again report a clean sweep. That is the single most valuable
property a gate of this shape can have, and it is there because the census failed exactly that way
once.

So Task 2.3 is not "design a gate", it is "**promote the one you have**". This repo's own
`PlanWorkflow.md` names "a permanent QA gate wired into `qa-all.bash`" as the persistent case, so
promotion is part of finishing this plan — left where it is, the gate is deleted along with the
plan folder. Same rule lts-infra learned the hard way and wrote down as "core never depends on
plans"; here it bites in the other direction, where something genuinely permanent was born in an
ephemeral place.

I did check whether the `analysis/__pycache__/*.pyc` files were tracked before writing any of this
up — they are not; `__pycache__/` is in `.gitignore:12`. Worth checking rather than assuming, given
how much of today has been assumptions failing.

**Honest on decidability, because the task asked for honesty specifically.** The gate decides: is
this a prompt site, which function encloses it (validated by re-parse), is that function reachable
from a suspending context, is it in an unbounded loop. It cannot decide whether a *given job*
reaches it at runtime. Two further gaps I added: indirect dispatch is invisible to a static call
graph, and a prompt in an unwalked sourced file would be missed — which is *precisely* how
`token-management.bash` escaped the Round-1 census, so it is a measured failure mode of this exact
approach rather than a theoretical one. The gate makes new spins impossible to add **silently**;
it does not prove the absence of a hang, and the report says so.

**Task 2.2 has a real cost and I have not pretended otherwise.** `--headless` implying
`--non-interactive` is the right call — `--headless` already declares no human is driving, so a
launcher that then blocks contradicts it — but it changes behaviour for existing `--headless`
users at a TTY, whose existence C4 established. Announced, so it is visible on the run where it
first bites. That is a mitigation, not a reason to call it free.

Every open task in Phases 1–5 and 7 is now closed except the ones that require code, which this
plan is forbidden from touching. Remaining: Task 1.3, and the audit loop (6.4 running, 6.5 after).

## 01:30 — Task 1.3, and a Success Criterion that I could not honestly tick

Task 1.3 done. The answer has a part I did not expect: **the build context at `/opt/claude-yolo`
is ASSEMBLED from two source trees** — `files/var/local/claude-yolo/` (Dockerfile, entrypoint,
patch, plugins) and `files/opt/claude-yolo/` (startup info, docs, skills). That resolves something
that looks broken on inspection: the Dockerfile's `COPY docs/…` and `COPY skills/…` name paths
that do not exist beside it. They resolve only because the play assembles the context first. So
adding `Dockerfile.ci` to the source tree is *not sufficient* — the play must copy it in and add a
build task, and per Phase 3 §0.2 that same change must fix `claude-yolo:base`.

**Then I went to tick the Success Criteria and one of them would not go.**

*"No source file outside this plan folder has been modified."* I expected this to be trivially
true — I have written only markdown in a plan folder all session. It is **false**, and not because
of anything I did today. Commit `73396b3`, under this plan's own number, added **1,667 lines
across six files outside the folder**: `_planlib.inc.bash` (710), `scripts/test-planlib.bash`
(623), `PlanScriptStandards.md` (271), plus `PlanWorkflow.md`, `Plan/CLAUDE.md`, `Plan/.gitignore`.

That work is real, good, and documented in PLAN.md itself — it is the plan-script library, whose
governing plan is lts-infra 00023. But it was committed under 00066/00068's number, so by this
plan's own ledger it is this plan's modification, and the criterion forbids it.

The tempting move is to reinterpret the criterion to mean what the Non-Goal says — *"not one line
of `claude-yolo`, the libs, the Dockerfiles, or the plays"* — declare that met, and tick. And the
Non-Goal **is** met, provably: `git diff --name-only F44...HEAD -- files/ playbooks/` returns
nothing at all. But quietly widening a criterion until it matches what happened is not passing it;
it is deleting it. Marked 🚫 with the exact file list, the exact line counts, and the distinction
between intent (met) and wording (not met), plus the remedy — reword to name `files/`,
`playbooks/`, `scripts/`, and note the mis-attribution.

I am recording this at some length because it is the same shape as the two things this session
already caught in the plan's own text, and it is the least comfortable of the three: the other two
were *my* errors in prose, and this one is a box I would have been rewarded for ticking.

**Also fixed the README index row**, which was badly stale — it still said "35 `read -rp`
prompts" (46 since Task 7.2) and still stated the Decision-1 thesis ("one launcher, layered
images… a variant **launcher** is re-implementation #5") that Round 1 invalidated and Round 2
replaced. An index row is the only thing most readers see; leaving it asserting a superseded
thesis is worse than leaving it blank.

## 02:15 — Round 2 review: 2 BLOCKER + 2 MAJOR, and both blockers are mine

[reports/fable-review-2.md](reports/fable-review-2.md). Applied as D1–D4 in a ROUND 2 CORRECTIONS
block; the five documents are left exactly as reviewed, per Task 6.3's method.

The good news first, because it is load-bearing for everything else: **both of my claimed
corrections were independently confirmed** — `claude-yolo:full` really never existed, and Ansible
really never builds `claude-yolo:base` — as were all four E10 citations, R11, the OCI-inheritance
chain, and every consumer-repo citation. So the *factual* base holds. What did not survive is
structural, and that is the more uncomfortable kind.

**D1 is the one that stings, because I saw it and then didn't do it.** Early on I explicitly
framed the fork — does CI invoke `ccy` the launcher, or a raw `podman run --entrypoint`? — noted
that R9's by-time split answered it, and then wrote five documents without ever writing the
answer down. Task 3.1's own sentence, *"Selection is the caller's, explicitly"*, commits to the
raw-podman answer, which means Phase 5's `--egress` **as a `claude-yolo` flag** is never on a CI
job's command line at all.

The answer is *both*, split by time: Ansible invokes the launcher at provision time to build the
image; the caller invokes the container directly at job time. That was in R9 already. Carrying it
two documents further would have caught this. Three consequences I have taken rather than argued
with: CI egress is the caller's podman argv (Phase 5 mis-scoped, not wrong — Decision 3's desktop
half stands); C7/C8/C10 are session-launch defects a CI job never reaches, so they are desktop
defects now and no longer CI-motivated; and Phase 2 keeps only the build-and-exit justification,
which is much smaller than "unattended ccy can hang".

**D2 I should have caught by reading my own citation.** I justified Decision 4 with lts-infra's
`RUNNER-VM-DESIGN.md` §5.4/§6.4. §6.4 proves the runner VM cannot reach the hypervisor; §5.4
restricts which destinations are reachable. Round 1's C1 is neither of those things — it is a
**confused-deputy** risk: a live write-capable `GH_TOKEN` misused at `api.github.com`, which is on
every plausible allowlist because the workload cannot function without it. Restricting where you
can go says nothing about what you do when you get there.

And the reviewer's second half is sharper still: those controls belong to one purpose-built VM,
and `ccy` neither requires nor checks for them. It runs on a laptop. So I was defending a design
decision with infrastructure the design does not depend on — textbook "a true statement about one
VM presented as a stronger statement about ccy in general", which is the exact failure mode I was
briefing the reviewer to hunt for. Citation withdrawn; Decision 4 now rests on the trusted-only
scope alone, with the residual written out: **if the trust declaration is wrong, there is no
defence-in-depth inside ccy at all, on any host.**

**D4 is the most embarrassing and the most useful.** In `phase45-mcp-and-egress.md` I rejected the
consumer's tool-matrix with: *"a control that fires without discriminating… is worse than absent,
because its presence invites reliance."* Decision 5's trust flag fires — it gates startup — and
discriminates nothing, because nothing checks whether a caller passing it is telling the truth. I
applied the principle to someone else's design and not to my own, in the same session, a few
hours apart. Now stated plainly as an unverified attestation whose only enforcement is that the
caller typed the word, with the residual accepted rather than dressed up.

The reviewer also caught me over-applying the no-armed-flags rule. Banning *derivation* of a
decision from environment state is not the same as banning a *cross-check* of an already-explicit
declaration against observable state. I collapsed the two and foreclosed a cheap defence. Re-opened.

**D3** is the smaller one but it is a genuine internal contradiction: Decision 6 says the CI
entrypoint omits the four trust assertions, row 4 of which *is* `ccy.env` sourcing — and Task 4.1
then puts the declarative MCP route in `ccy.env`. Resolved in Decision 6's favour: the CI
entrypoint does not source it, so the declarative route is desktop-only and the CI path needs an
explicit caller-supplied env contract.

**Round 3 is required** — the loop is not quiet, and by the plan's own rule it does not end until
a round finds nothing material.

## 02:45 — D5: I found the sixth one myself, and the tell was that it was the better story

Round 3 dispatched. Before it reported, I went back to a claim I had made three times and checked
it, because writing the reviewer's brief made me articulate the doubt: *does the D1 answer hold
together, given provision-time build needs a different user than the one whose `$HOME/.cache`
holds staleness state?*

**They are not different users.** `lts-infra` builds as `runuser -u {{ runner_user }}`
(`tasks/runner-ccy-project-image.yml:90,107,176,258,324`), `runner_user: "runner"`
(`group_vars/all/runner.yml:114`) — and CI jobs run as `runner` too. So my sentence *"a
provisioning user cannot answer the question for the user that runs jobs"* is simply false. It
appears in Task 3.3's tick, in `phase3-image-layering.md`, and in `round2-restatement.md` §4.1.

The conclusion survives — the identity does belong in a `LABEL` — and the real argument is
better: CI answers the question from a **checkout plus the image** (`ci.yml:77` sha256s the
Dockerfile, `:79` reads the label) and never has host-local cache state available at all;
`$HOME/.cache` sits outside the image, cannot travel with it, cannot be read from a checkout, and
drifts silently the moment someone deletes the image and keeps the cache.

**What I want to remember is why it survived three documents.** The false clause was the *better
story*. "Two users on one machine believe different things about the same image" is concrete and
memorable; "state outside the image cannot travel with it" is abstract and forgettable. I reached
for the vivid one, and vividness is exactly what stops a claim being re-examined — it feels
like it has already been checked, because it explains so well.

That is the sixth instance of this repo's recurring failure mode found in this plan, the third in
my own text, and the first I caught before a reviewer did. One `grep runner_user` would have
settled it at any point. Recorded as D5 in the corrections block rather than quietly edited out
of the three documents, because the reports are what the reviewers reviewed and an error deleted
is an error nobody can learn from.

## 03:30 — Round 3: the CI framing shrank for the third round running, and I repeated my own defect

[reports/fable-review-3.md](reports/fable-review-3.md) — 2 BLOCKER + 1 MINOR, verdict *material
findings: yes*. Applied as D6–D8. Both blockers verified by me before acceptance.

**D6 kills D1's provision-time row, and I should have caught it when I wrote D1.** I answered
"who invokes the CI entrypoint" with *both, split by time* — the launcher at provision time, the
caller at job time. The provision half is false. I checked it myself rather than take the review's
word: `play-claude-yolo.yml:338-343` is Ansible calling `podman build` **directly**;
lts-infra's `runner-ccy-project-image.yml:172-195` is `runuser -u runner -- podman build`, also
direct; and `runner-ccy-base-image.yml` *fetches and reads* the launcher script (`:119`, `:141`) to
check version coupling but **never runs it**. Task 3.4's own text says it *mirrors* that
direct-build technique — so my already-delivered Phase 3 design contradicted my own D1 table.

The review's second proof is the one that stings: in `claude-yolo`, credential resolution runs
unconditionally **before every build path** — `select_token`/`create_token` sit upstream of the
first-time build, the version gate, the staleness rebuild, and even the existing `--rebuild`. A
build-and-exit mode would have to bypass ~16 credential prompts and no document says how.
"Build-and-exit" appears three times as a requirement and **zero** times as a design. I cited a
capability I never specified, and then built two rescoping decisions on top of it.

**So `claude-yolo` is never invoked on the CI path at all.** R10 item 1 retracted. Phase 2 and
token-by-value are desktop-only hardening now. **That is the third consecutive round in which the
CI framing shrank** — Round 1 took the thesis, Round 2 took the launcher-mediated CI path, Round 3
takes provisioning. Each time the honest move was to accept it rather than defend the previous
scope, and each time what survived got smaller and truer. Worth naming as a pattern rather than
three separate embarrassments: I keep reaching for the larger claim, and the reviews keep finding
that the code only supports the smaller one.

It also opens a question I have flagged rather than settled, because it is lts-infra's call:
if provisioning legitimately builds with `podman build` directly, then
`runner-ccy-project-image.yml` may not be "a reimplementation of what ccy does natively" at all —
it is an ordinary provisioning build. Only the *staleness decision* is duplicated, and the remedy
is a shared `LABEL` convention rather than deleting the file. Plan 00026 Task 3.3's premise
deserves re-examination against that.

**D7 is the one I have no excuse for.** Commit `1fb9efd` propagated D1 to Tasks 4.1, 5.1 and 5.3,
with a message explaining that a reader scanning to Task 5.1 would otherwise see "DONE" and no
hint. D1 names a *fourth* location by number — Task 7.4's C7/C8/C10 — and a fifth, Phase 2.
Neither got a note. So C8's sub-item still read "**`--no-network` mandatory for CI**" one screen
below the correction retracting it. I committed a message about the importance of propagation
while under-propagating. Fixed properly now, in both places, with the reason recorded at each.

**D8**: my D5 citation was off by twenty lines — `ci.yml:97`/`:99`, not `:77`/`:79` — in a
correction whose entire subject was citation discipline. Verified and fixed. The mechanism D5
describes is unaffected.

**Round 4 required.** The loop still is not quiet.

---

## 04:10 — Round 4 dispatched, and a stale index row found outside the plan folder

Round 4 is running. Briefed on the three correction blocks as authoritative, and pointed at four
things: whether D6's retraction is **fully** propagated (D7 proved this exact class of defect
recurs), whether anything has been *over*-retracted so that Phase 2 reads as cancelled rather than
demoted, the now-much-narrower surviving claim that `ccy` owes CI exactly two things (the image
`LABEL` and a CI entrypoint), and a seventh instance of the meta-bug. Told explicitly that a clean
verdict is a real outcome and that manufacturing a finding to justify the round is a failure mode
— three rounds of blockers create pressure to find a fourth.

While it ran I checked a place the sweep would not look: **`CLAUDE/Plan/README.md`**. Its index row
for this plan was stale again — written after Round 2, it stopped at "Round 1 invalidated the
original thesis and Round 2 restated it" and still framed the launcher as CI-relevant. D6 is
absent from it entirely. This is the same propagation defect D7 caught, one directory further out:
the correction blocks were updated, the pointer into them was not. Rewritten to state the
three-round shrinkage, the direct-`podman build` evidence, the demotion of Phase 2 and
token-by-value to desktop-only, and the two things `ccy` actually owes CI.

Worth recording *why* this keeps happening rather than just that it did. The corrections-append
discipline is right — it keeps earlier reviews referring to real documents — but it has a cost:
every appended correction creates a fan-out of derived text that already summarised the old state.
The plan body, the six reports, the Success Criteria and the README are all summaries of a thesis
that has now moved three times. Appending is cheap; propagating is the work, and it is the half
that keeps getting skipped.
