# Plan 00068 — Journal 26-09-14

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 09:16 · finding · — — the truncation hash in PLAN.md pointed at another plan's commit

`PLAN.md` said the folder "was truncated at commit `0dde4f0`: twenty-two reports and one
probe". Both halves were wrong. `0dde4f0c` is *Plan 00072: ccy now ASSERTS the engine is
rootless instead of inferring it*, and it never touched this folder. The truncation is
`5785c55f`, *Plan 00068: truncate the plan folder to what is still true*, and it deleted
**twenty-nine** files, not twenty-three: twenty-five under `reports/`, the three `analysis/`
scripts, and `probe-label.bash`.

A wrong hash in a plan is worse than no hash — it sends the next reader to a diff that
disproves the sentence citing it. Corrected, with the wrong hash named so this entry and the
line agree.

## 09:18 · correction · — — the six dead journal links are dead ON PURPOSE, and an earlier correction is now itself wrong

The docs-QA sweep flags six unresolvable links in `JOURNAL/00068-Journal-26-07-30.md`
(lines 163, 164, 331, 390, 444, 498). Every target was deleted by `5785c55f`, the truncation
above — the owner's instruction at the time, quoted in `JOURNAL/00068-Journal-26-07-31.md`.
They are not typos and not moved files; there is nothing to repoint them at.

Four of the six carry a second, older fault: they omit the `../` prefix, so they resolve
against `JOURNAL/` rather than the plan root. That is what `JOURNAL/00068-Journal-26-07-31.md`
line 1049 set out to fix when it wrote **"All seven files exist; the correct prefix is
`../reports/`"**. That sentence was true when written and is false now — the files were
deleted the same day, later. This entry supersedes it.

Nothing is edited in either day-file: the journal is append-only, and a correction is a new
entry, never a rewrite. The sweep's finding stays advisory and now has an answer in the log
rather than an unexplained red mark.

## 09:20 · finding · T3.4 — a stale gate in reports/ci-flow.md contradicted Decision 9

`reports/ci-flow.md:53` listed "**no `--dangerously-skip-permissions`** — Decision 9's
restricted tool surface, **gated on E8**". E8 was dissolved on 2026-08-10 when the mechanism
was measured to *compose* with the flag; `DECISIONS.md` Decision 9 now says CI keeps
`bypassPermissions` and imposes the tool surface with `--disallowedTools` **alongside** it.
The report said the opposite of the decision it cited. Corrected in place — a report is a
supporting document, not an append-only log — and the line names what it used to say, so the
reversal is legible rather than silent.
