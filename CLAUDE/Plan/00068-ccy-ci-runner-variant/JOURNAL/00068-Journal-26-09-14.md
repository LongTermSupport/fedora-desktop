# Plan 00068 — Journal 26-09-14

> Append-only. New entries at the BOTTOM; never edit an earlier one.

## 09:16 · finding · — — the truncation hash in PLAN.md pointed at another plan's commit

`PLAN.md` said the folder "was truncated at commit `0dde4f0`: twenty-two reports and one
probe". Both halves were wrong. `0dde4f0c` is *Plan 00072: ccy now ASSERTS the engine is
rootless instead of inferring it*, and it never touched this folder. The truncation is
`5785c55f`, *Plan 00068: truncate the plan folder to what is still true*, and it deleted
**twenty-nine** files, not twenty-three: twenty-five under `reports/`, the three `analysis/`
scripts, and `probe-label.bash`.

A wrong hash in a plan is worse than no hash — it sends the next reader to a diff that
disproves the sentence citing it. Corrected, with the wrong hash named so this entry and the
line agree.

## 09:18 · correction · — — the six dead journal links are dead ON PURPOSE, and an earlier correction is now itself wrong

The docs-QA sweep flags six unresolvable links in `JOURNAL/00068-Journal-26-07-30.md`
(lines 163, 164, 331, 390, 444, 498). Every target was deleted by `5785c55f`, the truncation
above — the owner's instruction at the time, quoted in `JOURNAL/00068-Journal-26-07-31.md`.
They are not typos and not moved files; there is nothing to repoint them at.

Four of the six carry a second, older fault: they omit the `../` prefix, so they resolve
against `JOURNAL/` rather than the plan root. That is what `JOURNAL/00068-Journal-26-07-31.md`
line 1049 set out to fix when it wrote **"All seven files exist; the correct prefix is
`../reports/`"**. That sentence was true when written and is false now — the files were
deleted the same day, later. This entry supersedes it.

Nothing is edited in either day-file: the journal is append-only, and a correction is a new
entry, never a rewrite. The sweep's finding stays advisory and now has an answer in the log
rather than an unexplained red mark.

## 09:20 · finding · T3.4 — a stale gate in reports/ci-flow.md contradicted Decision 9

`reports/ci-flow.md:53` listed "**no `--dangerously-skip-permissions`** — Decision 9's
restricted tool surface, **gated on E8**". E8 was dissolved on 2026-08-10 when the mechanism
was measured to *compose* with the flag; `DECISIONS.md` Decision 9 now says CI keeps
`bypassPermissions` and imposes the tool surface with `--disallowedTools` **alongside** it.
The report said the opposite of the decision it cited. Corrected in place — a report is a
supporting document, not an append-only log — and the line names what it used to say, so the
reversal is legible rather than silent.

## 09:25 · action · T3.4 — the per-event tool surface specified; the honest part is what it does not buy

[reports/ci-tool-surface.md](../reports/ci-tool-surface.md). Decision 9 had already settled the
mechanism and two of the three sub-items — assert names not counts, prefer an allowlist where
the vocabulary is not ours — so the residue was sub-item 1, the per-class surfaces, and that is
what this specifies.

Two classes. `push`/`pull_request` keeps `Bash`, because without it there is no `ci.bash` run and
the job has no purpose, and loses `Edit`, `Write` and `NotebookEdit`. `issues`/`issue_comment`
loses `Bash` too — which is what makes the class worth separating, and which also removes `gh`,
so the one write it is allowed to do has to arrive as an enumerated MCP tool rather than an
unbounded shell. That is the design, not a cost of it.

MCP is allowlisted at **server** granularity (`--mcp-config` + `--strict-mcp-config`), and no
`mcp__*` name goes in a denylist. A typo in a server name yields a job that cannot post and says
so; a typo in a denied tool name yields a job that can do more than intended and says nothing.

The part worth writing down is §2's admission. `Bash` is a write vector, so removing `Edit` and
`Write` does not make a class-A job read-only, and what actually stops it pushing is **the
token's scopes** — a property of lts-infra's token store, which has no checkout here. It is named
as a required property to confirm, not asserted as a fact. A boundary described as stronger than
it is, is worse than a weak one described honestly, and Decision 9 half-conceded this already
("read-only cannot be literal") without following it to the consequence.

Four startup assertions, each able to fail. The one an implementer will want to skip as redundant
is assertion 3 — assert `Bash` is *present* for class A. It is the only one that catches an
over-tightened denylist, and once one list feeds two classes, over-tightening is the likelier
mistake. A class-A job whose `Bash` went missing would skip the suite and pass green: the exact
failure this plan exists to prevent, arriving through the mechanism meant to prevent it.

Deliberately not invented: the concrete default tool vocabulary. The 2026-08-10 measurement
recorded counts, not names. The spec names the primitives that must be absent and requires the
implementation to capture the real list and assert against it — a hand-written list of the other
26 names would be exactly the drift §4 forbids.

## 09:50 · action · T4.1 — the implementation plan filed as Plan 00113; this plan's task tree is now complete

[Plan 00113: ccy CI runner — implementation](../../00113-ccy-ci-runner-implementation/PLAN.md).
The dedupe scout checked 59 live plans first and found no overlap: 00089 is a landed subset
(token-first `ssh-handling.bash`), and 00080, 00092, 00093 and 00072 are different subsystems.

The filed plan restates none of this one's design — it links DECISIONS.md, `ci-flow.md` and
`ci-tool-surface.md`, and says in its Overview that **where the two disagree, 00068 wins** and
the disagreement is 00113's defect to fix. A plan that paraphrases its specification is a
second copy that will drift from it.

Three things were carried forward deliberately rather than assumed away:

- **Phase 0 re-measures what this plan derived.** `ci-flow.md` says about six prompt sites and
  says of itself that this is "a derivation, not a measurement: confirm by instrumenting the CI
  path before implementing". 00113 does that before Phase 1, because six versus fourteen
  changes the shape of the work. It also re-asserts the security-carrying flags exist, since
  ccy auto-updates the CLI daily and the binary is not the one E9 measured.
- **The token-scope property.** `ci-tool-surface.md` records that `Bash` is a write vector, so
  removing `Edit`/`Write` does not make a class-A job read-only — what stops it pushing is the
  token's scopes, which live in lts-infra and cannot be read from here. Filed as an open owner
  question, not as a solved problem.
- **§8's two unanswered owner questions** (`ccy.env` sourcing; the `/root/.claude` →
  `/workspace` symlink) travel with it rather than being silently dropped at the hand-off.

With 3.4 and 4.1 closed, every task in this plan's tree is ticked. What is not ticked are the
§9 obligations B1–B4 and C1/C2, which need a HOST `triage.bash` run — so the plan is
task-complete and evidence-incomplete, and 00113's Task 0.1 is where that run now belongs.

## 11:05 · correction · — — the dead-link census was nine, not six, and I reported a tool's number as the population

The 09:18 entry above says the sweep "flags six unresolvable links in
`JOURNAL/00068-Journal-26-07-30.md` (lines 163, 164, 331, 390, 444, 498)". It flags **nine**.
The three I left out are `reports/phase2-non-interactive.md` (:543),
`reports/fable-review-2.md` (:632) and `reports/fable-review-3.md` (:725) — same day-file, same
cause, all three deleted by `5785c55f`, all three absent today. Coverage is **9 of 9**, and
stating a coverage number rather than a list length is the correction that matters: I quoted a
count I had read off a filtered view without asking what the filter had dropped. That is this
repo's highest-recurrence defect and I reproduced it inside an entry whose entire job was to
correct a partial claim.

It bites where the supersession lands. `JOURNAL/00068-Journal-26-07-31.md:1049-1056` names
**exactly seven** files and asserts "All seven files exist" — and those three omitted links are
among the seven. Scoped to six, my correction left a reader chasing `fable-review-2.md` still
landing on "All seven files exist" with nothing overriding it. **All seven of that entry's files
are gone**, deleted by `5785c55f` later the same day; none of the nine links has anything to
repoint at, and none should be reconstructed.

A tenth casualty in the same truncation was not a journal link at all and so never appeared in
the six: `reports/mcp-and-egress.md:227` cited `ci-entrypoint-spec.md`. A report is a supporting
document, so that one is **fixed in place** — it now names the truncation, says the file must
not be reconstructed, and points at `ci-flow.md` for the live statement of the entrypoint's CI
behaviour. The nine journal links stay dead by rule: the journal is append-only, and this entry
is their answer in the log.

## 11:20 · correction · T4.1 — "the run now belongs to 00113 Task 0.1" was a hand-wave, and the review caught it

The 09:50 entry closes with the §9 obligations B1–B4 and C1/C2 needing "a HOST `triage.bash`
run — so the plan is task-complete and evidence-incomplete, and 00113's Task 0.1 is where that
run now belongs". `triage.bash` cannot discharge a single one of those six.

`probe-network.bash:224` says so in its own output: **"C1 and C2 are deliberately NOT probed
here"** — both need a listener on the host to mean anything, and a probe that opens host sockets
is no longer read-only. B1–B4 appear in no probe at all; `DECISIONS.md:341` classifies them
"interactive; needs real quota", and `JOURNAL/00068-Journal-26-07-31.md:124` spells out why —
they need a real container and a real session on the owner's machine, burning quota. I named a
method that the plan's own probe source states will not answer the question, and called six open
obligations transferred.

What `triage.bash` re-confirms is **E1, E6 and C3**, and re-confirmation is worth having because
Plan 00089 has changed the launcher since run `20260731-225344`. The 09:50 entry's implication
that the probes have never been run against the launcher is also wrong: they were run, on
2026-07-31, and Task 1.1 calls that run authoritative — what is missing is a run *since* 00089.

Rather than drop the word "transferred", the obligations now have methods. 00113 Phase 0 gains
Task 0.4 (B1–B4: `ccy --headless --prompt x < /dev/null` under a controlled token-file
population, hang-vs-exit observed) and Task 0.5 (C1/C2: a host listener, which is exactly what
makes them ineligible for a read-only probe). Task 0.1 now states that it re-confirms E1/E6/C3
and discharges nothing else.

## 11:35 · correction · T3.4 — two defects in ci-tool-surface.md that its own arguments already refute

Both from the review, both cases of an argument made in one section and not applied in another.

**§2 over-claimed about the credential.** It said "what actually stops a class-A job committing
or pushing is the credential, not the tool list". No credential stops a local `git commit`, and
none stops a shell redirect into the working tree — and Decision 9 forbids three things, "never
write to it, commit, or push", of which the token reaches only the third. It matters because the
runner keeps a *persistent* checkout, so a dirtied tree outlives the job that made it. §2 now
scopes the credential claim to push and files the dirty-tree half as a requirement on lts-infra
Plan 00030, since nothing in this repo can assert it.

**§5 assertion 2 was a denylist wearing the other hat.** It asserted only that the must-be-absent
names are absent — which is the exact fail-open shape §3 argues against for MCP, over a
vocabulary that is likewise not ours, in a CLI that assertion 1's own rationale says changes
daily. A renamed or newly added write primitive is absent-by-name, and every assertion passes
green over a class-A job that can now write. It is now a **diff against a declared expected
set**: an unexpected member fails. The cost is that a benign new read tool also fails the job
until someone declares it, and that review is the point — so it is written down as the trade-off,
not hidden. This makes Task 0.3 a prerequisite rather than a nicety: the set cannot be declared
until the names are captured, and the 2026-08-10 measurement recorded counts only.

Assertion 3 survives as its own item. It is formally the presence half of the diff, but its
failure mode is the silent one, and an implementer who reads assertion 2 as "the denylist" will
drop it.

## 11:45 · correction · T4.1 — two things the hand-off dropped, both now tasks in 00113

**Compose and networking.** `DECISIONS.md:279-299` records this as an owner requirement in the
owner's own words — *"i would not assume that CI doesn't need compose or podman network stuff"* —
with a function-by-function keep/drop table over `lib/network-management.bash` and the directive
to **pick one deliberately** on resolving the network before `podman run`, since `--network` is a
create-time argument. 00113 as filed had no task, no criterion and no Non-Goal covering any of
it. A requirement stated by the owner and then dropped at a hand-off is the worst kind of loss,
because the hand-off document reads complete. 00113 gains Phase 2b: the keep/drop split, the
before-vs-after network decision, declaration in `.claude/ccy/` rather than discovery, teardown
of only what CI started, and the untested no-SSH-key/no-`GITHUB_USERNAME` entrypoint path.

**Plan 00089 asserted as settled.** 00113 said "Plan 00089 has already landed the token-first
half of the credential story, so the launcher's hardest dependency is in place", and listed it as
"landed". The code landed; the proof did not. 00089 is still In Progress with its own success
criterion unticked — a `--no-ssh` launch with `GH_TOKEN` pre-exported, live on HOST. 00113 now
says written-but-unproven and carries Task 0.6 to discharge it, which is what Phase 0 is for.

## 11:10 · finding · — — round 2 discharged all fourteen, then found a report that has contradicted Decision 6 for six weeks

Review 2 (`subagent-reports/260914-review-00068-round2-opus-5.md`) confirms all fourteen round-1
findings are discharged, and returns **BLOCK** on one new blocking item plus three should-fixes.
The blocking one is the more interesting failure, because I created the conditions for it.

`reports/ci-required-config.md` §4.3(f) specifies that CI must **not** start compose —
"`_do_compose_start` … do not start; a job that needs compose starts it before invoking `ccy`" —
and closes "no compose opt-in flag is specified … a new flag would be a mechanism with no
caller". §4.3(c) files auto-connect as wrong for CI because it is "the opposite of a restricted
egress posture". `DECISIONS.md §6` reversed all of it on the owner's steer on **2026-08-01**,
one day after that report was last touched. It has stood contradicted ever since, and the
restricted-egress premise underneath §4.3(c) was itself dropped by Decision 8.

What made it blocking is my own Phase 2b: Task 2.5 now tells an implementer to **keep**
`_do_compose_start`, while a live report in the same folder tells them the CI answer for that
exact function is "do not start". Adding the missing requirement is what turned a dormant
contradiction into two live opposite instructions. The file already used supersession markers in
three other places, so the convention was sitting there unused for this one. Both sections now
carry notes pointing at §6.

Three more, all mine:

- **Task 0.4 took one clause from its source and dropped the two that refute it.** I cited
  `JOURNAL/00068-Journal-26-07-31.md` for "burning quota" and then prescribed "hang-vs-exit
  observed" under "a controlled token-file population". The same passage says the exit code
  **does not discriminate** — a spin bounded by `timeout` exits 124 and so does a real session
  killed by it, so only captured output tells them apart — and that creating that token-file
  population means scripting the owner's credential store, "which this plan will not do". I
  quoted the sentence that supported the task and skipped the two that ruled out its method.
  Rewritten: captured stdout/stderr, owner-arranged population, marked 🚫.
- **I re-offered the substitution round 1 blocked.** Task 0.4 invited settling B1/B2 via Task 3.2,
  which runs *after* Phase 1 closes every prompt site. B1/B2 are claims about the launcher
  *before* the fix; a post-fix pass cannot evidence pre-fix behaviour. Scoped to Task 0.2.
- **The set-diff derivation was refuted by a measurement two sections above it.** §4 said "what
  is subtracted becomes the deny string, what remains is what assertion 2 diffs against". §1
  records that removing `Bash` **adds** `Glob` and `Grep`: 29 − 3 = 26, measured 28. Subtraction
  predicts a set that would fail assertion 2 on every single run. The expected set has to be
  **captured per class with that class's deny string applied**, which is now what Task 0.3 does.
  §2's class B row had this right all along; I generalised from the wrong section.

Also fixed: `ci-tool-surface.md:19` claimed every assertion is "on a tool name being absent",
which the rewrite made false; Task 2.5's keep/drop list omitted `_compose_already_running`,
`network_has_running_containers`, and — the one that matters — the `read -rp` inside
`_do_compose_start` itself (`network-management.bash:586`), so following it literally would ship
a prompt onto a stdin-closed CI path; Task 2.9 was filed under "compose and networking" when it
is entrypoint work, and is now Task 0.7; `DECISIONS.md §6`'s line numbers had drifted ~40 lines
and Task 2.5 sends an implementer straight to that table; and `CLAUDE/Plan/README.md` described
this plan by two retracted mechanisms — the image `LABEL` and `ci-entrypoint-spec.md` — which
would have become its permanent Completed-list description.

One nit taken as instruction rather than noted: the timestamps on my earlier entries today ran
ahead of the commit that carried them. A log's times should be observed, not chosen. This entry's
is `date -u`.
