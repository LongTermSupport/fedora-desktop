# Plan 00068 — Journal 26-07-31

> **Append-only activity log** for plan 00068. Entries are added at the bottom, never edited.
> Corrections are new entries, not rewrites of old ones.

______________________________________________________________________

## 03:45 — "Blocked on human input" was true about the audit and false about the plan

Yesterday closed with the claim that the only remaining work needed a human. I checked the two
unblocking conditions again (`lts-infra` still absent; nothing new on the branch), and both were
unchanged — so the claim held *for the thing I had been doing*. It did not hold for the plan.

**Hardware-proof group F had no probe.** Group F was added to the checklist when the `LABEL`
convention spec was written under D10, and its **F1** is described there, in the plan's own words,
as *"the single most consequential unproven claim in the specification"*. The two probe scripts on
disk — `probe-engine.bash` and `probe-launcher.bash` — were written a day earlier and cannot answer
any of F1, F2 or F3. So the plan has been carrying its most consequential open question with a
remedy that pointed at nothing.

This is the propagation defect again, and worth counting as such: a correction (D10) that created
work, filed correctly in the right document, and never wired to anything that would do it. Twelve
instances of one failure mode and eleven of the other are recorded in this plan, and the eleventh
propagation instance was found by refusing to take my own status report at face value. This is the
twelfth, found the same way — the difference being that this time "blocked" was the report under
audit rather than a tick.

**What was written.** `probe-label.bash`, wired in as `triage.bash`'s third leg, so the same host
run the owner already owes settles group F too. It measures rather than asserts:

- Three throwaway `FROM scratch` label carriers: **no labels**, **one unrelated label**, **both
  canonical keys**. The first two are separated deliberately — an image with no labels at all may
  hand the Go template a nil map, which is a different code path from a non-nil map that lacks the
  key, and only the second is what a real pre-convention project image looks like. A probe that
  built only the empty case could answer F1 correctly and still say nothing about production.
- It records **exit code and byte count**, not "empty". Length is the fact; "empty string" is a
  description of it.
- It **executes both comparison shapes** — the naive one, to see whether the two-empties no-op
  actually reproduces, and the prescribed non-empty-assertion one. Reproducing the failure is worth
  more than asserting it.
- A **positive control** with a known label value, because a reader returning empty for
  *everything* would make every row above it meaningless. That is `bash-standards.md` §9: a control
  that fires is not necessarily a control that discriminates.

**Two things I had to be honest about rather than tidy away.**

`triage.bash`'s header said *"READ-ONLY: nothing is built, installed, pulled or written outside the
run directory."* Adding a leg that builds images makes that false. A document asserting a property
its own code contradicts is precisely the defect this plan has spent seven rounds cataloguing, so
the header now states the effect per-leg instead of making one blanket claim. The probe also
**refuses to run rather than reuse or overwrite a tag that already exists** — cleanup deletes what
it built, so reusing someone else's tag would destroy their image.

And: **written is not run.** Lint-clean proves it parses, nothing more. What I did verify here is
that it refuses the container correctly — `[FATAL] refusing to run inside a container`, exit 1, and
**no report file left behind**, so a wrong-environment run cannot leave a half-written artefact to
be mistaken for evidence. Every F-group measurement remains unmeasured until the owner runs it.

**QA.** `./scripts/qa-all.bash` exits 1, and none of it is mine: 0 shellcheck error-level findings
and no diagnostic naming either file I touched. The two red stages are inherited — a `ruff` set
that includes `.claude/untracked/venv/bin/pip` (the unanchored-exclusion class already documented
in `CLAUDE/QA.md` from Plan 00067), and one `qa-ansible.bash` fail-fast hit on
`play-systemd-user-tweaks.yml:242` which is a **false positive on a comment** that documents an
`ignore_errors` having been *removed*. `git status` shows the working tree differs from HEAD in
exactly two `.bash` files under this plan folder, which is what makes "inherited" a checked
statement rather than a convenient one. Both are core-IaC fixes this plan's Non-Goals forbid, and
fixing them here would also worsen the already-🚫 no-files-outside-the-plan-folder criterion.

**Standing blockers are unchanged** and still need the owner: the private repo names in pushed
public history (the only one with ongoing exposure), `lts-infra` not being checked out, and
`verify-dearmed.bash` plus the machine-account vault values.

______________________________________________________________________

## 04:05 — C3 too: the baselines are the probe, not the combination

Having found group F unrunnable, I checked the other groups rather than assuming F was the only
gap. Group A turned out to be substantially covered already — `probe-engine.bash`'s provenance
section prints exactly what A1, A2 and A4 ask for. Group C was not covered, and its **C3** is
flagged in the checklist as *"the one to re-measure first"*: it is the only borrowed claim that
drives a **hard failure** rather than a warning, since Task 5.1 specifies an error on the strength
of it. It was measured by a consumer on its own runner and never under ccy's container shape.

`probe-network.bash` measures it. The part worth recording is the experimental design, because the
obvious version of this probe is wrong:

Running `--network pasta:-T,3128 --network podman` and observing a non-zero exit would be
*consistent with* exclusivity — and equally consistent with pasta not being installed, the image
being unusable, or the engine being too old. The mundane explanation is usually the true one. So
the probe runs **each flag alone** as a baseline, and only treats a failing combination as evidence
of exclusivity when **both singles succeed**. If a baseline fails it reports C3 **UNANSWERED**,
explicitly noting that this is not a refutation. It also runs **both orderings**, because
"rejected" and "last flag wins" are different behaviours that a single ordering cannot tell apart —
and last-flag-wins is a third possibility C3 does not describe and Task 5.1 does not account for.

The branch I made sure to write out is the one where the flags turn out **not** to be exclusive.
That would invalidate Task 5.1's specification, and it is the outcome most worth having; a probe
whose report only reads well when it confirms the prior is not a probe.

**C1 and C2 are left open on purpose.** Both need a listener on the host to mean anything, and a
probe that opens host sockets stops being read-only. Recording them as still-borrowed is the honest
outcome — closing them with a probe that measured something adjacent and easier is the failure mode
this plan is named for.

Two probes now, both lint-clean, both verified to refuse the container and leave no report behind,
both **unrun**. The host run the owner already owed for Task 1.1 now settles groups C and F as
well, at no extra cost to them.

______________________________________________________________________

## 04:20 — Recording the groups that get no probe, and why

Having covered C and F, the temptation is to stop and let the remaining groups be quietly absent
from `triage.bash`. An unexplained gap gets re-derived by every later reader, and worse, gets
mistaken for a decision — so the checklist now carries a coverage map naming every group, its probe
if it has one, and the reason if it does not.

**Group B is the one that matters, and it is genuinely not automatable safely.** I worked out how
I would write it before concluding I should not:

- The discriminating signal is **not the exit code**. A spin bounded by `timeout` exits 124 — and
  so does a run that did *not* spin, instead launched a real Claude session, and got killed by the
  same `timeout`. Telling the two apart needs the captured output, which means the probe must
  actually launch a real container and a real session on the owner's machine, burning quota and
  producing side effects, to learn anything at all.
- B1 and B2 need a **specific token-file population** — at least two files for B1, exactly one for
  B2. Creating or removing those means a script manipulating the owner's credential store, which
  this plan will not do under the same rule that keeps it away from plaintext credentials.

So group B is an interactive investigation with the owner, not a hand-over script. That is a real
remaining gap and I am not dressing it as covered: B1 and B4 are the two the checklist itself
identifies as able to change a decision if they come back the other way.

**Group D is premature rather than hard** — it specifies the proof battery required before egress
may be *called* a control, against a proxy nobody has built. A requirement on a future
implementation, not a measurement available on today's host.

That is the end of what I can usefully do without the host or the owner. Both probes are written,
linted, wired in, committed and pushed; the standing blockers from yesterday are unchanged.

______________________________________________________________________

## 05:10 — Enumerating the boxes instead of trusting my summary found three more

The last tick I again reached "blocked only on human input". Having been wrong about exactly that
claim once already today, I checked it the way that worked rather than asserting it: `grep` every
unticked checkbox in PLAN.md and read each one. Three defects, all in the same family.

**D26 — a delivered deliverable still reported as outstanding, inside the completed task.** Task
6.5 was ✅ and carried a ⬜ child saying *"The one-page restatement — deliberately held until Round
2 reports"*. The restatement was written and is linked and ticked **three lines above**; this was
the original placeholder, never resolved when the real entry was written over it. A seven-round
hostile audit read this task as complete and never looked at its children.

**D27 — a correction applied to one of two siblings.** The spin-path item was converted ⬜ → ⏸️ on
the reasoning that, left ⬜, it *"read as outstanding work someone could pick up in this plan,
which would breach that Non-Goal"*. That reasoning covers the abort-diagnosability item
identically — same forbidden edit, same file — and a sentence four lines below calls them **"the
two implementation sub-items above"**, naming them as a pair. Only one was converted. The
countermeasure this plan already wrote down (*after writing a correction, grep for what else it
governs*) would have caught it in one command; it was not run. The version-bump precondition was
the third sibling and went the same way in this pass.

**D28 — the parent inherited its children's problem.** With all three sub-items deferred, nothing
under Task 7.3 is actionable here, yet the task was still 🔄 — *"currently being worked on"*. The
defect climbs: siblings deferred one at a time, the parent aggregating them asserting active work
throughout.

The distinction I had to be careful about: **Task 1.1 stays 🔄 deliberately.** Its remaining work
is a host run that will genuinely resume; Task 7.3's is an edit this plan may never make. Identical
as icons, entirely different as states — which is the whole reason to be exact.

Fifteen instances of the propagation defect now. The pattern in today's three is sharper than the
earlier ones: none needed new information, none needed `lts-infra`, none needed the host. They
needed me to enumerate rather than summarise. Both times today that I concluded "blocked only on
human input", the conclusion was about my *attention*, not about the plan — and the check that
refuted it cost one `grep` each time.

______________________________________________________________________

## 08:30 — D29: one of exactly two deliverables had no specification

Third time today I reported "blocked on human input", third time it was wrong. The external
blockers **are** real and I verified them rather than assuming: no `triage-runs/` (the host run has
never happened), `lts-infra` still absent, no new commits, branch level with origin. What was wrong
again was the leap from *the audit is blocked* to *the plan is blocked*.

The check that broke it: the plan says `ccy` owes CI **exactly two things**, so I compared them
side by side instead of restating the pair. Deliverable 1 has a 189-line `label-convention-spec.md`
— keys, algorithm, writer, reader, comparison. Deliverable 2 had no specification document at all.

**What existed for deliverable 2 was a mechanism, and it is genuinely good**: `Dockerfile.ci` ships
the entrypoint as a file, does not make it the default, selection is the caller's, it carries its
own version label. None of that says what the file *contains* or what it is *called*. The tell was
sitting in Round 2's own prose: `--entrypoint /opt/claude-yolo/<ci-entrypoint>` — a literal
placeholder, because the name did not exist, and the wrong directory besides (the desktop
entrypoint is in `/usr/local/bin/`, `Dockerfile:184`).

Round 4 then recorded it in bold as *"well specified"* and *"the one half of 'the two things' that
is not hand-waved"*, on evidence covering only shipping and selection. **A true statement about the
mechanism presented as a stronger statement about the deliverable** — this plan's signature defect,
operating one level up from a citation. And structurally it is D27 again: two siblings named as a
pair throughout, work done on one.

**How it went missing, precisely.** `round2-restatement.md:329` lists what remains open and names
"`Dockerfile.ci` **contents**". Task 3.1 closed exactly that — what the image layer adds and must
not add. The CI **entrypoint's** own contents were never on that list, or any list. So the item was
not neglected; it was never tracked, and the adjacent item's closure read as covering it.

**The second finding is the one that would have bitten.** Task 5.3 says the CI entrypoint *"prepares
nothing and authenticates nothing"*. That was derived as an **allowlist** claim and is correct as
one — it makes no network call, so the boot allowlist is empty. It has been carried forward as a
**behaviour** claim, and there it is false: preparing local state needs no network.
`entrypoint.sh:240-263` writes `hasCompletedOnboarding`, `bypassPermissionsModeAccepted` and
`projects["/workspace"].hasTrustDialogAccepted`. An entrypoint that genuinely prepared nothing
leaves a TTY-less CI job blocking on onboarding and trust prompts — **the group-B spin this plan
exists to prevent, reintroduced by its own specification.** "Empty boot allowlist" had become
"empty boot behaviour".

The propagation grep found **three** sites, and the third was the worst: `phase45-mcp-and-egress.md:188`
said *"prepares nothing, authenticates nothing, and asserts nothing about trust"* — naming trust
explicitly, which is exactly the clause that breaks it. One command, run because the countermeasure
this plan wrote down and then failed to run fifteen times says to run it.

**`tini`.** Specifying the artifact surfaced a defect in the mechanism the plan endorses.
`Dockerfile:215` is `ENTRYPOINT ["/usr/bin/tini", "--", "/usr/local/bin/entrypoint.sh"]`, and
`--entrypoint` replaces the **whole vector** — so "selection is the caller's, explicitly" silently
drops `tini`. Cost: no zombie reaping, and no signal forwarding, so a cancelled CI job hangs until
the runner's hard kill. Invisible in a short job, which is why it has to be written down rather than
discovered. The spec publishes the required caller form and **rejects** the tempting fix of having
the script re-`exec` itself under `tini` — that hides a process-model change inside a script and
double-wraps a caller who already did the right thing.

**What I did not do.** No `ci-entrypoint.sh` and no `Dockerfile.ci` were written — that is
implementation, forbidden by this plan's Non-Goals, and it needs a CCY version bump and host
testing. Review reports were left exactly as written: `fable-review-4.md` and `-6.md` are testimony
about what a reviewer concluded at the time, and rewriting them would destroy the audit trail that
makes D29 legible. PLAN.md and the design documents are current-state and were corrected **in
place**, per the owner's ruling that PLAN.md is a living git-tracked document.

**Honesty about the new material**: G1–G3 are unproven claims, recorded as such. G1 (`--entrypoint`
drops `tini`) is OCI-documented behaviour I have not measured; it is cheap and read-only and is the
natural fourth leg of `triage.bash` — deliberately **not** wired in now, because the owner has not
yet run the three legs that exist, and adding legs to an unrun script is churn, not coverage. G2 and
G3 are group-B-shaped and inherit group B's conclusion: distinguishing "blocked on a prompt" from
"killed by `timeout`" needs captured output and real quota, so they are an interactive investigation
with the owner, not a script.

Sixteen instances of the propagation defect, and fourteen of the meta-bug. The pattern in today's
four is unchanged: none needed the host, `lts-infra`, or new information. They needed me to
enumerate a pair rather than name it.

______________________________________________________________________

## 09:40 — D30: the owner's question found what seven rounds and a closed loop did not

The owner asked two things: does CI need to run `claude` for triage tasks, and will projects have to
maintain a separate `Dockerfile.ci` and keep tooling in sync between the two.

**The second answer is no**, and cleanly: `Dockerfile.ci` is *ours*, built by Ansible to
`claude-yolo:ci`. A project keeps one Dockerfile and changes one `FROM` line. Nothing to sync.

**The first answer retro-validates D29.** Running `claude` in CI is the primary case, and an
entrypoint that "prepares nothing" would leave it blocking on the onboarding and trust dialogs with
no TTY. Yesterday's correction wrote exactly the three flags that case needs. Pleasing, and also a
warning: D29 was found by enumeration, not by asking what the thing is *for*. Had I asked that
first, the same defect would have fallen out in one step.

**Then the second question walked into D30.** A project opting into `FROM claude-yolo:ci` has its
staleness measured against `claude-yolo:latest` — `claude-yolo:1477` inspects a **hard-coded
literal**, not the project's `FROM` line, and prints the assumption in its own user-facing message
(*"Project images inherit from claude-yolo:latest"*). So when `Dockerfile.ci` changes and `latest`
does not, both sides read `latest`, compare equal, and no rebuild fires: the project silently keeps
the **old CI entrypoint**, indefinitely. F1's shape — a check that runs and cannot discriminate —
except F1 fails on a transitional population that shrinks, and this fails permanently on exactly the
images the CI work exists for.

**I got the diagnosis wrong first, and the record corrected me.** I wrote that nobody had turned
`:1478` around. Then I read Task 3.2, which states the gap explicitly and hands it to 3.3 by name;
and Task 3.3, which accepts it, adopts "(C) move the staleness identity into an image LABEL", and
even finds a second cache file nobody had named. The handoff worked. What survived is subtler and
worse: option C moved **where** the base version is stored — cache file → label — and never touched
**which base** it refers to, which was the residual actually handed over. The new spec then
hard-coded `claude-yolo:latest` in its own writer, reproducing the defect it was adopted to fix.

That is a **different species** from this plan's other fifteen propagation instances. Those are
corrections that never reached the task they governed. This one reached it, was accepted, and the
remedy addressed the adjacent half while reading as though it subsumed the handed-over one. Every
review since has seen a closed loop — which is precisely why seven hostile rounds went past it. I
nearly filed the easy version ("nobody checked") and it would have been false in the direction that
flatters the reviewer.

**Fact 4 alone is not enough, which I only found by grepping for what else the fix governs.** Task
3.1 gives `Dockerfile.ci` its own `claude-yolo-ci-version`, gated independently — deliberately, so
CI churn does not rebuild every desktop image. But OCI labels are **inherited**, so `claude-yolo:ci`
also carries `claude-yolo-version` from its parent, unchanged. A reader that inspects the right
image and reads the customary key still gets the parent's value. The defect moves one level in
rather than being fixed. Two options tabled in §4.2; A (one canonical key each base overrides)
recommended on the project's own fewest-code-paths rule, and explicitly left as a **recommendation**
because it edits Task 3.1's scheme.

**Honesty about what is now load-bearing and unmeasured**: the whole of §4.2 rests on label
inheritance, which this plan has not measured. Recorded as **F4**. I also wrote down, as a stated
policy rather than a per-case judgement, why F4 and G1 are *not* being wired into `triage.bash` now:
both are one cheap leg each, but the script has never been run once, and a script that grows a leg
whenever someone thinks of a question never gets run at all. They go in together when the entrypoint
work is picked up; the outstanding run goes first.

Seventeen propagation instances now, of two distinct kinds. The one that matters most today is the
new kind, because the existing countermeasure — *after writing a correction, grep for the task that
owes it* — would not have caught it. The task that owed it did the work. A countermeasure for this
species has to ask whether the remedy covers the residual, not whether the residual was assigned.

______________________________________________________________________

## 11:05 — D31: I proposed degrading the thing this session is running in

I recommended baking the MCP binary into `claude-yolo:latest` to collapse the `:ci` base fork. The
owner's reply was that ccy is what is running this session, that desktop ccy is fundamental and
highly tuned, and that it **must not be degraded in any way — no context bloat, no MCP, nothing**.

The recommendation is withdrawn, and the reason I got it wrong is worth naming precisely rather than
apologising for: **I priced MCP as bytes on disk.** The cost of an MCP server is not image size, it
is tool surface registered into *every interactive session's context*, permanently, used or not.
That is the "context bloat" in the constraint, and my comparison table listed "image size" as the
only cost of the option I was recommending. Wrong unit, so the trade came out backwards.

**The far more important finding is that the constraint disqualifies the design already in the
plan**, not just my suggestion. Task 3.2 says a project opts into CI by writing
`FROM claude-yolo:ci` in `.claude/ccy/Dockerfile`. That is the project's **only** Dockerfile —
`claude-yolo:1452`, consumed at `:1457`, producing the image `:1633` selects for the desktop
session. So the specified opt-in hands the GitHub MCP to every desktop developer on that project,
forever. Seven rounds reasoned about which base CI builds from and never asked what the base flip
does to the desktop session on the other side of the same file. Same defect as always: a true
statement about the CI path taken as a statement about the project.

I had also answered the owner's sync question with *"no — one project Dockerfile, one changed `FROM`
line"*. True, and it missed the point so completely that it read as reassurance. The whole objection
lives in the words "one project Dockerfile".

**The corrected shape puts the CI payload ABOVE the project image**, as a per-project leaf
`claude-yolo-ci:<project>` that desktop never builds. Written up in
`reports/ci-layering-corrected.md`. What makes it more than a rearrangement:

- Desktop images are **byte-identical to today**. Not "minimally affected" — untouched.
- CI gets the **same tooling stack** because the leaf *is* the project image plus a layer. The
  owner offered tooling duplication as acceptable; the offer is declined, because deriving cannot
  drift and duplicating always does.
- The project-Dockerfile-may-be-absent case needs **no new rule** — `FROM` is whatever
  `:1457`/`:1633` already resolved.
- **D30 evaporates.** Its fix (fact 4) repairs staleness for project images built `FROM
  claude-yolo:ci` — an arrangement that must now never exist. The hard-coded literal at `:1477`
  becomes correct. I have kept fact 4 in the spec, marked superseded-pending-decision, rather than
  deleting a fix for a defect that is only conditionally moot.
- **G1 evaporates.** A leaf can set its own `ENTRYPOINT` including `tini`, because nothing pulls a
  leaf expecting desktop semantics. Task 3.1's objection to overriding `ENTRYPOINT` is sound for a
  shared base and vacuous for a leaf. Callers stop hand-rolling `--entrypoint` because there is
  nothing left to hand-roll — Decision 6's actual goal, reached instead of asked for.
- **A cross-contamination path gets promoted from tidiness to constraint.** D29 dropped the
  `/root/.claude` → `/workspace/.claude/ccy` symlink to keep CI state out of the checkout. Under
  desktop purity it is load-bearing: MCP registration is written to `/root/.claude/`, so with the
  symlink a CI job writes MCP config into the project tree, where the next desktop session reads it.
  The symlink is a direct MCP-into-desktop path.

**What I am not doing is pretending this is free.** It is an overlay on the project image, and Task
3.3 explicitly rejected a ccy-owned overlay. I put that at the top of the open questions rather than
in a footnote: the rejection was of a *security* overlay and rests on Decision 4, while this is a
*payload-separation* overlay — same mechanism, different job. Task 3.3's own caveat ("reverse
Decision 4 and the overlay returns immediately") and the owner's new "more locked down" steer reopen
the security half too, and `--dangerously-skip-permissions` sits awkwardly beside "more restricted".
None of that is mine to settle.

Also deliberately **not** specified: a project-level `.claude/ccy/Dockerfile.ci` extension point. The
owner said it would be acceptable; the layered design does not need it, and YAGNI says do not build
an extension mechanism before a project wants CI-only extra tooling.

The lesson I want to keep is not "ask before recommending". It is that I evaluated a trade-off using
the cheapest measurable quantity (megabytes) instead of the one that mattered (context, per session,
forever), and the plan's own recurring defect was sitting in the specified design the whole time
because nobody followed the project Dockerfile back to the desktop session it also builds.

______________________________________________________________________

## 12:20 — D32: asked to confirm two things, could confirm neither where it counts

The owner asked me to **confirm** that a project ccy Dockerfile change triggers a rebuild before the
CI workload, and that a subsequent workload launches quickly from the saved image. Two confirmations,
phrased as though the answer were yes.

This is the exact shape this plan has spent seven rounds cataloguing — *a true statement about a
check presented as a stronger statement about the world* — so I checked at source before answering
either way.

**Desktop: both true, cited.** `claude-yolo:1471` hashes `.claude/ccy/Dockerfile`; `:1496`/`:1498`
compare and rebuild with reason "Dockerfile changed"; `:1528` prints "Using cached project image" and
skips the build when nothing moved. Not in dispute, and it is what the owner uses daily.

**CI: neither, and for five stacked reasons** — the launcher is not on the CI path at all (Task
3.4's own DONE block says so), the comparison state is host-user-local at `:1454` and cannot travel
with an image or be read from a checkout, nothing is implemented, runner image-store persistence is
an `lts-infra` property I cannot verify, and — the sharp one — **F1 is precisely this hazard**. An
absent label read as `""` on both sides compares equal and reports FRESH: that delivers the *fast
launch* and *skips the rebuild*. The two questions the owner asked trade against each other under
one unmeasured defect, which is why answering "yes, both" would have been worst of all.

**The gap the question actually exposed.** Task 3.4 specifies the image as *"built by Ansible, never
per-job"*, on the sound grounds that the build needs egress a locked-down job must not have. That is
coherent for a **shared base** with no per-project content. It cannot hold for
`.claude/ccy/Dockerfile`, which is a **per-commit file in the repository** — Ansible provisioning
does not re-run per commit. So either CI rebuilds per job, contradicting 3.4 and widening egress at
job time, or a PR that changes the project Dockerfile runs against the previous tooling silently.

**I checked whether D31 caused this before writing it up, and it did not.** Under the original
layering the per-project image also had to be built somewhere in CI, and no task covered that either;
3.4 only ever covered the shared base. D31 makes the gap visible and gives it a second layer. Saying
so matters — the correction should not collect credit for finding a problem it partly enlarged, and
that framing was available and would have flattered the previous entry.

Three options tabled, B (two-phase image-prep with the wider egress, then a locked-down run)
recommended because it is the only one that keeps **both** properties the owner asked about *and* the
lockdown, reusing the estate's existing arm → build → drop shape. Not decided — it is a real design
choice and it interacts with Task 3.4, which is ✅.

Worth noting what made this findable: the question was about developer experience, not architecture.
"Does my Dockerfile change take effect, and is it fast afterwards" is the property a user actually
feels, and the plan had no answer for it under CI despite seven rounds of hostile design review. Two
of the last three findings came from the owner asking what something is *for* or what it *feels like*
to use, not from another pass over the design.

______________________________________________________________________

## 13:15 — D33: the project drives the image, and half the plan was answering an invented question

Four sentences from the owner, and they take out more of this plan than seven review rounds did:

> *"why are we using a label — this feels wrong … wait — built by ansible? so how would a project add
> a new tool for CI purposes? … no — the project drives the image. ansible provides the VM and we
> provide a safe mechanism for project podman containers to run CI workloads."*

**The label was wrong for a reason sharper than taste: it re-implements the container engine.**
`podman build` *is* the staleness check — it rebuilds exactly the layers whose inputs changed and is
a cache hit otherwise. The two properties the owner asked me to confirm this morning are **native
properties of `build`**. I spent that turn explaining why they could not be confirmed, tabling three
options for how to construct them, and never asked why we were constructing them at all.

It only looked necessary because Task 3.4 assumed the image was built **out of band by Ansible**.
Once the image lives somewhere the checkout is not, something else has to answer *"is this
current?"* — and that invented question is what the entire convention answers: keys, algorithm,
writer, reader, comparison, a migration across two consumer repos, four proof obligations. Remove
the assumption and none of it exists.

There is a nastier layer. The mechanism I modelled it on — ccy's `$HOME/.cache/…-dockerfile-hash`
— exists to avoid *invoking* a build, and **that optimisation is the source of the very
"state outside the image" defect the plan correctly diagnosed**. So the plan identified a workaround's
defect and then proposed porting the workaround into the one context that needed neither.

**Task 3.4 was wrong at the ownership level, and D32 asked the wrong question about it.** I framed
"built by Ansible, never per-job" as a *scheduling* problem — when does the rebuild fire — and tabled
three options. The owner's question settles the axis in one line: *how would a project add a new tool
for CI?* Ansible-built, it cannot; it files a change against this repo and waits for a re-provision.
Project-driven, it edits its own Dockerfile. That is a flat contradiction of the founding steer this
plan exists to serve — *"each project has its OWN ccy runner in the NORMAL WAY"* — and seven rounds
missed it because **every round audited the design against itself, never against the steer.** That is
the most useful thing I have learned from this plan, and it is not a bug in any one document.

**What dissolves.** Deliverable 1 retired. D30 moot — no ccy-owned base for a project to build
`FROM`, so the hard-coded literal at `:1477` is simply correct. **D31 withdrawn**, and with it the
overlay: a project that wants MCP puts MCP in its own CI Dockerfile, so the desktop-pollution problem
**disappears rather than being solved by layering**. Which means **Task 3.3's "no overlay" decision
stands unreversed** — it was never wrong; the pressure to reverse it came entirely from Phase 3's
mistaken premise. Yesterday I wrote that reversing a ✅ decision was the thing seven rounds are
supposed to catch. It was, and the catch was that the premise was wrong, not the decision.

**What survives, and it is the valuable half.** The safe run mechanism: MCP interface (4.1), `--egress`
and its three-probe proof (5.1–5.4), concurrency-safe naming (C7), token-from-env (C5), and Task
7.4's evidence-based decision that CI must **not** write `.claude/ccy/` into the checkout. None of
that is touched by this correction. The plan's real value accumulated in Phases 4/5 and 7.4 while
Phase 3 was building an answer to a question nobody had.

**One new open question**, stated rather than assumed: if the project builds the image, where does the
CI entrypoint come from? Recommended that the safe-run mechanism bind-mount it read-only — desktop
stays byte-identical rather than argued-about, and the mechanism already owns the `podman run` argv,
so G1's `tini` problem is solved by construction instead of by asking callers to be careful.

I have marked D33 **proposed** and restated the owner's model at the top of the document for
correction. It is a re-architecture derived from four sentences, and the failure mode of the last two
days has been confident elaboration on a premise nobody checked.

______________________________________________________________________

## 14:30 — Phase 3 redone against what exists, not against the specification

Took the "is there existing code that already handles this?" prompt literally, because D33 was
exactly that question going unasked. `claude-yolo` builds a hardened `podman run` on every desktop
launch — the reuse instinct says extract it.

Read it first (`claude-yolo:2770-2792`), and extraction is the wrong call for specific reasons rather
than taste: `--device /dev/dri:/dev/dri` is **unconditional** (E6, already owner-confirmed fatal on a
headless host — so that one flag makes the launcher unrunnable on a CI VM); `GUI_MOUNTS`/`SSH_MOUNTS`
are desktop session concerns; ~16 credential prompt sites precede every run path; and the command is
**hard-coded** to `claude --dangerously-skip-permissions`, while the owner's CI must run deterministic
QA tools as well. Refactoring it would edit the launcher every desktop session uses — forbidden.

So: written fresh, which is *also the simpler answer*. Strip the desktop-only concerns and the CI run
line is about eight flags. Duplicating eight flags beats extracting them from a script whose other
133 KB is precisely what must not run.

**Two findings from reading it.** The project image is `FROM claude-yolo:latest` and therefore
**inherits the desktop `ENTRYPOINT`** — so a CI job that does not override it runs `entrypoint.sh`,
including `ccy.env` **sourced from the branch under test**. Overriding is mandatory, not stylistic.
And overriding is exactly what drops `tini`, so **G1 is live again**. I had marked G1 retired
conditional on D31, and D33 withdrew D31 — leaving a stale RETIRED banner on a live obligation would
have been the propagation defect committed in the very document that catalogues it. Recorded as a
flip-flop (live → conditionally retired → live) rather than edited flat.

______________________________________________________________________

## 14:55 — PLAN.md crystallised: 2061 lines to 262

Owner: *"ensure plan.md is fully updated / do not bloat plan with journal type stuff / crystalise
plan on the concrete full plan truth with no bloat only clear facts."*

Measured before and after: **2061 lines / 150 KB → 262 lines / 16 KB.** Correction blockquotes 953 →
4. What went: the C1–C11 round-1 block and the D1–D33 correction narration, which had accumulated
under Task 6.3's self-imposed convention that *"corrections append; never rewrite a section a
reviewer has already reviewed"* — the exact convention Plan 00069 exists to retire, and which the
owner already ruled against (*"plan is a living git tracked document"*).

What stayed is plan truth: the ownership model, the two deliverables, constraints with their source,
the six technical decisions **as they currently stand**, every task with its real status, open
decisions, outstanding proof obligations, dependencies, success criteria, risks.

Three judgement calls worth recording:

- **Superseded Phase 3 tasks are marked ❌ cancelled, not deleted.** `reports/fable-review-*.md` cite
  them by number; deleting the rows would break citations that a reader can still follow.
- **The audit narration is not lost, it is relocated** — seven review rounds are already whole files
  in `reports/`, and the correction reasoning is in this journal and in git. Nothing was only in
  PLAN.md except the framing, which was the bloat.
- **The 🚫 success criterion is kept and labelled mis-specified-at-birth** rather than quietly
  dropped. It reads as a deliverable but restates a Non-Goal; retiring it silently would hide that a
  criterion was wrong rather than unmet.

The plan now reads as what it is: a design plan with two specified deliverables, one host run
outstanding, and three decisions waiting on the owner.

______________________________________________________________________

## 15:40 — Required CI config, and checking carefully caught my own error

Owner: establish the required configs for CI, fail fast and loud when they are wrong, and give the
GitHub runner enough context that problems are quickly diagnosed. Explicitly: *check carefully*.

Checking carefully immediately cost me one of my own claims. **`safe-run-mechanism.md` named
`ANTHROPIC_API_KEY` in its argv sketch. That variable does not appear anywhere in ccy** — the
credential is `CLAUDE_CODE_OAUTH_TOKEN` (`claude-yolo:2777`, `:2756`). I had written a plausible
variable name from general knowledge into a document whose entire premise is that every fact is read
from source. Corrected in place, with the correction stated rather than silently patched.

The same check found something better than a fix. The token is passed **by name**
(`-e CLAUDE_CODE_OAUTH_TOKEN`, no `=value`), and `lib/token-management.bash:107` records this as
deliberate: the value is read from the ambient environment so it never enters argv or the process
table. My sketch had also described it as passing "by value", which is exactly backwards. In CI that
property matters *more* than on the desktop, because `ps` output ends up in logs.

**Fifteen preconditions across three layers.** The organising insight is not the list — it is that
**each assertion is tagged with the layer that owns it**, because a CI failure is only quick to fix
if the reader knows whose problem it is. `[VM]` means re-run the play or file an infra issue; `[JOB]`
means edit the workflow YAML or repo secrets; `[PROJECT]` means edit the project Dockerfile. That tag
is the difference between "CI is broken" and "add `CLAUDE_CODE_OAUTH_TOKEN` to repo secrets".

**Two design points I had to reason about rather than assert.**

*Collect-all-then-abort vs exit-on-first.* A naive reading of fail-fast says exit on the first
failure. The project's rule is that a failure must never be **skipped and continued past** — and
nothing continues past anything here, because the preflight is a validation phase, not an operation.
Exiting on the first failure turns a three-mistake workflow into three CI round-trips of several
minutes each, each revealing exactly one more problem. Collecting is fail-*complete on diagnosis* and
still fail-fast on execution. Stated in the document with the reasoning, because it looks like a
violation until you read why.

*How to handle "claude tasks need a token, QA tasks do not" without an armed flag.* The banned
pattern is deriving a boolean from whether the credential exists — that converts a misconfiguration
into a silent behaviour change. The answer per the rule is that the **operator declares intent**:
explicit `--claude` / `--exec` modes, each asserting its own preconditions. A `--claude` job with no
token is an error, loudly. That is `_enabled`, not `_armed`. Same reasoning gave J6: a triage task
that needs `GH_TOKEN` **declares** it, rather than the mechanism guessing which workloads touch
GitHub and letting the rest fail deep inside `gh`.

**One distinction worth recording, because it looks like a contradiction.** P1 asserts the project
image carries `claude-yolo-version`. Decision 2 retired the LABEL convention. These do not conflict:
Decision 2 retired labels for **staleness**, because `podman build` answers that. P1 asks about
**provenance** — is this image ccy-derived at all — and label *inheritance* is precisely what makes
it work. The same inheritance property that made labels useless for staleness makes them correct for
descent.

Also specified: GitHub-specific `::error title=…::` annotations so failures surface at the top of the
run rather than inside a folded log — emitted **in addition to** the plain-text block and gated on
`GITHUB_ACTIONS=true` being actually set, never assumed, so a wrong detection loses no diagnostic.
And a hard rule that no credential value, prefix, suffix **or length** is ever printed: GitHub masks
registered secrets, but masking is not a property to design against.

______________________________________________________________________

## 16:25 — The credential update process does not exist, and the premise for it was wrong three ways

The owner said the OAuth token value is *"directly in this env, you are using it right now"*, and
assumed updating it means editing a vaulted string and running ansible. Checked all of it.

**The token is not in this container's environment.** `CLAUDE_CODE_OAUTH_TOKEN` is unset/empty. The
only `CLAUDE_*` variables present are Claude Code runtime ones (`CLAUDECODE`, `CLAUDE_CODE_SESSION_ID`,
`CLAUDE_CODE_ENTRYPOINT`). Nothing stripped it — `entrypoint.sh:31` unsets `GH_TOKEN` only. So this
session authenticates by some other path. I reported presence, never a value, and never will.

**There is no vaulted Claude credential.** And I nearly filed that as a finding off a grep that
returned nothing, which is the exact trap this plan is named for — a true statement about a check
presented as a statement about the world. So I checked the file first: it exists, 139 lines, 8
`ANSIBLE_VAULT` values, 18 variables — ftp, lastfm, nordvpn, qobuz, rclone, github-ssh, mok. None for
Claude. Now it is a checked absence.

**`play-claude-yolo.yml` deploys no token at all** — `:311-327` create only the *directories*
`~/.claude-tokens/ccy/{tokens,projects}`. The token is a **file** produced by an interactive
`ccy --create-token` OAuth device flow (`token-management.bash:199`). And `gh secret` appears nowhere
in `playbooks/`, `scripts/` or `docs/`, so nothing distributes any credential to GitHub Actions.

So the assumed process does not exist — not for CI, and not for the desktop either. Worth being blunt
about that rather than describing how it *would* work if it did.

**One constraint decides the shape, and it is a security one, not a convenience one.** The real vault
passphrase must never be on the runner. So the runner cannot obtain the credential by decrypting
vault, which kills "runner runs ansible" outright. Ansible's role is to push the secret **to GitHub**
from a host that legitimately holds the passphrase. Vault stays the source of truth; the GitHub secret
is derived, one direction only, so there is no two-store sync problem.

**The finding I did not expect: expiry.** Token filenames encode an expiry date, and there is a
`feature/ccy-token-expiry-colours` branch, so this is already a live desktop concern. In CI it is
worse — a GitHub secret is an opaque string with **no expiry metadata**, so an expired CI token fails
at job time with an auth error raised deep inside `claude`, naming nothing. That is exactly the
diagnosis failure the preflight exists to prevent, and **the preflight cannot catch it**: the runner
never sees the filename the expiry lived in. Two places can — the push play (filename available, so
the check is free) and a scheduled host-side warning. Recording expiry as a separate non-sensitive
GitHub *variable* would let J5 assert it at job time; proposed, not decided, because it adds a value
that can drift from the secret it describes.

Also specified, because it is the kind of thing that gets decided by accident: the CI secret must be a
**dedicated machine token**, never the operator's daily-driver. A GitHub secret is readable by every
workflow run in its scope, so the blast radius has to be something revocable without disrupting a
human's work.

______________________________________________________________________

## 17:30 — Four instances of one mistake, and the architecture was always simple

The owner, escalating: *"github has no need for the token either / ccy has token persistence at host
level ALREADY SOLVED / DO NOT, FOR FUCKS SAKE, GO OFF WHEEL REINVENTING AGAIN / use fedora-desktop
ccy / it is FULLY FEATURED / ci invokes ccy with args."*

Then the whole architecture in four lines: **fully non-interactive is needed; Ansible provides the
config, keys and tokens; the project provides the ccy image Dockerfile overrides; the CI runner fires
ccy with specific args.**

That is the plan. It was the plan when this started.

**Four things I specified that already existed:**

| # | Specified                                   | Already there                                       |
| - | ------------------------------------------- | ----------------------------------------------------- |
| 1 | A `LABEL` convention for image staleness    | `podman build`                                        |
| 2 | Ansible-built CI images                     | the project Dockerfile seam                           |
| 3 | Vault + `gh secret set` + expiry metadata   | ccy's host-level token store, expiry in the filename  |
| 4 | A purpose-built safe run mechanism          | `ccy`, invoked with args                              |

Each was internally coherent, cited to source, and unnecessary. The fourth is the worst, because I
wrote it **today, after the owner had already corrected the same mistake twice**, and I wrote an
explicit section justifying it. That section argued against reusing `claude-yolo:2770-2792` on four
grounds — unconditional `--device /dev/dri`, ~16 credential prompts, hard-coded `claude`, desktop-only
mounts. Every one of those is a **defect in ccy to fix**. I listed the reasons to repair the launcher
and concluded I should build a second one. Two of them (E6, Task 7.3) were already tasks in this very
plan.

**The consequence that matters most: Decision 3 was wrong.** I had reclassified `--non-interactive`
as "desktop-only hardening, not a CI enabler" — which followed from the launcher not being on the CI
path. CI invokes `ccy`, so the launcher **is** the CI path, and its prompt sites are the whole
blocker. Phase 2 and Task 7.3 are the core work, un-deferred. Everything I built in Phase 3 was an
elaborate detour around the thing that needed fixing.

**Also moot as a result**: G1 (`--entrypoint` dropping `tini`) — `ccy` never overrides the entrypoint,
which is exactly why `tini` stays PID 1. I flipped that obligation live → retired → live → moot
across one day, because each flip followed from an architecture I had invented rather than checked.

**The working rule, now written into the plan** so it is a standard rather than a lesson I keep
re-learning: *before specifying a mechanism, name the existing thing it replaces and state why that
thing cannot do the job.* In all four cases that sentence could not have been written truthfully. The
existing thing either already did the job, or had a fixable defect I was routing around.

I also owe a separate correction: I claimed the owner's assumed process was *"runner runs ansible,
ansible decrypts the vaulted string"* and refuted it on vault-passphrase grounds. **They never
suggested that** — they said they would update the vault and run ansible, the ordinary host workflow.
I invented a position, attributed it, then corrected it. That is the plan's signature defect pointed
at a person rather than a document.

PLAN.md rewritten to the four-line architecture. Phase 3 is cancelled in full except Task 3.3, whose
"no overlay" answer was right all along.

______________________________________________________________________

## 10:15 — Task 2.4: the fail-fast contract, and two more reinventions

Re-scoped `reports/ci-required-config.md` from "a standalone CI preflight" to "assertions inside
`ccy`". Applying the working rule to its 15 preconditions one at a time left **four**.

Seven were already done by ccy and I had specified replacements for them:

- V1 engine present → `common.bash:36-40` already names the engine, names the fix, exits 1.
- V2 base image present → `claude-yolo:1424-1433` builds it; a missing Dockerfile already exits 1
  naming the playbook.
- **V4 base version matches → `validate_container_version` (`common.bash:456`), called at
  `claude-yolo:1436`.** This is the fifth instance. I had written an assertion that prints expected
  vs found and aborts; ccy has compared exactly those two values since before this plan existed,
  and does something better than aborting — it rebuilds. I had even cited `Dockerfile:36`'s
  "⚠️ MUST MATCH REQUIRED_CONTAINER_VERSION" comment as evidence that *"an invariant a comment asks
  a human to maintain is one a machine should assert"*. The machine was seventy lines away.
- **J2 project image exists → `claude-yolo:1457-1528`.** Sixth instance. Same shape.
- J1 → `claude-yolo:56` `check_git_repo || exit 1`. J7 → `claude-yolo:728-740`. J6 → dropped as
  YAGNI; `entrypoint.sh:14-17` is already GH_TOKEN-or-die and nothing else would call it.

Four were moot once the retractions landed: V5 and J4 (no CI entrypoint, no second mode), P1 and P2
(ccy builds the project image `FROM` the base itself, and never overrides `--entrypoint`).

### The message format was already in the tree, twice

I was about to specify a four-field diagnostic block. `claude-yolo:728-740` already is one —
`print_error` headline, blank line, what to do, why, exit, all on stderr. And `common.bash:503-516`
already prints the expected/found shape for a wrong value. Task 2.4 adds no format; it makes those
two mandatory.

**The `[VM]`/`[JOB]`/`[PROJECT]` tag is dropped.** Nothing consumes it, and `claude-yolo:1428`
already demonstrates that a concrete remediation ("run playbooks/imports/play-claude-yolo.yml")
tells the reader whose problem it is better than a three-letter tag does. The taxonomy survives as
an authoring rule, not a printed field.

### Measured, because this plan has been wrong about bash before

Three facts the contract rests on, executed rather than reasoned:

1. `exit` inside a function invoked as `f || { … }` terminates the **whole shell** — the `||`
   handler does not run. Script exited 78, handler silent. That is the exact `select_token` shape at
   `claude-yolo:1004`/`:1117`.
2. In `v=$(f)` the same `exit` is **swallowed** — outer script continues, `v` empty, exit 0.
3. No prompt-bearing function is ever invoked in command substitution. Checked across all 12 owners;
   nothing found. A checked absence, not an assumption.

(1) collapses the design: a guard that exits cannot spin, so the 5 spin paths and the 32
undiagnosable aborts are **one** problem with **one** primitive, not two problems needing an
entry-point guard plus a per-read fix. (3) is a fact about today's tree, so Task 2.3's gate has to
assert it stays true.

### The finding I did not expect

Eight of the twenty toplevel `claude-yolo` prompts are `while true` → `read` → `${var:-DEFAULT}` on
the very next line. **The safe default is already written in the source.** They abort today only
because `set -e` kills the script *at the read*, one line before the default would have been
applied. The contract does not invent defaults for these — it declares the ones already there so
they can be announced.

But four of the eight are **wrong for CI**, and that is worth more than the eight:

- `:822` defaults to reusing the previous launch's token/ssh/network — a job would inherit `$HOME`
  state from whatever ran before, so the run stops being determined by its arguments.
- `:2091` defaults to *starting* compose while `:2428`, the other compose prompt, defaults to not
  starting. Two prompts for the same concern with opposite defaults.
- `:2266`/`:2301` auto-join a discovered container network — the opposite of a restricted egress
  posture.

The lesson generalises: **an interactive default is evidence of intent, not automatically the
unattended answer.** It was chosen for desktop convenience. Reading the source default as "the safe
value" would have imported four desktop conveniences into the mode the owner said must be *more*
restricted — and it would have looked well-grounded, because each one is cited to a real line.

### Also

Phase 2's outcome (i) is superseded. It said credential resolution would be *removed* from the
unattended path by token-by-value. That followed from CI not being a fedora-desktop VM. It is one,
ccy's store is already on it, so the branch is **guarded, not removed**.

And a cost I am not hiding: the retracted preflight collected every failure and printed them
together. A contract distributed through ccy cannot, in general — each assertion sits where its
value is first needed. The three survivors (V6/J3/J5) depend on nothing prior and can be asserted
together early, which recovers most of it. The rest is a second CI round-trip when an early failure
hides a later one. That is the price of not building a parallel runner, and it is smaller than the
price of building one.

______________________________________________________________________

## 10:40 — Task 7.3 closed; its own split was wrong

Task 2.4 discharged Task 7.3's specification half, so 7.3 is closed — but its framing was wrong and
that is the part worth keeping. It listed "five spin paths" and "32 abort sites" as separate fix
items, which follows from treating a spin and an abort as different defects needing different
remedies.

They are one defect. Measured: `exit` inside a function invoked as `f || { … }` terminates the whole
shell — the `||` handler never runs. So a guard that exits cannot spin, whatever the caller's errexit
state. One primitive covers all 46 sites. The split existed because the analysis reached the spin
question first (it is the more dramatic failure) and never asked whether the fix for the aborts also
fixed the spins.

Every task in the plan is now ✅ except Task 1.1, which is blocked on the owner's host run of
`triage.bash`. The plan stays In Progress on that one item.

### Out-of-scope, found while running the gate

`./scripts/qa-all.bash` exits 1 on a clean tree at HEAD. Neither cause is this plan's, and neither is
touched here; both are recorded so they are not rediscovered:

1. **ruff is unpinned.** `.github/workflows/qa.yml:45` does `pip install ruff`;
   `playbooks/imports/play-python.yml:20` installs `- ruff`. 46 findings across `helpers/`, `tests/`,
   `scripts/`, `clip-scan`. `ruff.toml:5` claims "Use default rule set (E, F, W)" — ruff 0.16.0's
   default is ~415 rules including UP/BLE/SIM/DTZ. Confirmed with `--isolated` that this is ruff's
   own default and not a config leaking in from the outer checkout, which was my first guess. The
   sibling lts-infra repo pins `ruff==0.16.0` and writes down precisely this reasoning; this repo has
   the same gate and no pin, so the gate reddens whenever upstream widens its defaults.
2. **The ansible fail-fast check has a false positive.** `scripts/qa-ansible.bash:70-76` greps raw
   lines case-insensitively with no YAML comment stripping, so
   `play-systemd-user-tweaks.yml:242` — a comment documenting that `ignore_errors: true` was
   **removed** — is flagged as a violation. Minimal fix: strip from the first `#` at line-start or
   preceded by whitespace before applying `FF_PATTERN`; that keeps flow-style `{ignore_errors: true}`
   detectable, which anchoring the pattern to key position would lose.

## 22:05 — Owner ran triage.bash on the HOST; two probes were measuring the wrong thing

Run `triage-runs/20260731-214921/`. Root resolution worked: the log records *"host check: no
container marker found, running on the host"* and a repo root of
`/home/<user>/Projects/LTS/lts-infra/untracked/repos/fedora-desktop` — resolved **from the
script**, not the cwd, which is the whole point of the R1 rewrite.

> Path scrubbed. The pre-commit scanner rejected the first version of this entry, correctly:
> the run report lives in `triage-runs/`, which is **gitignored precisely so an unscrubbed
> report is never committed** — and I copied the real path out of it into a tracked file,
> walking around the guard rather than through it. Quoting a gitignored artefact into a
> tracked one re-exposes exactly what the gitignore was protecting.

## 22:30 — "CI must never rebuild" WITHDRAWN. The design is ccy; stop inventing around it.

Owner challenged the claim and it does not survive at all. Withdrawn, not softened — I
softened it first, and that was still wrong.

**The claim, mine, at `probe-engine.bash:212`:** *"a mismatch forces a rebuild on the next
launch, which a CI job must never do."*

**Why it is wrong.** The image is built from the ccy Dockerfile. If the Dockerfile changed,
the image is stale, and rebuilding is ccy **working correctly**. The owner's standing agenda
is that each project gets its own ccy runner *in the normal way* — Dockerfile customisation,
custom tooling. The rebuild is part of the normal way. I converted correct behaviour into a
hazard and started designing around it.

This is the reinvention defect in its purest form yet. The working rule is: *before
specifying a mechanism, name the existing thing it replaces and state why that thing cannot
do the job — and if that sentence cannot be written truthfully, the mechanism is not needed.*
Here the existing thing does the job. There was no sentence to write.

**The mechanism I cited was wrong too, independently.** I claimed a rebuild collides with the
egress lockdown. It does not: `build_container_with_hash` runs `container_cmd build`
(`common.bash:556`) — a **host-side** `podman build`. The `--network pasta:-T,3128`
restriction is a `podman run` flag on the agent container. Restricting the running
container's egress does not constrain the build. The two never meet, so there was no conflict
to resolve.

**The reproducibility argument was answered by the context I had already been given:** this
is **private runner infra for private, self-owned repos**. It is not multi-tenant CI running
untrusted PRs. "The image tracks upstream the way my desktop does" is the behaviour the
operator already has and wants, not a defect. I imported a threat model from public CI that
nobody asked for.

**What is actually true and worth keeping:** a version/hash mismatch makes the first run
after a Dockerfile change spend the build before starting work. That is a **scheduling**
observation, not a design constraint. `2.22 == 2.22` on this host simply means no rebuild is
pending right now.

Probe text rewritten to say that plainly; `shellcheck -x` and `bash -n` clean. Both this
entry and the probe change were still uncommitted when the correction landed, so the wrong
version never entered the record.

**Steering note, because this keeps happening:** the recurring failure in this plan is
building a parallel thing beside ccy instead of using ccy. Recorded as a standing principle
at the top of `PLAN.md` so it is checked against, not rediscovered.

### The plan already contained the right answer, and I contradicted it

Sweeping for other instances found `reports/phase3-image-layering.md:229-238`, written
earlier in this plan:

> *"`CCY_AUTO_UPDATE=0` is not required and should not be relied on: a job that reached the
> point of needing that variable would already be running the launcher, which is the thing
> that is wrong. … 'we set `CCY_AUTO_UPDATE=0` in CI' is the plausible-looking answer that
> would hide the fact that the launcher was running at all."*

Last turn I proposed setting exactly that variable. The report had **named my error in
advance**, as the specific wrong answer to watch for. Re-reading the plan's own reports before
adding to them would have caught it; I reasoned from the source instead and reinvented a
worse answer.

### But that resolution now conflicts with the owner's steer — flagged, not resolved

`phase3-image-layering.md` disposes of the rebuild/update problem with a **by-time split**:
the launcher runs at *provision* time (Ansible, egress armed, auto-update wanted), and a job
runs an *already-built* image, so neither the rebuild nor the update ever fires at job time.

That is a coherent design, but it is **not** "fire `ccy` and let it behave normally", and it
sits against `PLAN.md`'s own architecture row — *"Invoking the workload | the CI runner,
firing `ccy` with specific args"*. If the launcher does not run at job time, the runner is not
really firing `ccy`; it is running a container ccy previously built.

The owner's steer today is explicit — *"if that is out of date then ccy should rebuild as it
does normally … what we want IS the ccy system"* — which points at the architecture row, not
at the by-time split. Under that reading the split is itself an instance of the very defect
this entry is about: a mechanism invented beside ccy.

**Not resolved unilaterally.** Reversing a core design decision in an existing phase report is
the owner's call, so it is raised rather than rewritten. The two candidate readings are
recorded above; whichever wins, one of `PLAN.md`'s architecture row and
`phase3-image-layering.md`'s split has to change, because they cannot both be true.

## 22:55 — the conflict I just flagged largely dissolves; the split IS the ccy system

Read `round2-restatement.md` §4 before letting that flag stand. It answers the question, and
against me.

**The by-time split is not invented — it is grounded in a live, measured refusal.** §4 quotes
the sibling repo (`lts-infra`, `tasks/runner-ccy-project-image.yml:19-23`), which hit it:

> *"The image needs Debian archives, go.dev, the Go module proxy and pypi — deliberately absent
> from the runtime allowlist, so a job that tried to build would be refused by squid,
> correctly."*

So there **is** a squid allowlist on the runner and a job-time build **was** refused in
practice. My "the build is host-side, so container egress does not apply" correction is wrong
for this estate: the runner VM's own egress goes through squid, so a host-side `podman build`
at job time is constrained too. I fixed one wrong claim with another.

**And the split is not a parallel system — it is ccy's normal way, timed.** §4 in its own
words: provision time builds *"the project's ccy image from its own `.claude/ccy/Dockerfile`.
This is exactly the owner's 'normal way', executed by the machine that will run the jobs."*
Same Dockerfile, same ccy staleness logic, same rebuild — run while the egress window is open
rather than at job time when squid refuses. The owner's steer and this design agree.

**So the conflict I raised was weaker than I stated.** "The CI runner fires `ccy` with specific
args" is about invoking the *workload*; the image build is a separate, earlier concern. Both
rows can stand. I should have read §4 before flagging.

### What IS real, from §4.1

1. **The reinvention to kill is in `lts-infra`, not here.** `tasks/runner-ccy-project-image.yml`
   reimplements ccy's project-image staleness gate (`claude-yolo:1457-1529`) in Ansible —
   sha256 instead of md5, an image label instead of the cache file. Its own header says so, and
   lts-infra Plan 00026 Task 3.3 already slates it for deletion.
2. **ccy's genuine gap, independent of CI:** build identity lives in
   `$HOME/.cache/claude-yolo-${PROJECT_NAME}-dockerfile-hash` (`claude-yolo:1454`) — state
   **outside the image**. Staleness recorded in one user's `$HOME` cannot answer *"was this
   image built from this Dockerfile?"* for anyone else, which is exactly what CI asks. An OCI
   `LABEL` carrying the Dockerfile digest is the fix, and is what group F's probe was
   measuring.
3. **`phase3-image-layering.md`'s "R9/R10" citations are dangling.** `round2-restatement.md`
   contains **no R9 and no R10** — its structure is §0–§7 plus Decisions 4/5/6. The substance
   is §4 and §4.1, unnumbered. Cheap to fix, and worth fixing: a citation that resolves to
   nothing is how a design premise stops being checkable.

Three legs clean. One leg reported `[INCOMPLETE]`. **The INCOMPLETE was the probe's fault, not
the host's.**

### The defect

`probe-network.bash` and `probe-engine.bash` both ran

```
podman run --rm <flags> claude-yolo:… true
```

intending `true` as a trivial no-op payload. But `true` is a **command**, and the ccy image
sets an **entrypoint**:

```
Dockerfile:215  ENTRYPOINT ["/usr/bin/tini", "--", "/usr/local/bin/entrypoint.sh"]
```

so `true` arrived as an argument `entrypoint.sh` ignores. The entrypoint ran, found no
`GH_TOKEN`, and exited 1. Both probes classified any non-zero as *the flag was rejected*.

Consequences in the report as written:

- **Group C**: both single-flag baselines "failed" (exit 1), which tripped the guard and
  declared **C3 UNANSWERED**.
- **`/dev/dri`**: `--device /dev/dri:/dev/dri` was reported **"EXIT 1 — REJECTED"** for a flag
  podman had *accepted*.

### This is §9 again, and the discriminator was already in the report

*A true statement about a check presented as a stronger statement about the world.* "The
baseline exited non-zero" was true. "Therefore the flag may be unsupported" does not follow —
the captured output says `ERROR: GH_TOKEN environment variable not set`, which is
`entrypoint.sh` speaking, not podman.

The report contains its own control, three sections apart:

| observed                                                      | speaker      | means                        |
| ------------------------------------------------------------- | ------------ | ---------------------------- |
| exit 1 + `ERROR: GH_TOKEN environment variable not set`       | entrypoint   | flag ACCEPTED, container ran |
| exit 125 + `Error: stat /dev/plan00068-…: no such file …`     | podman       | engine refused the CLI       |
| exit 125 + `Error: cannot set multiple networks …`            | podman       | engine refused the CLI       |

Both C3 orderings came back **125 with podman's own network-mode conflict text**, in two
*different* messages depending on order. The missing-device probe was accidentally safe for
the same reason — 125 fires before any container runs, so its entrypoint never executed.
Correct answer, reached by luck.

### Fix

`--entrypoint true` on all three container invocations, and a `125`-vs-other discrimination
in both probes so a non-zero exit can no longer be silently read as flag rejection. A future
image with a surprising entrypoint now degrades to a visibly-unusable probe instead of a
confident wrong answer. `shellcheck -x` and `bash -n` clean on both files.

### What the existing run already establishes (pending re-run confirmation)

Recorded as fact, not verdict (R9):

- **Deployed launcher vs checkout: sha256 IDENTICAL** (`0fd2dcfd…`). Every line-number
  citation in this plan's reports describes the running launcher. That risk is closed.
- **Image `claude-yolo:latest` carries `claude-yolo-version=2.22`**, and the launcher's
  `REQUIRED_CONTAINER_VERSION="2.22"` (`claude-yolo:39`) — **match**, so no rebuild would be
  forced. That is the comparison the report asks the reader to make.
- **Group F fully answered, with working positive controls.** An absent label reads back
  exit 0 / **0 bytes** — silent empty, so the naive `[[ "$wanted" == "$have" ]]` compares two
  unknowns and returns **FRESH**, reproduced exactly as the spec predicted. The prescribed
  form (assert `wanted` non-empty first) returns **REBUILD**. Controls discriminate: label
  `2.22` → FRESH, `9.99` → STALE.
- **`/dev/dri` exists on this host** (card0, card1, renderD128, renderD129), and **a missing
  `--device` path is FATAL** (podman exit 125). Those two together are the E6 answer: on a
  host without `/dev/dri` — a headless CI runner — ccy's unconditional
  `--device /dev/dri:/dev/dri` aborts the run. The conditional treatment `GUI_MOUNTS` already
  uses (`claude-yolo:2703-2727`) is the shape to copy.
- **C1 and C2 remain deliberately unprobed** — both need a host listener, which would stop
  the probe being read-only. Still borrowed and unverified, recorded rather than quietly
  closed by an easier adjacent probe.

Needs a second HOST run to confirm C3 and the `/dev/dri` acceptance under the corrected
probes.

### Correction — broken links in the 26-07-30 day-file (append-only, so corrected here)

`00068-Journal-26-07-30.md` links seven reports as `reports/<name>.md` from inside `JOURNAL/`,
so they resolve to `JOURNAL/reports/…` and 404: `round2-restatement.md`,
`phase3-image-layering.md`, `phase45-mcp-and-egress.md`, `task74-capabilities.md`,
`phase2-non-interactive.md`, `fable-review-2.md`, `fable-review-3.md`. **All seven files
exist**; the correct prefix is `../reports/`. Found by Plan 00070's link sweep. Not edited in
place — journal entries are append-only, so this note is the correction.

## 23:10 — I said "R9/R10 don't exist". They did. Deleting them was the defect.

Set out to fix what I had called a dangling citation in `phase3-image-layering.md`, and the
premise was wrong in the way this plan keeps being wrong.

**What I claimed last turn:** that `phase3-image-layering.md` cites "R9/R10" and neither
exists, so the citation resolves to nothing.

**What is actually true:** `R1`–`R11` were a real numbered block in `PLAN.md`, and
`9a53608` ("crystallise PLAN.md to current truth — 2061 lines to 262") deleted them.
Recovered verbatim with `git show 9a53608^:…/PLAN.md`, lines 164–226. My statement was true
of the current file and I presented it as a statement about the citation's validity — the
signature meta-bug, again, this time in my own bug report.

**The rule was already written down.** `PLAN.md:181-182`, about the Phase 3 tasks:

> Kept as cancelled rather than deleted because `reports/fable-review-*.md` cite them by number.

The `R` block was deleted in violation of that exact rule, in a commit whose stated purpose
was tidying. Five reports still cite it.

**Then it got worse — three schemes, two collisions.** Chasing the resolution turned up:

| Label        | Meanings                                                                                                   |
| ------------ | ---------------------------------------------------------------------------------------------------------- |
| `Decision 5` | `PLAN.md` = egress is independent of CI; `round2-restatement.md:135` = trusted-only scope is asserted       |
| `Decision 6` | `PLAN.md` = fail-fast contract; `round2-restatement.md:150` = a second CI entrypoint (**retracted**, T3.6) |
| `D6`         | a **review finding** (`26-07-30.md:728`) that retracted `R10` item 1 — cited 12 times across `reports/`    |

`Decision 4` is the only one of the three that agrees across both documents.

The live consequence: `round2-restatement.md:358` closes *"Decisions 4, 5 and 6 stand."* Read
against `PLAN.md` — where a reader looks a decision up — that endorses two propositions the
report never made and one that Task 3.6 retracted. True of its own document, false of the
plan. Same shape a third time in one investigation.

**Fixed, without rewriting history:** `reports/finding-numbers-key.md` restores `R1`–`R11`
verbatim with a current-status column (`R7` retracted, `R10` item 1 retracted, `R5` open, the
rest stand), states both collisions, and records that `D`-numbers are only defined in
`JOURNAL/`. `round2-restatement.md` and the `fable-review-*` files are **not** edited — later
rounds cite their line numbers, which is the same discipline that should have protected the
`R` block. `PLAN.md` gains a pointer above Decision 1.

**Also corrected — `probe-engine.bash`.** It still carried my earlier wrong reading: that a
host-side `podman build` (`common.bash:556`) escapes the container's `--network` restriction,
therefore the rebuild is unconstrained. First clause true, conclusion false on this estate —
the runner VM's own egress goes through squid, and
`lts-infra tasks/runner-ccy-project-image.yml:19-23` records a **live refusal**. Rewritten to
state the trap and then the by-time split, which is ccy's own rebuild scheduled when it can
reach the network — not a system built beside ccy.

**Still outstanding:** the second HOST run of `triage.bash`. Only one run directory exists
(`20260731-214921`) and it predates the entrypoint fixes — its line 181 still shows
`EXIT 1 — REJECTED` with the `GH_TOKEN` error, which is the bug, not the answer.

## 23:35 — Correction to the entry above, found by checking my own status column

The key I had just committed marked `R10` as *"item 1 retracted; items 2 and 3 stand"*. Wrong,
and wrong in this plan's characteristic direction — **retired scope written up as outstanding
work.**

- **Item 2 (build identity as an OCI `LABEL`)** is retired as a CI deliverable by `D33` — which
  is `PLAN.md` Decision 2 *and* an explicit Non-Goal *and* why F1–F4 are marked "not blockers".
  The underlying defect is real, but `project-drives-the-image.md:44-47` places it exactly: *"a
  real, pre-existing **desktop** question … not something ccy owes CI, and the owner's
  no-desktop-churn constraint means it is not this plan's business."*
- **Item 3** rested on `R7`/Decision 6, retracted by Task 3.6.

So all three items are retired, each for a different reason. The row now says so, with a note
per item, because "retired" without the reason is what lets an item quietly come back.

**This also corrects what I told the owner earlier this session**, where I listed the
`$HOME/.cache` → `LABEL` change as one of three real items falling out of §4.1. It is real as a
*desktop* observation and it is **not** work this plan owes — I dropped the qualifier that makes
the difference. That is the third instance today of asserting something true in a narrow scope
as though it were true of the plan, after `probe-engine.bash`'s build/egress claim and
`round2-restatement.md:358`'s "Decisions 4, 5 and 6 stand".

The pattern is specific enough now to state as a check: **when a finding is scoped
(CI-vs-desktop, this-document-vs-the-plan, the-container-vs-the-host), carry the scope into every
restatement of it. The scope is the finding.**

## 23:55 — The re-run had already happened. C3 is measured; E6 is a confirmed blocker.

Told the owner the re-run was outstanding. It wasn't — `20260731-225344` had landed while I was
working, and they said so. I had checked the directory once, early in the turn, and then reported
that stale reading as current at the end of it. **Check immediately before asserting, not once per
turn.**

The corrected probes discriminate, and `triage.log` closes with `all legs OK` — every leg exit 0,
where the first run had at least one INCOMPLETE.

**E6 — CONFIRMED BLOCKER.** `--device /dev/dri:/dev/dri` → exit 0 here (the node exists);
`--device /dev/plan00068-definitely-absent` → **exit 125**, `stat: no such file or directory`.
`podman` refuses the command line before any container runs. `ccy` passes the flag unconditionally
(`claude-yolo:2773`), so on a headless runner **every `ccy` invocation aborts before Claude
starts.** The fix already exists as a pattern in the file: build `--device` conditionally the way
`GUI_MOUNTS` is built at `:2703-2727`.

**C3 — CONFIRMED, first direct measurement.** Both singles now exit 0; both orderings of the
combination exit 125 with *different* messages (`cannot set multiple networks without bridge
network mode` / `can only set extra network names … conflicts with bridge`). Two distinct refusal
paths, which also rules out last-flag-wins. Task 5.1's hard error stops resting on a borrowed
claim.

**The part worth keeping.** The combination results were **byte-identical across both runs** —
exit 125, same two messages. Nothing about the host changed. What changed is that the baselines
now succeed, which is what licenses attributing the 125s to exclusivity rather than to pasta being
unavailable on this host.

So the first run contained the right bytes and the wrong conclusion, and the two design choices
that caught it were both arguments the probe made against its own convenience:

1. **Running the single-flag baselines.** Without them, run 1 showed a failing combination and it
   would have been easy to bank C3 as confirmed. The probe refused instead — *"This is not a
   refutation of C3 and must not be recorded as one."* The convenient reading and the correct one
   pointed the same way; only the baselines showed the reasoning was unsound.
2. **Discriminating exit 125 from every other non-zero.** Collapsing "non-zero" into "rejected" is
   exactly what produced both false answers.

Same lesson as three times earlier today, from the other side: **a true statement about a check is
not a statement about the world.** "The command exited non-zero" was true in both runs. "The flag
was rejected" was false in both.

Task 1.1 closes. E1, E6, C3 and F1–F4 settled; **C1/C2 stay borrowed and unproven** — they need a
host listener, which would stop the probe being read-only, and that is recorded rather than closed
by an easier adjacent probe. Verdicts: `reports/host-run-verdicts.md`.
