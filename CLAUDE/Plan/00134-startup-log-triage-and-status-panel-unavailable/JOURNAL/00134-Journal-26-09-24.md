# Plan 00134 — Journal 26-09-24

_Timestamps in this file are UTC._

> **Append-only activity log** for plan 00134. One file per day
> (`00134-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan; this file records what
> happened, in order.

## 07:21 · action · T2.6 — deploy re-run through meta; Thunar removed

The owner's 07:00 UTC `meta-deploy.bash` run put this plan first, since its deploy covers
the most, including the ccy launchers and basic-configs. The capture is
`untracked/plan-runs/_meta-deploy/20260924-080001/`.

- **Deploy:** `all legs OK` for all eight plays.
- **Thunar:** the `Remove packages this repo does not want` task in
  `play-basic-configs.yml` reported `changed`, so the package is gone.
- **Triage before and after:** the journal lines it reports are from the boot of
  2026-09-23, which has not ended. They include the duplicate `vivaldi` repo errors,
  firewalld's docker-policy conflict, and the iptables `COMMAND_FAILED` lines. They
  cannot change until the next boot.

Still open: the checks after the next boot and login, including Task 2.6's dbus-broker
line, and the owner's three decisions. The plan is out of `meta-deploy.bash`, since
nothing is left for a batch run until after that boot.

## 09:29 · decision · T3.1 T3.2   — owner picks A, the backend assert, and a narrowed criterion

The owner chose all three proposals:

- **T3.1:** option A. The relabel verdict applies whenever SELinux is not Disabled, so a
  Permissive host gets `:z` too.
- **T3.2:** yes, as proposed. `play-lxc-install-config.yml` asserts that Docker uses its
  iptables backend, the play is re-run once, and the firewalld per-boot noise stays.
- **Criterion:** the `RuntimeMaxSec= has no effect` half is narrowed to units this repo
  deploys. The other project's unit is out of scope.

A fork is implementing T3.1 and T3.2.

## 09:44 · action · T3.1 T3.2   — SELinux relabel on Permissive and the Docker backend assert, built and reviewed


The owner answered the three open decisions on 2026-09-24.

- **T3.1: option A, relabel whenever SELinux is not Disabled.** In CCY 3.65.0 the
  verdict (`selinux_enforcing_verdict`, `lib/common-pure.bash`) gives a Permissive host a
  new answer, `permissive`, when the engine labels. It relabels exactly as `enforcing`
  does:

  - the workspace is mounted `:z`;
  - the config import is mounted `Z`;
  - keys are staged into a private tmpfs;
  - the launch prints `🔒 SELinux permissive`.

  A Permissive host whose engine does not label still gets `off`, and a Permissive host
  with an unreadable engine report gets `unknown`, which also relabels. Every consumer
  tests `!= off`, so no caller changes meaning.

  - **Test:** `scripts/test-ccy-selinux-verdict.bash`. It was RED on two cases (Permissive
    with a labelling engine, and Permissive with an empty report) and is GREEN at 16/16.
  - **Docs:** `docs/ccy.md` (SELinux section) and `docs/ccy-changelog.md` (3.65.0).

- **T3.2: the proposal as written.** `play-lxc-install-config.yml` reads `docker info`
  (with `check_mode: false`, since it only reads) and asserts that the firewall backend
  is `iptables` or `iptables+firewalld`. The assert comes before the DOCKER-USER tasks,
  and only where dockerd is installed. Under the nftables backend, or when there is no
  `Firewall Backend` line, the play stops and names the reason. The per-boot firewalld
  `COMMAND_FAILED` noise stays unsilenced. `docs/playbooks.md` now mentions the stop.

- **Criterion:** the `RuntimeMaxSec= has no effect` half now covers only units this repo
  deploys. The warning from another project's unit is out of scope.

HOST, still owed:

- deploy `play-claude-yolo.yml` and restart the sessions, then read the next boot's
  `container_t` AVC count;
- run `play-lxc-install-config.yml` once. It shows the assert passing, and whether the
  reconcile unit gets installed on this host.

**Review:** the qa-reviewer passed it with nits
([260924-qa-reviewer-opus-5.md](../subagent-reports/260924-qa-reviewer-opus-5.md)). The branch
is merged. After the merge, nits 1 and 2 were fixed: the docs and the launch message now say
that on a Permissive host the unrelabelled mounts are readable but logged, and the
`check_mode` comment says `docker info` can start a stopped dockerd. The other nits are
accepted.
