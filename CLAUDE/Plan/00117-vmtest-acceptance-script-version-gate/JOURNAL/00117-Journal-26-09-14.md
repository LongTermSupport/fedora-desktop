# Plan 00117 — Journal 26-09-14

> **Append-only activity log** for plan 00117. One file per day
> (`00117-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:17 · action · — — plan scaffolded

Plan 00117 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:34 · finding · — — a third run, a third commit, the same stale checker: the diagnosis holds

`20260914T110446Z-desktop-fresh-install` against `be73d3b0`, verdict pass, 16 of 16, finished
11:33:17. Its extension check:

```
VMTEST-CHECK pass deployed-extensions-active COVERAGE: 8 of 1 declared ACTIVE (8 on disk)
```

Identical to run 2's, on a different commit, one that includes `ade40aba`. This was predicted
before the run finished and is recorded because a prediction that comes true is the cheapest
confirmation available: the checker does not track the commit under test, it tracks the host's
last deploy, and two consecutive commits to the checker changed nothing about what ran.

The `(8 on disk)` suffix and the `deployed_extensions` evidence key are again the tell — both
were removed by `d307ed28`, so the script that ran predates it by at least two commits now.

What run 3 *is* good for: its other fifteen checks are the first to exercise `ade40aba`'s play,
applier and verifier changes on a real fresh install, and all fifteen pass. The desktop provisions
cleanly at `be73d3b0`. That is worth having and is not what Plan 00112 Task 2.2 asked for.

It also ran in **29 minutes** against roughly 55 and 62 for runs 1 and 2 — a warm base, not a
faster product.
