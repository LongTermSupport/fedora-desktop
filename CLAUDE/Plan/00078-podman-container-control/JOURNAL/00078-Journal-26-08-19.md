# Plan 00078 — Journal 26-08-19

> **Append-only activity log** for plan 00078. One file per day
> (`00078-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:36 · action · — — plan scaffolded

Plan 00078 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:52 · action · P0 — failsafe recovery cron created

Non-durable hourly recovery cron created for this session: job ID `1e735fce`
(fires at :23). Session-only; auto-expires with the session.

## 12:04 · finding · P0 — research log: mechanisms, rootless, CCY identity, tooling survey

Authoring context: inside a CCY container — `podman` is **not installed**
here, so no runtime probe was possible. Everything host-runtime below is
doc-sourced and queued for `triage.bash` on the HOST.

**Mechanism mapping.** The user's four verbs collapse to one rootless-capable
mechanism:

- `podman pause`/`unpause` = cgroup freezer. The stable podman-pause(1) page
  (docs.podman.io, fetched today) documents `--all` and `--filter` with
  filters `id, name, annotation, label, status, ancestor, before, since,
  volume, health, pod, network, restart-policy` — so **network-scoped bulk
  pause is native** (`podman pause --filter network=<net>`); the tool's job is
  selection UX + safety, not plumbing. Current stable docs list **no rootless
  restriction** on pause (older releases required cgroups v2 for rootless
  pause; Fedora has been cgroups-v2-by-default for years) → H1, verify on
  host.
- `podman container checkpoint`/`restore` = CRIU, **root-only**. Multiple
  sources agree rootless checkpoint is unsupported (CRIU needs elevated
  capabilities): podman.io/docs/checkpoint, criu.org/Podman, Fedora Magazine
  "Podman checkpoint", docs.podman.io checkpoint man page (web research
  today). Additionally CCY containers run `--rm` (claude-yolo:2944), and
  checkpoint documents rejecting `--rm` containers except with `--export`
  (H5, moot given the rootless block). → checkpoint/restore excluded as a
  non-goal; "suspend" == pause here. Frozen containers do not survive reboot.

**CCY identity.** `get_next_container_name()` (lib/common.bash:598) yields
`<project>_yolo[_N]` / `<project>_browser[_N]`. The `container_cmd run` call
(claude-yolo:2944) passes `--name` but **no `--label`**. The image carries
`LABEL claude-yolo-version="2.26"` (Dockerfile:36) and
`claude-yolo-dockerfile-hash` (Dockerfile:326); OCI containers normally
inherit image labels, so `--filter label=claude-yolo-version` *should* match
(H2, verify on host). Plan adds explicit run-time labels
(`ccy=true`, `ccy-project=<project>`) — CCY_VERSION minor bump required.

**Tooling survey** (test the "roll our own" suspicion rather than agree):

- **podman-tui** (containers org): the only Fedora-packaged Podman-native TUI
  (packaged in Fedora per packages.fedoraproject.org); needs the user
  `podman.socket`. Its docs list per-entity screens (containers, pods,
  networks, volumes, images, secrets) but do **not** enumerate container
  verbs — whether its container menu includes pause was NOT confirmable from
  the README/docs fetched today (H4); no sign anywhere of multi-select bulk
  actions or act-by-network.
- **lazydocker**: Docker-first (works via docker-compatible socket); not in
  Fedora repos; no network-bulk verbs.
- **ctop**: single-container ops, largely dormant upstream; not in Fedora
  repos.
- **Cockpit + cockpit-podman**: Fedora-packaged web GUI; per-container
  actions; no bulk-by-network, no CCY grouping.
- **dockge**: compose-stack manager, Docker-oriented; wrong shape entirely.

None covers: network-scoped bulk freeze, a CCY-group verb, self-freeze
protection, or numbered/fzf terminal UX consistent with this repo. → build
`podman-freeze` (D2/D3); podman-tui stays a fine *complement* for browsing but
is not bundled into this plan (YAGNI).

**Repo precedents.** fzf is already deployed with a numbered-menu fallback by
`play-open-command.yml` / `files/home/.local/bin/open` — the picker pattern to
copy. Interactive UX rules bind (`CLAUDE/InteractiveScripts.md`), stderr
hygiene binds (`CLAUDE/StderrHygiene.md`).

Sources (fetched 2026-08-19):

- https://docs.podman.io/en/stable/markdown/podman-pause.1.html
- https://podman.io/docs/checkpoint
- https://criu.org/Podman
- https://fedoramagazine.org/podman-checkpoint-in-fedora-linux/
- https://github.com/containers/podman-tui
- https://packages.fedoraproject.org/pkgs/podman-tui/podman-tui/

## Phase 0 host triage — run, and the decision gate passes

`triage.bash` ran on the host. Identifiers are deliberately generalised below:
the raw log names real projects, networks and home paths, and lives only in this
plan's gitignored `logs/`. This repo is public.

### H1 — CONFIRMED. The gate passes.

```
podman version 5.8.4
rootless=true cgroupVersion=v2 cgroupManager=systemd runtime=crun
```

Rootless pause needs cgroups v2 with a systemd manager; all three hold. Nothing
downstream is blocked, and the checkpoint non-goal stands unchanged.

### H2 — CONFIRMED, and it removes a prerequisite

`podman ps --filter label=claude-yolo-version` returned **exactly** the running
CCY containers and nothing else. Every one carries the inherited image labels
`claude-yolo-version` and `claude-yolo-dockerfile-hash`.

So the CCY group is selectable **today**, with no launcher change and no CCY
version bump. Phase 1 existed to create that handle; the handle already exists.
Demoted from prerequisite to enhancement, and D4 revised in place rather than
quietly rewritten.

Phase 1 is *kept*, not deleted, for two things the inherited label cannot do:
it means "built from the CCY image", not "is a live CCY session", and it carries
no project name, so per-project verbs still need `ccy-project`.

### H3 — CONFIRMED

`podman pause --help` lists `-f, --filter stringArray`, and `ps --filter
network=<n>` partitioned all six networks correctly, including returning empty
sets for the three with no attached containers. An empty set being *reported*
rather than erroring matters for the tool: "no containers on this network" must
be distinguishable from "the filter did not work".

### H4 — PARTLY, and now moot

Packaging confirmed: `podman-tui 1.11.3-1.fc44` in `updates`, 9.6 MiB, Fedora
Project vendor. The user `podman.socket` it requires is **already active** — so
adopting it would cost one package task in a play and no other setup.

Its bulk-verb capability is still unverified; that would need the TUI running
interactively. It no longer changes anything: the network-scoped and CCY-group
verbs are being built regardless, so podman-tui remains an optional complement
for browsing, deliberately not bundled.

`fzf` confirmed present at `/usr/bin/fzf`, so D3's picker has its dependency.

### Two facts nobody predicted — and they are the safety case

The triage was written to answer H1-H4. The container inventory it printed
answered something else:

- **A CCY container shares a user-defined network with a ten-container compose
  application stack.** Freezing "that network" to pause the app would also
  freeze a live Claude session.
- **Seven CCY containers sit on the default `podman` network.** Freezing that
  network freezes nearly every Claude session on the machine at once.

Neither is guessable from the network's name, which is exactly why the tool must
*show the resolved set before acting* rather than describe the selector back to
the user. This moves the preview from "good practice" to the feature the network
verb is unusable without, and it is now recorded as F11/F12 rather than left as
a design instinct.

Recorded as new facts rather than folded into the hypotheses table, because they
were not hypotheses — they were unknowns nobody had thought to be uncertain
about, which is the category triage is actually best at surfacing.
