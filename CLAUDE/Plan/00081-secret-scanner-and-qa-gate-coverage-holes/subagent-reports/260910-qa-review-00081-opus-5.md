# QA Review — Plan 00081 (secret-scanner and QA-gate coverage holes), 8 commits on F44

> **Editor's note, filed by the dispatching session.** This is the `qa-reviewer` agent's
> report, transcribed verbatim from the neutral artefact it wrote; the agent is read-only
> and has no Write tool, so filing it here is my step, not its. **Every finding below has
> since been resolved** — see the disposition table at the end of this note. Two
> corrections to the report's own text, neither affecting a verdict:
>
> - Finding 7 says F12's citation of `…Journal-26-08-19.md:233` is wrong. Confirmed: I
>   re-measured and `0369468b` touched line **283** only. The `:233` came from my own
>   earlier edit to F12 this session and was a transcription slip.
> - Finding 18 was already half-actioned before the report landed: the stale gate count in
>   `.github/workflows/qa.yml` was corrected in `f0851a1f` while this review was running.
>
> The report's central verification — 13/13 PASS at HEAD, 8/13 FAIL at `0369468b~1` with
> the failing set `{1,2,5,6,7,9,10,11}` — I reproduced independently before reading the
> report, and it holds exactly.
>
> **Disposition**: findings 1 and 2 (BLOCK) fixed in `20fa7555` and `e01888cf`; findings
> 3–6 and 9–15, 18 fixed in `5ea9e0c6` and the commit filing this report; findings 7, 8,
> 16 and 17 are plan-document corrections made alongside it. Finding 17 is deliberately
> left for Task 3.2, which is the commit that moves the plan to `Completed/`.

**Verdict**: **BLOCK** — two items must land before this plan is marked Complete. Everything else is FIX-BEFORE-MERGE or nit.

The plan's central evidence claim reproduces exactly: `acceptance.bash` gives 13/13 PASS against the current hooks and 8/13 FAIL against `--hooks-dir` at `0369468b~1`, with the failing set exactly `{1,2,5,6,7,9,10,11}` as the file states. I re-ran both. That is a genuinely re-runnable proof and it holds.

The findings below are all in the *fixes*, which is where this plan asked to be attacked.

---

## Blocking

### 1. A denylisted private identifier is still live in the tracked working tree of a public repo

`/workspace/CLAUDE/Plan/00049-full-repo-audit/research/security.md:188`

Confirmed with the repo's own scanner (`hook_scan_text_for_private` against the denylist the hooks build): this is the **only** hit across all tracked files, source field `lastpass_accounts`. F12 names this exact file and line, then declines to act, reasoning at `PLAN.md:128` that "the two files are now un-editable without scrubbing, which is the gate working."

That reasoning covers the wrong thing. The Non-Goal is *rewriting published history*; this is the **current tree**, removable by an ordinary edit and commit. `0369468b` already did exactly that for the 00079 journal. Leaving it means the identifier stays in the repo people clone today, not merely in history.

Also stale: F12 says "two tracked files". `00065-headless-server-cloud-base-blocker-fixes/PLAN.md` now scans **clean**. Only 00049 remains.

**Fix**: substitute a placeholder per `CLAUDE/ExampleValues.md` at `security.md:188` (`PlanTriage.md`: redact by substitution, never by dropping the line) and commit. History stays the owner's call.

### 2. `CCY_HASH` covers six of the seven libraries — the F7 fix enumerated the set and the set was already wrong

`/workspace/files/var/local/claude-yolo/claude-yolo:25` and `:84-91`

`CCY_LIBS=(common token-management ssh-handling network-management dockerfile-custom docker-health)` — six. But `/workspace/files/var/local/claude-yolo/lib/common-pure.bash` (9,850 bytes) is sourced unconditionally by `/workspace/files/var/local/claude-yolo/lib/common.bash:12`, and has existed since 2026-06-10 — **two months before `f585873e`**. It is not hashed.

Measured (probe, tree restored, `git status` clean): appending a line to `common-pure.bash` leaves `CCY_HASH` at `870f4606bceb9ed2` — unchanged. The "DEVELOPER ERROR: CCY script modified without version bump" guard at `claude-yolo:393` is blind to it.

The two halves of the fix now disagree about what "the CCY script" is: `/workspace/scripts/git-hooks/pre-commit:129` matches `"$CCY_LIB_DIR"/*.bash` and **does** cover `common-pure.bash`; the hash does not. `CCY_LIBS` was introduced specifically so the presence check, the load order and the hash could not disagree — and it disagrees with the *pre-commit gate*, which is the other half of the same F7 fix.

Three documents now assert something false:

- `/workspace/docs/ccy-changelog.md:368` — "`CCY_HASH` now hashes the launcher plus every library it sources, in load order"
- `/workspace/CLAUDE/ContainerRules.md:95` — "the six libraries the launcher sources"
- `/workspace/CLAUDE/AgentNotes.md:334` (row 9) and `:356-359` — "six libraries had grown around it… **When a program grows a second file, every check that names the first one by path is now partial.**" The lesson is written down beside a fix that did not apply it.

**Fix**: derive, don't enumerate. Hash `"$SCRIPT_DIR"/lib/*.bash` sorted, keeping `CCY_LIBS` for load *order* only; or at minimum add `common-pure` to `CCY_LIBS`. Deriving is the one that cannot go stale on the eighth library. Needs a `CCY_VERSION` bump, an updated comment and a changelog entry, plus corrections to the three docs above.

---

## Should fix (before Complete)

### 3. "Every fix has a gate that fails against the unfixed code" is not satisfied — three fixes have none

The criterion is correctly left unticked at `PLAN.md:239`; it should not be ticked as-is.

- **F9** — `/workspace/scripts/qa-ansible-syntax.bash:76-96`. Zero guard only, **no partial guard**. `qa-bash.bash` and `qa-python.bash` both assert *discovered ⊇ tracked* and exit 2 on a shortfall; this gate has no yardstick at all. Re-narrowing the population drops silently, and the breakdown line is not something anyone diffs commit-to-commit. Related: the predicate at `:80` (`^\s*-\s+(hosts|import_playbook):`) requires the key to lead the list item, so the legal `- name:`-first play shape would fall out unseen. No live instance today — I checked `^\s+hosts:` repo-wide and the only match, `environment/localhost/hosts.yml`, is an inventory and correctly outside.
- **F10** — `/workspace/scripts/qa-ansible.bash:62-64`. No fixture anywhere. Reverting `FF_FALSEY`/`FF_TRUTHY` to the single-spelling regex turns nothing red.
- **F4** — `/workspace/scripts/qa-deployed-drift.bash:138-147`. The `.j2 → playbook dest:` assertion is a **pure source-tree invariant**, but it sits after three host-availability early exits (`:33` CCY container, `:46-52` linked worktree, `:54` no `~/.local/bin`). In the CCY container — where this diff was authored and where I ran QA — the script exits 0 at line 34 and the assertion never executes. Today's `qa-all` output: `✓ deployed-drift: skipped (CCY container …)`. **Fix**: move the template→dest loop above the host skips.

### 4. `qa-python.bash` covers 52 of 53 tracked Python programs; the 53rd is 349 lines and no gate opens it

`/workspace/files/home/.local/bin/git-account-helper.j2`

`#!/usr/bin/env python3`, 349 lines, exactly one Jinja expression. `/workspace/scripts/qa-discovery.bash:187,220,262,289` excludes `*.j2` from discovery **and** from the tracked yardstick, so the shortfall assertion can never see it.

F3's number counts it. I enumerated mode-0644 python-shebang blobs at `8091a943~1` and got exactly seven: the six `wsi-*` files **plus this one**. So `PLAN.md:71`'s "7 tracked repo-owned files, 4,003 lines… never compiled or linted" is the **enumerated** set; the delivered coverage is **6**, and the seventh is still never compiled or linted — with no note recording it as accepted. That is precisely the "does the number count what was checked, or what was intended" failure, inside the plan that names it.

**Fix**: state it as a known accepted hole in `CLAUDE/QA.md` and `PLAN.md`, or render-then-compile in the gate.

### 5. `CLAUDE/QA.md:30` still documents the population F9 replaced

The row reads `playbooks/playbook-main.yml` + standalone `playbooks/imports/**`. The gate now derives repo-wide and prints `✓ ansible-syntax: 78 playbooks OK (75 under playbooks/imports/, 3 elsewhere)`. `4344fe39` edited two other parts of QA.md and left this one.

### 6. F11's generalisation stopped at the two gates QA.md happened to list

`/workspace/scripts/test-ccy-rootless-guard.bash` is a pure-function unit suite (no podman needed, by design), run by `/workspace/.github/workflows/qa.yml:109` and by nothing local. `qa-all.bash` does not run it; `CLAUDE/QA.md` does not mention it. That is the F11 shape exactly, still live after two rounds (`4344fe39`, then `6d454a83` which added two *other* suites). (`test-ccy-ssh-probe.bash` needs a real host and gh token, so it is correctly not a gate — but it is documented nowhere either.)

### 7. F12's fourth published location cannot be re-verified, and half its citation is wrong

`PLAN.md:116-131`, added by `c490152c` (the newest commit under review)

- It cites `00079-Journal-26-08-19.md:233,283`. Measured: `0369468b` changed **exactly one line** of that file — line **283**, 84 → 77 characters. Line 233 was never touched, and the current file scans clean.
- The pre-scrub blob at `0369468b~1` matches **no** token in today's denylist — neither word-bounded via `hook_scan_text_for_private` nor as a raw substring — and none of the five entries in `.claude/public-token-allowlist.yml`.
- The inventory is derived from a gitignored, mutable file, so it is unfalsifiable by construction. F12 already says "treat it as a floor"; that is the right instinct, but a floor nobody can re-measure is a claim, not a record.

**Fix**: drop `:233`, and record which source field and which date the entry was established from, so a later reader can distinguish "the token left `localhost.yml`" from "the entry was wrong".

### 8. `PLAN.md` carries three different acceptance-suite numbers, two now wrong

`:173` says 9 checks / 6 of 9 fail; `:254` says 10 checks / 7 of 10 fail; `:182-185` and `acceptance.bash:399-405` say 13 / 8 of 13. Re-measured today: **13 checks, 8 fail at `0369468b~1`**. The first two are historical progress notes left in a document that is supposed to state current state — in a plan whose thesis is that stale numbers mislead.

---

## Nits

### 9. `acceptance.bash:399` hardcodes its own coverage number

`COVERAGE: 13 check(s)` is a literal; `PASS+FAIL` is the real count. One line: `[ $((PASS+FAIL)) -eq 13 ] || { echo "COVERAGE LINE STALE"; exit 1; }`.

### 10. The `ignore_unreachable` asymmetry F10 named is only half fixed

`/workspace/scripts/qa-ansible.bash:64`. `ignore_errors:` is checked for both boolean spellings **and** the templated `"{{` form; `ignore_unreachable:` gets booleans only. A templated `ignore_unreachable: "{{ … }}"` is exactly as statically unverifiable and is not flagged. (Case *is* handled — `grep -rni` at `:84`, `-qiE` at `:156`.)

### 11. Any denylist-builder return code other than 1 or 2 is read as "built OK"

`/workspace/scripts/git-hooks/pre-commit:307-315`, `/workspace/scripts/git-hooks/commit-msg:137-144`. If `lib/secret-scan.bash` parses but no longer defines `hook_build_private_denylist`, the call returns 127, falls to the `else`, `[ -s "$DENYLIST_FILE" ]` is false, the whole denylist stage is skipped and the hook prints its pass line. `source` under `set -e` catches a *syntax* break, not a *missing-function* one. **Fix**: hard-fail on `DENY_RC` not in {0,2}, or `declare -F` the three functions right after sourcing.

### 12. The playbook asserts the library exists, not that it is non-empty

`/workspace/playbooks/imports/play-git-hooks-security.yml:44`. `failed_when: not hooks_lib.stat.exists` passes on a zero-byte file (Plan 00067's shape). Add `or hooks_lib.stat.size == 0`. The hooks themselves do fail loudly in that case, so this is a nit.

### 13. Stale merge-index comment

`/workspace/scripts/qa-all.bash:135` (introduced by `4344fe39`) and `/workspace/CLAUDE/QA.md:36` both say `.[0]..[5]`; the merge is `.[0]..[6]` (seven stages, `docs` at index 6). `qa-all.bash:100` carries the same, pre-existing.

### 14. Two gates now hold opposite policies on `CLAUDE/Plan/**` and neither says so

`qa-all.bash:86-87` records that `qa-docs.bash` excludes the plan tree "so archiving a plan can never change this gate's verdict"; `qa-ansible-syntax.bash` now includes it — `CLAUDE/Plan/00062-disk-reclaim-tui/podman-store-repair.yml` is one of the "3 elsewhere". Defensible, but worth one sentence in the gate header.

### 15. F9's "the two Ansible gates disagree about the population and neither says so" is still true

Measured: syntax gate 78, `qa-ansible.bash` hygiene/scope 76. The delta is `playbooks/playbook-main.yml` (documented, import-only) and `CLAUDE/Plan/00062-…/podman-store-repair.yml` (undocumented). The syntax gate now states its breakdown; `qa-ansible.bash:533` still prints a bare `76 playbook(s)`.

### 16. No JOURNAL entry for 2026-09-10

`c490152c` changed F12 that day; the only day-file is `00081-Journal-26-08-20.md`. `plan-qa --sweep` flags 00081 under journal-freshness.

### 17. `/workspace/CLAUDE/Plan/README.md:133` is stale

Still says "Remaining phases cover `qa-python.bash` and `qa-deployed-drift.bash`" — both done. Rewrite when moving to `Completed/` at Task 3.2.

### 18. `.github/workflows/qa.yml:102,116` now duplicate steps `qa-all.bash` performs

Helper tests and extension compat. Harmless; the `helpers` job's remaining unique value is finding 6.

---

## Checked and clean

- **Fail-fast**: no new `failed_when: false` / `ignore_errors: true` without annotation anywhere in the diff. `_hook_grep_v` (`secret-scan.bash:36-48`) correctly distinguishes "no match" (rc 1) from "grep broke" (rc >= 2), and the error branch is provably live — acceptance check 10 goes red at `0369468b~1`. `hook_keep_unwhitelisted`'s `rc` handling traces correctly across lines and across an early `break`.
- **The Python edits are safe**: `Label`, `time` and `struct` are genuinely unreferenced after removal (grepped); `SCRIPT_VERSION` is defined at `wsi-article:21` and used at `:189`, so the move below the imports cannot NameError.
- **`ruff.toml`'s per-file ignore works**: `wsi-article-window` reports 7 E402 under `ruff --isolated --select E402`, 0 under the repo config. Scoped to one file, not a global disable, not an inline suppression.
- **Placement**: no new play was created — the library check went into the play that already owns the hooks. `lib/secret-scan.bash` and `qa-discovery.bash` sit with their owners. No new abstraction without a second caller. The `qa-shell-discovery.bash` -> `qa-discovery.bash` rename left no live references (only historical journals and one comment recording the rename).
- **CCY versioning**: `f585873e` bumped 3.40.1 -> 3.41.0 with a rewritten comment and a changelog entry; `8091a943` touched no CCY file. `REQUIRED_CONTAINER_VERSION` correctly unchanged — nothing baked into the image changed.
- **Public-repo safety of the diff's own files**: clean. `acceptance.bash` assembles every test identifier at runtime and otherwise uses RFC 2606 domains, so the tracked file holds no plausible address.
- **Stderr hygiene / interactive rules**: `acceptance.bash` is a report script (its text *is* the payload) and sends the log-path notice to stderr; `--help` works, unknown options fail fast, `--hooks-dir` validates its argument.
- **`6d454a83` (the later commit) broke nothing 00081 established.** It preserved F11's attribution, added two more suites in the same shape, and corrected genuine pre-existing drift in QA.md's gate table (13 = 7 merged + 6 hard, which I verified against `qa-all.bash`). It did leave finding 13 stale and did not touch finding 5.

## Success criteria — actual status

| Criterion                                                                                 | Status                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `qa-python.bash` covers every tracked repo-owned Python file, fails loudly on a shortfall | **Partly.** Verified for `.py` + shebang: 6 mode-0644 files exist that a reverted discovery would miss, so the assertion would exit 2. **Not** satisfied for the `.j2` (finding 4) |
| Every fix has a gate that fails against the unfixed code                                  | **No** — F9, F10 and F4 have none (finding 3)                                                                                                                                      |
| `./scripts/qa-all.bash` passes                                                            | **Yes** — exit 0, 682 files                                                                                                                                                        |

## Mechanical gates

- **`qa-all.bash`**: PASS, exit 0, 682 files. (Advisories only: 147 shellcheck info/style, 14 semgrep partial-parse notes — both pre-existing advisory channels, not gates.)
- **`plan-qa --sweep`**: exit 1 — **0 block**, 2 advise (staleness-nag; journal-freshness, which lists 00081 -> finding 16).
- **`ansible-playbook --syntax-check`**: PASS on `playbooks/imports/play-git-hooks-security.yml`, the only playbook in the diff.
- **`qa-helper-tests.bash`**: not triggered by this diff (no `helpers/` or `tests/helpers/` change). Ran anyway inside `qa-all`: 255 tests.
- **`helpers.gnome.check_extension_compat`**: not triggered (no `extensions/` metadata change). Ran anyway inside `qa-all`: 4 extensions OK.
- **`eslint`**: not triggered — no extension JS in the diff.
- **`acceptance.bash`**: 13/13 PASS current; 8/13 FAIL at `--hooks-dir 0369468b~1`, failing set exactly `{1,2,5,6,7,9,10,11}`.

---

## Appendix — every finding, one line, most severe first

| #   | Sev   | Where                                                                                        | Claim                                                                                                                                      |
| --- | ----- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | BLOCK | `/workspace/CLAUDE/Plan/00049-full-repo-audit/research/security.md:188`                      | A denylisted private identifier is live in the tracked tree of a public repo; scrubbing it needs no history rewrite                        |
| 2   | BLOCK | `/workspace/files/var/local/claude-yolo/claude-yolo:25,84-91`                                | `CCY_HASH` misses `lib/common-pure.bash`; measured — editing it leaves the hash unchanged, while `pre-commit:129` does gate it             |
| 3a  | FIX   | `/workspace/scripts/qa-ansible-syntax.bash:76-96`                                            | Zero guard, no partial guard and no tracked yardstick — the one source gate of the three without a coverage assertion                      |
| 3b  | FIX   | `/workspace/scripts/qa-ansible.bash:62-64`                                                   | The F10 spelling fix has no fixture; reverting it turns nothing red                                                                        |
| 3c  | FIX   | `/workspace/scripts/qa-deployed-drift.bash:138-147`                                          | The `.j2 -> dest:` source-tree invariant sits behind three host skips, so it never runs in the CCY container or a clean checkout           |
| 4   | FIX   | `/workspace/scripts/qa-discovery.bash:187,220,262,289`                                       | `git-account-helper.j2` — 349 lines of Python — is excluded from discovery *and* the yardstick; F3 counted 7, the fix delivered 6, no note |
| 5   | FIX   | `/workspace/CLAUDE/QA.md:30`                                                                 | Still documents the `playbooks/imports/**` population that F9 replaced                                                                     |
| 6   | FIX   | `/workspace/scripts/qa-all.bash` (absent) / `.github/workflows/qa.yml:109`                   | `test-ccy-rootless-guard.bash` is CI-gated, not run by `qa-all`, not in QA.md — F11's shape, still live after two rounds                   |
| 7   | FIX   | `/workspace/CLAUDE/Plan/00081-secret-scanner-and-qa-gate-coverage-holes/PLAN.md:116-131`     | F12's `:233` citation is wrong (only `:283` was ever scrubbed) and the entry is unverifiable against the current denylist                  |
| 8   | FIX   | `PLAN.md:173`, `:254` vs `:182`                                                              | Three acceptance-suite numbers in one document; two are stale                                                                              |
| 9   | NIT   | `/workspace/CLAUDE/Plan/00081-secret-scanner-and-qa-gate-coverage-holes/acceptance.bash:399` | `COVERAGE: 13` is a literal, not `PASS+FAIL`                                                                                               |
| 10  | NIT   | `/workspace/scripts/qa-ansible.bash:64`                                                      | Templated `ignore_unreachable: "{{ … }}"` still unflagged while `ignore_errors:` is                                                        |
| 11  | NIT   | `pre-commit:307-315`, `commit-msg:137-144`                                                   | A builder rc outside {0,1,2} (e.g. 127) is read as "built OK" and skips the denylist stage silently                                        |
| 12  | NIT   | `/workspace/playbooks/imports/play-git-hooks-security.yml:44`                                | Asserts the library exists, not that it is non-empty (Plan 00067's shape)                                                                  |
| 13  | NIT   | `/workspace/scripts/qa-all.bash:135`, `/workspace/CLAUDE/QA.md:36`                           | `.[0]..[5]` is stale; the merge is `.[0]..[6]`                                                                                             |
| 14  | NIT   | `/workspace/scripts/qa-ansible-syntax.bash:76-83`                                            | Includes `CLAUDE/Plan/**` while `qa-docs.bash` deliberately excludes it; neither cross-references the other                                |
| 15  | NIT   | `/workspace/scripts/qa-ansible.bash:533`                                                     | The two Ansible gates still disagree (78 vs 76) and this one still prints a bare count                                                     |
| 16  | NIT   | `/workspace/CLAUDE/Plan/00081-secret-scanner-and-qa-gate-coverage-holes/JOURNAL/`            | No 2026-09-10 day-file for `c490152c`; flagged by `plan-qa --sweep`                                                                        |
| 17  | NIT   | `/workspace/CLAUDE/Plan/README.md:133`                                                       | "Remaining phases cover qa-python / qa-deployed-drift" — both done                                                                         |
| 18  | NIT   | `/workspace/.github/workflows/qa.yml:102,116`                                                | Duplicates two steps `qa-all.bash` now performs                                                                                            |

**Counts**: 2 BLOCK, 6 FIX-BEFORE-MERGE (3 split into 3a/3b/3c), 10 NIT — **18 findings**.

**Safe to mark Complete?** Not yet. Findings 1 and 2 must land first — one is a live leak on a public repo, the other is the plan's own flagship fix delivered incomplete with three documents asserting it is complete. Findings 3 and 4 must either be fixed or explicitly written into `PLAN.md` as accepted gaps, because the unticked criterion at `PLAN.md:239` is currently the honest state and ticking it would be false. Findings 5-8 are cheap and should go in the same pass. The nits can ride into the `Completed/` move.
