# Plan 00090 — Journal 26-08-25

> **Append-only activity log** for plan 00090. One file per day
> (`00090-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:31 · action · — — plan scaffolded

Plan 00090 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:50 · decision · P1 — scope: reviewed PR, not a bypass push; merge itself stays the owner's call

`F*` branches are PR-protected; a direct push to `F44` only works via the owner's
bypass, which is not "clean" for a 90-commit resync. This plan lands a QA-green,
`qa-reviewer`-passed PR into `F44` (merge commit or rebase merge — `allow_squash_merge`
is already `false` repo-wide) and stops there; clicking merge is left to the human
owner, same as any other PR on their own repo. PLAN.md's Non-Goals records this
explicitly so it isn't read as an oversight later.

`gh auth status` confirms write access to this repo is available from here if opening
the PR is needed. Repo settings confirmed: `allow_squash_merge=false`,
`allow_rebase_merge=true`, `delete_branch_on_merge=true`.

## 10:52 · decision · P1 — sequencing: land Plan 00089 first

Plan 00089's `ssh-handling.bash`/`claude-yolo` fix is still uncommitted in the working
tree at this point. Merging the branch now would make that uncommitted work either
get swept into the merge commit undifferentiated, or conflict confusingly with the
branch's own (unimplemented-in-code) Plan 00068 material. Committing and pushing
00089 first means the merge base cleanly includes it, and any three-way conflict in
`claude-yolo`/`ssh-handling.bash` during the resync is a real conflict against the
branch's content, not against my own uncommitted work.

## 11:30 · finding · T1.1 — Plan 00089's own review caught a fact error in THIS plan too

Plan 00089's second `qa-reviewer` pass found this plan's Overview (written before the
merge started) asserted `git diff --stat origin/F44...origin/plan-00066-ccy-ci-runner`
"touches no file under `files/var/local/claude-yolo/`" — false. It touches 10 files,
`lib/ssh-handling.bash` among them (128 lines). Checked properly this time: read the
full diff, not just the stat. Every hunk is the branch's copy **missing** F44-only
robustness work added since the fork (`resolve_token_owner_login()`'s retry/validate
logic, CCY 3.36.0) — stale, not a competing "token-first" implementation. The narrower
claim that matters (no code on the branch does what Plan 00089's guard does) still
holds, but the file was wrongly assumed untouched, which matters here specifically:
`ssh-handling.bash` was NOT in the original `git merge-tree` 18-file conflict list
(computed against F44 before Plan 00089's edit existed) but may become one once that
edit lands, since it sits next to a hunk both sides already touch differently.

Corrected this plan's Overview and Task 2.1 to name `ssh-handling.bash` as a
possibly-newly-conflicting file with an explicit resolution rule (keep F44's side:
Plan 00089's guard plus the retry/validate logic; the branch's copy is stale, never
preferred). Same lesson as Plan 00089's own journal: verify a diff-stat claim against
the actual diff before writing it into a plan document, especially one this plan's
own conflict-resolution steps will rely on later without re-checking.

## 12:15 · action · T1.3 — integration branch created, merge started

`git checkout -b resync-ccy-ci-runner-f44` from `F44` (post-00089, `808ec31`), then
`git merge --no-ff origin/plan-00066-ccy-ci-runner`. `ssh-handling.bash` did NOT
newly conflict — auto-merged clean, confirmed absent from
`git status --porcelain=v1 | grep '^UU'`. 18 files conflicted, matching the
dry-run `git merge-tree` count exactly.

## 13:40 · action · T2.1 — code/script conflicts resolved (8 files)

Worked through each file reading both sides' intent rather than picking one side
wholesale. Two recurring patterns:

- **F44's side is a later refinement of the same mechanism** (`scripts/qa-bash.bash`,
  `scripts/qa-js.bash`): the branch's copy predates a Plan 00076-lineage refactor
  (shared `scripts/qa-discovery.bash` library) — took F44's side.
- **The branch's side has a genuinely unique, still-relevant feature** F44 never
  got: `engine_assert_rootless()` (Plan 00072, real security hardening —
  "unknown is a refusal, not a pass" for the container-engine rootless check),
  `check_legacy_grub_cgroup()` (Plan 00074, fixes a `grubby | grep` pattern that
  couldn't distinguish "no legacy config" from "grubby itself failed"),
  `ff_strip_comment()` usage in `scripts/qa-ansible.bash` (the helper existed
  in F44 too but sat unused — the branch's side is the only one that actually
  calls it), and the ruff-version-assertion block in `scripts/qa-python.bash`
  (Plan 00071) — took the branch's side / merged it in for all four.

Two version-number collisions across the diverged histories, same pattern each
time: both sides independently reused a version slot for unrelated work after
forking. `run.bash` "1.12.0" (branch: Plan 00074's grub fix; renumbered F44's
own 1.13.0–1.15.0 upward to 1.16.0, added `docs/run-bash-changelog.md` — a
branch-only file, auto-merged in cleanly, expanded with the renumbering
rationale) and `claude-yolo` "3.28.0" (kept both, added a disambiguating note
in `docs/ccy-changelog.md`, bumped the new work to 3.44.0).

`scripts/qa-python.bash` needed actual content-splicing, not a pick-a-side:
inserted the branch's ruff-version-assertion block ahead of F44's shared
discovery-library call (cheap check before expensive discovery), then had to
delete a second copy of that same block plus the branch's now-superseded old
`PY_EXCLUDES`-array discovery logic that was still sitting between the merge's
`=======` and `>>>>>>>` markers — the first edit only replaced the *opening*
marker and prepended content, leaving the rest of the conflict region
untouched. Caught by `lint_on_edit` re-firing on the leftover markers; fixed
by reading the file fresh and removing the whole stale span in one edit.

## 14:10 · action · T2.2 — doc/prose conflicts resolved (10 files)

Same two patterns as the code conflicts, plus a third specific to docs: **stale
descriptions of a feature that changed shape after the branch forked.**
`docs/headless-provisioning.md` and `docs/headless-server-install.md` both had
the branch's copy asserting "GitHub is mandatory in v1" / `none` as "a planned
follow-up" — this is the *pre*-Plan-00082 state; F44 shipped
`RUN_BASH_GITHUB_ACCOUNTS=none` support since. Took F44's side both times,
verified against `run.bash`'s actual behaviour (25 `RUN_BASH_GITHUB_ACCOUNTS`
references survive the merge, matching the pre-merge F44-only count).

`docs/fast-file-manager.md` had the reverse shape once: F44's own variable-name
list (`disable_thumbnails`, `apply_gsk_fix`, no prefix) contradicted its own
customisation example six lines above (`fast_file_manager_disable_thumbnails`)
— checked against the actual play's `vars:` block
(`playbooks/imports/optional/common/play-fast-file-manager.yml:32-33`) to
confirm the branch's prefixed table was the one matching reality, not just the
one that "sounded more complete".

`docs/architecture.md`: branch's `play-mask-intel-lpmd.yml` description ("where
it only adds boot noise" / "No-op on hosts where the unit is absent") checked
against the actual play source and confirmed accurate — it gates on unit
*presence*, not literally CPU vendor, which is what F44's shorter "on Intel-CPU
hosts" description implied. Also kept the branch's entire new "Desktop or
server — `provisioning_profile` / `scope` pair" section (F44's side of that
conflict was empty), since the underlying mechanism it documents already exists
in F44 (confirmed via `CLAUDE/AnsibleStyle.md`'s existing "Provisioning Profile
Self-Guard" section).

`CLAUDE.md`'s topic-file index table and `CLAUDE/Plan/README.md`'s Completed
section both needed a genuine union rather than a pick — verified with
`ls CLAUDE/*.md` that all 16 files either side's table referenced actually
exist on disk, so nothing in the merged table is aspirational.

`CLAUDE/QA.md` was the largest single resolution: kept F44's much more
detailed gate documentation (Plans 00075/00076/00081 lineage — coverage
assertions, the semgrep parse-tree section, `qa-deployed-drift.bash`) as the
base, added the branch's `qa-docs.bash` as a genuinely new 7th JSON-merged-gate
row, and deleted the branch's older "shellcheck is optional in `qa-bash.bash`"
caveat outright — that claim is now false for the merged codebase, since F44's
`qa-bash.bash` (already the side taken for that file) requires shellcheck and
gates on `error`+`warning` findings (Plan 00075).

A markdown-table-formatter hook fires on every Edit/Write to `.md` files and
repeatedly mangled conflict markers mid-resolution — `<<<<<<< HEAD` into
`\<<\<<\<<< HEAD`, a bare `=======` absorbed into a table row or escaped to
`\=======`, `>>>>>>> origin/...` into `> > > > > > > origin/...`. Each time this
meant re-reading the file fresh rather than trusting the original conflict
markers still matched, and once meant using placeholder text markers
(`STALE_BLOCK_START`/`STALE_BLOCK_END`) plus a small Python `re.sub` to delete
a large mangled span precisely (not `sed`, which this repo blocks for file
edits) after two Edit-tool attempts failed to match the exact mangled
whitespace.

## 14:35 · action · T3.1–T3.3 — full QA green, union of stages confirmed, feature survival confirmed

`./scripts/qa-all.bash` → exit 0, 619 files checked (`bash: 178 files OK`,
`python: 43 files OK`, `patterns: 178 files OK`, `ansible`/`ansible-syntax` OK,
`js: 5 files OK`, **`docs: 56 files OK`** — confirms the branch's `qa-docs.bash`
addition is actually wired in and passing, not just present in
`CLAUDE/QA.md`'s prose — `deployed-drift` OK, **`helper-tests: Ran 203 tests`**
(up from the pre-merge 161, confirming the branch's `_planlib.inc.bash` test
suite landed), `extension-compat` OK). Log at `/tmp/qa-all-output.log`.

`grep -c RUN_BASH_GITHUB_ACCOUNTS run.bash` → 25, `grep -c check_legacy_grub_cgroup
run.bash` → 3, `grep -c engine_assert_rootless files/var/local/claude-yolo/claude-yolo`
→ 2, `grep -c ff_strip_comment scripts/qa-ansible.bash` → 3 — all four
branch-only or F44-only features named in this plan's Goals survive the merge
intact.

Dispatched `infra-reviewer` (this repo's `qa-reviewer` equivalent, per
`CLAUDE/QA.md`'s "run before marking a plan Complete" requirement) over the
18-file resolution — Task 3.4, in progress. Merge commit withheld until its
verdict comes back; will resolve any BLOCK/FIX-BEFORE-MERGE finding before
committing.

## 15:20 · finding · T3.4 — infra-reviewer verdict: FIX-BEFORE-MERGE (1 blocker + 3 advisories, all fixed)

Reviewer recomputed the conflict set independently (`git merge-tree --write-tree
--name-only`) rather than trusting this journal's own file list — found it was 17
files plus one clean add (`docs/run-bash-changelog.md`), matching my 18 once that
distinction is made. Three-wayed every file against BOTH parents rather than
trusting my descriptions; found 5 discrepancies in my own summary (4 harmless, 1
real — see below) plus one real blocker and two real advisories in the merged
CONTENT itself.

**B1 (blocker, FIXED) — `ruff.toml` declared a 413-rule gate in its header while
enforcing a 59-rule one.** My earlier resolution took this as "adjacent additions,
union both sides" and missed that the two sides' HEADER TEXT described mutually
exclusive designs: the branch's `select` was never enumerated (ruff's default,
measured 413 rules); F44's `select = ["E4","E7","E9","F"]` is a later, deliberate
REVERSAL of that exact design (Plan 00071 incident: an unpinned default turned
`main` red with no commit behind it). I kept F44's `select` line but also kept the
branch's header describing the old, reversed-away design — so the file
self-contradicted (`ruff.toml:4` "deliberately NOT enumerated" directly above
`ruff.toml:20` "DECLARE THE RULE SET EXPLICITLY. Do not go back to..."). The
branch's `clip-scan` BLE001 per-file-ignore compounded it: reviewer measured
`ruff check --no-cache files/home/.local/bin/clip-scan` → "All checks passed!"
even WITHOUT the ignore (BLE isn't in the enforced 59-rule set at all), so the
ignore and its 18-line justification comment described a rule evaluation that
never happens — inert config with a comment claiming it was evaluated and
correctly waived.

Fixed: deleted the branch's contradictory header (`ruff.toml` old lines 4-17),
replaced with a short pointer to the `[lint]` section's own (correct, F44-native)
rationale; deleted the inert `clip-scan` BLE001 ignore entirely (re-verified: `ruff
check` still passes clean without it); corrected the same false "ruleset IS ruff's
default" claim where it had propagated — `scripts/qa-python.bash:29-34`'s
ruff-version-assertion comment, and `.claude/rules/qa-gates.md` (a NEW file this
merge introduced from the branch, never in my 18-file conflict list since it
auto-merged clean, so its content had never been reviewed until now). Re-verified:
`python3 -c "import tomllib; tomllib.load(...)"` (valid TOML), `ruff check
--no-cache files/home/.local/bin/clip-scan` (still clean), `bash -n
scripts/qa-python.bash` (clean).

**A2 (advisory, FIXED) — `run.bash:348-358` kept both sides of one hunk.** A
verbatim 4-line comment + `hl_resolve_secret SUDO_PASSWORD HL_SUDO_PASSWORD`
statement appeared twice consecutively. Reviewer checked (not assumed) this was
harmless — `hl_resolve_secret` is a pure function of the environment, so calling
it twice yields the same value, and the only real degradation path (a one-shot fd
consumed by the first call) is caught loudly by the sudo-credential assertion a
few lines later — but flagged it because no gate sees a harmless duplicate call
(`bash -n`/shellcheck/`qa-all.bash` were all green on it). Deleted the second
copy; `bash -n run.bash` clean; exactly one `hl_resolve_secret SUDO_PASSWORD
HL_SUDO_PASSWORD` call confirmed via grep.

**A3 (advisory, FIXED) — a SECOND version collision in `docs/run-bash-changelog.md`
that I never noticed.** I had renumbered F44's post-1.10.0 chain by exactly +1
(1.11→1.12→1.13→1.14 became 1.12→1.13→1.14→1.15) to clear the ONE collision I'd
spotted, at 1.11.0 (branch's Plan 00073 vs. F44's Plan 00065 — the
`server-recommended` bundle keyword). That shift landed F44's shifted 1.11.0
(→1.12.0) directly on top of the branch's OWN real 1.12.0 (Plan 00074,
legacy-grub) — a second collision I hadn't checked for, and Plan 00065's entry
was silently dropped rather than renumbered into the clear. The doc also claimed
divergence happened "at 1.11.0"; reviewer checked the actual merge-base
(`eb14ba2`, `RUN_BASH_VERSION="1.10.0"`) and found the real divergence point was
1.10.0 — both lines then independently minted 1.11.0. Capability was never lost
(`server-recommended` bundle machinery is fully intact in `run.bash` and
`playbooks/imports/optional/server-recommended.bundle`, confirmed by reviewer) —
only the changelog ENTRY documenting it had been dropped.

Fixed by shifting the whole F44 post-1.10.0 chain by +2 instead of +1 (recovered
the real per-commit version history via `git log --oneline 808ec31 -- run.bash`
to get the exact commits/plans in order): 1.13.0=Plan 00065 (new entry, recovered
from `git show 808ec31:run.bash`'s trailing version comment, which still held the
original wording before this merge replaced it), 1.14.0=Plan 00082 (was 1.13.0),
1.15.0=Plan 00084 (was 1.14.0), 1.16.0=Plan 00085 (was 1.15.0), and this merge's
own entry moved from 1.16.0 to **1.17.0** (bumped `RUN_BASH_VERSION` in `run.bash`
to match). Corrected the divergence-point claim to 1.10.0 and named both
collisions explicitly. Fixed one stale internal cross-reference this renumbering
exposed (1.15.0's entry said "composes with 1.13.0's ...none" — corrected to
1.14.0). Re-verified `run.bash:167`'s existing "History: docs/run-bash-changelog.md
1.12.0; reasoning: Plan 00074" comment stays correct unchanged (that slot was
never touched by this fix). `grep -n "^## "` confirms strict descending order,
no gaps, no collisions, 1.17.0 through 1.9.0.

**A4 (nit, no action)** — `helpers/gnome/verify_extension.py`'s mode changed
644→755 as a side effect of the merge (branch's copy was 755: shebang +
`__main__` guard, correct). Direction is right; just noted.

**Corrections to my own earlier summary** (reviewer's method note): `qa-ansible.bash`
is a genuine UNION, not "took the branch's side" — F44's `FF_FALSEY`/`FF_TRUTHY`
fix and Check 5 fact-var sweep both survive alongside the branch's
`ff_strip_comment()`. `qa-js.bash` took the BRANCH's zero-discovery refusal (F44
had none there), unioned with F44's anchoring. `check_legacy_grub_cgroup` did not
exist on F44 at all — it and `fatal()` are entirely the branch's. `docs/features/README.md`'s
F44 side was NOT empty (it already links `docs/ccy.md`) — dropping the branch's
stale "Coming Soon" was a strict upgrade, not a pick between two emptinesses.
`docs/architecture.md` is byte-identical to the branch's copy, confirming nothing
F44-specific was lost there. None of these corrections changed which resolution
was kept — all five were already the right call, just mis-described in my own
account of them.

Re-ran `./scripts/qa-all.bash` after all fixes: exit 0, 619 files, identical
shape to the pre-fix run (`bash: 178`, `python: 43`, `patterns: 178`,
`docs: 56`, `helper-tests: 203`). All fixes staged. Ready to complete the merge
commit.
